# B22 — 처방 둘을 넣고 돌린 첫 실물 판 판독

**실행**: 2026-09-18 17:50~18:30(**40분 17초**) · **Job** `POQSettleBatch22`(새 이름 — B21 은 감사 증거라 덮지 않았다) · **커밋** `f237f56d` · **로그** `output/logs-planonly-POQSettleBatch22/`
**설정(B21 과 동일)**: Consolidator `OpenRouter / openai/gpt-5.6-sol`(백엔드 `openai` 못박음·폴백 끔) · `PromptContextScope=Narrow` · 계획 시도 2 회 · Actor `claude-cli/claude-sonnet-5`
**규모**: AI 호출 25 · 입력 2,496,999 토큰 · 출력 296,212 · 실비 ≈ **$7.95** · 재생성 2 · **L1/L2 자동 검증 모두 통과** · 단계 19
**라우팅 가드**: Consolidator 응답 **24/24 `OpenAI`**(남은 1 은 못박지 않은 Critic `z-ai/glm-5.3` → `GMICloud`)

## 1. 한 줄 결론

**처방 넷이 요청에 전부 도달했고(13/13), 겨냥한 두 모양이 배송본에서 사라졌다** — `begin` 이 `TRY` 앞인 단계가 **9/17 → 0/17**, 신설 단계의 (러) 거짓 전제 문장이 **2 → 0**. 가드 순서는 검출된 다섯 자리 전부 트랜잭션 앞이다. **다만 「목차 요구 대응」은 이 판으로 확정할 수 없다** — B22 목차가 B21 과 다른 것을 요구해 통제된 비교가 아니다(§4).

## 2. 기전 — 요청 본문 실측

| 재료 | 단계 섹션 요청 13 건 | 골격 1 건 | B21 대비 |
| :-- | --: | --: | :-- |
| 목차 요구 블록 `[Approved Step Requirements` | **13** | 0 | B21 은 **0/24** |
| 대응 계약 `[Requirement Coverage Contract]` | **13** | 0 | 없던 것 |
| 금지 한 줄(레거시 기원 없는 단계만) | **3** | 0 | 없던 것 · 신설 5 중 3 에 실렸다 |
| 가드 순서 칸(헤더·판정) | **13** | 1 | 없던 칸 |
| 가드 순서 조항 + 규칙 5 재확인 | **13** | 1 | 없던 조항 |
| Few-Shot `TRY` 골격 예시 | **13** | 1 | 예시 구간에 `TRY` 가 0 건이었다 |

> **자를 열 번째로 틀렸다.** 조항을 `open this step's transaction only` 로 찾으니 0/13 이 나왔다. **JSON 이 아포스트로피를 `'` 로 이스케이프**한다 — 고쳐 재면 14/14 다. 한글 대문자 16진 이스케이프(`통`)는 이미 알고 있었는데 **ASCII 기호도 이스케이프된다**는 것은 몰랐다.

## 3. 배송본 — 겨냥한 두 모양

### 3-1. `begin` 이 `TRY` 앞 : 9/17 → **0/17**

같은 자(업무 트랜잭션만 · 제어 트랜잭션 제외)로 재면 B22 는 **17 단계 전부 `TRY` 안**에서 연다. 해당 없음 2(S14·S15 — 업무 tx 나 `TRY` 가 없다).

**어휘가 또 달랐다.** B21 은 `businessTx`(119 회), B22 는 `tx`(15)·`controlTx`(6)·`failureTx`(20)·`sharedTx`·`lockTx`·`initializeTx` 다. 이름으로 키를 붙이는 자는 판마다 다시 맞춰야 한다 — 이번엔 어휘를 **먼저 세고** 탐침을 짰다.

### 3-2. 가드가 트랜잭션 앞 : 검출 5 자리 전부 ✔

S04·S05·S08·S10·S11 에서 `-9` 가드가 업무 트랜잭션 개시보다 앞이다. B21 에서는 S10·S11 이 「열고 롤백」이었다(🟡 둘).

### 3-3. (러) 신설 단계의 거짓 전제 문장 : 2 → **0**

| | 신설 단계(목차 JSON 의 `LegacyProcedures` 가 빈 것) | (러) 문장 |
| :-- | :-- | --: |
| B21 | S01·S02·S03·S12·S19·S20 | **2** (S12:308 · S19:20) |
| B22 | S01·S02·S03·S18·S19 | **0** |

**자를 감사의 실제 문장으로 교정했다** — 두 문장 다 「원본」·「레거시」 낱말이 없다. 교정한 자가 B21 배송본에서 정확히 2 를 내 감사와 일치한다. 이 교정이 **(나) 판독 §4 의 앞선 「정정」이 틀렸다는 것**도 드러냈다(그 문서에 「정정의 정정」으로 적었다).

## 4. 이 판이 확정하지 못하는 것

- **「목차 요구 대응」은 통제된 비교가 아니다.** B22 목차는 새로 생성돼 **다른 것을 요구한다** — 예로 「자연 키 중복」·「최종 체크포인트」 요구가 아예 없고, 「그룹별」 불릿은 다른 것을 가리킨다. 개념 다섯으로 거칠게 재면 B22 는 목차가 요구한 5 자리 중 **5 가 같은 개념을 적었고** B21 은 11 중 3 이었지만, **분모가 다르므로 이 수를 효력으로 쓰지 않는다.** 확정은 **B22 축 B 감사**가 한다.
- **B21 의 12 자리와 1:1 대응이 없다.** 목차가 바뀌어 자리 자체가 옮겨졌다.
- **(가)·(다) 의 계약·목차 오라클 넷**(S15·S06·S02·S12)은 이번 처방 범위 밖이었고, 그중 **S14·S15 가 §3-1 의 「해당 없음」**으로 나온 것은 따로 읽어야 한다.
- **한 판은 표본 1 이다.** `begin<TRY` 0/17 은 강한 신호지만 분산을 재지 않았다.

## 5. 다음

1. **B22 축 B 감사** — 목차 요구 대응을 확정하고 새 가족을 뽑는 유일한 길이다. 캐시는 B21 것과 키가 달라 전량 미스다.
2. **S14·S15 의 「해당 없음」 읽기** — 업무 tx 나 `TRY` 가 없는 것이 결함인지.
3. **(가)·(다) 의 계약·목차 오라클 넷** · **(나-2) V 절** · **(라)** · **축 A 감사 31 객체**.
