# B21 정합성 감사(축 B) 판독 — 결함 46 · 가족 20 · 무거운 자리 10

**감사 시점**: 2026-09-18 · **코드**: main `cc7cf241` · **보고서 전문**: `output/Jobs/POQSettleBatch21/consistency/ConsistencyReport.md`(`output/` 는 git 무시라 디스크에만 있다) · **단위 캐시**: 같은 폴더 `.cache.json`(20 단위).

## 0. 왜 B21 이었나

L1·Critic 이 말해 주는 결함만으로 처방을 골라 왔다. 그 둘이 못 보는 축(원본 로직 대 계획서)에 무엇이 남는지, **지금 프롬프트 계약으로 만든 최신 산출물**에서 재려고 B21 을 골랐다. 범위는 **축 B 20 단계**로 잡았다(사람 결정) — 축 A 는 §5.

## 1. 한 줄 결론

**B21 은 축 B 에서 결함 46 건(🟠 10 · 🟡 36)이 나왔고, 그중 가장 넓은 가족은 「목차·계약이 건 요구에 대응 절·항목이 없다」(12 건 / 9 단계)다.** 기계 검사(L1)도 Critic 도 이 축을 보지 않는다 — B21 판 안에서 L1 은 통제명 둘만 잡았고 Critic 은 두 건을 냈다.

## 2. 수치

| | 값 |
| :-- | :-- |
| 단위 | 20(축 B 전 단계) · 신규 검증 20 · 캐시 적중 0 |
| 판정 | 결함 11 · 정합 4(S05·S07·S08·S09) · 검증 불가 5(신설 단계 — 다섯 다 결함을 겸한다) |
| 등급 | 🔴 0 · 🟠 10 · 🟡 36 |
| 조립 대조 | 20/20 단위에서 5/5 적중(S01 은 전수 51/51) |

## 3. 가족 (상위가 정규화했다 — 단위마다 이름이 갈렸다)

| 가족 | 자리 | 최고 등급 |
| :-- | :-- | :-- |
| (나) 목차·계약 요구에 대응 없음 | 12 건 / S01·S03·S04·S12·S13·S14·S18·S19(4)·S20 | 🟠 |
| (가) 트랜잭션이 사전 가드보다 먼저 열린다 | 5 / S02·S10·S11·S13·S15 | 🟠 |
| (라) 죽은 DECLARE | 5 / S11·S13·S15·S16·S18 | 🟡 |
| (다) 제어 DML 의 트랜잭션 경계 | 3 / S02·S06·S12 | 🟠 |
| (마) 검증 SQL 을 선언만 하고 호출 안 함 | 2 / S02·S13 (도달 4/6) | 🟡 |
| (자) 실패 문장 이름 귀속 오류 | 2 / S03·S20 | 🟡 |
| (러) 없던 원본 힌트를 제거했다고 적는다 | 2 / S12·S19 | 🟡 |
| 단발 13 | (사)S17🟠 ·(서)S04 ·(어)S11 ·(바)S18 ·(아)S17 ·(차)(카)S03 ·(타)S02 ·(파)(하)(거)(너)S20 ·(더)(머)(버)S12 | 🟠~🟡 |

**가장 무거운 둘**
- **S17**: 커밋 전 검증의 술어가 원본 `DELETE` 범위(`OUTYMD >= reqYmd`)를 빠뜨려, **레거시가 성공하는 데이터 상태에서 판이 `4000` 으로 실패하고 S15~S17 공유 트랜잭션이 통째로 롤백**된다.
- **S15**: 저널 `Running`·체크포인트 `Pending` 시작이 아예 없다. 실패가 저널에 안 남고, 체크포인트 성공이 0 행이라 **재시작이 이미 성공한 S15 를 다시 실행**할 수 있다. S03 단위가 「S03 은 다른 단계 행을 만들지 않고 아키텍처가 그 사전 등록을 금지한다」고 관측해 구제 경로가 없음을 확인했다.

## 4. 이 감사가 새로 드러낸 축 셋

1. **기존 결함 ①(S19↔S12 통제명)은 2 분열이 아니라 3 분열이다.** S12 가 쓰는 10 · S19 가 읽는 5(교집합 0) · 검증 세트가 읽는 2(나머지 8 은 아무도 안 읽는다). **이름만 맞춰도 안 닫힌다** — S19 의 현재값 수집이 `YMD` 한 범위뿐이라 `Extra*` 5 에 대응할 현재값이 없다.
2. **기준값끼리 어긋난 자리가 셋**(§보고서 5-4): 등록·잠금 원자성(목차 대 계약), 잠금 충돌 게시 경로(아키텍처 대 계약), 제어 표 스키마(`AttemptNo`·열 이름). 산출물이 계약을 따른 것은 옳으나 **충돌을 해소했다고 적지 않았다.**
3. **검증 오라클이 순환인 자리**: V09 의 항등식은 S14 가 같은 식으로 계산해 넣은 값을 다시 본다 — 사실상 외부 변조만 잡는다.

## 5. 이 판독이 보증하지 않는 것

- **축 A 를 돌리지 않았다.** B1·B4 의 축 A 캐시 31 건은 **31/31 미스**다 — 2026-09-08 명세서 재생성으로 재료가 전부 바뀌었고 그 뒤 축 A 감사가 없다. **지금 명세서는 원본 DDL 과 대조된 적이 없고**, 이번 축 B 판정은 전부 「명세서가 옳다」는 전제 위에 있다.
- 후속 단계 간섭은 20 단위 전부 계약대로 보류했다. 실행 대조도 없다(DB 접속 없음).
- 단위 간 등급이 갈린 자리(트랜잭션 개시 위치: ⚪~🟠)는 재판정하지 않고 갈렸다는 사실로 남겼다.

## 6. 다음 후보 (도달 순)

| 후보 | 도달 | 성격 |
| :-- | :-- | :-- |
| **(나) 목차 요구 대응 없음** | 12 건 / 9 단계 | 프롬프트·검사 어느 층이든 「목차가 건 요구를 단계가 덮었는가」를 재는 자가 없다. 기계 검사로 만들려면 목차 항목과 단계 절의 대응을 어떻게 판정할지가 관건 |
| **(가)·(다) 트랜잭션·제어 경계** | 8 건 | 공통 골격(`01-step-contract.md`)이 원인이라 **골격 한 줄이 여덟 자리를 만든다** — 프롬프트 층 처방이 가장 싸다 |
| **(라) 죽은 DECLARE** | 5 건 | 「배치 경계에서 소멸한다」는 사실을 계약이 말하지 않는다 |
| **축 A 감사** | 31 객체 미검증 | 축 B 전체가 이 전제 위에 서 있다 |
