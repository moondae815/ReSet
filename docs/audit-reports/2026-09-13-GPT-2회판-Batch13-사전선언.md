# GPT Consolidator 계획 시도 2 회 판 — `POQSettleBatch13` (사전 선언)

**작성 시점**: 2026-09-13, **실행 전**. 합격선을 결과보다 먼저 못박는다.
**코드**: main `03cb1e3e`(가드 술어 대조 · 1 회 판 Critic 방침 포함) + 이 문서 커밋.
**방침**: `docs/audit-reports/2026-09-13-1회판-Critic-지적-방침.md` 의 결정 A 를 처음 적용하는 판이다.

## 0. 물음

1. **2 차 시도가 Critic 이 지적한 결함을 닫는가.** claude-cli 두 판(Batch7·8)에서는 E 지적 17 중 11 이 닫혔고, 거짓 지적 추종 1 · 재생성 단계의 새 회귀 ≥ 5 가 따라왔다.
   GPT Consolidator 에서도 그런가.
2. **최고점 채택이 수정을 버리는가.** B8 에서는 뒤 시도가 L1 에 떨어져 수정본이 통째로 버려졌다.
3. **판 안 검사가 2 차 시도의 재료가 되어 결함을 닫는가.** 1 회 판은 「잡느냐」까지만 답했다(Batch12 §0). 이번에는 K1 · 가드 술어 대조(단계 층)와
   K2(문서 층 → 다음 시도의 지목 재료)가 발화하면 **닫히느냐**까지 본다.
4. **추가 시도의 실제 비용** — 방침 문서의 추정(지목 단계 1 개 기준 5~7 분 · GPT $0.5 미만 + Critic 1 회)이 맞는가.

## 1. 실행 조건 — Batch12 에서 시도 예산만 다르다

```
PLANONLY_MAX_L2_ATTEMPTS=1 PLANONLY_CONSOLIDATOR_PROVIDER=OpenRouter \
PLANONLY_CONSOLIDATOR_MODEL=openai/gpt-5.6-sol PLANONLY_OPENROUTER_ONLY_BACKEND=openai \
scripts/run-plan-only-job.sh POQSettleBatch13
```

- 계획 시도 **2 회**(총 시도 = 1 + 1). L1 수리 예산(`MaxL1RepairAttempts` 2)은 따로다.
- Batch12 와 다른 코드: `cab233f9` → 이 문서 커밋. 가드 술어 대조(단계 층 `SafeCheck`)가 새로 들어갔다.
- `Narrow` · SP 14 편 · Critic `z-ai/glm-5.3` 동일. 목차는 판마다 새로 선다(Batch11 22 단계 · Batch12 18 단계).

## 2. 예측

| # | 예측 | 틀리면 |
| :-- | :--- | :--- |
| P1 | 1 차 리뷰 `HasDefects = true` → **2 차 시도가 돈다** (근거: B10·B11·B12 1 차 리뷰 3/3 이 결함 있음) | 돌지 않으면 이 판은 1 회 판과 같다 — 물음 1·2·3 은 「표본 없음」으로 적는다 |
| P2 | 2 차 시도의 재생성 단계 **1~4 개** | 5 이상이면 추가 시도 비용 추정이 틀린다 — 단계 수를 적는다 |
| P3 | 벽시계 **30~50 분** · Consolidator 비용 **$6~9** | 범위 밖이면 추정의 어느 항이 틀렸는지 국면별로 적는다 |
| P4 | `SQL_CURRENT_RUN_ID` 누락 재생성 **0~2** | 3 이상이면 GPT 판의 0 이 두 판짜리 우연이었다 |
| P5 | 1 차 Critic 의 E 지적 중 2 차에서 닫히는 비율은 **예측하지 않는다** — claude-cli 표본(11/17)을 GPT 에 옮길 근거가 없다 | — |

## 3. 합격선

| # | 항목 | 합격 |
| :-- | :--- | :--- |
| G1 | 실행 | 종료 코드 0 · 번들 내보내기 완료 · HTTP 실패 0 |
| G2 | 라우팅 | Consolidator 요청 전부 백엔드 고정 · 응답 `provider` 전부 `OpenAI` |
| G3 | 비용 | Consolidator 응답 `usage.cost` 합 **≤ $25** |
| G4 | 시간 | **≤ 90 분** |
| G5 | 점수 | 채택 점수 **≥ 74** (신호일 뿐) |
| G6 | 앵커 | SELECT 앵커 도달률 **5/5** · 불일치 0 · (종류,서수) 불일치 0 |
| G7 | 게이트 | 새 Job 을 넣은 코퍼스로 전체 시험 실패 0 · 건너뜀 0 |
| G8 | 두 시도 대조 — 재료 | 시도별 계획서 **전문**을 로그의 Critic 요청 페이로드에서 꺼내 단계별로 자르고, 디스크 `steps/*.md` 가 어느 한 시도의 절과 바이트 일치함을 확인한다(디스크는 단계마다 마지막 판만 남는다 — 방침 문서 §측정) |
| G9 | 두 시도 대조 — 판정 | 1 차 리뷰의 결함 항목 **전부**에 운명을 붙인다: 닫힘(2 차 본문으로 확인) · 지속 · 재생성 안 됨 · 되돌림으로 사라짐 · 거짓 지적 추종 · 판정 못 함. **E 항목은 전부** 원본 DDL 로 참·거짓을 판정한다. 재생성된 단계는 1 차와 맞대 **새 결함**을 원본 DDL 로 찾아 적는다(Critic 이 잡았나/놓쳤나를 나눠서) |
| G10 | 채택 | 채택된 시도와 배송본 `docs/BatchMigrationPlan.md` 의 단계별 일치를 적는다. **2 차에서 닫힌 E 결함이 있는데 1 차가 채택됐으면** 「채택 규칙이 수정을 버림」으로 적는다(실패가 아니라 물음 2 의 답이다) |
| G11 | 판 안 검사 | 판 안(로그)·배송본(스윕·문서 층 재측정)의 K1 · K2 · 가드 술어 대조 발화를 **전부** 원본 DDL·SQL 을 열어 판정하고 **오탐 0**. 1 차에 발화했으면 2 차에서 닫혔는가를 적는다. 발화 0 이면 「대상이 있었는가」를 적는다(가드: 원본 가드를 옮긴 단계가 있었나 · 통제 합계: 쓰고 읽는 단계가 둘 이상이었나) |

**기록(합격선 아님)**: 국면별 시간(1 차 / 2 차 재생성 / 2 차 Critic) · 호출 수와 비용을 시도별로 · Critic 비용 2 회분 · 하한 재생성 사유별 횟수 ·
배너 수(하한 미달과 목차 결함을 나눠서) · 스윕 Job 별 발화 · 캐시 읽기 비율 · Batch11·12 와 나란히 놓은 표.

---

# 실행 결과 (2026-09-13 23:43:33 ~ 2026-09-14 00:24:34 · 41 분 01 초)

실행 워크트리 `1e31d6b4` · 종료 코드 0 · 목차 **19 단계** · 2 차 시도(86)가 1 차(94)를 못 넘어 **1 차 채택**.
두 시도 대조 재료·메모는 판독 세션 스크래치에 있다 — 아래 수치 중 **E/D 등급·새 결함·참거짓은 판독자 판단**, 바이트 일치·호출·비용·시간은 파일·로그 실측이다.

## 합격선

| # | 항목 | 실측 | 판정 |
| :-- | :--- | :--- | :--- |
| G1 | 실행 | 종료 코드 0 · 번들 내보내기 완료 · HTTP 실패 0 | **합격** |
| G2 | 라우팅 | Consolidator 응답 30/30 `OpenAI`(요청 `order:["openai"]`) · Critic 2 회 `GMICloud` | **합격** |
| G3 | 비용 ≤ $25 | Consolidator **$7.19** (Critic $0.68 별도) | **합격** |
| G4 | 시간 ≤ 90 분 | **41 분 01 초** | **합격** |
| G5 | 점수 ≥ 74 | 채택 **94/100** (연동 7 로 축 기준 미달 배너) | **합격** — 신호일 뿐 |
| G6 | 앵커 | 도달률 **5/5** · SELECT 앵커 10 · 불일치 0 · (종류,서수) 14 단계·92 문장·불일치 0 | **합격** |
| G7 | 게이트 | 새 Job 포함 코퍼스 전체 시험 **4364** 통과 · 실패 0 · 건너뜀 0 · 경고 0 | **합격** |
| G8 | 두 시도 대조 — 재료 | 로그 Critic 페이로드 2 개에서 전문 추출. **두 시도 차이는 골격(전면 재작성)과 S18 뿐**, 17 단계 바이트 일치. 배송본·`agent/steps` 는 19/19 단계와 골격이 **1 차**와 일치. `raw/attempts/run-001` 의 S18·골격·manifest 는 **2 차**라 배송본과 어긋난다 | **합격** |
| G9 | 두 시도 대조 — 판정 | 아래 표 — 1 차 E 2·D 1 전부 2 차에서 닫힘, E 지적 참 4/4 · 거짓 0 | **합격(전 항목 판정)** |
| G10 | 채택 | **「채택 규칙이 수정을 버림」** — 2 차가 닫은 E 2 건(R1-1 · R1-3)이 배송본에 그대로 있다 | **해당**(물음 2 의 답) |
| G11 | 판 안 검사 | 가드 1 발화 → 재생성으로 **닫힘** · K1 0 · K2 0 — **K2 는 대상이 있었는데 못 봤다**(아래) · 오탐 0 | **오탐 0 합격 · 사각지대 1** |

## 예측

| # | 예측 | 실측 | |
| :-- | :--- | :--- | :--- |
| P1 | 2 차 시도가 돈다 | 돌았다(1 차 `HasDefects` · `SkeletonDefective`) | 적중 |
| P2 | 재생성 단계 1~4 개 | **골격 전면 재작성 + S18** | 단계 수는 적중, **골격 재생성은 예측 밖** |
| P3 | 30~50 분 · $6~9 | 41 분 · $7.19 | 적중 |
| P4 | `SQL_CURRENT_RUN_ID` 누락 0~2 | **0** (별개로 이름 있는 저널 블록 미정의 S02·S11 2 회) | 적중 |
| P5 | 닫힘 비율 예측 안 함 | 1 차 E 2/2 닫힘 | — |

## G9 — 1 차 지적(94 점, `DefectiveSteps` [S18] · `SkeletonDefective`)

| # | 위치 | 주장 | 등급 | 참/거짓 | 2 차 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| R1-1 | 골격 V15 ↔ S18 | V15 가 S18 표 정의에 없는 컬럼(`CheckCode`·`ResultStatus` 등)을 조회 — 실행 오류 | E | 참 | **닫힘**(골격 계약·S18 정의·V11 통일) |
| R1-2 | 골격 권장 컬럼표 ↔ S18 | 같은 표 정의가 둘 | D | 참 | 닫힘(단 `DifferenceCount` 타입이 새로 어긋남 — Critic 2 놓침) |
| R1-3 | 최종 게이트 | 「FAIL 없음」 조건을 평가할 수 없다(자리는 S19 가 아니라 골격 V15/V16) | E | 참 | **닫힘**(V11) |

## G9 — 2 차 지적(86 점, `DefectiveSteps` [S02, S09, S10, S11] · `SkeletonDefective`)

인용된 네 단계는 **1 차와 바이트 동일**하다 — 결함은 모두 **재작성된 골격** 쪽 문장에서 났다(바이트 같은 본문에 대한 순수 변동 0).

| # | 주장 | 등급 | 판정 |
| :-- | :-- | :-- | :-- |
| R2-1 | S11 이 쓰는 `SettleFact*` 와 V04 가 읽는 이름 불일치 | E | 참. **1 차(배송본)에도 같은 계열** — V04 가 `TSettleMst.*` 이름으로 S11 몫을 읽어 FULL OUTER JOIN 이 늘 거짓 실패. Critic 1 은 놓쳤고, 2 차는 「늘 0 행 = 조용한 통과」로 더 나쁜 형태가 됐다 |
| R2-2 | 공통 규약 「원본이 코드를 안 준 실패 = SQL Server 오류 번호」 vs S09/S10 본문 NULL | E | 모순은 참이고 **골격 재작성이 새로 만들었다**. 원본 DDL(INS_EXTRA 63·254, EXTRA4PLCARD 52·179)에 `@@ERROR` 검사가 없어 **맞는 쪽은 S09/S10 의 NULL — Critic 2 의 권고 방향은 DDL 과 반대** |
| R2-3 | 골격의 「잠금 → BatchRun INSERT」 순서 보장 vs S02 본문 | D | 참, 새로 생긴 모순(S02 실행 자체는 두 시도 동일) |

재작성 골격의 새 결함 중 **Critic 2 가 놓친 것**: D 3(S02 가 S01 저널을 저장한다는 거짓 서술 · 인터페이스 표 `p_businessYmd` vs 단계 `p_ymd` · `DifferenceCount` 타입) + 판정 못 함 1(V05 부호 검사).
**두 Critic 모두 놓친 E(배송본)**: S19 게시 전제가 S01 체크포인트 `Succeeded` 를 요구하는데 S01 체크포인트를 쓰는 곳이 없다 — 이대로면 S19 는 늘 -9190.

## G10 — 채택이 버린 것과 막아 준 것

- **버린 수정**: R1-1(E) · R1-3(E) · R1-2(D). 배송본에 V15 가 없는 컬럼을 조회하는 SQL 이 그대로 있다.
- **막아 준 것**: R2-2 의 새 E 모순, R2-1 의 조용한 통과 형태, D 4 건.
- **점수가 떨어진 원인은 Critic 변동이 아니라 골격 재작성이 만든 새 모순**이었다(예외 10→7 · 정합성 10→9). 즉 이 판에서 채택 규칙은 「수정본을 버렸다」와 「회귀본을 막았다」를 동시에 했다 — 2 차가 지적 자리만 고치고 골격을 통째로 다시 쓴 것이 둘을 묶었다.

## G11 — 판 안 검사

| 검사 | 판 안 | 배송본 | 판정 |
| :-- | :-- | :-- | :-- |
| 가드 술어 대조 | **S03 1 발화** — 원본에 없는 `PLTID = 13`(GPT 세 판째 PLTID 조건을 지어냈다: `'POQ'`·`1`·`13`) → 하한 재생성 | 0 — 배송 S03 은 원본 조건 `YMD = @pi_strYMD AND OutState IN (1,5) AND OutYMD IS NOT NULL` 그대로 | **발화 참 · 재생성으로 닫힘** — 이 검사가 만들어진 이유가 실물 판에서 처음 작동했다 |
| K1 | 0 | 0 | 대상 없음 — 통제 합계 표를 전부 정본 `batch.BatchControlTotal` 로 불렀다 |
| K2 | 0 | 0 | **대상이 있었다** — V04(통합 검증 SQL 세트)가 `StepCode = N'S11'` 몫을 `TSettleMst.RowCount`·`TxAmt`·`CLTotal`·`PGTotal` 로 읽고 S11 은 `SettleFactRowCount` 등 6 개로 쓴다(교집합 0). **K2 는 단계 섹션과 공통 규약만 읽고 검증 SQL 세트는 안 받는다**(`VerificationPipelineOrchestrator` K2 호출부) — 사각지대. 결함은 배송됐다 |

## 기록

| | Batch11 | Batch12 | **Batch13** |
| :--- | ---: | ---: | ---: |
| 계획 시도 | 1 | 1 | **2** |
| 목차 단계 | 22 | 18 | **19** |
| 벽시계 | 35 분 | 25 분 | **41 분**(1 차 31:23 · 2 차 9:38) |
| Consolidator 호출 · 청구 | 33 · $6.43 | 23 · $5.42 | **30 · $7.19**(1 차 28 · $5.90 / 2 차 2 · $1.29 — 골격 $0.74·4 분 · S18 $0.55) |
| Critic | 1 회 | 1 회 · $0.44 | **2 회 · $0.68**(3:39 · 4:38) |
| 하한 재생성(1 차) | 8 | 2 | **6** — S03 가드 · S02·S11 저널 블록 미정의 · S06 술어 · S08 조인 짝 · S19 저널 행 생성 없음 |
| `SQL_CURRENT_RUN_ID` 누락 | 0 | 0 | **0** |
| 채택 점수 | 80 | 90 | **94**(2 차 86) |
| 배송 배너 | — | — | 검증 불가 4 단계(S01·S02·S14·S19 — 목차 결함) · 품질 불합격(연동 7) |

- **추가 시도 비용 추정이 틀렸다** — 방침 문서는 「지목 단계 1 개 · 5~7 분 · GPT $0.5 미만」이었는데 `SkeletonDefective` 가 골격 전면 재작성을 불러 **9 분 38 초 · GPT $1.29 + Critic $0.26** 이었다.
- **스윕**(`sweeps/2026-09-14-step-sweep-batch13.md`): 미분류 97 → 106(+9, 전부 Batch13), 다른 검사 행 변화 0. 9 중 **4 는 측정 인공물**이다 — 「`### ` 헤딩으로 시작하지 않습니다」가 검증 불가 4 단계에만 났고, 그 파일 머리에 파이프라인이 붙인 **검증 불가 배너**가 헤딩 앞에 있다(판 안 하한 검사는 그 단계들을 건너뛰었다). 나머지 5 는 목차 결함 배너의 사유(S01 TargetTables 빈칸 · S02·S19 `BatchRunLock` 목차 누락 · S14 트랜잭션 분할 2) — 개별 진위는 이 판독에서 재지 않았다.
- 배송본 머리말 「분석 AI 정보: claude-cli (claude-sonnet-5)」와 manifest `ReuseKey.Provider = claude-cli` 는 실제 Consolidator(GPT)와 다르다 — Batch12 배송본도 같은 표시라 이번 판의 새 결함은 아니다.

## 이 판이 답한 것

1. **2 차 시도는 지적을 닫는다 — 이번엔 E 2/2.** 다만 표본은 전이 1 개다.
2. **채택 규칙은 수정을 버렸다.** 그리고 버린 이유는 2 차의 **골격 전면 재작성이 새 모순을 만든 것**이었다 — 「지목 자리만 고친다」가 골격에는 성립하지 않는다.
3. **판 안 검사는 2 차를 기다리지 않고 1 차 안에서 닫는다** — 가드 표류는 하한 재생성으로 1 차에서 닫혔다. 2 차 시도가 더한 가치는 Critic 지적 쪽이다.
4. 새로 드러난 결함 가족 둘: **K2 가 검증 SQL 세트를 안 읽는다** · **체크포인트 게이트가 아무도 쓰지 않는 체크포인트를 요구한다**(S19 ← S01).
