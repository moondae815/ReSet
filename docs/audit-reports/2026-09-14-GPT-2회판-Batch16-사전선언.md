# GPT 계획 시도 2 회 판 — `POQSettleBatch16` (사전 선언)

**작성 시점**: 2026-09-14, **실행 전**. **코드**: main `8cba67c2`(가드 조건 표 병합) + 이 문서 커밋.

## 0. 물음

1. **가드 조건 표(`[Original Guard Conditions]`)가 실물 판에서 듣는가.** 재생 실험(요청 재전송, `2026-09-14-가드PLTID-날조-원인-측정.md` §1-3)은 조건 블록을 넣은 요청에서 PLTID 조건 0/3 이었다. 판 전체에서도 가드 술어 대조가 판 안에서 발화하지 않는가.
2. **그 표가 실제 요청에 실렸는가**(배선). 단위 시험은 세 경로의 프롬프트를 잠갔지만, 실물 판의 요청 본문에서 따로 센다 — 안 실렸으면 1 의 결과는 표의 효과가 아니다.
3. **아직 표본이 없는 검사·경로**(B14·B15 에서 대상 없음): RunId 게이트 · K2 검증 세트판 · 생산자 검사 귀속 · Critic 골격 패치 · T25(위치 INSERT) · RunId 수명 계약(목차 모양에 따라).

## 1. 실행 조건 — Batch15 와 같다

```
PLANONLY_MAX_L2_ATTEMPTS=1 PLANONLY_CONSOLIDATOR_PROVIDER=OpenRouter \
PLANONLY_CONSOLIDATOR_MODEL=openai/gpt-5.6-sol PLANONLY_OPENROUTER_ONLY_BACKEND=openai \
scripts/run-plan-only-job.sh POQSettleBatch16
```

## 2. 예측

| # | 예측 | 틀리면 |
| :-- | :-- | :-- |
| P1 | 25~50 분 · Consolidator $5~9 | 국면별로 적는다 |
| P2 | **판 안 가드 술어 대조 발화 0**(B13 1 · B14 2 · B15 2). 표에 실린 가드는 넷 — CMRate_Ins 20 · SETTLE_INS 25·39 · INS_EXTRA 30 | 발화하면 시도·단계·추가/누락 조건을 전부 적고, 그 요청에 표가 실렸는지 대조한다 |
| P3 | 배송본 CMRate_Ins 가드 질의에 `PLTID` 조건 0 — 여섯 판째에 처음으로 끊긴다 | 끊기지 않으면 표가 실렸는데도 모델이 무시한 것 — 원인 측정으로 되돌아간다 |
| P4 | 표에 **없는** `INS_EXTRA4PLCARD` 19 행(조인 가드)은 이전 판들처럼 원본대로 옮긴다 — 대조군 | 조건이 바뀌면 적는다(검사가 못 보는 자리라 사람이 판독) |
| P5 | 목차 모양은 예측하지 않는다 | — |

## 3. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| G1~G7 | 실행 · 라우팅 · 비용 ≤ $25 · 시간 ≤ 90 분 · 점수 ≥ 74(신호) · 앵커(놓친 선언 행 0 · SELECT·(종류,서수) 불일치 0) · 게이트(실패 0 · 건너뜀 0) | Batch15 와 같다 |
| G8 | **배선** | 로그의 Consolidator 요청 본문에 `[Original Guard Conditions]` 가 있고, 가드가 있는 단계(S 코드는 목차에 따라)의 단계 섹션 요청마다 실렸다. 0 이면 1·2 의 판정은 「표 효과 판정 불가」 |
| G9 | **가드 효과** | 판 안 가드 술어 대조 발화 수(로그 `섹션이 원본 가드(` 문구) · 배송본 단계 검사 가드 발화 수 · 배송본 CMRate_Ins 가드 질의의 WHERE 를 원본 20 행과 대조. 판정 문장에 「표본 1 판」을 붙인다 |
| G10 | 새 검사 발화 판정 | 판 안(로그)·배송본의 RunId 게이트 · K2(단계·검증 세트) · 생산자 · T25 발화를 **전부** 판정, 오탐 0 |
| G11 | 닫힘 · 골격 패치 | 판 안 위반마다 귀속 자리 · 다음 시도에서 사라졌나. `SkeletonDefective` 가 나면 「골격 패치」 상태문 · 두 시도 대조 |
| G12 | 계약 준수 | RunId 발급 단계 · 발급 전 단계 · 그 단계들의 저널·체크포인트 쓰기 · 게이트가 요구하는 단계를 배송본에서 잰다. 발급 전 단계 0 이면 「표본 없음」 |
| G13 | 배너 · 스윕 | 배송본 배너 전부 판정 · `--sweep` 재실행에서 다른 Job 행 변화 0 |

---

# 실행 결과 (2026-09-14 13:42:30 ~ 14:30:21 · 47 분 51 초)

실행 워크트리 `d20a0fcf` · 종료 코드 0 · 목차 **21 단계** · 1 차 **94 · 결함 1(S03)** → 지목 재생성 → 2 차 **96 · 결함 없음**.

## 한 줄 결론

**가드 조건 표가 들었다(표본 1 판).** 표는 단계 섹션 요청 38/38 과 골격 요청에 실렸고, 판 안 가드 술어 대조 발화는 **0**(B13 1 · B14 2 · B15 2)이며,
배송본의 표 안 가드 넷과 표 밖 조인 가드 하나가 **전부 원본 조건 그대로**다 — CMRate_Ins 가드의 PLTID 날조가 **여섯 판째에 처음 끊겼다.**
대신 판 시간을 태운 것은 다른 자리다: 단계 21 중 16 이 **공통 규약에 이미 정의된 저널 블록을 자기 절에 다시 정의하라는 하한 검사**에 걸려 재생성됐다(§발견 1).

## 합격선

| # | 항목 | 실측 | 판정 |
| :-- | :-- | :-- | :-- |
| G1 | 실행 | 종료 코드 0 · 번들 완료 · `[ERR]` 0 | 합격 |
| G2 | 라우팅 | Consolidator 응답 41/41 `OpenAI` · Critic 2 회 `GMICloud` | 합격 |
| G3 | 비용 ≤ $25 | **$8.20**(Critic $0.75 별도) | 합격 |
| G4 | 시간 ≤ 90 분 | **47 분 51 초** — 브레인스토밍·목차 6 분 · 골격 4 분 · 단계 25 분(재생성 16 포함) · Critic 두 번 12 분 | 합격 |
| G5 | 점수 | 1 차 94 → **96** | 합격 — 신호 |
| G6 | 앵커 | 도달 **5/5**(놓친 선언 행 0) · SELECT 10 · 불일치 0 · (종류,서수) 14 단계·92 문장·불일치 0 | 합격 |
| G7 | 게이트 | 새 Job 포함 4433 통과 · 실패 0 · 건너뜀 0 · 경고 0 | 합격 |
| G8 | **배선** | 로그의 `[Original Guard Conditions]` 39 — 요청 종류로 가르면 **단계 섹션 38/38 · 골격 1/1** · 브레인스토밍·목차·리뷰 0(설계대로 안 실림). 단일 호출 폴백은 이 판에서 안 불렸다 | 합격 |
| G9 | **가드 효과** | 판 안 가드 발화 **0** · 배송본 단계 검사 가드 발화 0 · 배송본 SQL 을 원본과 손으로 대조 — S04 CMRate_Ins 20(`CASE WHEN EXISTS`) · S05 SETTLE_INS 25(`SELECT TOP (1)`) · S05 SETTLE_INS 39 · S10 INS_EXTRA 30(7 항) · S11 INS_EXTRA4PLCARD 19(조인, 표 밖) **다섯 모두 조건 동일**. 침묵이 검사의 맹목이 아님을 보이려고 배송본 모양 그대로 변이: S04 `+PLTID = 16` 발화 · S05 `+PLTID = 16` 발화 · S10 `−ExtraSettleFlag = 1` 발화 · S11 조인 가드 `+A.PLTID = 16` **가드 검사 침묵**(알려진 한계 — 조인 가드는 표에도 검사에도 없다) | **합격(표본 1 판)** |
| G10 | 새 검사 발화 | 판 안 통합 L1 두 시도 모두 0 · 배송본 재측정 통합 L1 0 · T25 0 · K2 단계 0 · K2 검증 세트 0 | 오탐 0 — 발화 대상 없음 |
| G11 | 닫힘 · 골격 패치 | Critic 1 차: S03 이 `batch.BatchRunLock` 소유권 승계(S02 가 예약값 0 으로 잡은 잠금을 발급된 RunId 로 갱신)를 빠뜨림 → S03 지목 재생성(변경 비율 98.6 %) → 배송본 S03 에 승계 `UPDATE batch.BatchRunLock` 있음 · 2 차 결함 없음. **다만 고친 S03 은 목차가 `BatchRunLock` 을 선언하지 않아 「검증 불가」로 배송됐다** — Critic 이 고치게 한 섹션을 기계 검사가 못 봤다(§발견 2). `SkeletonDefective` 없음 → 골격 패치 표본 없음 | 닫힘 · 골격 패치 표본 없음 |
| G12 | 계약 준수 | 발급 단계 **S03** · 발급 전 단계 **[S01, S02]** · 두 단계의 저널·체크포인트 쓰기 **0**·저널 블록 호출 0 · 요구 단계 목록을 가진 게이트 셋(`SQL_VERIFY_PREDECESSORS` S04~S11 · `SQL_FIND_FIRST_INCOMPLETE_STEP` S03~ · `SQL_VALIDATE_REQUIRED_STEP_COMPLETION` S03~S19)의 **S01·S02 요구 0** | **합격(표본 2 — B15 에 이어)** |
| G13 | 배너 · 스윕 | 배너는 「검증 불가」 3 — S01(목차 `TargetTables` 빈칸) · S03(목차에 없는 `BatchRunLock`) · S14(원본 트랜잭션이 S14·S15·S16 으로 갈림, B13·B15 와 같은 목차 결함). **「하한 미달」 0**. 스윕(`sweeps/2026-09-14-step-sweep-batch16.md`): 미분류 78 → 82(+4, 전부 Batch16 — 위 세 배너의 사유 넷) · 다른 Job·검사 행 변화 0 | 합격 |

## 예측

| # | 예측 | 실측 | |
| :-- | :-- | :-- | :-- |
| P1 | 25~50 분 · $5~9 | 48 분 · $8.20 | 적중(상단) |
| P2 | 판 안 가드 발화 0 | 0 | **적중** |
| P3 | 배송본 CMRate_Ins 가드에 PLTID 조건 0 | 0 — `SELECT PLTID` 는 투영에만 남음 | **적중** |
| P4 | 표 밖 조인 가드는 원본대로 | 원본대로(6 항 + 조인 조건) | 적중 |

## 발견

1. **하한 재생성 16 이 전부 「이름 있는 SQL 블록 미정의」다** — S04~S11 · S13~S19 · S21 의 첫 초안이 `SQL_INSERT_STEP_START` · `SQL_MARK_STEP_SUCCEEDED` · `SQL_MARK_STEP_FAILED` 를 부르기만 했다.
   그 셋은 골격 공통 규약(배송 번들 `agent/common/01-step-contract.md`)에 **정의돼 있다.** `CheckUndefinedSqlBlockReference` 가 정의를 **그 단계 절 안의 SQL 펜스에서만** 찾기 때문에, 재생성은 공통 블록을 절마다 베껴 넣는 것으로 닫혔다 —
   사본 45(16 · 14 · 15)이 공백 정규화 후 **공통 정의와 전부 같다.** 결함을 고친 재생성이 아니라 사본을 만든 재생성이고, 단계 국면 25 분의 상당 부분이 여기 들었다.
   같은 사유가 B15 에서는 1(S04)이었다 — 골격이 저널 블록을 공통 규약에 싣는 판마다 커질 수 있다. 처방 후보: 검사가 공통 규약(`ExtractSharedConventions`)의 정의도 인정(K2 가 이미 그 재료를 받는다). 이 판의 합격선 밖 — 따로 잰다.
2. **Critic 이 고치게 한 섹션이 기계 검사를 못 받고 배송됐다.** S03 첫 초안은 하한 검사를 통과했는데, Critic 지적(잠금 승계 누락)대로 `BatchRunLock` 갱신을 넣자 목차의 대상 테이블 선언과 어긋나 「검증 불가」가 됐다. 목차는 재생성되지 않으므로 **Critic 이 목차 밖 테이블을 요구하는 지적은 고쳐도 검증 불가로 끝난다.**
3. 프롬프트 지시문이 본문에 옮겨졌다 1 곳 — S04 250 행 「`PLTID`는 `EXISTS`의 투영 컬럼일 뿐 추가 조건이 아니다」. 사실 문장이라 해는 없다(규칙 층 처방이 본문으로 새는 알려진 모양).
4. S11 가드 블록 첫 줄에 쓰이지 않는 `DECLARE @v_strReqYMD VARCHAR(8) = '';` — 원본 변수 흔적. 조건에는 안 쓰인다(B10 류 「변수」 기계 검사 후보의 표본).

## 나란히

| | B13 | B14 | B15 | **B16** |
| :-- | --: | --: | --: | --: |
| 단계 수 | — | 16 | 19 | **21** |
| 발급 단계 · 발급 전 단계를 요구한 게이트 | S02 · 2 | S01 · — | S02 · 0 | **S03 · 0** |
| 가드 조건 표 | 전 | 전 | 전 | **실림** |
| 가드 발화(판 안) | 1 | 2 | 2 | **0** |
| CMRate 가드 PLTID 조건 | `13` | `'POQ'` | `15` | **없음** |
| 하한 재생성(로그 「하한 검사를 통과하지 못해 다시 생성」 줄) | — | — | 4 | **16**(전부 이름 블록 미정의) |
| 벽시계 · 청구 | 41 분 · $7.19 | 27 분 · $5.47 | 29 분 · $5.81 | **48 분 · $8.20** |
| 채택 점수 | 94 | 94 | 94 | **96** |

B15 판독(§판 안 단계 하한 재생성)은 「5」라 적었지만 같은 로그 문구로 세면 4(S03 · S04 · S06 · S09)다 — 위 표는 두 판을 같은 자로 잰 값이다.
