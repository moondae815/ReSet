# K2 쓰기 수집기가 CTE 안 `UNION ALL` 리터럴 이름에 눈멀다 (사전 선언)

**작성 시점**: 2026-09-17, **구현 전**. **코드**: main `9cbce8e4` + 이 문서 커밋. 브랜치 `fix/k2-cte-union`(격리 워크트리).
**출처**: B20 판독(`2026-09-17-GPT-2회판-Batch20-사전선언.md` §발견 1)의 처방 후보와 합격선 ①~⑤.

## 0. 착수 전에 잰 것

### 0-1. 분모 — B20 판독의 「20 중 12」는 관할이 `raw/attempts/run-001/steps` 다

같은 수집기(`ControlTotalNameFacts.Collect`, 리플렉션 · 표 이름은 제품과 같이 `BatchControlContract.Tables` 에서 `ControlName` 칸이 있는 표와 별칭 → `BatchControlTotal`·`ControlTotal`)로 두 관할을 쟀다.

| 관할 | Job | 제어 합계를 쓰는 단계 | 앎 | 모름 |
| :-- | --: | --: | --: | --: |
| `raw/attempts/run-001/steps`(B20 판독) | 13 | 20 | 8 | 12 |
| `agent/steps`(배송 단계 번들, 이 문서) | 16 | **22** | **9** | **13** |

차이는 run-001 이 없는 옛 판 둘이다 — B1 S16(모름) · B4 S16(앎). 두 값은 모순이 아니라 관할이 다르다. **이하 이 문서의 수는 `agent/steps` 22 기준이다.**

### 0-2. 「모름」 13 의 모양 전수 분류 — 원문을 열어 봤다

B20 판독은 「12 중 9 는 본문에 `UNION ALL` 이 있다(grep) · 나머지 셋은 모양 미측정」이라고 적었다. 열어 보니 grep 9 중 **CTE-UNION 쓰기는 6** 이고, 미측정 셋은 전부 매개변수 헬퍼였다.

| 모양 | 수 | 단계 | 이 처방 뒤 |
| :-- | --: | :-- | :-- |
| **A. CTE 안 `UNION ALL` 가지마다 리터럴 이름 → `INSERT … SELECT [한정자.]ControlName FROM <CTE>`** | **6** | B14 S15 · B15 S11 · B15 S18 · B17 S20 · B19 S20 · B20 S11 | **앎**(겨냥) |
| B. 매개변수 헬퍼(`VALUES (…, @p_controlName, …)` · `SELECT @p_controlName … WHERE NOT EXISTS`) | 5 | B1 S16 · B10 S17 · B12 S17 · B16 S19 · B18 S19 | 모름 유지(정당 — 이름이 호출부에 있다) |
| C. CTE-UNION 위의 **런타임 조합**(`MetricName + N'.Expected'` · `+ N'.Actual'`) | 1 | B13 S18 | 모름 유지(정당 — K2 세트 사전 선언 R3 의 그 실물) |
| D. **파싱 실패**(`COUNT_BIG(*) AS RowCount` — `ROWCOUNT` 예약어, 펜스 13 행 구문 오류) | 1 | B19 S12 | 모름 유지(범위 밖 — 이름 자체는 직접 `UNION ALL` 리터럴이라 파싱만 되면 이미 앎이다) |

모양 A 의 여섯에서 `ControlName` 참조는 한정자 있음 넷(`C.`·`M.`) · 없음 둘(B15 S18 · B19 S20) 이다. B19 S20 의 CTE 는 다른 CTE 셋을 앞에 두고, 가지 일부가 `FROM PostBillingSource` 처럼 **다른 CTE 에서 값을** 끌어오지만 이름 자리는 전부 리터럴이다.

**D 는 SQL 자체의 결함일 수 있다**(`RowCount` 를 따옴표 없이 별칭으로 쓰면 SQL Server 에서도 구문 오류다). 이 회차는 기록만 한다.

### 0-3. 비대칭 — 읽기 쪽은 이미 이 모양을 푼다

`Visitor.LiteralNames`(조인으로 끌어온 이름 · B13 V04 처방)는 **같은 문장 CTE 의 모든 가지가 `ControlName` 위치에 리터럴**이면 이름을 낸다. 쓰기 쪽 `ReadInsert` 의 `ColumnReferenceExpression` 가지는 `SourceInlineTables → ResolveInline` 으로 **인라인 `VALUES` 표만** 따라가서, 출처가 CTE 여도 그 CTE 안의 `VALUES` 만 찾고 `UNION ALL` 리터럴 SELECT 는 못 본다. 같은 모양을 읽기로는 알고 쓰기로는 모른다.

### 0-4. 새로 「앎」이 될 소유자를 누가 읽는가 — 배송본 발화 예측

모양 A 여섯의 소유자 몫(`StepCode = N'Sxx'`)을 읽는 자리를 배송본(`agent/steps` · `agent/verification` · `docs/BatchMigrationPlan.md`)에서 찾았다.

| 소유자 | 다른 자리의 읽기 | 이름 | 예측 |
| :-- | :-- | :-- | :-- |
| B14 S15 · B15 S18 | 없음 | — | 0 |
| B15 S11 · B17 S20 | 자기 단계의 `NOT EXISTS` 자기 대조뿐(이름 리터럴 없음 → 재료 `Read` 아님) | — | 0 |
| B19 S20 | 검증 세트 `StepCode = N'S20' GROUP BY ControlName`(이름 리터럴 없음) | — | 0 |
| B20 S11 | **배송본 S18** — `IN (N'LedgerRowCount', …, N'ForeignSettleAmtSum')` 8 이름 | S11 이 쓰는 8 과 **전부 일치** | 0 |

**그래서 이 처방의 배송본 효력은 0 으로 예측한다.** 효력의 근거는 배송본이 아니라 **B20 1 회차 시도본**이다(§1).

## 1. 실물 — B20 1 회차

로그 `output/logs-planonly-POQSettleBatch20/reset-20260917.log`(gitignore, 이 머신에만 있다)의 Consolidator 응답 본문:

| 행 | 단계 | 비고 |
| --: | :-- | :-- |
| 1358 | S11 | 유일한 S11 응답 · `SQL_INSERT_FROZEN_LEDGER_TOTALS`(모양 A · 8 이름) |
| 1975 | S18 | **1 회차** · `SQL_READ_S11_LEDGER_TOTALS` — S11 몫을 `LedgerTxAmt`·`LedgerCLTotal`… 로 읽음(교집합 `LedgerRowCount` 하나) |
| 19454 | S18 | 2 회차(배송본) · 8 이름 일치 |

픽스처는 이 응답의 `message.content` 를 **바이트 그대로 오린다**(지어내지 않는다). 교집합 0 변이는 S11 의 `N'LedgerRowCount'` **한 리터럴만** 다른 이름으로 바꾼다 — 변이 자리와 이유를 픽스처 파일에 적는다.

## 2. 설계

`ReadInsert` 의 `SelectInsertSource` 가지에서, `ControlName` 자리가 `ColumnReferenceExpression` 이고 인라인 `VALUES` 로 안 풀리면:

1. 열의 출처를 기존 규칙대로 고른다 — 한정자가 가리키는 출처, 한정자가 없으면 FROM 의 **유일한** 출처.
2. 그 출처가 **같은 문장 CTE**(스키마 없는 이름)면, CTE 열 목록 또는 첫 가지의 별칭·열 이름에서 **그 열의 위치**를 찾는다(`LiteralNames` 와 같은 규칙 — 새로 짓지 말고 열 이름을 받게 넓혀 공유한다).
3. `Flatten` 한 **모든 가지**의 그 위치가 `StringLiteral` 이면 이름으로 담는다. **하나라도 아니면 「모름」**(모양 C 가 여기서 걸린다 — `BinaryExpression`).

**하지 않는 것**: CTE 가 다른 CTE 의 열을 이름 자리로 끌어오는 연쇄(한 겹만 푼다) · 파생 표(`FROM (SELECT …) AS X`) · 모양 D 의 파싱 실패 · MERGE. 규칙 ①~⑤ 불변 — **⑤(교집합이 비었을 때만)도 그대로다.** 틀리는 방향은 계속 「덜 보고한다」.

**B20 실물은 이 처방으로도 안 잡힌다** — 1 회차 교집합이 1 이다. 교집합 0 변이에서만 발화한다. 「부분 겹침」 규칙은 따로 사전 선언한다(정당한 부분 비교와 가르는 오라클이 필요하다).

**캐시 버전**: 프롬프트 불변 · L1 재료만 넓힌다 → 올리지 않고 §3 C6 스윕 차분을 근거로 적는다(`reset-l1-check` 선례). main 값 22.

## 3. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| C1 | 실물이 열린다 | B20 1 회차 S11(1358)·S18(1975) **교집합 0 변이**에서 K2 단계 발화 **1** · 메시지의 쓰는 이름이 S11 원문 가지 8(변이 반영)과 일치 |
| C2 | ⑤ 유지 | **변이 없는** 1 회차 쌍(교집합 1)은 발화 **0** |
| C3 | 모양 A 가 정확히 열린다 | `agent/steps` 22 에서 **앎 9 → 15 · 모름 13 → 7** · 새 앎이 §0-2 의 여섯과 **정확히** 같고 단계마다 이름 수 = 원문 가지 수 |
| C4 | 정당한 모름 유지 | 모양 B 5 · C 1 · D 1 은 모름 그대로 · **가지 하나가 비리터럴인 CTE-UNION**(B13 S18 실물)은 모름 |
| C5 | 되돌림 | (n1) 새 CTE 가지를 걷어내면 C1·C3 빨강 · (n2) 「모든 가지가 리터럴」 가드를 「리터럴인 가지만 담는다」로 바꾸면 C4 빨강 — **n2 를 가르는 입력은 모양 C 실물이어야 한다**(합성 픽스처만으로 재지 않는다) · (n3) 한정자 없는 경로(B15 S18 · B19 S20)를 끊으면 빨강 |
| C6 | 오탐 0 · 차분 | 배송본 16 Job(단계 번들 + 제품 조립기로 합친 문서)과 `raw/attempts` 시도 조립본에서 K2 단계·K2 세트 발화를 **고치기 전후로** 잰다 — 배송본 차분 **0**(§0-4 예측) · 새 발화는 **전부 원문으로 판정**, 오탐 0 |
| C7 | 옆 검사 불변 | 단계 스윕 발화량 차분 **0**(이 재료를 쓰는 검사는 K2 둘뿐이어야 한다 — 호출부 `MechanicalValidator` 1118 · 1219 를 먼저 확인) |
| C8 | 게이트 | 실패 0 · 건너뜀 0 · 경고 0(`dotnet build --no-incremental`) · `CorpusPathsTests` 다섯 도는 것 먼저 확인 |

**예측을 틀리게 만들 수 있는 것**: (a) 모양 A 여섯 중 CTE 열 위치를 첫 가지에서 못 찾는 것이 있으면 C3 가 15 에 못 미친다 — 그때 규칙을 넓히지 말고 원문 모양을 적는다 · (b) 시도 조립본에서 새 발화가 나면 그 쌍이 진짜 교집합 0 인지 원문으로 본다 — 진짜면 이 처방의 **두 번째 실물**이다.

## 4. 한 벌 여섯 (착수 전 기록)

| # | 물음 | 답 |
| :-- | :-- | :-- |
| 1 | 행 수 | 관할 22 중 모양 A **6** |
| 2 | 도달 가능성 | 여섯 모두 펜스 파싱 성공(구문 오류 0) · `InsertStatement` 방문 경로에 있다 |
| 3 | 그 자를 쓰는가 | K2 단계는 `CollectControlTotalFacts` → `Collect` 의 `Write.Unknown` 으로 소유자를 거른다 — 이 가지가 바뀌면 대조 대상이 바뀐다 |
| 4 | 발화 | 배송본 0 예측 · 1 회차 변이 1(C1) · 시도 조립본은 잰다 |
| 5 | 오탐 다수 아님 | 새 발화 전부 원문 판정(C6) |
| 6 | 없앴을 때 | C5 n1~n3 |

**우연히 사 주던 커버리지**: 지금 「모름」은 여섯 소유자를 대조에서 **빼 주고** 있다 — 그 여섯의 이름이 옳게 쓰였는데 읽는 쪽이 정당한 부분 집합만 읽는 자리가 있으면 ⑤ 가 막는다(교집합 ≠ 0). §0-4 로 배송본에서는 그런 자리가 없음을 확인했다.

---

# 결과 (구현 `36efd6c0`)

## 합격선

| # | 항목 | 실측 | 판정 |
| :-- | :-- | :-- | :-- |
| C1 | 실물이 열린다 | B20 1 회차 S11(로그 1358)·S18(로그 1975) 교집합 0 변이에서 발화 **1** · 귀속 **S18** · 메시지의 쓰는 이름이 변이 반영 8(`FrozenRowCount`·`TxAmtSum`…) | ✔ |
| C2 | ⑤ 유지 | 변이 없는 쌍 발화 **0** | ✔ |
| C3 | 모양 A 가 정확히 열린다 | `agent/steps` 22 에서 **앎 9 → 15 · 모름 13 → 7** · 새 앎이 §0-2 의 여섯과 정확히 같다 · 이름 수 14·5·17·24·27·8 이 원문 CTE 의 리터럴 가지 수와 **여섯 모두 일치**(B14 S15 는 가지 안 스칼라 부질의까지 SELECT 가 28 이고 리터럴 가지는 14) | ✔ |
| C4 | 정당한 모름 유지 | 남은 7 이 §0-2 의 B 5 · C 1 · D 1 과 정확히 같다 · 가지 하나를 매개변수로 바꾼 변이는 침묵 | ✔ |
| C5 | 되돌림 | 아래 표 | ✔ |
| C6 | 오탐 0 · 차분 | 배송본 16 Job + 디스크 시도 조립본(`raw/attempts/run-*`, 11 Job, 제품 조립기 `BatchPlanAssembler.Assemble`) — K2 단계 **2 → 2** · K2 세트 **8 → 8** · 출력 바이트 **동일**(§0-4 예측대로). 기준선은 격리 워크트리 `9cbce8e4` 빌드 | ✔ |
| C6′ | 차분 0 의 양성 대조 | 같은 하네스에 B20 배송 번들을 복사하고 S11·S18 만 1 회차 변이 쌍으로 갈아 끼웠다 — 기준선 **0** · 고친 뒤 **1**(S18). 하네스가 이 변화를 **볼 수 있는데** 코퍼스에서 0 이다 | ✔ |
| C7 | 옆 검사 불변 | `--sweep` 을 기준선·고친 워크트리에서 각각 돌려 보고서 차분 — **커밋 줄 하나만** 다르다(A 0 · B 2 · C 1 · D 0 · E 0 · P 2 · 미분류 23 · 측정 쌍 306). `Collect` 호출부는 K2 둘뿐(`MechanicalValidator` 1118 · 1219) | ✔ |
| C8 | 게이트 | `dotnet build --no-incremental` 경고 0 · 오류 0 · 시험 **4563 통과 · 실패 0 · 건너뜀 0**(B20 의 4556 + 새 시험 7) | ✔ |

## 되돌림 (K2 시험 43 개 기준)

| 뮤턴트 | 빨강 |
| :-- | :-- |
| n1 새 CTE 가지 끄기 | 4 — C1 · C3 셋 |
| n2 비리터럴 가지를 건너뛰고 리터럴 가지만 담기 | 2 — `CteUnionWriterWithOneNonLiteralBranch_IsUnknown` · **읽기 쪽 기존 `JoinedNameSourceWithANonLiteralName_IsSilent`**(판정을 `CteLiteralColumn` 하나로 공유한 것이 드러난다) |
| n3 한정자 없는 경로 끊기 | 2 — C3 의 B15 S18 · B19 S20 |
| n4 `_ctes` 를 `ReadInsert` 뒤에 채우기(방문 순서) | 4 — n1 과 같은 넷 |

## 선언과 달라진 것

1. **C5 n2 의 「모양 C 실물(B13 S18)로 가른다」는 틀렸다.** B13 S18 의 조합 `MetricName + N'.Expected'` 는 **CTE 가지가 아니라 INSERT 의 SELECT 자리**에 있어서, 새 가지에 닿기 전에 종전 `default` 가 「모름」으로 만든다 — n2 를 못 가른다. 코퍼스 22 에서 「CTE 가지 일부만 리터럴」인 쓰기는 **0** 이다(C3·C4 의 분류가 전수). 그래서 n2 는 **실물 B20 S11 의 가지 하나를 매개변수로 바꾼 변이**로 갈랐다. B13 S18 시험은 회귀 방지로 남겼고 주석에 판별력이 없음을 적었다.
2. C6 의 「시도 조립본」은 디스크에 남은 **단계마다 마지막 시도**뿐이다. B20 1 회차 S18 처럼 판 안에서 덮인 시도는 로그에만 있고, 그 하나는 C1 픽스처로 옮겼다. 다른 판의 로그 속 중간 시도는 재지 않았다.

## 남은 것

- **B20 실물(교집합 1)은 여전히 조용하다** — 「부분 겹침」 규칙은 따로 사전 선언한다.
- 모양 D(B19 S12 `AS RowCount`)는 SQL 구문 결함 후보다. 이 검사의 일이 아니다.
- 모양 A 는 여섯 모두 배송본에서 읽는 쪽 이름이 맞거나 이름 리터럴이 없어, 이 처방의 **판 안 효력은 다음 판에서야 표본이 생긴다.**
