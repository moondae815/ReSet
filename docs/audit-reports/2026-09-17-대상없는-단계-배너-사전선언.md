# 아무것도 쓰지 않는 단계가 「검증 불가」로 배송된다 (사전 선언)

**작성 시점**: 2026-09-17, **구현 전**. **코드**: main `fc5d3308` + 이 문서 커밋.

## 0. 잰 것

B18 판독 §발견 3 의 자리다. 배송본과 목차를 전수로 맞춰 봤다.

| Job | 빈 `TargetTables` 단계 | 그 단계 본문(SQL 펜스) | 쓰기 키워드 | `INTO` | `EXEC` | 동적 SQL | 배송 배너 |
| :-- | :-- | --: | --: | --: | --: | --: | :-- |
| B11 | S01 | 3 | 0 | 0 | 0 | 0 | 검증 불가 |
| B12 | S01 | 2 | 0 | 0 | 0 | 0 | 검증 불가 |
| B13 | S01 | 3 | 0 | 0 | 0 | 0 | 검증 불가 |
| B15 | S01 | 2 | 0 | 0 | 0 | 0 | 검증 불가 |
| B16 | S01 | 1 | 0 | 0 | 0 | 0 | 검증 불가 |
| B17 | S01 | 3 | 0 | 0 | 0 | 0 | 검증 불가 |
| B18 | S01 | 4 | 0 | 0 | 0 | 0 | 검증 불가 |

**일곱 판 전부** 한 단계(S01, 읽기 전용 사전 점검)이고 **일곱 다 아무것도 쓰지 않는다.** B1~B10·B14 에는 빈 `TargetTables` 가 없다.

판정 자리는 `MechanicalValidator.ValidateBatchStep`(`:507`)이다 — `TargetTables` 에 이름이 하나도 없으면 **`PlanDefects`** 에 담고, 오케스트레이터가 `RegenerationCanFix == false` 로 읽어 「목차 결함으로 하한 검사를 실행할 수 없습니다 - 재생성으로 고쳐지지 않아 건너뜁니다」로 건너뛴 뒤 **「검증 불가」 배너**를 붙인다. 스윕에서는 그 메시지가 검사 A~E·P 어디에도 안 맞아 **미분류**로 센다(현재 30 중 일곱이 이 사유로 추정 — 고침 뒤 차분으로 확인한다).

**지금 코드가 그렇게 정한 근거도 코드에 있다**(`:517` 주석): 「출신이 없다는 것과 쓰는 테이블이 없다는 것은 다른 사실이고, **아무것도 쓰지 않는다는 선언은 그 자체로 확인이 필요하다**」. 이 회차는 그 「확인」을 **사람 배너에서 기계 판정으로 옮긴다** — 본문이 쓸 수 있는지를 파서로 보면 확인이 자동으로 닫힌다.

## 1. 설계 — 「쓸 수 있는가」를 파서로 묻는다

빈 `TargetTables` 를 `PlanDefects` 로 드는 조건에 한 가지를 더한다: **그 단계 본문이 쓸 수 있을 때만** 든다.

- **쓸 수 있다**의 판정(보수적으로 넓게): SQL 펜스를 ScriptDom 으로 읽어 `INSERT`·`UPDATE`·`DELETE`·`MERGE`·`TRUNCATE`·`SELECT … INTO`·`EXEC(UTE)`(프로시저 호출·동적 SQL 포함) 가 **하나라도** 있으면 쓸 수 있다. **파싱에 실패한 펜스도 「쓸 수 있다」로 센다**(모르면 결함을 유지한다 — 느슨해지는 방향으로 기울지 않는다).
- 쓸 수 없으면 `PlanDefects` 에 담지 않고 **`Log.Information` 한 줄**만 남긴다. 바로 아래 `ErrorCodes` 축이 이미 쓰는 관례다(`:531` — 「흔적까지 지우면 대조 항목 0 과 대조해서 깨끗함이 구별되지 않는다」).
- 그 결과: 그 단계는 다른 하한 검사를 **정상적으로 통과**하고(대상 표 대조 루프는 0 회 돌 뿐이다), 「검증 불가」 배너가 붙지 않으며, 스윕 미분류에서 그 행이 사라진다.
- **쓰는데 선언이 없으면 종전대로 결함이다.** 그 경우 어느 표를 빠뜨렸는지는 이미 T36(목차 밖 쓰기, 계약 표 면제)이 따로 말한다.

**범위 밖**: `ErrorCodes` 축(레거시 출신이 있을 때만 드는 기존 규칙 그대로) · 배너 문구 자체 · 목차 계약에 「대상 없음」 신호를 새로 만드는 것(빈 배열이 이미 그 신호이고, 이 회차는 그 신호를 **믿을 근거**를 본문에서 만든다).

## 2. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| R1 | 실물 침묵 | B18 S01(쓰기 0)에서 `PlanDefects` **0** · 그 단계가 하한 검사를 통과 · 「검증 불가」 사유에서 사라진다 |
| R2 | 쓰면 결함 유지 | 같은 S01 본문에 실물 `INSERT` 펜스를 **자리만 옮겨** 붙이면 `PlanDefects` **1**(종전과 같은 메시지) |
| R3 | `EXEC`·동적 SQL 도 쓰기로 센다 | `EXEC`·`sp_executesql` 이 있으면 결함 유지(실물 펜스로 시험) |
| R4 | 파싱 실패는 보수적으로 | 깨진 SQL 펜스가 있으면 결함 유지 |
| R5 | 되돌림 | 쓰기 판정 · 파싱 실패 보수 · `EXEC` 포함 세 갈래마다 걷어내면 빨강 |
| R6 | 코퍼스 전수 | 빈 `TargetTables` 일곱 자리 **전부** 결함 0 으로 바뀐다 · **다른 단계·다른 검사 발화 변화 0** · 단계 스윕 미분류 **30 → 23**(예측, 차분으로 확인) |
| R7 | 게이트 | 실패 0 · 건너뜀 0 · 경고 0 |

**오탐 방향**: 이 변경은 발화를 **줄인다**. 놓침 위험은 R2·R3·R4 가 막는다 — 「쓸 수 있다」를 넓게 잡고 모르면 결함을 유지한다.
**효력 판정**: 판을 새로 돌리지 않는다. R1·R6 이 근거이고, 다음 판에서는 배너가 둘에서 하나로 줄어드는 것으로 보인다.

## 결과

| # | 항목 | 실측 | 판정 |
| :-- | :-- | :-- | :-- |
| R1 | 실물 침묵 | B18 S01(읽기 전용) `PlanDefects` **0** · 그 단계가 하한 검사를 **통과**한다(건너뜀이 아니다) | ✔ |
| R2 | 쓰면 결함 유지 | 같은 본문에 B18 S02 의 실물 잠금 `INSERT` 펜스를 **자리만 옮겨** 붙이면 결함 **1**(종전 메시지 그대로) | ✔ |
| R3 | `EXEC`·동적 SQL | **B1 의 공통 계약 템플릿**(`agent/common/01-step-contract.md`)에 있는 실물 `sp_executesql` 펜스를 붙이면 결함 **1** — 개별 Job 단계 본문이 아니라 배치 간 공유 템플릿에서 왔다(리뷰 Minor) | ✔ |
| R4 | 파싱 실패는 보수적으로 | 깨진 펜스가 있으면 결함 **1** | ✔ |
| R5 | 되돌림 | **다섯 전부 빨강** — q1 쓰기 판정 제거(항상 결함) · q2 반전(항상 침묵) · q3 파싱 실패 보수 제거 · q4 `EXEC` 제외 · q5 `INSERT` 제외 | ✔ |
| R6 | 코퍼스 전수 | 단계 스윕 미분류 **30 → 23**(−7, 예측과 일치) · 사라진 행: B13·B15·B16·B17·B18 각 1 이 통째로, B11 4→3 · B12 3→2 · **검사 A·B·C·D·E·P 발화 전부 불변**(0·2·1·0·0·2) | ✔ |
| R7 | 게이트 | 통과 **4534** · 실패 0 · 건너뜀 0 · 경고 0 | ✔ |

**의미 변경 하나**: 기존 시험 `GenerateBySplitAsync_SplitProcedureObligationDefect_MergesWithExistingStepFloorViolation` 이 「검증 불가 + 품질 미달」 **병합**을 재면서 그 검증 불가 사유를 `TargetTables` 문자열로 고정하고 있었다. 그 시험의 S01 스텁 본문이 `SELECT 1;`(읽기 전용)이라 이제 그 축은 결함이 아니므로, 같은 병합을 `ErrorCodes` 사유로 재게 바꿨다(축을 바꾼 이유를 주석에 적었다). 병합 자체의 커버리지는 그대로다.

**남은 한계**: 이 판정은 **SQL 펜스만** 본다. 의사코드(C#) 안에서 리포지터리 호출로 쓰는 단계는 SQL 펜스가 없으면 「쓸 수 없다」로 읽힌다 — 코퍼스의 일곱 자리는 전부 SQL 펜스를 갖고 있어(18 개) 이 한계에 걸리지 않았고, 그 모양이 실물로 나오면 그때 가르는 시험을 붙인다.

## 최종 리뷰 반영 (`6840be77` 범위)

Critical **0** · Important **2** · Minor **2**. 리뷰가 독립으로 재현한 것: 미분류 **30 → 23** 과 사라진 행의 자리 · 검사 A~E·P 불변(0·2·1·0·0·2) · 되돌림 q1~q5 다섯 전부 빨강 · 픽스처 셋의 바이트 대조 · 코퍼스 펜스 태그 히스토그램(```sql 5,487 · ```pseudocode 671 · ```csharp 374 · 다른 SQL 태그 **0**).

| 등급 | 지적 | 조치 · 내가 다시 잰 것 |
| :-- | :-- | :-- |
| **Important 1** | 의사코드(C#) 펜스에서만 쓰는 단계는 이 판정이 영구히 못 본다 — 놓침 방향이고, **그 한계를 재는 시험이 하나도 없다** | 한계를 **시험으로 못박았다**(`KnownLimit_AWriteOnlyInAPseudocodeFence_IsNotSeen`) — 언젠가 의사코드까지 읽게 되면 이 시험이 빨개지고, 그때 기대를 바꾸는 것이 의도된 절차다. 코드를 지금 넓히지 않는 근거는 리뷰가 잰 태그 분포와 일곱 자리 전부 SQL 펜스를 가진다는 실측이다 |
| **Important 2** | `TruncateTableStatement`·`SELECT … INTO`·`ExecuteAsStatement` **셋을 동시에 걷어내도 시험 여섯이 초록**이었다 | 실물을 찾으러 갔다 — ScriptDom 으로 코퍼스 SQL 펜스 **5,514 개** 전수를 읽으니 세 모양 모두 **0 건**이다(B11 의 `INTO` 는 `INSERT … INTO` 였다). 실물이 없어 **최소 SQL 을 직접 쓴 합성 시험**을 더하고 합성임을 주석·README 에 적었다 → **되돌림 q6(셋 동시 제거) 빨강**(3 실패) |
| Minor 1 | 픽스처 디렉터리에 README 가 없다 · `exec-fence.md` 출처가 「B1 의 실물 펜스」로 뭉개져 있다(실은 공통 계약 템플릿) | README 를 썼고(출처·자리·합성 재료 표기) 위 R3 줄도 정정했다 |
| Minor 2 | 새 주석 아래의 기존 주석(「아무것도 쓰지 않는다는 선언은 그 자체로 확인이 필요하다」)이 이번 변경과 모순되게 읽힌다 | 그 자리에 한 줄을 붙였다 — 그 「확인」은 위 분기가 파서로 닫고, ErrorCodes 축은 그 판정을 쓰지 않는다 |
| (리뷰 미확인) | 게이트 4534 전체 | 반영 뒤 **4538 통과 · 실패 0 · 건너뜀 0 · 경고 0** 으로 다시 돌렸다 |
