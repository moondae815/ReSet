# 이름 있는 SQL 블록 검사가 공통 규약의 정의를 인정한다 (사전 선언)

**작성 시점**: 2026-09-14, **구현 전**. **코드**: main `75d21e95`.

## 0. 문제

`CheckUndefinedSqlBlockReference`(단계 하한 검사)는 단계가 `execute(SQL_X, …)` 로 부른 이름의 정의(`-- SQL_X`)를 **그 단계 절 안의 SQL 펜스에서만** 찾는다.
그런데 골격의 공통 규약(`BatchPlanAssembler.ExtractSharedConventions` — 단계 섹션 생성 요청에 그대로 실리고, 배송 번들 `agent/common/01-step-contract.md` 가 된다)이 저널·RunId 블록을 한 번 정의하고 모든 단계가 부르는 것이 설계다.
검사는 그 정의를 못 봐서, 호출만 한 단계를 「정의 없음」으로 재생성시키고 재생성은 공통 블록을 절마다 베껴 닫힌다.

- POQSettleBatch16: 하한 재생성 16 이 전부 이 사유 · 베낀 사본 45(16·14·15)이 공백 정규화 후 공통 정의와 **전부 같다**(`2026-09-14-GPT-2회판-Batch16-사전선언.md` §발견 1).

## 1. 측정 (구현 전, 자는 검사와 같은 정의 규칙 — SQL 펜스 안 · 호출부를 지운 뒤에도 이름이 남는 줄 · 별표 접두사)

### 1-1. 판 안 발화 — 요청 본문이 비순환 기준

OpenRouter 판은 단계 섹션 요청 본문이 로그에 남는다. 발화마다 **그 단계가 받은 요청 본문**에서 부른 이름의 정의를 찾았다.

| Job | 발화 | 부른 이름 전부가 요청 본문(공통 규약)에 정의됨 |
| :-- | --: | --: |
| POQSettleBatch11 | 2 | 2 |
| POQSettleBatch13 | 2 | 2 |
| POQSettleBatch15 | 1 | 1 |
| POQSettleBatch16 | 16 | 16 |
| **계** | **21** | **21** |

claude-cli 판(B7~B10)은 요청 본문이 로그에 없고, 디스크의 `raw/attempts/run-*/skeleton.md` 는 마지막 시도의 골격이라 발화 시점 골격과 다를 수 있다(B8 은 디스크 골격에 `SQL_CURRENT_RUN_ID` 정의가 없는데 배송 공통 규약에는 있다). 그래서 이 판들은 판정하지 않는다.

### 1-2. 배송본 — 스윕과 같은 입력(`agent/steps` + `agent/common/01-step-contract.md`)

오늘 스윕의 이 검사 발화 16(전부 「미분류」 칸):

| 분류 | 수 | 자리 |
| :-- | --: | :-- |
| 부른 이름 전부가 공통 규약에 정의 — **침묵해야 한다** | **9** | B1 S02·S11·S12·S14 · B7 S03 · B8 S09·S10 · B10 S08·S15 (전부 `SQL_CURRENT_RUN_ID` · B7 S03 은 `SQL_CREATE_AND_CAPTURE_SHADOW` 포함) |
| 공통 규약에도 없는 이름이 남음 — **계속 발화, 남은 이름만 지목** | **7** | 지목 이름 수 전→후: B1 S08 19→18 · S09 9→8 · S13 9→8 · S15 3→2 · B5 S08 4→4 · S09 4→4 · S10 8→8 |

B1·B8·B10 의 `SQL_CURRENT_RUN_ID` 공통 정의는 `-- SQL_CURRENT_RUN_ID` 뒤 `SELECT RunId FROM batch.BatchRun …` 인 실제 SQL 블록이다(눈으로 확인). 남은 항목 「claude-cli `SQL_CURRENT_RUN_ID`」가 이 오탐이었다.

## 2. 설계

1. `ValidateBatchStep` 에 선택 인자 `sharedConventions`(null 이면 종전 동작). `CheckUndefinedSqlBlockReference` 는 정의를 **단계 절의 SQL 펜스 + 공통 규약의 SQL 펜스**에서 찾는다. 정의 규칙(호출부 줄 제외 · 별표 접두사)은 두 텍스트에 똑같이 적용한다.
2. 호출 수집은 단계 절에서만 한다 — 공통 규약이 부르는 이름은 이 단계의 책임이 아니다.
3. 오류 문구·처방은 그대로(남는 이름은 여전히 이 절에 정의해야 할 이름이다).
4. 배선: 오케스트레이터의 단계 검증 호출이 단계 생성에 쓴 그 `conventions` 를 넘긴다. 스윕은 `SweepJob.SharedConventions` 로 받고, `SweepCommand` 가 배송 번들 `agent/common/01-step-contract.md` 를 읽는다(배송 단계 파일과 짝이 맞는 유일한 공통 규약 — 디스크 골격은 §1-1 처럼 어긋날 수 있다).
5. 프롬프트 불변 · L1 만 느슨해짐 → 캐시 버전 올리지 않음(스윕 차분으로 대신한다).

## 3. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| N1 | 침묵 — 실물 | B16 S04 **첫 초안**(로그 응답 본문에서 오림) + B16 골격(`ExtractSharedConventions`): 공통 규약 없이 이 검사 발화 1(세 이름) · 공통 규약과 함께 0 |
| N2 | 남은 이름만 — 실물 | B1 S08 배송본 + B1 `01-step-contract.md`: 발화 1, 지목 이름 19 → **18**(`SQL_CURRENT_RUN_ID` 빠짐, `SQL_U1_…`~`SQL_U5_…` 남음) |
| N3 | 호출부는 정의가 아니다 | N2 의 공통 규약에서 `-- SQL_CURRENT_RUN_ID` 정의 줄만 지우면 `SQL_CURRENT_RUN_ID` 가 다시 지목된다(공통 규약 의사코드의 `queryScalar(SQL_CURRENT_RUN_ID` 호출은 정의가 아님) |
| N4 | 재료 없음 = 종전 | `sharedConventions` null · 빈 문자열이면 N1 첫 초안이 종전대로 발화 |
| N5 | 배선 | 오케스트레이터 `ValidateBatchStep` 호출이 `sharedConventions: conventions` 를 넘긴다(소스 잠금) · `StepSweepService` 가 `SweepJob.SharedConventions` 를 넘긴다(동작 시험) · `SweepCommand` 가 `01-step-contract.md` 를 읽는다(소스 잠금) |
| N6 | 되돌림 | 공통 규약 정의 탐색 · 오케스트레이터 배선 · 스윕 서비스 배선 · 스윕 명령 배선을 하나씩 걷어내면 해당 시험 빨강 |
| N7 | 스윕 | 이 검사 발화 16 → **7**(§1-2 의 9 자리만 사라짐) · 남은 7 의 지목 이름 수가 §1-2 와 같다 · 다른 검사·Job 행 변화 0 |
| N8 | 게이트 | 경고 0 · 실패 0 · 건너뜀 0 · `CheckGuardPolicyTests` 자리 수 불변(새 `SafeCheck` 없음) |

**오탐 방향**: 이 변경은 발화를 줄이기만 한다 — 새 거짓 양성은 원리상 없다. 대신 **놓침**(공통 규약에 정의가 있다는 이유로 진짜 결함을 침묵)이 위험이다: 공통 정의가 있으면 구현자가 번들에서 그 SQL 을 읽을 수 있으므로 「구현 불가」라는 이 검사의 존재 이유가 성립하지 않는다 — 놓침이 아니다. 공통 정의와 절 안 사본이 **다른** 경우는 이 검사의 관할 밖이다(기록).

## 정정 (구현 중, 합격선 커밋 `440bffc0` 뒤)

§1-2 의 「남은 이름 수」(5·7·8·2·4·4·8)와 N2 의 「지목 이름 5」는 **230 자로 잘린 스윕 메시지에서 센 값**이었다 — N2 시험이 18 을 내서 드러났다.
단계 파일에서 직접 다시 재면 위 표의 전→후 값이다. 발화 자리(16 → 7, 사라지는 9 자리)는 잘림과 무관해 그대로 맞다. 판 안 발화(§1-1)는 로그의 잘리지 않은 줄에서 셌다.

## 결과

| # | 결과 |
| :-- | :-- |
| N1 | B16 S04 첫 초안: 공통 규약 없이 발화 1(`SQL_INSERT_STEP_START` · `SQL_MARK_STEP_SUCCEEDED` · `SQL_MARK_STEP_FAILED`) · B16 골격 공통 규약과 함께 **0** ✔ |
| N2 | B1 S08: 발화 1 · 지목 이름 **18**(`SQL_CURRENT_RUN_ID` 빠짐, `SQL_U1_…`~`SQL_U5_…` 순서대로 남음) ✔ |
| N3 | 공통 규약에서 `-- SQL_CURRENT_RUN_ID` 정의 줄만 지우면 다시 지목 ✔ |
| N4 | null · 빈 문자열이면 종전대로 발화 ✔ |
| N5 | 오케스트레이터 소스 잠금 · 스윕 서비스 동작 시험 · 스윕 명령 소스 잠금 ✔ |
| N6 | 되돌림 4 — 공통 규약 정의 탐색 제거(N1·N2·스윕 서비스 3 빨강) · 오케스트레이터 배선(1) · 스윕 서비스 배선(1) · 스윕 명령 배선(1) ✔ |
| N7 | 스윕(`sweeps/2026-09-14-step-sweep-named-block.md`, 기준 `…-batch16.md`): 미분류 82 → **73** — B1 16→12 · B7 3→2 · B8 14→12 · B10 11→9, 곧 §1-2 의 9 자리. 남은 이 검사 발화 7 의 지목 이름 수를 실제 코드로 다시 쟀다 — 18 · 8 · 8 · 2 · 4 · 4 · 8(정정된 §1-2 와 같음) · `SQL_CURRENT_RUN_ID` 지목 0. 다른 검사(B·C·P)·Job 행 변화 0 ✔ |
| N8 | 경고 0 · 실패 0 · 건너뜀 0 · 4441(새 시험 8) · `CheckGuardPolicyTests` 통과(자리 수 불변) ✔ |

**거부된 시도 코퍼스**: 이 변경은 발화를 줄이기만 해서 새 오탐을 만들 수 없다 — 재지 않았다.
**캐시 버전**: 프롬프트 불변 · L1 만 느슨해짐 → 올리지 않음(근거는 N7 스윕 차분).
**시험하지 않은 가지**: 두 텍스트의 펜스를 따로 찾는 것(이어 붙이면 절 끝의 닫히지 않은 펜스가 공통 규약 펜스와 짝지어진다) — 코퍼스에 그 모양이 없어 실물 표본이 없다.
**효과 판정은 다음 판**: 판 안 하한 재생성이 이 사유로 나지 않는지(B16 은 16)는 실물 판에서 잰다.

## 최종 전체 리뷰 반영

| 등급 | 지적 | 조치 · 판정 |
| :-- | :-- | :-- |
| **Critical C1** | 공통 규약의 **템플릿 블록**을 정의로 인정한다 — B1 `-- SQL_DELETE_CHUNK` → `DELETE FROM <TargetTable> WHERE <원본 필터 조건>` 처럼 단계가 구체 블록에 쓰는 이름과 같은 이름의 템플릿이 있다. 리뷰 프로브: B1 S05 배송본에서 세 정의 줄을 지우면 공통 규약 없이 3 · 함께 **0**. 또 이름을 부분 문자열로 맞춰 `SQL_CREATE_AND_CAPTURE_SHADOW_TEMPLATE` 정의가 `SQL_CREATE_AND_CAPTURE_SHADOW` 호출을 덮었다. §「오탐 방향」의 「공통 정의가 있으면 구현 가능」은 템플릿에서 거짓이었다 | 공통 규약은 블록 머리 줄 ~ 다음 머리 줄(펜스 끝)을 한 블록으로 보고 자리표시자(`<문자…>` · `SourceTable` · `TargetTable` · `SXX` — 전부 코퍼스에서 뽑음)가 있으면 그 이름을 정의로 치지 않는다. 이름은 토큰으로 맞춘다(단계 절 안 규칙은 종전 그대로). 코퍼스의 템플릿 블록: B1 4 · B10 1 · B16 7. 배송 단계 SQL 펜스 전체에서 꺾쇠 모양이 걸린 7 곳은 전부 진짜 자리표시자(연산자 오인 0). **효과 불변 확인**: 배송본 16 → 7(같은 9 자리) · OpenRouter 판 안 21 → 0. 시험 N9(B1 S05 세 정의 줄 삭제 → 세 이름 지목) · N10(공통 정의 `SQL_MARK_STEP_FAILED_V2` 는 `SQL_MARK_STEP_FAILED` 를 덮지 않음) |
| Important I1 | 골격을 다시 만들고 섹션은 동결하는 회차(골격 수리 · 재개)에서 동결 섹션은 다시 검증되지 않는다 — 새 골격이 공통 블록을 잃거나 개명하면 호출이 정의 없이 배송된다. 이 브랜치가 연 길(종전엔 섹션마다 사본이 있었다) | `GenerateBySplitAsync` 가 골격을 새로 만든 회차에 동결 섹션을 새 공통 규약으로 다시 대고(`MechanicalValidator.UndefinedSqlBlockNames` 공용), **이번 골격 변경 때문에** 정의를 잃은 단계만 재생성 대상에 넣는다. 직전 골격을 모르면 새 규약에 없는 호출 전부. 시험 넷: 잃음 → S02 재생성(S01 은 안 함) · 유지 → 안 함 · 직전에도 없던 이름 → 안 함 · 직전 골격 모름 → 재생성 |
| Minor M1 | 소스 잠금이 주석에 속는다 | 두 잠금 모두 주석 줄을 지우고 실제 식(`sharedConventions: conventions` · `Path.Combine(jobDir, "agent", "common", "01-step-contract.md")` · `SharedConventions = File.Exists(stepContractPath) ? …`)을 본다 |
| Minor M2 | 펜스를 텍스트마다 따로 찾는 가지는 시험이 없다 | 공용 함수로 옮기며 단계 절은 자기 펜스만, 공통 규약은 자기 블록 해석만 한다 — 이어 붙이는 코드가 사라져 가지 자체가 없어졌다 |
| Minor M3 | 공통 규약의 별표 접두사가 전 단계를 덮을 수 있다 | 코퍼스 공통 규약 21 개의 SQL 펜스 안 별표 접두사 0(리뷰 실측). 템플릿 제외는 별표 머리 블록에도 같이 걸린다. 기록 |
| Minor M4 | 스윕이 최종 커밋에서 돌지 않았다 | `7c5b16fe`(작업 트리 깨끗)에서 다시 돌려 교체 — 이전 스윕과 검사별 발화량 이하 차분 0 |

**되돌림(구현을 바꿨으니 전부 다시)**: 공통 규약 무시(5 빨강) · 템플릿 제외 제거(N9) · 토큰→부분 문자열(N10) · 동결 섹션 재대조 제거(2) · 직전 골격 차분 제거(「직전에도 없던 이름」 1) · 직전 골격 모름 가지 침묵(1) · 오케스트레이터 검증 배선(1) · 스윕 서비스 배선(1) · 스윕 명령 배선(1) — 아홉 모두 해당 시험 빨강.
**게이트**: 경고 0 · 실패 0 · 건너뜀 0 · 4447.
**남긴 한계**: 공통 규약의 **구체** 블록이 특정 단계 전용(B8 `SQL_CREATE_AND_CAPTURE_SHADOW` 는 S05 의 `TSettleMst` 고정)인데 다른 단계가 그 이름을 부르면 침묵한다 — 「구현 가능」은 참이지만 대상이 맞는지는 이 검사의 관할 밖이다. 자리표시자 모양 셋 밖의 템플릿 표기(예: `{Table}`)는 못 알아본다.
