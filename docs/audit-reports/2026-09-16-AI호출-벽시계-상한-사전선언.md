# AI 호출 하나가 53 분을 태웠다 — 벽시계 상한 (사전 선언)

**작성 시점**: 2026-09-16, **구현 전**. **코드**: main `1fbda365` + 이 문서 커밋.

## 0. 잰 것

B17 판독 §발견 3 의 자리다. 로그 **19 개 디렉터리 · AI 응답 574 건**을 전수로 재서 두 가지를 갈랐다.

### (1) 본문 빈 응답 — 드물고, 이미 복구된다

| 자 | 값 |
| :-- | :-- |
| 빈 본문 응답 | **6 / 574**(1.0%) — `z-ai/glm-5.3` 3 · `deepseek/deepseek-v4-pro-0813` 3 |
| 그 호출의 출력 토큰 | 165 ~ 12,995(전량 추론 토큰, 본문 0 자) |
| 그 호출의 비용 합 | 약 **$1.03** |
| 벽시계 | 0.1 · 0.7 · 1.7 · 2.7 · 3.7 분 — **그리고 52.9 분 한 건**(B17) |
| 복구 | **6/6 복구됐다** — 파이프라인이 파싱 실패를 「결함을 신고했으나 자리를 대지 못함」으로 받아 재요청하고, 그 재요청은 `MaxL2Attempts` 를 **소모하지 않는다**(B17 로그의 시도 번호가 1/2 로 유지된다) |

**그래서 「빈 본문 조기 재요청」은 만들지 않는다.** 이미 복구되고 있고, 빈 응답 다섯은 1~4 분이라 아낄 벽시계가 거의 없다. B17 의 손실은 **빈 본문이 아니라 그 호출이 53 분 걸린 것**이다 — 원인을 그쪽으로 옮긴다.

### (2) 호출 길이 — 상한 값의 근거

| 요청 종류 | 건수 | 중앙값 | p90 | 최대 | 최대의 본문 |
| :-- | --: | --: | --: | --: | :-- |
| 단계 섹션 생성 | 235 | 0.7 | 3.4 | 6.6 | 정상 |
| 명세서 생성 | 143 | 4.5 | 7.8 | **24.9** | 14,189 자(정상) |
| 명세서 리뷰 | 52 | 2.1 | 4.5 | 7.5 | 정상 |
| **통합 계획서 리뷰** | 38 | 5.5 | 8.4 | **52.9** | **0 자(B17 — 병리)** |
| 골격·브레인스토밍·목차 | 25 | 2.2~4.0 | ~5 | 7.8 | 정상 |
| **전체** | **493** | **2.1** | — | 52.9 | p95 7.5 · p99 **11.8** |

10 분을 넘은 호출은 **8 건**이고, **본문을 낸 최장 호출은 24.9 분**(명세서 생성)이며 **리뷰 중 최장 정상은 11.8 분**(출력 53,317 토큰)이다. 즉 **리뷰에 20 분 상한**을 걸면 이제까지 성공한 호출은 **하나도** 자르지 않고 병리 한 건만 20 분에서 끊는다(33 분 절약).

`AiSettings:TimeoutSeconds` 는 3600 이라 이 자리에 걸리지 않는다.

## 1. 설계 — 재시도 헬퍼에 상한을 얹는다

이음매는 `AiCallRetry.ExecuteAsync` 다. 이미 재시도·분류·비취소 예외 승격을 다 갖고 있다.

- 새 오버로드: `ExecuteAsync<T>(Func<CancellationToken, Task<T>> factory, CancellationToken, RetryPlan?, TimeSpan? deadline)`. `deadline` 이 있으면 사용자 토큰과 **연결한 토큰**(`CreateLinkedTokenSource` + `CancelAfter`)을 팩토리에 넘긴다.
- 상한이 끊으면 `OperationCanceledException` 이 오는데 **사용자 토큰은 취소되지 않았다** → 기존 `AiRetryPolicy.Classify` 가 그것을 **`Transient`** 로 판정한다(그 자리의 주석이 「구분은 우리가 넘긴 토큰이다」라 이미 적고 있다). 따라서 재시도 계획대로 한 번 더 부르고, 다 쓰면 `AiCallFailedException`(**`OperationCanceledException` 을 상속하지 않는다**)으로 올라가 호출부 55 곳의 `when (ex is not OperationCanceledException)` 이 잡는다 — 판이 죽지 않고 「리뷰 미실행」 배너로 산다.
- 사용자 취소는 그대로 `Cancelled` 로 올라간다(연결 토큰이 아니라 사용자 토큰이 취소됐으므로).
- 적용 자리: **통합 계획서 리뷰 호출 하나**(`VerificationPipelineOrchestrator` 의 `_criticService.ReviewConsolidatedPlanAsync`). 값은 **20 분**, 설정 `AiSettings:CriticCallDeadlineMinutes`(0 이면 끔)로 덮을 수 있게 둔다.

**범위 밖**: 다른 호출 종류(명세서 생성 최장 24.9 분 등)에는 상한을 걸지 않는다 — 병리 관측이 0 이고, 상한을 잘못 잡으면 성공하던 호출을 자른다. 빈 본문 조기 재요청도 만들지 않는다(§0-(1)).

**대가를 적어 둔다**: 끊긴 호출도 생성 토큰만큼 청구된다(관측 $0.01~$0.31). 20 분 상한은 그 비용을 한 번 더 낼 수 있다는 뜻이고, 판 하나에서 33 분을 아끼는 값과 맞바꾼다.

## 2. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| R1 | 상한이 끊는다 | 팩토리가 상한보다 오래 걸리면 상한 시점에 취소되고, 계획대로 **한 번 더** 불린다 |
| R2 | 판이 죽지 않는다 | 두 번 다 상한에 걸리면 `AiCallFailedException`(비 OCE)이 오른다 — 호출부의 기존 catch 가 잡는 형식이다 |
| R3 | 사용자 취소는 취소로 남는다 | 사용자 토큰을 취소하면 `OperationCanceledException` 이 그대로 오른다(상한으로 둔갑하지 않는다) |
| R4 | 상한 안이면 무변화 | 상한보다 빨리 끝나는 호출은 결과·호출 횟수가 같다 · `deadline` 이 null 이면 종전과 완전히 같은 경로 |
| R5 | 배선 | 통합 계획서 리뷰 호출이 실제로 상한을 넘긴다(설정 기본 20 분) · 다른 호출 자리는 null |
| R6 | 되돌림 | 상한 전달 · 연결 토큰 · 비 OCE 승격 세 갈래마다 걷어내면 빨강 |
| R7 | 게이트 | 실패 0 · 건너뜀 0 · 경고 0 |

**효력 판정**: 판을 새로 돌리지 않는다 — 이 변경은 병리 한 건을 20 분에서 끊는 것이고, 그 효력은 다음 판에서 「리뷰 호출이 20 분을 넘겼는가」로만 보인다. R1~R5 가 근거다.
