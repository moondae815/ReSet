# GPT 판 여섯(B11~B16) 결함 교차 분류 (사전 선언)

**작성 시점**: 2026-09-14, **측정 전**. **코드**: main `2f6483eb`. **API 호출 0**(읽기·스크립트만).

## 0. 왜 하는가

오늘은 「GPT 한 판 → 새 결함 하나 → 수정 → 다시 한 판」을 반복했다(B14 · B15 · B16, 약 $19 · 1 시간 45 분). 판마다 목차부터 16 · 19 · 21 단계로 달라
새 결함은 판을 돌리는 한 계속 나오고, 한 판 표본으로 고치고 한 판 표본으로 확인하는 동안 **무엇이 가장 중요한지**는 드러나지 않는다.
이미 있는 여섯 판을 가로질러 결함을 **가족으로 묶어 빈도 · 심각도 · 탐지자 · 현재 상태**로 세운다. 이 표가 (1) 다음에 무엇을 고칠지와
(2) 기본 설정(계획 시도 1~2 회 · GPT Consolidator)을 지금 정할 수 있는지를 정한다.

## 1. 분모 — 여섯 판

| 판 | 실행 커밋 | 계획 시도 | 단계 | 채택 점수 |
| :-- | :-- | --: | --: | --: |
| B11 | `75227b97` | 1 | 22 | 80 |
| B12 | `cab233f9` | 1 | 18 | 90 |
| B13 | `1e31d6b4` | 2 | 19 | 94 |
| B14 | `9a60d0dc` | 2 | 16 | 94 |
| B15 | `ea1829a6` | 2 | 19 | 94 |
| B16 | `d20a0fcf` | 2 | 21 | 96 |

판마다 코드가 다르다(그 사이 수정이 들어갔다). 그래서 빈도는 「몇 판에서 났나」와 함께 「고친 커밋 이후 판에서 재발했나」를 따로 적는다.

## 2. 재료(출처) — 다섯

1. **판 안 단계 하한 재생성 사유** — 실행 로그 「하한 검사를 통과하지 못해 다시 생성」 줄, 사유를 ` / ` 로 쪼개 하나씩.
2. **판 안 통합 L1** — 로그 「통합 계획서 기계적 검증 완료」(여섯 판 전부 채택 시도에서 오류 0 — 재료로는 빈다).
3. **Critic 지적** — `raw/attempts/run-001/reviews/attempt-*.json` 의 `DefectiveSteps` · `SkeletonDefective` · `FeedbackComment`.
4. **배송본 배너** — `docs/BatchMigrationPlan.md` 머리의 검증 불가 · 하한 미달 · 생성 실패 항목.
5. **사람 판독** — 오늘까지의 판독 문서(`2026-09-13-*` · `2026-09-14-*` 사전 선언의 실행 결과 절)에 적힌 **배송된** 결함.
6. 보조: 현재 main 의 스윕(`sweeps/2026-09-14-step-sweep-named-block.md`) — 배송 단계 파일에 지금 검사를 다시 댄 값.

## 3. 분류 축

결함 하나(판 · 자리 · 사유)를 아래로 적고, 같은 **메커니즘**끼리 가족으로 묶는다(자리·단계 코드로 묶지 않는다 — 판마다 번호가 다르다).

| 축 | 값 |
| :-- | :-- |
| 층 | 목차 · 골격(공통 규약·흐름) · 단계 본문 · 검증 SQL 세트 · 파이프라인(배너·표시) |
| 결과 | 판 안에서 잡혀 닫힘 · 배송(배너로 표시) · 배송(조용) |
| 탐지자 | L1 단계 · L1 통합 · Critic · 사람 판독 · 없음 |
| 심각도 | **S1** 실행하면 틀린 데이터(행 집합·금액) · **S2** 실행 불가·배치 실패·재시작 불능(권한·게이트·정의 없음) · **S3** 구현자 혼란·비용(중복·모순 산문·재생성 낭비) · **S4** 표시만 |
| 지금 | 닫힘(커밋) · 열림 · 검사 오탐이었음 |

## 4. 판정 규칙

- 「판 안에서 잡혀 닫힘」은 **판 비용**으로만 센다(재생성 횟수) — 배송 결함 수에 넣지 않는다.
- 검사 오탐으로 판명된 사유(예: 이름 블록 · 공통 규약, T25 위치 INSERT)는 결함 가족이 아니라 「검사 오탐」 가족으로 따로 둔다.
- 같은 판 한 자리에서 난 같은 사유는 시도가 여럿이어도 1.
- 사람 판독 출처는 문서 파일·절을 칸에 적는다. 문서가 「배송」이라 적지 않은 것은 배송으로 세지 않는다.

## 5. 이 분류가 못 보는 것 (맹점 — 먼저 적는다)

**탐지자 「없음」 가족은 원리상 이 표에 안 나온다.** 여기 실리는 것은 L1 · Critic · 사람 판독 중 누군가 본 것뿐이고, 사람 판독은 판마다 그날의 물음(RunId 게이트 · 가드 · 통제 합계)만 봤다.
그래서 이 표의 빈도는 **탐지 편향**을 안는다 — 검사가 있는 가족은 많이, 없는 가족은 0 으로 보인다. 결론 문장에 이 한계를 붙인다.
맹점을 줄이려면 단계 단위 축 B 감사(원본 DDL · 명세서 ↔ 단계 섹션)가 필요하고 그것은 API 호출이 드는 별도 작업이다 — §결과 뒤에 비용과 함께 선택지로만 적는다.

---

# 결과 (2026-09-14)

재료: 로그 하한 재생성 사유 70 줄(판 안) · Critic 지적 8 건(시도별) · 배송본 배너 · 판독 문서 7 편 · 현재 main 문서 층 검사를 여섯 배송본에 다시 댄 값(스크래치 프로브, `planonly-run` `d20a0fcf` 코어).
판 안 재생성은 같은 단계·같은 시각을 한 번으로 셌다.

## 한 줄 결론

1. **판 안 하한 재생성 41 중 25(61 %)가 검사 오탐만으로 났다** — 이름 블록 · 공통 규약(고침 `2f6483eb`) 21 자리 중 오탐 단독 분과, **새로 찾은 오탐 하나**: `UP_UTIL_SETTLE_EXCEPTION_PROC` UPDATE 3 의 최상위 술어를 **여섯 판 전부** 「원본에 없다」고 재생성시켰는데, 모델 식과 원본은 함수 인자를 감싼 **괄호 하나**만 다르다(아래 §증거).
2. **배송된 결함 중 가장 흔한 것은 목차 층이다** — 원본 트랜잭션 분할 5/6 · 목차 밖 제어 테이블 쓰기 4/6. 둘 다 목차를 다시 만들지 않는 구조라 판 안에서 못 고치고 「검증 불가」 배너로 배송된다. 특히 목차 밖 테이블은 **그 섹션 전체가 기계 검증을 못 받는다**(B16 은 Critic 이 고치게 한 S03 이 그랬다).
3. **실행 불가(S2) 제어 계약 결함은 B11~B13 에서 판마다 1~2 가족씩 배송됐고, 그중 둘(RunId 게이트 · 통제 합계 이름)은 지금 기계가 잡는다** — 현재 코드를 배송본에 다시 대면 B11 3 · B12 3 · B13 3 발화, B14~B16 0. **남은 하나 — 제어 테이블 이름·컬럼 계약 분열(B11 · B13 배송) — 은 기계 검사가 없고 Critic 만 잡았다.**
4. 단계 본문 SQL 표류(가드 · 조인 짝 · 술어)는 L1 이 판 안에서 닫는다 — 배송은 검사가 없던 B11·B12 의 가드뿐. 다만 **조인 짝은 GPT 가 다섯 판에서 같은 자리에 같은 조건(`A.ClientID = B.ClientID`)을 더한다** — 판마다 재생성 1 회를 쓰는 모델 버릇이다.

**맹점**: 이 표는 L1 · Critic · 사람 판독 중 누군가 본 것만 싣는다(사전 선언 §5). 「탐지자 없음」 가족의 빈도는 0 으로 보이지만 0 이 아니다.

## 가족 표

판 칸: 배송 = **굵게**, 판 안에서 닫힘 = 보통, `·` = 없음. S1 틀린 데이터 · S2 실행 불가/재시작 불능 · S3 구현 혼란·비용 · S4 표시.

### 가. 검사 오탐 — 판 비용

| 가족 | B11 | B12 | B13 | B14 | B15 | B16 | 판 | 지금 |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- | --: | :-- |
| 이름 블록 · 공통 규약 정의 불인정 | S07·S16 | · | S02·S11 | · | S04 | 16 단계 | 4 | 닫힘 `2f6483eb` |
| **최상위 술어 괄호 정규화**(EXCEPTION_PROC UPDATE 3) | S08 | S06 | S06 | S06 | S06 | S07 | **6** | **열림 — 새로 찾음** |
| T25 위치 INSERT | · | · | · | **S01 거짓 하한 미달 배너** | · | · | 1 | 닫힘 |

### 나. 목차 층 — 판 안에서 못 고쳐 「검증 불가」로 배송

| 가족 | B11 | B12 | B13 | B14 | B15 | B16 | 판 | 심각도 | 지금 |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- | --: | :-- | :-- |
| 원본 `UP_Util_Settle_Summary` 트랜잭션이 여러 단계로 갈림 | **S14** | **S12** | **S14** | · | **S14** | **S14** | **5** | S1 잠재(부분 커밋) — **진위 미판독**: B11 Critic 은 「S14~S16 공유 트랜잭션 설계 적절」이라 적었다 | 열림 |
| 목차에 없는 제어 테이블에 씀 → 섹션 전체 검증 불가 | **S13** `BatchControlTotal`(목차는 별칭 `ControlTotal`) | · | **S02·S19** `BatchRunLock` | **S15** `BatchValidationIssue` | · | **S03** `BatchRunLock` | **4** | S2(권한·DDL 이 목차를 따라감) + 검증 사각 | 열림 |
| S01 목차 대상 표 빈칸 | **S01** | **S01** | **S01** | · | **S01** | **S01** | 5 | S4 추정(쓰기 없는 사전 검증 단계 — 미판독) | 열림 |

### 다. 제어 계약 — 단계 사이 · 골격 · 검증 세트

| 가족 | B11 | B12 | B13 | B14 | B15 | B16 | 판 | 탐지자 | 지금 |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- | --: | :-- | :-- |
| RunId 발급 전 단계를 게이트가 요구 | **1** | **3** | **2** | 대상 없음 | 0 | 0 | **3 배송** | 사람(B13 판독) | 검사 + 수명 계약 — 배송본 재측정 B11~B13 에서 6 발화 |
| 통제 합계 이름 분열(쓰는 단계 ↔ 읽는 단계·검증 세트) | **S13↔S20·V** | 대상 없음 | **S11↔V04** | · | · | · | **2 배송** | Critic(B11) · 사람(B13) | K2 · K2 검증 세트판 — 배송본 재측정 B11 2 · B13 1 발화 |
| **제어 테이블 이름·컬럼 계약 분열**(같은 표를 둘로 정의·없는 컬럼 조회) | **`ControlTotal`/`ReconciliationResult` vs `Batch*`** | · | **`BatchReconciliation` 컬럼 두 벌 · V15 가 없는 컬럼 조회**(2 차가 고쳤는데 채택이 버림) | · | · | · | **2 배송** | Critic 만 | **기계 검사 없음 — 열림** |
| 제어 행 생성 없이 UPDATE | S21 | · | S19 | S16 | · | · | 3 | L1 | 판 안 닫힘 |
| 잠금 소유권 승계 누락 | · | · | · | · | · | S03(Critic → 2 차 닫힘, 단 검증 불가로 배송) | 1 | Critic | 닫힘(표본 1) |
| 공통 규약 ↔ 본문 모순(오류 코드 NULL · 잠금 순서) | · | · | 2 차 골격 재작성만(배송 안 됨) | · | · | · | 1 | Critic | 골격 패치 병합 뒤 표본 0 |
| 검증 세트가 단계에 쓰기 요구(V05↔S06) | · | · | · | 판정 보류 | · | · | 1 | 사람 | 열림(판정 보류) |

### 라. 단계 본문 SQL 표류 — L1 이 판 안에서 닫음

| 가족 | B11 | B12 | B13 | B14 | B15 | B16 | 판 | 심각도 | 지금 |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- | --: | :-- | :-- |
| 가드 조건 표류(PLTID 날조 · 조건 뜻 변경) | **S05 · S11(보류)** | **S03 · S10** | S03 | S03·S09 | S03·S09 | 0 | 5(배송 2) | S1(기지급 차단 무력화) | 검사(B13~) + 가드 조건 표(B16, 표본 1) |
| **조인 짝 추가**(`EXPECT_PROC` UPDATE 11 `A.ClientID = B.ClientID`) | S10 | S08 | S08 | S08 | · | S09 | **5** | S1 잠재 — 검사가 매번 닫음 | 열림(판마다 재생성 1) |
| 최상위 술어 실제 추가(`ABS(IIF(…)) = ABS(E.Amt)`) | S10 | · | · | · | · | · | 1 | S1 | 판 안 닫힘 |
| 유령 테이블 | S01 | · | · | · | · | · | 1 | S2 | 판 안 닫힘 |

### 마. 문서 표기 · 파이프라인

| 가족 | 판 | 심각도 | 지금 |
| :-- | :-- | :-- | :-- |
| 저널 의사코드 함수명이 단계마다 다름(`writeStepSuccess`/`markStepSucceeded`/…) | B11 · B12(Critic, 1 회 판이라 배송). B13~B16 은 Critic 언급 없음 — **안 쟀다** | S3 | 열림 |
| 바인딩명 · 헤딩 기호 불일치 | B11(Critic) | S3 | 열림 |
| 프롬프트 지시문 본문 유출 · 쓰이지 않는 `DECLARE` | B16 각 1 | S3·S4 | 열림 |
| 배송본 머리말 「claude-cli (claude-sonnet-5)」 오표시 | **6/6** | S4 | 열림 |
| `raw/attempts` 가 배송본과 어긋남 | B13(확인) | S4 — 감사를 오도 | 열림 |
| 채택 규칙이 수정을 버림 · 골격 백지 재작성 | B13 | — | 골격 패치 병합 뒤 표본 0 |

## 증거 — 최상위 술어 괄호 오탐

```
원본 UP_UTIL_SETTLE_EXCEPTION_PROC      dbo.UF_GET_CLIENTSECTIONRATE(A.CLIENTID,A.PGNAME,A.MALLID, (A.TXAMT-ISNULL(A.NonSettleAmt,0)))
B16 S07 첫 초안(재생성됨)               dbo.UF_GET_CLIENTSECTIONRATE( A.CLIENTID, A.PGNAME, A.MALLID, A.TXAMT - ISNULL(A.NonSettleAmt, 0) )
B16 S07 둘째 초안(통과)                 dbo.UF_GET_CLIENTSECTIONRATE ( A.CLIENTID, A.PGNAME, A.MALLID, (A.TXAMT - ISNULL(A.NonSettleAmt, 0)) )
```

여섯 판의 발화 메시지가 이 식 하나로 같다. 인자를 감싼 괄호는 값을 바꾸지 않는다. 조인 짝은 반대로 **진짜**다 — B16 S09 첫 초안이 `ON A.ClientID = B.ClientID AND A.MPLTID = B.PLTID`, 원본은 `ON A.MPLTID = B.PLTID` 뿐이고 둘째 초안이 원본대로 돌아갔다.

## 판 안 재생성 — 오탐 몫

| | B11 | B12 | B13 | B14 | B15 | B16 | 계 |
| :-- | --: | --: | --: | --: | --: | --: | --: |
| 하한 재생성 | 8 | 2 | 6 | 5 | 4 | 16 | 41 |
| 그중 오탐 사유만으로 난 것 | 3 | 1 | 3 | 1 | 2 | 15 | **25** |

(오탐 = 이름 블록 · 공통 규약 + 최상위 술어 괄호. 목차 결함 사유가 함께 실린 것은 목차 결함이 재생성을 부르지 않으므로 오탐 단독으로 셌다.)

## 이 표가 정하는 것

**다음 수정 순서(심각도 × 배송 빈도 × 비용)**

1. **원본 트랜잭션 분할의 진위 판독**(5/6 배송) — 먼저 잰다. 다섯 판의 S14~S16 이 원본의 한 트랜잭션을 실제로 갈랐는지(부분 커밋 = S1), 앱 쪽 공유 트랜잭션으로 원자성을 지켰는데 검사가 못 알아보는지(= 배너 오탐). 판독만으로 끝나고 API 호출 0.
2. **목차 밖 제어 테이블 쓰기**(4/6) — 목차가 `batch.*` 제어 테이블을 선언하지 않는 구조 문제. 목차 보강(`PlanStructureEnricher` 가 이미 정적 분석으로 대상 표를 보강한다)이나 제어 스키마 취급을 정하면 네 판의 「섹션 전체 검증 불가」가 사라진다.
3. **최상위 술어 괄호 오탐**(6/6) — 작고 확실하다. 판마다 재생성 1 회.
4. **제어 테이블 계약 분열 검사**(2/6 배송, S2, 검사 없음) — 새 L1 검사 설계가 필요하다.
5. 조인 짝 버릇(5/6, 비용만) — 가드 조건 표와 같은 프롬프트 층 처방 후보.

**기본 설정(계획 시도 1 회 ↔ 2 회)**: 기계 검사가 없는 제어 계약 결함은 Critic 만 잡았다 — 1 회 판 B11 은 그대로 배송, 2 회 판 B16 은 2 차가 닫았다(B13 은 2 차가 닫았지만 채택이 버렸다 — 골격 패치 이후 표본 없음). 여섯 판 표본으로는 「2 회가 배송 결함을 줄인다」를 수로 말할 수 없다(전이 2 건). 결정에 필요한 것은 판 수가 아니라 가족 3 의 검사다 — 그 검사가 생기면 2 차 시도가 사 주던 값의 대부분이 판 안 L1 로 옮겨간다.

**맹점을 줄이려면**: 단계 단위 축 B 감사(원본 DDL · 명세서 ↔ 단계 섹션)를 여섯 판의 같은 SP 끼리 나란히 한다 — 판이 서로 다르게 옮긴 자리는 적어도 하나가 틀린다. 14 SP × 6 판이라 서브에이전트 호출이 드는 별도 작업이다.

---

# 후속 1 — 원본 트랜잭션 분할 배너의 진위 (사전 선언, 판독 전)

**물음**: 다섯 판(B11 · B12 · B13 · B15 · B16)이 「원본 트랜잭션이 단계로 갈렸다」 배너를 배송했다. 배송된 단계 섹션이 실제로 원자성을 잃었는가(S1), 아니면 단계를 넘어 한 트랜잭션을 이어 원자성을 지켰는데 배너가 목차만 보고 붙었는가(배너 오탐).

**원본**(`UP_Util_Settle_Summary` DDL): 23 행 `BEGIN TRAN` → 요약 DML 8 문장(각 `@@ERROR` 시 `ROLLBACK TRAN`) → 221 행 `EXEC UP_Util_Settle_Summary_AcqManual` → 230 행 `EXEC UP_UTIL_SETTLE_SUMMARY_EXTRA`(각 반환 코드 ≠ 0 이면 `ROLLBACK TRAN`) → 239 행 `COMMIT TRAN`. 두 피호출자는 자기 트랜잭션이 없다(`BEGIN TRAN` 0).
**곧 원본의 계약**: 세 프로시저의 쓰기 전부가 한 번에 커밋되거나 한 번에 되돌려진다 — 뒤 둘 중 하나가 실패하면 앞의 요약 쓰기도 사라진다.

**검사가 보는 것**(`CheckTransactionSpanSplit`): 목차에서 피호출자가 **다른 단계**로 갔는가뿐. 섹션 본문의 트랜잭션 처리는 읽지 않는다 — 그래서 배너 자체로는 진위를 말하지 못한다.

**대조군**: B14 는 세 SP 를 한 단계(S11 「통합 요약 원자 재구축」)로 묶어 배너가 없다.

## 판정 기준 (판마다 한 줄)

배송 단계 파일(`agent/steps/`)의 세 섹션에서 트랜잭션 시작 · 커밋 · 롤백 자리와 실패 경로를 읽어:

| 판정 | 조건 |
| :-- | :-- |
| **보존** | 세 SP 의 쓰기가 한 트랜잭션(같은 연결)에서 돌고, 뒤 단계 실패가 앞 단계 요약 쓰기까지 되돌린다 — 단계를 넘어 트랜잭션을 이었든 한 단계가 셋을 실행했든 |
| **보상 복구** | 단계마다 커밋하지만, 뒤 단계 실패 뒤 **재실행**이 앞 단계 쓰기를 원본 결과와 같게 다시 만든다(멱등 재구축 · 명시적 보상) — 실패와 재실행 사이에 **다른 소비자가 섞인 요약을 볼 수 있는 창**이 있으면 그 사실을 적는다 |
| **깨짐** | 단계마다 커밋하고, 뒤 단계 실패가 앞 단계 쓰기를 되돌리지도 재실행으로 바로잡지도 않는다 |
| 판정 불가 | 섹션이 트랜잭션 경계를 말하지 않는다 |

**배너 오탐의 뜻**: 「보존」이면 배너는 거짓이다(검사가 섹션을 안 읽어서). 「보상 복구」면 배너는 원자성 상실이라는 사실에선 참이고 영향에선 과장이다. 「깨짐」이면 참이다.
**함께 적는다**: 그 설계를 목차·골격·섹션 중 어디서 정했는가, 산문의 주장(「원본은 자동 커밋이었다」 같은 거짓)이 있는가.

## 후속 1 결과 — **다섯 판 전부 「보존」, 배너는 오탐**

| 판 | 단계 | 트랜잭션 시작 | 중간 단계 | 커밋 | 실패 경로 | 판정 |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| B11 | S14·S15·S16 | S14 `sharedTx = sharedConn.beginTransaction()`(40 행) — 「S14 의 여덟 DML 이 성공해도 커밋하지 않고 S15·S16 에 넘긴다」(29 행) | S15 「S14 에서 시작한 공유 비즈니스 트랜잭션을 그대로 사용하며 S15 에서는 커밋하지 않는다」(9 행) | S16 `sharedBusinessUnit.commit()`(121 행) | S16 「실패하면 S14 와 S15 의 변경까지 함께 롤백」(3 행) · `rollbackIfOpen` | **보존** |
| B12 | S12·S13·S14 | S12 `workTx = conn.beginTransaction()`(68 행) · 「S12 완료 시에는 커밋하지 않는다」(21 행) | S13 「S12 에서 시작한 SNAPSHOT 업무 트랜잭션 … S14 검증까지 성공한 경우에만 한 번 커밋」(23 행) | S14 `compositeTx.commit()`(131 행) | S14 `compositeTx.rollback()`(137 행) · S12 「후속 S13·S14 실패 시 복합 트랜잭션 전체를 롤백」(22 행) | **보존** |
| B13 | S14·S15·S16 | 「요약 처리 조정 구성요소가 S14 시작 전에 공유 트랜잭션을 연 뒤 S16 종료 후 한 번만 커밋」(S14 20 행) | S15 「자체 커밋을 수행하지 않는다」(21 행) · S16 「단독 트랜잭션을 시작하거나 커밋하지 않고 S14 가 연 공유 트랜잭션에 참여」(15 행) | 조정 구성요소 | S14 「S14, S15 또는 S16 에서 실패하면 공유 트랜잭션을 전부 롤백하므로 네 대상 테이블은 S14 시작 전 상태로」(468 행) | **보존** |
| B15 | S14·S15·S16 | S14 `executeSummaryUnit` 이 `tx = conn.beginTransaction()` 뒤 `executeS14 → executeS15 → executeS16`(99~105 행) | S15 「별도 트랜잭션을 시작하거나 커밋하지 않고 S14 가 시작한 공유 트랜잭션을 사용」(35 행) | S14 조정부 `tx.commit()`(107 행) | `rollbackIfOpen(tx)` · S16 「실패하면 공유 트랜잭션 전체를 롤백」(44 행) | **보존** |
| B16 | S14·S15·S16 | 「S14 는 공유 트랜잭션을 시작하지만 커밋하지 않으며, 최종 커밋은 S16」(S14 9 행) | S15 「S14 에서 시작된 공유 트랜잭션에 참여하고 S15 에서는 커밋하지 않는다」(9 행) · `workConn.rollback(); // S14부터 누적된 공유 트랜잭션 전체 롤백`(109 행) | S16 `conn.commit(); // S14~S16 업무 변경과 성공 상태를 한 번에 확정`(134 행) | S16 `conn.rollback()`(139 행) | **보존** |

다섯 판 모두 저널·체크포인트 시작 행만 업무 트랜잭션 밖에서 먼저 커밋하고(재시작 기록), 요약 테이블 넷의 쓰기는 한 트랜잭션에 묶었다. 원본의 「셋이 한꺼번에 커밋되거나 한꺼번에 되돌려진다」와 같다.
산문의 거짓 주장(「원본은 자동 커밋이었다」 류) 0. 설계는 다섯 판 모두 **목차가 셋으로 가르고 섹션이 공유 트랜잭션으로 이었다**(골격이 아니라 섹션 서술). B11 Critic 의 「S14~S16 공유 트랜잭션 설계는 적절」과도 맞는다.

**판정**: 이 배너는 **다섯 판 5/5 거짓**이다 — `CheckTransactionSpanSplit` 이 목차(피호출자가 다른 단계로 갔나)만 보고 섹션의 트랜잭션 처리를 읽지 않기 때문이다.
배송 영향은 둘이다: (1) 다섯 판의 요약 첫 단계가 「대조할 재료가 목차에 없어 검증되지 못했다」는 **틀린 이유**의 「검증 불가」 배너를 달고 나갔다. (2) 구현자·감사자가 원자성이 깨졌다고 읽을 수 있다.
판 비용은 0 이다 — 목차 결함(`PlanDefects`)이라 재생성을 부르지 않는다.

## 교차 분류 결론 정정

「한 줄 결론」 2 의 「배송된 결함 중 가장 흔한 것은 목차 층 — 원본 트랜잭션 분할 5/6」은 **틀렸다** — 그것은 결함이 아니라 배너 오탐이었다(가족 표 「나」 첫 행 → 「가. 검사 오탐」으로 옮긴다).
정정 뒤 배송 결함의 최빈은 **목차 밖 제어 테이블 쓰기(4/6, 섹션 전체 검증 불가)** 이고, 검사 오탐 가족은 셋(이름 블록 · 괄호 정규화 · 트랜잭션 분할 배너)이 된다.

**처방 후보(구현 전 — 결정 필요)**: 섹션 산문으로 판정하면 오탐이 난다(이 검사 주석이 실측 15 중 14 오탐으로 산문 판독을 금했다). 기계로 읽을 자리가 둘이다 —
(가) 목차 JSON 에 「트랜잭션 묶음」 칸을 두어 목차가 스스로 선언하게 하고, 검사는 선언된 묶음 안의 중간 단계 의사코드에 커밋이 없는지만 본다.
(나) 이 검사를 「검증 불가」가 아니라 **Critic 확인 요청**으로 내린다(배너 대신 Critic 프롬프트의 확인 항목).
(가)는 목차 계약이 바뀌어 캐시·프롬프트 영향이 크고, (나)는 작지만 기계 강제가 사라진다.

## 교차 분류 정정 2 — 「목차 밖 제어 테이블 쓰기」

가족 표 「나」 둘째 행의 심각도 「S2(권한·DDL 이 목차를 따라감) + 검증 사각」과 결론의 「섹션 전체가 기계 검증을 못 받는다」는 **둘 다 틀렸다** — 고치기 전에 재서 드러났다(`2026-09-14-목차밖-계약표-면제-사전선언.md` §0).
목차 결함만 있는 섹션도 본문 검사는 모두 돌고 통과했으며, 목차 대상 표가 권한·DDL 을 끌고 가는 코드는 없다. 이 가족의 여섯 판 발화는 전부 **제어 계약 표**였다 — 결함이 아니라 **검사 오탐**(「가」로 옮긴다). 정정 뒤 여섯 판에서 **판 안에서 못 고치고 배송된 진짜 결함**의 가족은 「다」의 제어 테이블 계약 분열(B11 · B13)과 「마」의 표기·표시 가족만 남는다.
