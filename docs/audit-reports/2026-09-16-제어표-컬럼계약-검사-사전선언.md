# 계약 밖 batch 표의 컬럼 계약 분열 검사 (사전 선언)

**작성 시점**: 2026-09-16, **구현 전**. **코드**: main `9c6bb10b`.
**계기**: GPT 판 교차 분류가 「판 안에서 못 고치고 배송된 진짜 결함」으로 남긴 마지막 가족이다 — 제어 테이블 계약 분열(B11 · B13, 실행 불가, 기계 검사 없음, Critic 만 잡았다).

## 0. 실물

`POQSettleBatch13` 배송본: S18 이 `batch.BatchReconciliation` 을 만들고 V15 가 그 표를 읽는데 **컬럼 집합이 겹치지 않는다.**

```sql
-- 8835 행(S18)  CREATE TABLE batch.BatchReconciliation
--   RunId, StepCode, ReconciliationName, ExpectedValue, ActualValue, DifferenceValue, IsMatched, CheckedAtUtc
-- 10390 행(검증 SQL 세트 V15)
SELECT CheckCode, CheckName, ResultStatus, DifferenceCount, DetailMessage
  FROM batch.BatchReconciliation WHERE RunId = @p_runId AND ResultStatus = N'FAIL';
```

다섯 컬럼 중 **하나도 정의에 없다** — 배포하면 컬럼 없음 오류이고, S19 의 게시 전제(`FAIL 없음`)를 평가할 수 없다. 1 차 Critic 이 잡았는데 2 차가 골격을 통째로 다시 써 점수가 떨어져 **채택이 그 수정을 버렸다**(`2026-09-13-GPT-2회판-Batch13-사전선언.md` G10).

## 1. 잰 것 — 도달과 오탐 (배송본 12 편, 프로브)

| 자 | 발화 | 판정 |
| :-- | --: | :-- |
| 계약 밖 `batch.*` 표의 **정의에 없는 컬럼 참조**(단일 테이블 FROM · INSERT 컬럼 목록) | **1** | B13 `BatchReconciliation` — 진짜 양성. 다른 11 편 0 |
| (참고) 계약 밖 `batch.*` 표를 **CREATE TABLE 없이** 참조 | 8(5 편) | **범위 밖으로 둔다** — B1 `BatchPublishLog`·B8 `BatchRunParameter` 는 산문이 컬럼을 적고(「다음 컬럼을 가진다고 전제합니다: …」) DDL 을 부트스트랩에 맡긴다. 판정에 산문을 읽어야 하고 그 방식은 실측 오탐 15 중 14 였다 |

## 2. 설계

- **자리**: `ValidateConsolidated`(문서 층). 정의(S18 섹션)와 참조(검증 SQL 세트)가 **다른 절**에 살기 때문에 단계 검사로는 볼 수 없다. `SafeCheck` 로 감싸고 감싼 자리 수 10 → 11 로 올린다.
- **오라클**: 문서 자신의 `CREATE TABLE`(자기일관성). 계약 표(`BatchControlContract`)는 제외한다 — 그 컬럼은 계약이 정하고 K1·어휘 검사의 관할이다.
- **판정**: 계약 밖 `batch.*` 표에 `CREATE TABLE` 이 **하나 이상 있을 때만** 본다. 정의 컬럼은 그 표의 모든 `CREATE TABLE` 과 `ALTER TABLE … ADD` 의 **합집합**(보수적).
  참조는 (가) 단일 테이블 `FROM` 질의의 컬럼 참조 (나) `INSERT … (컬럼 목록)`. 테이블이 둘 이상인 질의는 비한정 컬럼의 소속을 말할 수 없어 **침묵**한다(작성 계약 7).
- **발화**: 표마다 한 번, 정의에 없는 컬럼을 모아 적는다. 문구는 사실만 — 「`batch.X` 를 읽는 문장이 그 표 정의에 없는 컬럼 `…` 을 씁니다. 이 문서가 정의한 컬럼은 `…` 입니다 — 배포하면 컬럼 없음 오류입니다.」
- **귀속**: 참조가 단계 절 안이면 `OwnerStepCode`, 절 밖(검증 SQL 세트·골격)이면 `Lexemes` 에 **그 컬럼이 있는 원문 줄**(작성 계약 9 — 고정 문구의 백틱이 멀쩡한 단계를 열지 않게).
- **범위 밖(기록)**: 같은 표를 서로 다른 두 `CREATE TABLE` 로 정의하는 것(코퍼스 0 — 합집합으로 삼켜진다) · 정의 없이 참조(위 §1) · 동적 SQL 안의 문장(리더가 못 본다) · `SELECT *`.
- **캐시**: 프롬프트 불변 · L1 만 조인다 → 캐시 버전은 올리지 않고 **스윕 차분**으로 기존 산출물이 새 기준을 통과하는지 보인다(그 차분이 B13 1 건이어야 한다).

## 3. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| C1 | 실물 발화 | B13 배송본에서 `BatchReconciliation` 한 건, 지목 컬럼이 `CheckCode · CheckName · ResultStatus · DifferenceCount · DetailMessage` 다섯 |
| C2 | 계약 표 침묵 | `batch.BatchControlTotal` 같은 계약 표는 이 검사가 보지 않는다(컬럼은 계약이 정한다) |
| C3 | 정의 없음 침묵 | `CREATE TABLE` 이 없는 계약 밖 표는 침묵(B1 · B7 · B8 · B11 의 여덟 자리) |
| C4 | 다중 테이블 침묵 | 두 테이블을 조인한 질의의 비한정 컬럼은 침묵 |
| C5 | 합집합 | `CREATE TABLE` + `ALTER TABLE ADD` 의 컬럼을 합쳐 본다(둘 다 있는 합성 입력에서 침묵) |
| C6 | 귀속 | 단계 절 안이면 `OwnerStepCode`, 밖이면 `Lexemes` 가 그 원문 줄 |
| C7 | 되돌림 | 정의 수집 · 참조 수집 · 계약 면제 · 다중 테이블 침묵 · `SafeCheck` 배선을 하나씩 걷어내면 해당 시험 빨강 |
| C8 | 스윕·게이트 | 배송본 스윕에서 이 검사 발화 1(B13) · 다른 검사·Job 행 변화 0 · 경고 0 · 실패 0 · 건너뜀 0 · `CheckGuardPolicyTests` 10 → 11 |

**오탐 방향**: 새 발화를 만드는 변경이므로 오탐이 곧 재생성 낭비다. 그래서 침묵 조건 넷(계약 표 · 정의 없음 · 다중 테이블 · 동적 SQL)을 시험으로 먼저 못박고, 배송본 도달을 1 로 제한한 상태에서 붙인다.
**거부된 시도 코퍼스**: 이 검사는 처방문(「…하십시오」)을 담으므로, 그 처방을 따른 텍스트가 새 오탐을 만들 수 있다. 처방은 「정의를 고치거나 조회 컬럼을 정의에 맞추라」이고 둘 다 SQL 변경이라 산문 오탐 경로가 없다 — 그래도 재생성 뒤 로그의 발화를 다음 판에서 읽는다.

## 결과

| # | 결과 |
| :-- | :-- |
| C1 | 실물 발화 1 — B13 배송본에서 `batch.BatchReconciliation`, 지목 컬럼 `CheckCode · CheckName · DetailMessage · DifferenceCount · ResultStatus` 다섯. 메시지가 문서 정의 여덟 컬럼도 함께 적는다 ✔ |
| C2 | 계약 표(`batch.BatchControlTotal`)는 침묵 ✔ |
| C3 | `CREATE TABLE` 없는 계약 밖 표는 침묵(B1 `BatchPublishLog`·B7 `BatchStepError`·B8 `BatchRunParameter`·B11 `ReconciliationResult` 여덟 자리 전부) ✔ |
| C4 | 조인 질의의 **비한정** 컬럼은 침묵 · **한정된** 참조는 조인에서도 판정 ✔ — 첫 시험이 컬럼을 모두 한정해 이 가지를 못 갈랐고, 되돌림 m4 가 그것을 드러내서 가르는 입력을 따로 더했다 |
| C5 | `CREATE TABLE` + `ALTER TABLE … ADD` 합집합 ✔ |
| C6 | 귀속 — 참조가 검증 세트(절 밖)면 `OwnerStepCode` null · `Lexemes` 7 줄(그 컬럼이 있는 원문 줄) · 단계 절 안이면 `OwnerStepCode = S18` ✔ |
| C7 | 되돌림 여섯 — 정의 수집(4 빨강) · INSERT 참조 수집(1) · 계약 표 면제(1) · 다중 테이블 비한정 침묵(1) · ALTER 합집합(1) · `SafeCheck` 배선(4, `CheckGuardPolicyTests` 포함) ✔ |
| C8 | 배송본 코퍼스 도달 **1**(B13) · 오탐 0 · 경고 0 · 실패 0 · 건너뜀 0 · **4493**(새 시험 10) · `CheckGuardPolicyTests`·`CheckIsolationTests` 의 `ValidateConsolidated` 자리 수 10 → 11 ✔ |

**스윕은 이 검사의 자가 아니다** — 스윕은 단계 검사(A~E·P)만 돈다. 문서 층 발화는 위 프로브(12 편 전수, 배송본 재측정)로 쟀고 스윕 차분은 0(단계 검사 불변 확인). 캐시 버전은 올리지 않았다 — 프롬프트 불변이고, 기존 배송본이 새 기준에서 내는 발화는 B13 그 한 건(진짜 결함)뿐이다.
**다음 판에서 볼 것**: 이 검사가 판 안에서 발화하면 재생성으로 닫히는가(B13 은 Critic 이 잡았는데 채택이 버렸다 — 기계 검사는 시도마다 돌므로 그 경로가 닫힌다).
