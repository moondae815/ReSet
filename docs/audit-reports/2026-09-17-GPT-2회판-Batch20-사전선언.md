# GPT 계획 시도 2 회 판 — `POQSettleBatch20` (사전 선언)

**작성 시점**: 2026-09-17, **실행 전**. **코드**: main `868ac0c7`(순서 오라클 병합) + 이 문서 커밋.

## 0. 물음

B19 는 L1 에서 멈춰 L2 에 못 갔다 — 오탐 아홉(문서 순서)이 수리 회차를 먹고, 진짜 하나(제어표 컬럼)가 귀속 방향 때문에 안 닫혔다. 앞의 것은 고쳤고(`868ac0c7`) 뒤의 것은 원인만 쟀다(B19 판독 §추가).

| 물음 | 기준선(B19) | 이 판이 재는 것 |
| :-- | :-- | :-- |
| 순서 오라클이 실물에서 듣는가 | 첫 회차 게이트 오탐 **9** | RunId 게이트·발급 전 잠금의 판 안 발화 — **오탐 0** 인가(목차에 떠돌이 `### Sxx` 헤딩이 또 나오는지도 센다) |
| 제어표 컬럼 결함 | 11 → 1 → 1, 골격만 열려 소진 | 대상이 생기는가 · 생기면 귀속이 **정의 자리를 여는가**(안 열면 B19 와 같은 모양으로 소진될 것이다 — **예측**) |
| L2 도달 | 미도달 | L2 까지 가는가 · 점수 |
| 어휘 계약 두 번째 표본 | 정본 넷뿐 · 혼용 0 | 같은가 |
| 저널 헬퍼 이름 | 여섯 이름(안 들음) | 같은가(이번 판은 이 축을 고치지 않았다) |

## 1. 실행 조건 — B19 와 같다

```
PLANONLY_MAX_L2_ATTEMPTS=1 PLANONLY_CONSOLIDATOR_PROVIDER=OpenRouter \
PLANONLY_CONSOLIDATOR_MODEL=openai/gpt-5.6-sol PLANONLY_OPENROUTER_ONLY_BACKEND=openai \
scripts/run-plan-only-job.sh POQSettleBatch20
```

## 2. 예측

| # | 예측 | 틀리면 |
| :-- | :-- | :-- |
| P1 | 35~70 분 · $6~10 | 국면별로 적는다 |
| P2 | RunId 게이트·발급 전 잠금 **오탐 0**(발화하면 전부 진짜인지 원문으로 판정) | 오탐이면 목차가 넘어갔는지(로그의 L1 호출 인자는 안 찍히므로 결과로 판정) |
| P3 | 제어표 컬럼 결함이 **또 나면 판 안에서 안 닫힌다**(귀속 경로를 안 고쳤다) | 닫히면 어느 자리가 열렸는지 적는다 — 예측이 틀린 것이다 |
| P4 | 어휘: 정본 넷뿐 · 혼용 0 · 저널 헬퍼 이름은 여전히 여럿 | 벗어난 자리를 적는다 |
| P5 | 점수·L2 도달은 예측하지 않는다 | — |

## 3. 합격선

B19 의 G1~G14 를 그대로 쓴다(게이트 경고 0 은 `dotnet build --no-incremental` 로 따로 확인 · K2 두 변종은 따로 부른다 · 시도 조립본은 제품 조립기로). 추가:

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| G15 | 순서 오라클 | 판 안 L1 오류문 전수에서 「보다 먼저 실행되어」 계열 발화를 **원문으로 판정** — 오탐 0 |
| G16 | 귀속 수렴 | 판 안 L1 오류마다 **다음 회차에서 사라졌는가** · 안 사라진 것은 그 회차에 **어느 자리가 열렸는지** |
