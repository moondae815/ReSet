# POQSettleBatch1 재생성 판독 — 축 B 로드맵 5

사전 선언: `docs/superpowers/plans/2026-09-04-axis-b-roadmap5-readout.md` (실행 중에 작성·커밋).
아래는 그 문서의 순서를 그대로 따른다.

## 1. 완주 — 했다

`17:27:12 → 20:06:39` (2시간 39분). 마지막 줄 「통합 마이그레이션 지시서 번들 내보내기 완료」.
커밋 `f69039d2`, 입력 지문 대조 통과(명세서 12편 · 드리프트 0).

## 2. 회차와 채점 — 사전 선언한 부정 신호에 걸렸다

| | 값 |
| :--- | :--- |
| 궤적 | **46 → 42 → 40** |
| 채택 | 46 (최고점 채택 · 되돌림 없는 하락 0) |
| 총 회차 / 채점 못한 회차 | 6 / **3** |
| 검증 상태 | **품질 미달** |

**전(감사 대상)은 통과 88이었다.** 축별로는 정합 8→3 · CRUD 9→3 · 인터페이스 9→3 ·
예외 9→4 · 가독성 9→10.

**채점 못한 회차 3은 전부 L1 귀속 실패다**(로그의 「수리 회차: 아니오」 = 전량 재생성).
내역: 축약어 「위와 동일」 2건 · 규칙 3-1 1건.

- 축약어 2건은 **설계상 귀속 불가**다 — `ForbiddenShortcuts`는 금지 토큰을 작은따옴표로 싣고
  백틱으로 싣지 않아 `ViolationLexemes`가 빈 어휘를 낸다. `known-defects.md`가 이 부류를
  「자리를 모른다」로 이미 적어 둔 그것이다.
- 규칙 3-1 1건은 **원인 미상이다.** 그 회차의 문서가 어디에도 안 남아 재구성할 수 없다 —
  오늘 올린 「반려된 판을 아무도 안 남긴다」가 같은 날 두 번째로 물었다.

**이 회차의 골격 귀속(2026-09-04 신규)은 한 번도 발동하지 않았다**(골격 수리 0). L2의
`SkeletonDefective`는 2회 발동했다(기존 기능). 「전량을 진짜 전량으로」 변경은 작동했다 —
전량 재생성마다 골격도 다시 만들어 골격 호출이 6회다.

## 3. 검사별 발화량 — Batch1 12 → 0

| 대상 | A | B | C | D | E | 미분류 | 계 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Batch1 (전) | 2 | 1 | 0 | 0 | 3 | 6 | **12** |
| Batch1 (후) | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| 코퍼스 전체 (전) | 20 | 26 | 20 | 0 | 59 | 1095 | |
| 코퍼스 전체 (후) | 18 | 25 | 20 | 0 | 56 | 1089 | |

코퍼스 총계가 정확히 12만큼 줄었다 — 다른 Job은 손대지 않았다는 확인이다.

**그러나 이것을 성과로 읽으면 안 된다.** 사전 선언의 혼입 (다) 그대로다 — 이 회차가 만드는
것이 세대 정합이고, 세대 차이에서 오던 발화가 사라지는 것은 **성과가 아니라 이 회차의 정의**다.
그리고 같은 산출물의 Critic 점수는 88 → 46으로 떨어졌다.

**기계 발화 0이 품질을 뜻하지 않는다** — 한 회차 안에서 그 둘이 정면으로 갈렸다.

## 4. 🔴 EXCEPTION_PROC — 메워졌다. 다만 기계가 민 것이 아니다

**단계 번호가 밀렸다**(16단계 → 18단계). EXCEPTION_PROC는 **S07 → S08**로 옮겼다.
번호로 대조하면 다른 원본을 비교하게 된다.

| | 전 (S07) | 후 (S08) |
| :--- | ---: | ---: |
| UPDATE 문 | 8 | **18** |
| 주석 자리표시자 | 17 | 18 |

감사가 이름으로 지목한 산식·UDF가 실렸다.

| 앵커 | 전 | 후 |
| :--- | ---: | ---: |
| `LOLLETTER4` (갱신 6의 IIF 상수) | 0 | 4 |
| `UF_GET_ROUND4VAT` (갱신 8·9) | 2 | 6 |
| `UF_GET_COMM4CLIENT4PARTIALCANCEL` (갱신 14, 9인자) | 1 | 2 |
| `UF_Get_CLComm4MobileCo` (갱신 15, 6인자) | 0 | 2 |

UPDATE 18은 감사가 말한 「18개 갱신」과 정확히 맞는다.

**귀속: 모델 재량이다.** 사전 선언이 갈라 둔 두 갈래 중 (b)다. 하한 미달 재생성 57건은
S05(54)·S02(31)·S09(21)·S14(20)·S06(14)·S15(11)·S04(10)에서 났고 **S08은 한 건도 없다.**
기계가 그 자리를 민 적이 없다.

**그러므로 다음 걸음은 재생성을 더 하는 것이 아니라 검사를 만드는 것이다**(사전 선언 §6-(3)).
근거가 하나 더 좋아졌다 — 오늘 모델이 그 본문을 실제로 썼으므로, 그 검사는 **만족 가능하다**.
강제가 없는 채로 두면 다음 재생성에서 조용히 사라진다. 검사 D가 그렇게 꺼졌다.

## 5. 판정하지 않는 것

축 B 46건의 해소 여부. 이 회차의 수치로 대신할 수 없다(사전 선언 §6-(4)).

**재감사(로드맵 6)에 넘기는 제약 하나**: **감사 앵커인 단계 번호가 재생성을 못 견딘다.**
16→18로 밀려 EXCEPTION_PROC가 S07→S08이 됐다. 재감사는 단계 번호가 아니라 **원본 프로시저로
대조**해야 한다.

## 6. 새로 생긴 것 — Critic이 지적한 청크 경계

1차 리뷰가 지적했다: **S02~S06의 청크 범위가 `>= @p_from AND < @p_to`인데 경계가 `MIN/MAX`라
최대 키 행이 누락된다.** 정적으로 읽으면 실재하는 결함이고, 전 산출물에 같은 것이 있었는지는
이 회차가 답하지 않는다. 축 B 재감사가 볼 자리다.

## 7. 남긴 것

- 전(감사 대상)은 `output.bak-batch1-preregen-20260904/`에 그대로 있다(111파일 + MANIFEST).
- 후는 `output/Jobs/POQSettleBatch1/`이다. 되돌리려면 사전 선언 §8.
