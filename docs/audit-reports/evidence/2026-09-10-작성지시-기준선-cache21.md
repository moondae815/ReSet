# 되돌림 기준선 — 캐시 v22 재생성 **전**의 배송본

`CheckDocumentInstructsItsAuthor` 가 캐시 v21 산출물에서 낸 발화 전량이다.
v22 재생성이 `output/` 을 덮으면 이 상태는 사라진다 — `output/` 은 gitignore 라
이 파일이 그 상태의 유일한 기록이다.

**이 수가 재생성 뒤 어떻게 움직이는가**가 (a)+(b) 의 판정이다.
「프롬프트 쪽 목록이 0 이 됐다」는 그 자리의 증거가 못 된다 — 표지는 프롬프트에
있고 유출은 산출물에 있다.

## 기준선: 발화 11 줄 · 8 편 (배송본 31 편 중)

### External/SETTLE_CARD_DB/Functions/dbo.UF_GET_COMM4CLIENT4PARTIALCANCEL:40

```
`(없음)`은 이 함수가 스키마 바인딩(`SCHEMABINDING`)되어 있지 않음을 확정하는 값이다. 스키마 바인딩 여부가 확인 불가하다고 서술해서는 안 된다.
```

### Functions/dbo.UF_GET_OUTYMD4REFUND:26

```
`(없음)`은 이 함수가 스키마 바인딩(`WITH SCHEMABINDING`)되지 않았음을 확정한 값입니다. 스키마 바인딩 여부를 판단할 수 없다고 서술해서는 안 됩니다.
```

### Functions/dbo.UF_GET_OUTYMD4REFUND:92

```
SELECT 1은 함수 내부의 변수 대입 조회이므로, 위 술어들은 이 문장이 읽어들이는 행(즉 `MIN(OutYMD)` 집계에 참여하는 행)을 좁히는 필터로 해석해야 하며, 어떤 INSERT/UPDATE/DELETE의 대상 행을 제한하는 것으로 서술해서는 안 됩니다.
```

### Procedures/dbo.UP_UTIL_SETTLE_COMM_UPD:255

```
> `기준일 파라미터 적용` 칸의 `아니오`는 **최상위 WHERE에 없다**는 뜻일 뿐이다. 하위 질의·파생 테이블 안에서 기준일을 쓰는 문장(UPDATE 7)이 있으므로, 이 칸을 근거로 "이 문장은 기준일을 사용하지 않는다"고 서술해서는 안 된다.
```

### Procedures/dbo.UP_UTIL_SETTLE_EXCEPTION_PROC:329

```
> `기준일 파라미터 적용` 칸의 `아니오`는 **최상위 WHERE에 없다**는 뜻일 뿐이다. 하위 질의·파생 테이블 안에서 기준일을 쓰는 문장(UPDATE 18)이 있으므로, 이 칸을 근거로 "이 문장은 기준일을 사용하지 않는다"고 서술해서는 안 된다.
```

### Procedures/dbo.UP_UTIL_SETTLE_EXCEPTION_PROC:720

```
위 표에는 같은 코드를 쓰는 문장이 있습니다: -1→UPDATE 3·UPDATE 4 · -2→UPDATE 5·UPDATE 6. 따라서 반환 코드만으로는 실패 지점을 특정할 수 없습니다. 대부분의 코드가 실제로 서로 다르더라도, 이 공유 관계 때문에 반환 코드 전체를 "고유하다"거나 "서로 다르다"고 표현해서는 안 되며, 실패 지점을 정확히 특정하려면 코드 값뿐 아니라 트랜잭션 로그·프로파일러 등 추가 진단 수단이 필요하다는 사실을 그대로 서술해야 합니다.
```

### Procedures/dbo.UP_UTIL_SETTLE_INS:135

```
> `기준일 파라미터 적용` 칸의 `아니오`는 **최상위 WHERE에 없다**는 뜻일 뿐이다. 하위 질의·파생 테이블 안에서 기준일을 쓰는 문장(INSERT 1)이 있으므로, 이 칸을 근거로 "이 문장은 기준일을 사용하지 않는다"고 서술해서는 안 된다.
```

### Procedures/dbo.UP_UTIL_SETTLE_INS_EXTRA:183

```
> `기준일 파라미터 적용` 칸의 `아니오`는 **최상위 WHERE에 없다**는 뜻일 뿐이다. 하위 질의·파생 테이블 안에서 기준일을 쓰는 문장(INSERT 1)이 있으므로, 이 칸을 근거로 "이 문장은 기준일을 사용하지 않는다"고 서술해서는 안 된다.
```

### Procedures/dbo.UP_UTIL_SETTLE_INS_EXTRA4PLCARD:147

```
> `기준일 파라미터 적용` 칸의 `아니오`는 **최상위 WHERE에 없다**는 뜻일 뿐입니다. INSERT 1은 하위 질의(파생 테이블 X)의 WHERE에서 `A.REQYMD = @pi_strYMD`로 기준일을 사용하므로, 이 칸을 근거로 "이 문장은 기준일을 사용하지 않는다"고 서술해서는 안 됩니다.
```

### Procedures/dbo.UP_UTIL_SETTLE_INS_EXTRA4PLCARD:204

```
NOLOCK 힌트가 적용된 스캔(IF 1의 TSettleMst, INSERT 1 파생 테이블의 TExtraTxMst/TPLCardTxMst/TClientCMRate/TPGProperty(P), 최상위 TPGProperty(Y), UPDATE 1/2의 TSettleMst)은 트랜잭션 격리수준을 사실상 READ UNCOMMITTED로 완화하므로, 동시에 다른 트랜잭션이 커밋되지 않은 데이터를 읽는 더티 리드(dirty read), 존재하지 않던 행이 읽히거나 이미 삭제된 행이 반영되는 비일관성 위험이 있습니다. 반면 DELETE 1의 TSettleMst/TPGProperty, UPDATE 1/2의 TPGProperty(PG)에는 힌트가 없어 기본 격리수준(일반적으로 READ COMMITTED)의 잠금 규칙이 적용됩니다. 동일 테이블이라도 문장·별칭에 따라 힌트 유무가 다르므로 일괄적으로 서술해서는 안 됩니다.
```

### Procedures/dbo.UP_UTIL_STAT_PGCOLLECT_INS:105

```
> `기준일 파라미터 적용` 칸의 `아니오`는 **최상위 WHERE에 없다**는 뜻일 뿐이다. 하위 질의·파생 테이블 안에서 기준일을 쓰는 문장(INSERT 1)이 있으므로, 이 칸을 근거로 "이 문장은 기준일을 사용하지 않는다"고 서술해서는 안 된다.
```

