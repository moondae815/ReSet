# 예외 감점의 실제 사유 — 의사코드 API 이름이 문서 안에서 갈린다 (사전 선언)

**작성 시점**: 2026-09-17, **구현 전**. **코드**: main `d5f9504f` + 이 문서 커밋.

## 0. 잰 것 — 배너의 축 이름이 사유를 말하지 않는다

「품질 불합격 — 예외 기준 미달」 배너가 매 판 붙는데 그 사유가 판독된 적이 없었다. 리뷰 JSON **23 개**(Job 10 편)를 전수로 읽었다.

| 축 | 평균 | 8 미달 |
| :-- | --: | :-- |
| 정합성 | 8.0 | 9/23 |
| CRUD | 9.0 | 3/23 |
| **인터페이스** | 7.7 | **11/23** |
| **예외** | 7.8 | **10/23** |
| 가독성 | 9.0 | 1/23 |

「예외」가 8 미달인 열 자리의 **실제 사유는 예외 처리가 아니다**. 네 가족으로 갈린다 — 통제 합계 이름 계약(B7·B8·B13·B17, K2 가족) · 트랜잭션 경계 파기(B18 1 차) · 배치 객체 이름 계약(B11) · 조인 키 오류(B8). 즉 Critic 은 **아무 축에나 감점을 싣는다**(축 이름은 사유가 아니다).

### B18 이 남긴 배너의 사유(이 회차의 자리)

B18 은 1 차의 트랜잭션 결함을 **2 차에서 닫았고**(예외 6 → 7, 총점 90 → 92) 배송본은 옳다 — S16·S17 이 `groupTx` 를 공유 문맥에서 받고 「자체 트랜잭션을 시작하거나 커밋하지 않는다」고 명시하며, 여는 것은 `controlConn`(저널·체크포인트) 뿐이다. 남은 감점 사유는 **「한 문서 한 표기」 위반**이다:

- 다중 행 조회가 세 이름으로 — `queryAll`(S01·S14·S18) · `queryMany`(S16·S19) · `query`(S12)
- 실패 저널 기록이 두 이름으로 — `writeFailureJournal`(대부분) · `markStepFailedInSeparateControlTransaction`(S07·S09)

**원인은 계약 공백이다.** 프롬프트는 의사코드 리포지터리 API 이름을 **아예 규정하지 않는다**(`AiService` 의 프롬프트 전문에서 `repository` 는 다른 규칙 한 줄에만 나온다). 그래서 모델이 단계마다 이름을 새로 짓고, Critic 이 그 산포를 감점한다.

### 이름 분포 (배송본 **14** 편 전수 · `repository.` 접두사가 붙은 호출만)

| 이름 | 호출 | 문서 |
| :-- | --: | --: |
| `execute` | 610 | 7 |
| `queryScalar` | 71 | 5 |
| `queryRows` | 37 | 4 |
| `queryRow` | 33 | 4 |
| `queryOne` | 9 | 1 |
| `queryAll` | 6 | 1 |
| `query` | 4 | 3 |
| `queryMany` | 3 | 1 |
| `writeStepJournal`·`writeCheckpoint`·`queryRowOrNone`·`querySingle` | 각 1~3 | 각 1 |

**한 문서 안에서 같은 연산을 두 이름 이상으로 쓰는 문서: 14 편 중 6**(접두사 무관 재집계 — 다중 행 B5·B10·B14·B18 · 단일 행 B12·B17). 접두사 자체도 시대별로 갈린다 — `repository.execute` 610 대 바닥 `execute` 642(B1·B4·B5·B7·B8 이 바닥형, B10·B12·B14·B17·B18 이 접두사형).

## 1. 설계 — 계약으로 못박는다(검사는 이번에 만들지 않는다)

정본 이름을 **관측에서** 고른다(가장 많이 쓰인 것, 뜻이 다른 것은 남긴다):

| 연산 | 정본 이름 | 근거 |
| :-- | :-- | :-- |
| 문장 실행(쓰기·DDL) | `execute` | 610 회·7 편, 경쟁 이름 없음 |
| 스칼라 하나 | `queryScalar` | 71 회·5 편, 경쟁 이름 없음 |
| 행 여러 개 | `queryRows` | 37 회·4 편(최다) — `queryAll`·`queryMany`·`query` 를 대체 |
| (호출 모양) | `repository.<이름>(conn, tx, SQL_NAME, parameters)` | `conn, tx` 명시형이 **234 회** 관측된다(B12 110 · B18 124 — 가장 최근 두 판의 관행). 그 밖의 문서는 `execute(SQL_NAME, …)` 형이라 **이 부분은 최근 관행을 따르는 변경**이다 |
| 행 하나 또는 없음 | `queryRow` | 33 회·4 편(최다) — `queryOne`·`querySingle`·`queryRowOrNone` 를 대체(없을 수 있음을 계약에 적는다) |

- 싣는 자리: `AiService.AppendSharedStepContext`(단계·골격·단일 호출 폴백 **세 경로**가 함께 쓴다).
- 문구에 담을 것: 이 넷만 쓴다 · 같은 연산에 다른 이름을 만들지 않는다 · 저널·체크포인트 기록도 문서 안에서 한 이름으로 쓴다(이름은 모델이 고르되 **하나**여야 한다).
- **검사는 이번에 만들지 않는다.** 이유는 둘이다 — ① 이름 정확 대조 검사는 계약 **전에** 생성된 배송본 14 편 중 **아홉 편**에 발화해 스윕(내 회귀 기구)을 덮는다 ② B18 이 가르쳐 준 대로 **계약이 검사보다 앞선다**. 다음 판에서 계약이 듣는지 보고, 들으면 그때 회귀 방지용 검사를 붙인다(들지 않으면 그 검사에 실물 대상이 생긴다).

**범위 밖**: 저널·체크포인트 헬퍼 이름의 정본 지정(모델 자유, 한 이름만 요구) · 「인터페이스」 축 감점(11/23 으로 더 흔하다 — 다음 후보) · 이미 배송된 문서의 이름 통일.

## 2. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| R1 | 배선 | 정본 표가 **세 경로**(단계·골격·폴백) 프롬프트에 실린다 — 경로마다 잠금 시험 |
| R2 | 문구 | 「이 넷만」·「같은 연산에 다른 이름 금지」·「행 하나 또는 없음」·「저널 이름은 문서 안에서 하나」가 프롬프트에 있다 |
| R3 | 되돌림 | 정본 표를 걷어내면 R1·R2 가 빨강 |
| R4 | 게이트 | 실패 0 · 건너뜀 0 · 경고 0 |
| R5 | 기준선 기록 | 이 문서의 이름 분포와 「한 문서 혼용 4/13」을 다음 판의 판정 기준으로 남긴다 |

**캐시**: 계획서 프롬프트만 바뀐다 — `CacheManager.CurrentCacheFormatVersion`(명세서 캐시)은 그대로, `PlanAttemptJournal.ContractVersion` 은 **2 → 3**(옛 시도를 재사용하면 새 계약이 안 돈다).
**효력 판정**: 다음 판(B19)에서 ① `repository.*` 이름이 정본 넷 안인가 ② 한 문서 혼용이 0 인가 ③ 예외 축 감점이 이 사유로 다시 나오는가.

## 결과

| # | 항목 | 실측 | 판정 |
| :-- | :-- | :-- | :-- |
| R1 | 배선 | 정본 표가 **세 경로** 프롬프트에 실린다 — 단계 섹션 · 골격 · 단일 호출 폴백 각각 잠금 시험(단계 인터페이스가 없어도 실리는 고정 자산임을 네 번째 시험이 못박는다) | ✔ |
| R2 | 문구 | `[Pseudocode Repository API]` · 네 정본(`execute`·`queryRows`·`queryRow`·`queryScalar`) · 「one row or none」 · **밀려난 이름 금지 목록**(`queryAll`·`queryMany`·`query`·`queryOne`·`querySingle`·`queryRowOrNone`) · 「never invent a second name for the same operation」 · 저널·체크포인트는 「one name per operation for the whole document」 | ✔ |
| R3 | 되돌림 | **넷 전부 빨강** — r1 정본 표 제거 · r2 금지 목록 제거 · r3 「한 문서 한 이름」 제거 · r4 `ContractVersion` 3 → 2 | ✔ |
| R4 | 게이트 | 통과 **4543** · 실패 0 · 건너뜀 0 · 경고 0 | ✔ |
| R5 | 기준선 | 위 §0 의 이름 분포와 「한 문서 혼용 4/13」이 다음 판의 판정 기준이다 | ✔ |

**캐시**: `CacheManager.CurrentCacheFormatVersion` 그대로(명세서 프롬프트 불변) · `PlanAttemptJournal.ContractVersion` **2 → 3**(r4 가 그 자리를 지킨다).
**프롬프트 크기**: 정본 표는 제어 계약 표와 같은 자리(공유 접두사)에 붙는 **고정 자산 11 줄**이다 — 단계마다 갈리지 않으므로 접두사 캐시를 깨지 않는다.

**다음 판(B19)에서 볼 것**: ① `repository.*` 이름이 정본 넷 안인가 ② 한 문서 혼용이 0 인가 ③ 예외 축 감점이 이 사유로 다시 나오는가. **들면** 그때 회귀 방지용 이름 정확 대조 검사를 붙이고(그때는 계약 이후 산출물이 기준선이다), **안 들면** 그 검사에 실물 대상이 생긴다.

## 최종 리뷰 반영 (`dcd5211d` 범위)

Critical **0** · Important **5** · Minor **2**. 리뷰가 독립으로 재현한 것: 리뷰 JSON 23 개와 축별 8 미달 분포(정합성 9·CRUD 3·인터페이스 11·예외 10·가독성 1) 일치 · 접두사 기준 이름 분포 수치 전부 일치 · **B18 배송본이 옳다는 내 최종 판단 확인**(S16·S17 은 `groupTx` 공유, `controlConn` 은 저널·체크포인트 전용) · 되돌림 r1·r4 빨강.

| 등급 | 지적 | 조치 · 내가 다시 잰 것 |
| :-- | :-- | :-- |
| **Important 1** | B7 을 「통제 합계 이름」 가족에 잘못 넣었다 — 그 리뷰 레코드에 「통제」·`ControlName` 이 없다 | 맞다. **다섯째 가족(표기·접두사 불일치)으로 옮겼다** — B7 의 사유가 바로 이 계약이 겨냥하는 자리다(§0 정정 블록) |
| **Important 2** | 배송본은 **14** 편이다(내 13 은 off-by-one) | 내 손으로 `find` 해서 14 를 확인했다. 문서·시험 주석의 분모를 전부 고쳤다(혼용 6/14 · 금지 이름 사용 9/14) |
| **Important 3** | 「혼용 4/13」은 `repository.` 접두사 붙은 호출만 센 값이라 과소평가다 | 접두사 무관으로 다시 세어 **6/14**(B5·B10·B12·B14·B17·B18) — 리뷰의 8/14 과도 다르다(리뷰는 산문 언급까지 포함한 듯하다. 내 자는 `(?:repository\.)?이름(` 이다). 접두사 자체의 갈림(610 대 642 · 시대별)도 적었다 |
| **Important 4** | 정본 호출 모양 `(conn, tx, SQL_NAME, parameters)` 는 **관측에 없는 새 규격**이다 | **이 지적은 틀렸다.** `conn, tx` 명시형이 **234 회** 관측된다(B12 110 · B18 124 — 가장 최근 두 판). 다만 나머지 문서는 `execute(SQL_NAME, …)` 형이라 「최근 관행을 따르는 변경」임을 표에 적었다 |
| **Important 5** | 새 시험 파일이 **CS8625 경고**를 낸다 — 「경고 0」 판정이 거짓이다 | 맞다. `dotnet test` 요약에 경고가 안 찍혀 내가 놓쳤다(`dotnet build --no-incremental` 이 잡는다). `null` 을 `Array.Empty<StepInterface>()` 로 바꿔 **경고 0** 을 확인했고, 같아져 버린 두 시험 중 하나를 **재료 있는 경로**로 되돌려 둘이 다른 것을 재게 했다 |
| Minor | `queryRow`/`queryRows` 한 글자 차이 혼동 우려 · `queryRowOrNone` 흡수 | 코퍼스에 그 오기 용례는 없다(리뷰도 확인) — B19 에서 그 패턴만 본다 |
| (리뷰 미확인) | 되돌림 r2·r3 | 내가 이미 개별로 돌렸고 둘 다 빨강이다(4 실패/5) |

**게이트(반영 뒤)**: 통과 **4543** · 실패 0 · 건너뜀 0 · **경고 0**(`dotnet build --no-incremental` 로 따로 확인).

## 재생 실험 (2026-09-17, 계약 병합 뒤 · main `1e3d7594`)

B18 의 **S19 첫 초안 요청**(그 판에서 `queryMany`·`queryOne` 둘을 함께 쓴 단계)을 제품이 붙이는 자리(제어 계약 표 절 뒤)에 어휘 블록만 끼워 3 회씩 재전송했다. 백엔드는 `openai` 고정 · 비용 합 **$1.89**.

| 팔 | 응답 | 나온 이름 | 판정 |
| :-- | --: | :-- | :-- |
| 그대로 | 1 | `execute` · **`query`** | 비정본 |
| 그대로 | 2 | `execute` · **`query`** | 비정본 |
| 그대로 | 3 | `execute` · **`queryAll`** | 비정본 |
| 어휘 블록 | 1 | `execute` · `queryRows` | **정본만** |
| 어휘 블록 | 2 | `execute` · `queryRow` · `queryRows` · `queryScalar` | **정본만** |
| 어휘 블록 | 3 | `execute` · `queryRows` · `queryScalar` | **정본만** |

**그대로 3/3 이 비정본 · 블록 3/3 이 정본만.** 이 실험은 **판별력이 있다** — 대조군이 결함을 3/3 재현했다(발급 전 잠금 회차의 재생 실험은 대조군 3/3 이 이미 합법이어서 판별력이 0 이었다. 그 차이를 이 회차가 보여 준다).

한 가지 더 적어 둔다: **그대로 팔이 낸 비정본 이름은 B18 실물과 달랐다**(`queryMany`/`queryOne` 대신 `query`/`queryAll`). 즉 「어떤 틀린 이름이 나오는가」는 흔들리고 **「정본 밖으로 나간다」가 안정적인 성질**이다 — 다음 판 판정도 특정 이름이 아니라 **정본 집합 밖인가**로 세야 한다.

**남은 판정**: 이 실험은 단계 섹션 한 자리(S19)를 3+3 으로 본 것이다. 문서 전체의 「한 문서 한 표기」(혼용 6/14 → 0)는 판 하나를 돌려야 보인다 — 다음 판(B19)의 관측 항목으로 남긴다.
