# GPT 계획 시도 2 회 판 — `POQSettleBatch14` (사전 선언)

**작성 시점**: 2026-09-14, **실행 전**. 합격선을 결과보다 먼저 못박는다.
**코드**: main `0e2fafd6` + 이 문서 커밋.

## 0. 물음 — 오늘 main 에 들어간 넷을 실물 판에서 처음 돌린다

1. **RunId 발급 전 단계 게이트**(`CheckGateRequiresStepsBeforeRunId`, 통합 L1): GPT 세 판이 3/3 이 결함을 배송했다. 판 안에서 발화하는가, **L1 수리로 닫히는가**
   (단계 절 게이트 → `OwnerStepCode` 로 그 단계 지목 재생성 · 골격 게이트 → 골격 패치 수리).
2. **K2 검증 세트판**(`ValidateVerificationControlTotalNames`, 통합 L1): 발화하면 골격 패치로 닫히는가. 코퍼스 GPT 판 2/3(B11 · B13)이 결함을 배송했다.
3. **Critic 골격 지적 → 패치**(방침 A 변경): `SkeletonDefective` 가 나면 2 차가 백지가 아니라 패치로 가는가, 그리고 Batch13 처럼 수정이 버려지지 않는가.
4. **생산자 검사**(`CheckControlTotalProducer`): 발화하면 단계로 좁혀지는가(전에는 늘 전량 재생성).

**새로 생긴 위험을 먼저 적는다**: 새 통합 L1 검사가 발화하면 1 차 시도는 Critic 에 가기 **전에** L1 에서 멈추고 L1 수리 예산(2)을 쓴다. 수리가 닫지 못하면 예산 소진 뒤
구제 채택할 채점 후보가 없어 **L1 배너가 붙은 문서가 배송될 수 있다.** 이 판은 그 위험이 실제로 나는가도 잰다.

## 1. 실행 조건 — Batch13 과 같다

```
PLANONLY_MAX_L2_ATTEMPTS=1 PLANONLY_CONSOLIDATOR_PROVIDER=OpenRouter \
PLANONLY_CONSOLIDATOR_MODEL=openai/gpt-5.6-sol PLANONLY_OPENROUTER_ONLY_BACKEND=openai \
scripts/run-plan-only-job.sh POQSettleBatch14
```

계획 시도 2 회 · L1 수리 예산 2 · `Narrow` · SP 14 편 · Critic `z-ai/glm-5.3`. Batch13(`1e31d6b4`)과 다른 것은 코드뿐이다(위 넷 + 가드 짝 일대일 등은 Batch13 전에 이미 들어갔다).

## 2. 예측

| # | 예측 | 틀리면 |
| :-- | :-- | :-- |
| P1 | 벽시계 **35~60 분** · Consolidator **$7~11** | 범위 밖이면 국면별로 어느 항이 늘었는지 적는다(L1 수리 호출 수를 따로) |
| P2 | RunId 게이트 검사가 **판 안에서 발화한다**(근거: GPT 3/3) | 발화 0 이면 「이번 판은 발급 단계가 첫 단계였나 / 게이트가 S01 을 요구하지 않았나」를 목차·본문으로 가른다 |
| P3 | 발화했다면 **L1 수리 예산 안에서 닫힌다** — 배송본의 통합 L1 재측정에서 이 검사 발화 0 | 닫히지 않으면 처방문·귀속 중 어디가 막았는지 로그 시도별로 적는다 — 계약 층 수정(남은 작업 2)의 근거가 된다 |
| P4 | K2 검증 세트판 · 생산자 검사 · Critic 골격 지적은 **예측하지 않는다** — 표본이 2~3 판뿐이다 | — |
| P5 | `SQL_CURRENT_RUN_ID` 누락 재생성 0~2 | 3 이상이면 적는다 |

## 3. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| G1 | 실행 | 종료 코드 0 · 번들 내보내기 완료 · HTTP 실패 0 |
| G2 | 라우팅 | Consolidator 응답 전부 `OpenAI` · 요청 백엔드 고정 |
| G3 | 비용 | Consolidator `usage.cost` 합 ≤ $25 |
| G4 | 시간 | ≤ 90 분 |
| G5 | 점수 | 채택 점수 ≥ 74 (신호일 뿐) |
| G6 | 앵커 | 도달률 5/5 · SELECT 불일치 0 · (종류,서수) 불일치 0 |
| G7 | 게이트 | 새 Job 포함 전체 시험 실패 0 · 건너뜀 0 |
| G8 | 새 검사 발화 판정 | 로그의 `L1 기계 검증 오류 발견` 전부를 검사별로 갈라 **전부** 원본 DDL·계획서 본문으로 판정 — 오탐 0. 오탐이면 그 검사를 「실패」로 적는다 |
| G9 | 닫힘 | 판 안에서 발화한 새 검사 위반마다: 귀속 자리(로그 「… 자리로 좁혀」) · 다음 시도에서 사라졌는가 · 배송본 통합 L1 재측정 결과. **L1 배너가 붙은 배송**이면 「L1 이 수렴하지 않음」으로 적는다(실패가 아니라 물음 1 의 답) |
| G10 | 골격 패치 | `SkeletonDefective` 가 났으면 로그에 「골격 패치」 상태문과 골격 요청 「수리 회차: 패치」, 앵커 가드 재시도 여부를 적는다. 2 차가 있었으면 Batch13 과 같은 두 시도 대조(닫힘 · 새 결함 · 채택이 수정을 버렸나)를 한다 |
| G11 | 채택 | 채택 시도와 배송본의 단계별 일치 |

**기록(합격선 아님)**: 국면별 시간 · 시도별 호출·비용 · L1 수리 호출과 그 비용 · Critic 비용 · 하한 재생성 사유별 횟수 · 배너 수(하한 미달 · 목차 결함 · L1) · Batch11~13 과 나란히 놓은 표.

---

# 실행 결과 (2026-09-14 11:17:42 ~ 11:44:46 · 27 분 04 초)

실행 워크트리 `9a60d0dc` · 종료 코드 0 · 목차 **16 단계** · 1 차 시도 **94/100 · 결함 없음**(`HasDefects = false`) → **2 차 시도 없음**.

## 한 줄 결론

**오늘 넣은 넷 중 어느 것도 실물에서 판정받지 못했다 — 넷 다 대상이 없었다.** 이번 판은 S01 이 스스로 RunId 를 발급했고(환경 검증과 실행 등록을 한 단계로),
통합 L1 위반 0 · Critic 결함 없음이라 L1 수리·골격 패치·2 차 시도가 한 번도 돌지 않았다. 대신 **가드 술어 대조가 판 안에서 두 번 발화해 둘 다 닫았고**,
기존 검사 T25 의 **거짓 「하한 미달」 배너**가 배송됐다.

## 합격선

| # | 항목 | 실측 | 판정 |
| :-- | :-- | :-- | :-- |
| G1 | 실행 | 종료 코드 0 · 번들 내보내기 완료 · HTTP 실패 0 | 합격 |
| G2 | 라우팅 | Consolidator 응답 24/24 `OpenAI`(요청 `order:["openai"]`) · Critic 1 회 `GMICloud` | 합격 |
| G3 | 비용 ≤ $25 | Consolidator **$5.47** (Critic $0.41 별도) | 합격 |
| G4 | 시간 ≤ 90 분 | **27 분 04 초** | 합격 |
| G5 | 점수 ≥ 74 | **94** | 합격 — 신호일 뿐 |
| G6 | 앵커 | 도달률 **4/4**(놓친 선언 행 0) · SELECT 앵커 10 · 불일치 0 · (종류,서수) 12 단계·92 문장·불일치 0 | 합격 — **분모가 5 가 아니라 4** 다(목차가 달라 선언 단계 수가 달라짐). 사전 선언의 「5/5」 는 분모를 고정한 잘못이다 |
| G7 | 게이트 | 새 Job 포함 4407 통과 · 실패 0 · 건너뜀 0 · 경고 0 | 합격 |
| G8 | 새 검사 발화 판정 | 로그 `L1 기계 검증 오류 발견` **0** — RunId 게이트 · K2 검증 세트판 · 생산자 검사 판 안 발화 0. 배송본 재측정도 통합 L1 0 · K2(단계·검증 세트) 0 | **대상 판정**: RunId 게이트 — **대상 없음**(S01 이 `INSERT INTO batch.BatchRun` 으로 발급, 발급 전 단계 0). 생산자 검사 — 자기 제외 읽기 없음. K2 검증 세트판 — 아래 「판정 보류」 |
| G9 | 닫힘 | 새 검사 발화가 없어 **표본 없음** | 표본 없음 |
| G10 | 골격 패치 | `SkeletonDefective` 없음 · 골격 요청 1 회(「수리 회차: 아니오」) | 표본 없음 |
| G11 | 채택 | 1 차 단독 채택 | — |

## 예측

| # | 예측 | 실측 | |
| :-- | :-- | :-- | :-- |
| P1 | 35~60 분 · $7~11 | 27 분 · $5.47 | **아래로 틀림** — 2 차 시도가 없었다 |
| P2 | RunId 게이트가 발화한다(GPT 3/3 근거) | **발화 0 — 대상 없음** | **틀림** — 목차가 S01 에서 RunId 를 발급하게 섰다. 3/3 은 결함 빈도가 아니라 목차 모양의 빈도였다(이번 판 1/4 로 내려감) |
| P3 | 닫힌다 | — | 표본 없음 |
| P5 | RUNID 누락 0~2 | **0** | 적중 |

## 판 안에서 실제로 돈 것 — 단계 하한 재생성 5 · 목차 결함 1

| 단계 | 사유 | 배송본 |
| :-- | :-- | :-- |
| **S03** | **가드 술어 대조** — `CMRate_Ins` 20 행 가드에 원본에 없는 `PLTID = 'POQ'`(**GPT 네 판 연속 PLTID 조건을 지어냈다**: `'POQ'`·`1`·`13`·`'POQ'`) | 닫힘 — 배송 S03 단계 검사 발화 0 |
| **S09** | **가드 술어 대조** — `INS_EXTRA` 30 행 가드에서 `OutYMD IS NOT NULL` 빠짐 | 닫힘 |
| S06 | 앵커 술어 대조 — UPDATE 3 에 원본에 없는 최상위 술어 | 닫힘 |
| S08 | 조인 짝 — UPDATE 11 자기 조인 짝 | 닫힘 |
| S16 | 저널 행 생성 없이 UPDATE 만 | 닫힘 |
| S15 | 목차 결함(검증 불가) — `BatchValidationIssue` 가 목차에 없음 | 배너 |

## 발견

1. **T25(`ValidateControlStatusTerminalWrites`) 오탐이 「하한 미달」 배너로 배송됐다.** S01 배너: 「`batch.BatchRun` 의 `RunStatus` 에 `Running` 을 이 Job 어느 단계도 기록하지 않습니다」 —
   그러나 S01 은 `INSERT INTO batch.BatchRun (JobName, BatchYmd, RunStatus, …) VALUES (N'POQSettleBatch14', @p_batchYmd, N'Running', …)` 로 **위치로** 기록한다.
   검사가 `RunStatus = N'Running'` 대입 모양만 세서 INSERT 의 위치 값을 못 본다. 문서 층 하한이라 1 회뿐인 판에서는 재생성 없이 배너로만 남았다. **고칠 후보.**
2. **판정 보류 — V05 가 S06 에게 제어합계 쓰기를 요구하지만 S06 본문에는 없다.** 검증 세트 V05 가 `V05_CONTROL_TOTAL_INSERT`(`@p_stepCode`·`@p_controlName`)를 스스로 정의하고
   「애플리케이션이 각 예외 갱신 직전·직후 값을 저장」한다고 적은 뒤, V05_01 이 `StepCode = N'S06'` 몫의 `Update01.RowCount`~`Update18.RowCount` 를 요구한다. S06 섹션에는 제어합계 언급이 0 이다.
   저장 주체가 S06 인지 검증 하네스인지 계약이 모호하다. K2 검증 세트판은 검증 세트가 스스로 쓰면 침묵하는 규칙(최종 리뷰 Minor 3)대로 조용했다 — 옳은 침묵이지만 이 가족은 기계로 판정하지 못한다.
3. 스윕(`sweeps/2026-09-14-step-sweep-batch14.md`): 미분류 106 → 109(+3, 전부 Batch14) — 파이프라인이 붙인 배너 인공물 2(S01 하한 미달 배너 · S15 검증 불가 배너가 헤딩 앞에 와서 「`### ` 헤딩으로 시작하지 않습니다」) + S15 목차 결함 1. 다른 검사 행 변화 0.

## 나란히

| | Batch11 | Batch12 | Batch13 | **Batch14** |
| :-- | --: | --: | --: | --: |
| 계획 시도 | 1 | 1 | 2 | **2(1 차에서 끝)** |
| 목차 단계 | 22 | 18 | 19 | **16** |
| RunId 발급 단계 | S02 | S02 | S02 | **S01** |
| 벽시계 | 35 분 | 25 분 | 41 분 | **27 분** |
| Consolidator 호출 · 청구 | 33 · $6.43 | 23 · $5.42 | 30 · $7.19 | **24 · $5.47** |
| 하한 재생성 | 8 | 2 | 6 | **5**(가드 2 포함) |
| 통합 L1 위반 | 0 | 0 | 0 | **0** |
| 채택 점수 | 80 | 90 | 94 | **94** |
