# RunId 발급 전 단계가 잠금 행을 쓴다 — 계약과 검사 (사전 선언)

**작성 시점**: 2026-09-16, **구현 전**. **코드**: main `259a0be3`(B17 판독) + 이 문서 커밋.

## 0. 잰 것

B17 판독 §발견 1 에서 나온 자리다. 배송본 **13 편 전수**로 「발급 절보다 앞선 절이 `batch.BatchRunLock` 에 쓰는가」를 쟀다(발급 절 = 문서 순서로 `batch.BatchRun` 에 처음 INSERT 하는 절 — `CheckGateRequiresStepsBeforeRunId` 와 같은 오라클).

| Job | 발급 절 | 잠금 쓰기 절 | 발급 전 잠금 | 비고 |
| :-- | :-- | :-- | :-- | :-- |
| B1 · B4 · B7 · B8 · B10 | S01 | S01 · 뒤쪽 해제 | 없음 | 발급 절이 잠금도 잡는다 |
| B5 | S00 | S00 · S15 | 없음 | 같음 |
| B12 · B13 · B15 | S02 | S02 · 뒤쪽 | 없음 | 같음 |
| B11 | S02 | S03 · S22 | 없음 | 발급 뒤에 잡는다 |
| B14 | S01 | S02 | 없음 | 발급 뒤에 잡는다 |
| **B16** | S03 | **S02** · S03 · S21 | **있음** | `OwnerRunId` 에 **예약값 0** 을 지어냈고 Critic 이 S03 에 승계 UPDATE 를 넣게 했다 |
| **B17** | S03 | **S02** · S22 | **있음** | 지어내지 않고 「선행 설계 차단 사항」으로 선언 → **S02 가 항상 -9020 으로 죽는 계획서를 배송** |

즉 **도달 2/13**, 그리고 **11/13 은 이미 발급 단계 또는 그 뒤에서 잠금을 잡는다** — 계약으로 못박는 것은 다수가 이미 하는 모양이라 회귀 위험이 작다.

계약이 잠금을 앞으로 끌어당기는 자리는 `ControlRowOrigin.FirstStepInserts` 의 프롬프트 문구다: 「이 표를 대상으로 선언한 **첫** 단계가 이 행을 INSERT 한다」. 그런데 계약의 `batch.BatchRunLock.OwnerRunId` 는 `bigint NOT NULL` 이고 이 표에는 IDENTITY 컬럼이 없다 — 발급 전 단계가 **합법적으로 쓸 수 없다.** L1 은 둘 다 통과시켰다: RunId 게이트 검사(`CheckGateRequiresStepsBeforeRunId`)는 발급 전 단계의 **저널·체크포인트 쓰기와 게이트 요구 단계**만 본다.

**선례**: 「RunId 수명」 문단(2026-09-14, 같은 가족의 첫 자리)이 이미 계약에 있고 저널·체크포인트를 이름으로 막는다. 이번 것은 **같은 문단이 못 막은 통로**다.

## 1. 설계 — 사람 결정: 잠금을 발급 뒤로

두 층으로 고친다.

### ① 계약(프롬프트)

「RunId 수명」 문단에 한 문장을 잇는다(새 칸을 만들지 않는다 — 이 문단이 이미 이 축의 정본이고, `ControlRowOrigin` 열거값은 검사 셋이 읽는 축이라 의미를 바꾸지 않는다):

> 실행 잠금 행(`batch.BatchRunLock`)은 **발급 절 또는 그 뒤** 절이 INSERT 한다. `OwnerRunId` 는 발급된 run id 이며 **예약값·자리표시값(0 같은 것)을 만들지 않는다**. 중복 실행 판정은 잠금을 잡는 자리에서 하고, 그 시점에 이미 만들어진 실행 행은 실패로 닫는다.

### ② 검사(통합 L1)

발급 절보다 **앞선** 절이 제어 계약의 **run id 를 담는 `NOT NULL` 컬럼**(`batch.BatchRunLock.OwnerRunId` · 다른 표의 `RunId`)에 INSERT·UPDATE·MERGE 하면 발화한다. 컬럼 목록은 이름으로 짐작하지 않고 **계약에서 읽는다**(`BatchControlContract`: `Nullable == false` 이고 IDENTITY 가 아니며 이름이 run id 를 가리키는 컬럼) — 이 저장소가 이름 추정으로 두 번 실패한 자리라 계약 조회로 고정한다.

- **귀속**: 그 단계 절(`OwnerStepCode`), 어휘는 그 쓰기가 있는 원문 줄.
- **처방**: 「잠금 획득을 발급 절이나 그 뒤로 옮기십시오」 — 계약이 합법 모양을 하나 주므로 재시도가 따를 수 있다(처방을 따를 수 없는 고발이 재시도를 태우는 것을 B13 에서 봤다).
- **침묵 규칙**: 발급 절 자신의 쓰기 · 발급 뒤 절의 쓰기 · 발급 절을 못 찾은 문서(`batch.BatchRun` INSERT 가 없으면 무엇이 앞인지 모른다) · 읽기만 하는 문장.

### 캐시

명세서 프롬프트는 안 바뀌므로 `CacheManager.CurrentCacheFormatVersion` 은 **그대로 둔다**(22). 계획 시도 재사용 키인 `PlanAttemptJournal.ContractVersion` 을 **1 → 2** 로 올린다 — 안 올리면 옛 시도가 재사용돼 새 계약이 안 돈다.

## 2. 합격선

| # | 항목 | 합격 |
| :-- | :-- | :-- |
| R1 | 실물 재현 | B17 S02 절(픽스처는 배송본에서 오려 온다)에서 **발화 1** · 귀속 S02 · 어휘에 그 쓰기 줄 |
| R2 | 발급 뒤 침묵 | 같은 쓰기가 발급 절 **뒤** 절에 있으면 발화 0(B11·B14 모양) |
| R3 | 발급 절 자신 침묵 | 발급 절이 같은 절에서 잠금을 잡으면 발화 0(11/13 의 모양) |
| R4 | 예약값도 발화 | B16 모양(`OwnerRunId` 에 `0`)도 발화 — 「지어낸 값」이 면제가 아니다 |
| R5 | 되돌림 | 앞선 절 판정 · 계약 컬럼 조회 · 쓰기 문장 판정 · 침묵 규칙 넷마다 걷어내면 빨강 |
| R6 | 오탐 0 | 배송본 13 편 전수 + **배송 안 된 모델 텍스트**(B11~B17 로그 응답 본문) 전수에서 **오탐 0**, 진짜 발화는 B16·B17 둘 |
| R7 | 게이트 | 실패 0 · 건너뜀 0 · 경고 0 |
| R8 | **재생 실험(계약이 듣는가)** | B17 의 **목차 생성 요청**을 새 계약 문구로 3 회 재전송해, 잠금 단계가 발급 절 또는 그 뒤로 가는지 센다(그대로 보낸 대조군 3 회와 비교). 판 한 번($8)보다 수십 배 싸고, 계약 문구의 효력은 이것만이 말한다 |

**오탐 방향**: 이 검사는 「발급 전 절이 run id 컬럼에 쓴다」만 본다. 놓침 위험은 R6 의 미배송 텍스트 전수가, 오탐 위험은 R2·R3 의 침묵 시험이 막는다.
**범위 밖**: 목차 순서를 코드가 재배치하지 않는다(목차는 모델 산출) · 잠금 해제 절·하트비트는 보지 않는다 · K2 `VALUES` 짝표(B17 발견 2)와 Critic 빈 본문(발견 3)은 이번 회차가 아니다.

## 결과

| # | 항목 | 실측 | 판정 |
| :-- | :-- | :-- | :-- |
| R1 | 실물 재현 | B17 S02+S03 문서에서 발화 **1** · 귀속 **S02** · 어휘에 `SET OwnerRunId = @p_ownerRunId` 등 쓰기 줄 | ✔ |
| R2 | 발급 뒤 침묵 | B11(S02 발급 · S03 잠금) 발화 **0** | ✔ |
| R3 | 발급 절 자신 침묵 | B12(한 절에서 발급+잠금) 발화 **0** | ✔ |
| R4 | 예약값도 발화 | B16(`OwnerRunId = CAST(0 AS BIGINT)`) 발화 **1** | ✔ |
| R5 | 되돌림 | **아홉 전부 빨강** — m1 순서 판정 · m2 발급 절 조기 반환(첫 시도는 무해한 변이라 살아남아 다시 설계했다) · m3 SET 절 한정 · m4 계약 컬럼 필터 · m5 계약 표시(OwnerRunId) · m6 계약 문구 · m7 어휘 WHERE 필터(처음 **살아남았다** → 가르는 입력을 실물에서 만들었다) · m8 목차 순서 규칙 · **m9 계약 축을 광의로 교체**(리뷰가 찾았다, 아래 Important 1) | ✔ |
| R6 | 오탐 0 | 코퍼스 **23 문서**(배송본 13 + 시도 조립 10) · 발화 **4**(B16·B17 의 배송본·조립 각 하나) · **오탐 0**. 리뷰가 독립으로 다시 재서 같은 값 | ✔ |
| R7 | 게이트 | 통과 **4514** · 실패 0 · 건너뜀 0 · 경고 0 | ✔ |
| R8 | 재생 실험 | **판별력 없음** — 대조군(그대로 보낸 목차 요청)이 결함을 재현하지 못했다. 6 회 중 **전부 합법 배치**(잠금이 발급 절과 같은 절 또는 그 뒤). 결함 기저율이 배송본 13 편 중 2 라 3 회로는 잡히지 않는다 | **불가** |

**R8 을 이렇게 적는 이유**: 「규칙을 넣으니 0/3 이다」는 이 실험에서 **아무것도 증명하지 않는다** — 규칙 없이도 3/3 이 합법이었다. 판별력 없는 실험을 효력 근거로 쓰는 것은 이 저장소가 스윕 차분 0 에서 이미 한 번 밟은 자리다(`2026-09-16-술어항-괄호정규화-사전선언.md` R6 재라벨). **효력 판정은 다음 판(B18)으로 넘긴다** — 그 판의 목차에서 잠금이 발급 절 또는 그 뒤인지, 그리고 이 검사가 발화하는지 둘을 같이 본다.

## 재료(`ControlRunIdWriteFacts`)의 도달 범위 — 리플렉션 프로브 실측

| 담는다(쓰기 1) | 흘린다(0) |
| :-- | :-- |
| 컬럼 목록 있는 INSERT · UPDATE SET · **별칭 UPDATE**(`UPDATE L … FROM batch.BatchRunLock AS L`) · MERGE 의 INSERT·UPDATE 절 · CTE 뒤 INSERT · **대괄호 이름**(`[batch].[BatchRunLock]`) · **스키마 없는 이름**(`BatchRunLock`) · 저널 `RunId` | 컬럼 목록 없는 `INSERT … VALUES`(위치 대응을 짐작하지 않는다) · WHERE 에만 있는 컬럼 · `OUTPUT deleted.OwnerRunId` 만 · 동적 SQL 문자열 · **이름 변주**(`LockOwnerRunId`) · 파싱 실패 펜스 |

이름 변주가 조용한 것은 이 검사의 구멍이 아니다 — 계약이 모르는 컬럼 이름은 `CheckBatchControlVocabulary`(단계 하한, `BatchControlContract.Tables` 의 `known` 집합과 대조)의 관할이다.

## 최종 리뷰 반영 (`51988c78` 범위)

Critical **0** · Important **1** · Minor **1**. 리뷰가 독립으로 확인한 것: 픽스처 8 편이 실물과 바이트 일치(README 의 출처 행 번호까지 대조, 8/8) · 코퍼스 재측정 23 편·발화 4·오탐 0(내 값과 일치) · 목차 규칙이 초안·재작성 **같은 메서드**라 두 경로에 다 실린다 · `ContractVersion` 은 `TryResume` 의 `ReuseKey` 비교에 실제로 쓰이고 `CacheManager` 22 유지가 타당.

| 등급 | 지적 | 조치 · 내가 다시 잰 것 |
| :-- | :-- | :-- |
| **Important 1** | 이 설계의 핵심(`ControlColumn.ReferencesRunId` — 계약이 run id 자리를 **말한다**)이 **어떤 시험으로도 보호되지 않는다**. 축을 「`NOT NULL`·비-IDENTITY」 광의로 갈아도 시험 열둘이 전부 초록이었다. 그 상태에서는 발화 메시지가 `LockStatus`·`JobName`·`AcquiredAtUtc` 까지 「발급된 RunId 를 담는 자리」라고 **사실과 다르게** 지목한다 | 가르는 입력을 실물에서 만들었다 — B17 S22 의 `SQL_RELEASE_RUN_LOCK`(=`LockStatus` 만 쓰고 `OwnerRunId` 는 WHERE·SELECT 에서만 읽는다)을 **자리만 옮겨** 발급 전 절에 놓은 픽스처. 시험 하나 추가 → **m9 빨강** |
| Minor 1 | `if (issuer.Code == null) return;` 이 죽은 코드다(바로 아래 `before.Count == 0` 이 같은 경우를 막는다 — 지워도 빨개지는 시험이 0) | 남기되 근거를 주석에 적었다 — 뒤 코드가 `issuer.Code` 를 메시지에 쓰므로 null 이 거기 못 간다는 사실을 국소적으로 보이는 값이 있다 |
| (리뷰가 미확인으로 남긴 것) | 재료가 CTE·OUTPUT·동적 SQL·컬럼 목록 없는 INSERT 에서 정말 침묵하는가 · 다른 검사와 처방이 충돌하는가 | 둘 다 내가 쟀다 — 위 도달 범위 표(프로브 15 모양)와 아래 정정 |

**내가 틀렸던 것(정정)**: 구현 주석과 커밋 메시지에 「『쓰기를 지우라』는 처방은 `ValidateControlStatusTerminalWrites` 가 반대를 요구해 못 쓴다」고 적었는데 **틀렸다**. 그 검사는 펜스를 **모든 단계에서 합쳐**(`body = string.Join("\n", fences)`, 모든 절의 펜스) 「이 Job 어느 단계도 그 상태값을 쓰지 않는가」를 보고 결함만 행 생성 단계에 귀속한다 — 다른 단계가 쓰면 조용하다. 그러니 「이 절에서 쓰기를 빼고 발급 뒤 절이 쓴다」도 합법이며, 두 처방 모두 한 절씩 다시 쓰는 것으로 따를 수 있다. 처방 문구는 그대로 두고(둘 다 유효하다) 근거만 고쳤다.
