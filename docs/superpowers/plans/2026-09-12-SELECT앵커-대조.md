# `SELECT n` 앵커 주석 층 대조 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 단계 지시서의 `SELECT n` 앵커(블록형 `/* SELECT n: … */` · 대시형 `-- SELECT n: …` 둘 다 — 코퍼스 실측 8·8)가 그 단계 레거시 SP 의 명세서 DML 범위 표에 실재하는 행을 가리키는지 코퍼스 전수로 재고, 계약에 `SELECT n` 표기(블록형 하나)를 못박는다.

**Architecture:** 리더의 **문장 집합을 건드리지 않는다.** `StepSqlStatementReader` 에 주석만 훑는 `ReadSelectAnchors` 를 더하고(펜스 열거·토큰화·꼬리 주석 배제는 리더가 이미 쓰는 것을 재사용), 그 서수를 `SpecStatementFactsExtractor` 의 DML 행과 맞대는 코퍼스 시험을 새로 세운다. 계약 조항 한 줄과 프롬프트 잠금 하나가 표기를 고정한다.

**Tech Stack:** C# / .NET 10 · xUnit · `Microsoft.SqlServer.TransactSql.ScriptDom`(`TSql160Parser`)

**Spec:** `docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md`

## Global Constraints

- **리더의 문장 집합을 넓히지 않는다.** `DmlCollector` 에 `Visit(SelectStatement)` 를 더하지 마라 — 설계서 §2 가 실측으로 기각했다(진짜 탐지 0 · 새 오탐 1).
- **`ReadAnchor` 를 재사용하지 않는다.** 문장 사이의 토큰 창에서 찾는데 SELECT 는 창이 없다.
- **날 마크다운에 정규식을 대지 않는다.** 반드시 `FencePattern` → 토큰화 → `PrecededByNewline` 경로로만 주석을 고른다.
- **`IF n` 은 범위 밖이다.** DML 범위 표에 IF 행이 0 이라 대조가 설 수 없다.
- **L1 검사(`MechanicalValidator`)를 추가하지 않는다.** 실측 결함이 0 이라 발화 표본이 없다.
- **기존 검사 수가 하나라도 움직이면 멈춘다**: 스윕 `A 0 · B 11 · C 1 · D 0 · E 0 · P 2 · 미분류 38`, 앵커 시험 `대조 단계 56 · 앵커 보유 문장 354 · 불일치 0`.
- **게이트**: 빌드 `-warnaserror --no-incremental` 경고 0 · 오류 0 · 시험 실패 0 · 건너뜀 0. 착수 시점 통과 수는 4282.
- **격리 워크트리에서 작업한다**(빌드 겹침은 격리만이 막는다). 워크트리에 `output` 심링크를 걸어라 — 안 걸면 코퍼스 시험이 건너뛴다.
- 커밋은 `git commit -m <메시지> -- <경로>` 로 경로를 못박는다(공유 인덱스가 샌다).

---

### Task 1: `ReadSelectAnchors` — 주석에서 SELECT 서수를 읽는다

**Files:**
- Modify: `src/ReSet.Core/Services/StepSqlStatementReader.cs`
- Test: `tests/ReSet.Core.Tests/SelectAnchorReaderTests.cs` (신규)

**Interfaces:**
- Consumes: 없음
- Produces: `public static IReadOnlyList<int> StepSqlStatementReader.ReadSelectAnchors(string? stepMarkdown)` — SQL 펜스 안에서 줄 첫머리 주석으로 적힌 `SELECT n` 의 서수를 **나온 순서대로** 낸다. 중복을 합치지 않는다(같은 서수가 둘이면 둘 다 낸다 — 중복 자체가 결함 신호다).

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/ReSet.Core.Tests/SelectAnchorReaderTests.cs` 를 만든다. 픽스처는 전부 실물에서 오렸다(`POQSettleBatch7/S15`·`POQSettleBatch1/S06`).

````csharp
using System.Linq;
using ReSet.Core.Services;
using Xunit;

namespace ReSet.Core.Tests
{
    /// <summary>
    /// <see cref="StepSqlStatementReader.ReadSelectAnchors"/> 가 리더가 보는 자리
    /// (SQL 펜스 안 · 줄 첫머리 주석)에서만 <c>SELECT n</c> 을 집는지 본다.
    ///
    /// [왜 DML 앵커보다 엄격한가] <see cref="StepSqlStatementReader"/> 의 앵커 정규식은
    /// 주석 안 어디서든 가장 왼쪽 매치를 잡는다. 그 규칙을 그대로 쓰면
    /// <c>/* U1: … (SELECT 1) */</c> 처럼 설명에 종류를 적은 주석이 SELECT 앵커로 읽힌다.
    /// 여기서는 <b>주석이 `SELECT n:` 으로 시작할 때만</b> 앵커로 본다 - 다만 표기
    /// 갈래는 좁히지 않는다. 계약이 규정하는 형식은 블록형 하나뿐이지만 리더는
    /// 대시형도 똑같이 받는다(코퍼스 절반).
    /// </summary>
    public class SelectAnchorReaderTests
    {
        /// <summary>POQSettleBatch7/S15 그대로(줄 그대로 오려냄, 트리밍 없음) - 블록형
        /// <c>/* SELECT n: … */</c> 앵커 둘. 실물에는 <c>DECLARE … CURSOR</c> 문이
        /// 없다 - 앵커 1 앞은 평범한 최상위 <c>SELECT … GROUP BY … ORDER BY</c>,
        /// 앵커 2 앞은 <c>SELECT ID FROM …</c>(대입문이 아니다). 대신 각 앵커 앞에는
        /// 그것대로의 잡음이 있다 - 라벨 주석(<c>-- SQL_CURSOR_SOURCE</c>, `SELECT n:`
        /// 으로 시작하지 않아 앵커가 아니다)과 스칼라 <c>DECLARE @v_intIssueType
        /// TINYINT = 15;</c> 문(커서가 아니라 변수 선언). 이 시험의 값: 이 코퍼스에서
        /// 블록형 표기의 유일한 실물 양성 커버리지다.</summary>
        private const string BlockSelectAnchorStep = @"### S15 단계

```sql
-- SQL_CURSOR_SOURCE
/* SELECT 1: Cur_SettlePost 커서 소스 - 고객사/거래일/지급일 단위 집계 */
SELECT A.ClientID AS ClientID,
       A.YMD      AS YMD,
       A.OutYMD   AS OutYMD,
       SUM(A.CLTotal)                AS CLTotal,
       SUM(A.CLTotal - A.CLVT) * -1  AS CLComm,
       SUM(A.CLVT) * -1              AS CLVT
  FROM SETTLE_POQ_DB.dbo.TSettleMst          AS A
  JOIN SETTLE_POQ_DB.dbo.TClientSettleRate   AS B
    ON A.YMD = B.YMD AND A.ClientID = B.ClientID AND A.PGName = B.PGName AND A.MallID = B.MallID
  JOIN SETTLE_POQ_DB.dbo.TClient             AS C
    ON A.ClientID = C.ClientID
 WHERE ISNULL(B.TaxFGBill, 2) = 1     -- 세금계산서 청구유형코드(1:청구,2:영수)
   AND A.YMD = @p_batchYmd
   AND A.OutState = 2
 GROUP BY A.ClientID, A.YMD, A.OutYMD
 ORDER BY A.OutYMD, A.ClientID;
```

```sql
-- SQL_CHECK_EXISTING_MISS
DECLARE @v_intIssueType TINYINT = 15;
/* SELECT 2: TSettleMiss 기등록여부 확인 */
SELECT ID
  FROM SETTLE_POQ_DB.dbo.TSettleMiss
 WHERE ClientID = @p_clientId
   AND OutYMD = @p_outYmd
   AND OutState = 2
   AND IssueType = @v_intIssueType;
```
";

        /// <summary>POQSettleBatch1/S06 모양 - SELECT 앵커가 없고 DML 앵커만 있다.</summary>
        private const string DmlOnlyStep = @"### S06 단계

```sql
-- SQL_DELETE_CHUNK : /* DELETE 1: 정산 데이터 삭제 */ 원본 필터 + PLTID 청크 범위
DELETE FROM SETTLE_POQ_DB.dbo.TSettleMst WHERE YMD = @p_ymd;

SELECT MIN(A.PLTID), MAX(A.PLTID) FROM SETTLE_POQ_DB.dbo.TSettleMst A WHERE A.YMD = @p_ymd;
```
";

        [Fact]
        public void ReadsSelectAnchorsWrittenWithTheBlockCommentFormFromRealCorpus()
        {
            Assert.Equal(new[] { 1, 2 }, StepSqlStatementReader.ReadSelectAnchors(BlockSelectAnchorStep));
        }

        [Fact]
        public void DoesNotInventSelectAnchorsWhereOnlyDmlAnchorsExist()
        {
            Assert.Empty(StepSqlStatementReader.ReadSelectAnchors(DmlOnlyStep));
        }

        [Fact]
        public void IgnoresSelectMentionedInsideAnotherAnchorsProse()
        {
            // 실물 모양(POQSettleBatch6/S13 이 `/* U1: … (DELETE 1) */` 로 적었다).
            // 종류 이름이 설명 안에 있으면 앵커가 아니다.
            const string step = "### S 단계\n\n```sql\n" +
                "/* U1: 후회수 취소 기존 집계 삭제 (SELECT 1) */\n" +
                "DELETE FROM SETTLE_POQ_DB.dbo.TSettleByOUT WHERE YMD = @p_ymd;\n```\n";

            Assert.Empty(StepSqlStatementReader.ReadSelectAnchors(step));
        }

        [Fact]
        public void IgnoresTrailingComments()
        {
            // 꼬리 주석은 앞 문장의 것이지 뒤 문장의 앵커가 아니다 - 리더의 규칙과 같다.
            const string step = "### S 단계\n\n```sql\n" +
                "DELETE FROM SETTLE_POQ_DB.dbo.TSettleMst WHERE YMD = @p_ymd; /* SELECT 1: 꼬리 */\n```\n";

            Assert.Empty(StepSqlStatementReader.ReadSelectAnchors(step));
        }

        [Fact]
        public void IgnoresNonSqlFences()
        {
            const string step = "### S 단계\n\n```csharp\n/* SELECT 1: 앵커가 아니다 */\n```\n";

            Assert.Empty(StepSqlStatementReader.ReadSelectAnchors(step));
        }

        [Fact]
        public void KeepsDuplicatesBecauseADuplicateIsItselfADefectSignal()
        {
            const string step = "### S 단계\n\n```sql\n" +
                "/* SELECT 1: 첫째 */\nSELECT 1 AS X;\n" +
                "/* SELECT 1: 둘째 - 같은 서수 */\nSELECT 2 AS Y;\n```\n";

            Assert.Equal(new[] { 1, 1 }, StepSqlStatementReader.ReadSelectAnchors(step));
        }
    }
}
````

- [ ] **Step 2: 시험이 실패하는지 확인한다**

Run: `dotnet build 2>&1 | grep -E "error"`
Expected: `ReadSelectAnchors` 가 없어 **컴파일 오류**. 이 단계에서는 시험 실행이 아니라 빌드 실패가 기대값이다.

- [ ] **Step 3: 최소 구현을 쓴다**

`src/ReSet.Core/Services/StepSqlStatementReader.cs` 의 `AnchorPattern` 정의 **바로 뒤**에 정규식을 더한다:

```csharp
        /// <summary>
        /// `SELECT n` 앵커. <see cref="AnchorPattern"/> 보다 <b>엄격하다</b> - 주석이
        /// `SELECT n:` 으로 시작할 때만 받는다.
        ///
        /// [왜 엄격한가 - 착수 전 실측] 느슨하게 잡으면 `/* U1: … (SELECT 1) */` 처럼
        /// 설명에 종류를 적은 주석이 앵커로 읽힌다. 실물이 그 모양이다
        /// (`POQSettleBatch6/S13`). 엄격함의 관할은 표기 갈래(`/*` 대 `--`)가 아니라
        /// 줄 첫머리 요구다 - `SELECT n:` 이 줄 맨 앞에서 시작해야만 받고 산문 속
        /// 언급은 배제한다. 그것만으로 오탐 기제가 구조적으로 사라진다.
        /// 설계: docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md §3
        /// </summary>
        private static readonly Regex SelectAnchorPattern = new(
            @"^\s*(?:/\*|--)\s*SELECT\s*(?<ordinal>\d{1,2})\s*:",
            RegexOptions.Compiled | RegexOptions.IgnoreCase);
```

그리고 `Read(string?, out int)` 정의 **바로 뒤**에 메서드를 더한다:

```csharp
        /// <summary>
        /// SQL 펜스 안에서 <c>SELECT n:</c> 로 시작하는 주석 - 블록형 <c>/* SELECT n: … */</c>
        /// 와 대시형 <c>-- SELECT n: …</c> 둘 다 - 에서 앵커의 서수를 나온 순서대로 낸다.
        ///
        /// [왜 문장이 아니라 주석인가 - 실측] 세 겹이 막는다.
        /// ① <c>DmlCollector</c> 는 Update·Delete·Insert 만 방문한다 - SELECT 는 문장이 되지 않는다.
        /// ② 방문자를 더해도 <c>AnchorPattern</c>(이 파일 :236)에 <b>SELECT 대안이 없어</b>
        ///    <c>SELECT n</c> 주석이 앵커로 안 읽힌다 - 그 문장들은 전부 <c>Anchor = null</c> 이 된다.
        /// ③ 그런데 <b>방문자만 넓혀도</b> 다른 종류의 앵커 주석이 앞에 있는 SELECT 는 그 앵커를
        ///    물려받는다 - <c>AnchorPattern</c> 의 <b>기존</b> <c>INSERT</c> 대안이 산문의 `INSERT 1` 에
        ///    맞기 때문이다. 실물 <c>POQSettleBatch1/S06:114</c> 바로 뒤가 SELECT 이고, 프로브의
        ///    「앵커 보유 문장 354→355」 가 정확히 그 하나다(곧 오탐이다).
        /// <b>주석의 자리는 이 셋과 무관하므로</b> 주석을 세면 전부 도달한다.
        ///
        /// [한계 - 알고 쓴다] 이 값은 「라벨이 명세서에 있는가」만 답한다. 「그 라벨이 옳은
        /// 문장에 붙었는가」는 답하지 못한다 - 문장에 결합하지 않기 때문이다.
        /// </summary>
        public static IReadOnlyList<int> ReadSelectAnchors(string? stepMarkdown)
        {
            var ordinals = new List<int>();
            if (string.IsNullOrWhiteSpace(stepMarkdown)) return ordinals;

            foreach (Match fence in FencePattern.Matches(stepMarkdown))
            {
                var lexer = new TSql160Parser(initialQuotedIdentifiers: true);
                var tokens = lexer.GetTokenStream(
                    new StringReader(fence.Groups["sql"].Value), out var tokenErrors);

                // 토큰화가 실패하면 주석 경계를 알 수 없다 - 이 펜스는 건너뛴다.
                // Read(…)의 같은 분기와 같은 판단이다.
                if (tokens == null || tokenErrors is { Count: > 0 }) continue;

                for (int i = 0; i < tokens.Count; i++)
                {
                    var token = tokens[i];
                    if (token.TokenType is not (TSqlTokenType.SingleLineComment
                                                or TSqlTokenType.MultilineComment))
                    {
                        continue;
                    }

                    if (!PrecededByNewline(tokens, i)) continue; // 꼬리 주석은 앵커가 아니다.

                    var match = SelectAnchorPattern.Match(token.Text);
                    if (!match.Success) continue;

                    ordinals.Add(int.Parse(match.Groups["ordinal"].Value));
                }
            }

            return ordinals;
        }
```

- [ ] **Step 4: 시험이 통과하는지 확인한다**

Run: `dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~SelectAnchorReaderTests"`
Expected: 실패 0 · 통과 6 · 건너뜀 0 · 경고 0

- [ ] **Step 5: 되돌림으로 판정한다 — 치환 적용을 해시로 확인한다**

정규식 치환이 조용히 안 먹어 「빌드도 초록도 나는데 변이가 없는」 판을 이 저장소가 겪었다. 파일 해시로 적용 여부를 판정하라.

```bash
R=src/ReSet.Core/Services/StepSqlStatementReader.cs
cp "$R" /tmp/r.bak; B=$(md5 -q "$R")
# 변이: 줄 첫머리 요구를 없앤다(느슨하게 만든다)
python3 - "$R" <<'PY'
import sys; p=sys.argv[1]; s=open(p,encoding='utf-8').read()
old = r'@"^\s*(?:/\*|--)\s*SELECT\s*(?<ordinal>\d{1,2})\s*:"'
new = r'@"\bSELECT\s*(?<ordinal>\d{1,2})\b"'
assert s.count(old) == 1, f"치환 자리가 {s.count(old)} 개다"
open(p,'w',encoding='utf-8').write(s.replace(old,new))
PY
[ "$(md5 -q "$R")" = "$B" ] && { echo "!!! 안 바뀌었다 - 여기서 선다"; exit 1; }
dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~SelectAnchorReaderTests"
# 기대: IgnoresSelectMentionedInsideAnotherAnchorsProse 가 RED
cp /tmp/r.bak "$R"; [ "$(md5 -q "$R")" = "$B" ] && echo "복원 확인"
```

Expected: 변이에서 `IgnoresSelectMentionedInsideAnotherAnchorsProse` 가 **RED**. 초록이면 픽스처가 실물보다 단순한 것이니 픽스처를 다시 보라 — 정규식을 숫자에 맞추지 마라.

- [ ] **Step 6: 커밋**

```bash
git add -- src/ReSet.Core/Services/StepSqlStatementReader.cs tests/ReSet.Core.Tests/SelectAnchorReaderTests.cs
git commit -m "feat: SELECT n 앵커를 주석에서 읽는다 - 커서 안의 SELECT 도 도달한다" \
  -- src/ReSet.Core/Services/StepSqlStatementReader.cs tests/ReSet.Core.Tests/SelectAnchorReaderTests.cs
```

---

### Task 2: 코퍼스 대조 시험 — 그 서수가 명세서에 실재하는가

**Files:**
- Test: `tests/ReSet.Core.Tests/SelectAnchorPairCorpusTests.cs` (신규)

**Interfaces:**
- Consumes: `StepSqlStatementReader.ReadSelectAnchors(string?)` → `IReadOnlyList<int>` (Task 1)
- Produces: 없음(시험이 종단이다)

- [ ] **Step 1: 시험을 쓴다**

`AnchorKindOrdinalPairTests` 의 판정부를 그대로 옮긴다 — 기대값을 재구현하지 않고 제품 리더 둘을 맞댄다.

```csharp
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using ReSet.Core.Services;
using Xunit;
using Xunit.Abstractions;

namespace ReSet.Core.Tests
{
    /// <summary>
    /// 단계 본문의 <c>SELECT n:</c> 앵커 - 블록형 <c>/* SELECT n: … */</c> 와 대시형
    /// <c>-- SELECT n: …</c> 둘 다 - 가 <b>그 단계 레거시 SP 명세서의 DML 범위
    /// 표에 실재하는가</b>를 코퍼스 전수로 잰다.
    ///
    /// [왜 <see cref="AnchorKindOrdinalPairTests"/> 가 못 잡나] 그 자는 문장에 붙은 앵커만
    /// 본다. SELECT 는 리더가 문장으로 담지도, 앵커로 읽지도 않는다(설계서 §2 세 겹) - 불일치로는
    /// <b>절대</b> 드러나지 않는 조용한 공백이다.
    ///
    /// [한계 - 아는 채로 쓴다] 라벨이 명세서에 있는지만 본다. 그 라벨이 옳은 문장에
    /// 붙었는지는 못 본다. 설계서 §7 참고.
    ///
    /// 설계: docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md
    /// </summary>
    public class SelectAnchorPairCorpusTests
    {
        private readonly ITestOutputHelper _output;

        public SelectAnchorPairCorpusTests(ITestOutputHelper output) => _output = output;

        [SkippableFact]
        public void EverySelectAnchorPointsAtARowTheSpecActuallyDeclares()
        {
            var root = CorpusPaths.RepoRootIfCorpusPresent();
            Skip.If(string.IsNullOrEmpty(root), CorpusSkip.Reason);

            var outputRoot = Path.Combine(root, "output");
            var jobsDir = Path.Combine(outputRoot, "Jobs");
            Skip.IfNot(Directory.Exists(jobsDir), CorpusSkip.Reason);

            var facts = SpecStatementFactsExtractor.Extract(ReadSpecs(outputRoot));
            Skip.If(facts.Count == 0, CorpusSkip.Reason);

            var mismatches = new List<string>();
            var scanned = 0;
            var perJob = new List<(string Job, int Scanned, int Mismatches)>();

            foreach (var jobDir in Directory.EnumerateDirectories(jobsDir)
                         .OrderBy(d => d, StringComparer.Ordinal))
            {
                var plan = Path.Combine(jobDir, "raw", "PlanStructure.md");
                var stepsDir = Path.Combine(jobDir, "agent", "steps");
                if (!File.Exists(plan) || !Directory.Exists(stepsDir)) continue;

                var steps = BatchStepPlanParser.TryParse(File.ReadAllText(plan));
                if (steps == null) continue;

                var jobScanned = 0;
                var jobMismatches = 0;

                foreach (var step in steps)
                {
                    var file = Path.Combine(stepsDir, step.Code + ".md");
                    if (!File.Exists(file)) continue;

                    var declared = new HashSet<int>();
                    foreach (var procedure in step.LegacyProcedures)
                    {
                        if (!facts.TryGetValue(BareName(procedure), out var procedureFacts)) continue;
                        foreach (var row in procedureFacts.DmlRows)
                        {
                            if (row.Kind.Equals("SELECT", StringComparison.OrdinalIgnoreCase))
                            {
                                declared.Add(row.Ordinal);
                            }
                        }
                    }

                    foreach (var ordinal in StepSqlStatementReader.ReadSelectAnchors(
                                 File.ReadAllText(file)))
                    {
                        scanned++;
                        jobScanned++;

                        if (declared.Contains(ordinal)) continue;

                        jobMismatches++;
                        mismatches.Add(
                            $"{Path.GetFileName(jobDir)}/{step.Code} · SELECT 앵커 {ordinal} — " +
                            $"명세서에 그 행이 없다(그 단계 SP 가 선언한 SELECT 서수: " +
                            $"{(declared.Count == 0 ? "없음" : string.Join(", ", declared.OrderBy(x => x)))})");
                    }
                }

                if (jobScanned > 0) perJob.Add((Path.GetFileName(jobDir), jobScanned, jobMismatches));
            }

            _output.WriteLine($"SELECT 앵커 {scanned} · 불일치 {mismatches.Count}");
            foreach (var job in perJob)
            {
                _output.WriteLine($"  [Job] {job.Job} · SELECT 앵커 {job.Scanned} · 불일치 {job.Mismatches}");
            }
            foreach (var mismatch in mismatches) _output.WriteLine("  " + mismatch);

            // 재료가 0 이면 이 단언은 아무것도 안 지킨다 - 값 0 을 게이트 통과시키지 않는다.
            Skip.If(scanned == 0, CorpusSkip.Reason);

            Assert.Empty(mismatches);
        }

        private static IReadOnlyList<(string FileName, string Content)> ReadSpecs(string outputRoot)
        {
            var specs = new List<(string, string)>();
            foreach (var file in Directory.EnumerateFiles(outputRoot, "Spec.md", SearchOption.AllDirectories))
            {
                var normalized = file.Replace('\\', '/');
                if (!normalized.Contains("/docs/") || normalized.Contains("/Jobs/")) continue;

                var objectDir = Path.GetDirectoryName(Path.GetDirectoryName(file));
                if (objectDir == null) continue;

                specs.Add((Path.GetFileName(objectDir), File.ReadAllText(file)));
            }

            return specs;
        }

        private static string BareName(string? qualified)
        {
            if (string.IsNullOrWhiteSpace(qualified)) return string.Empty;
            var dot = qualified.LastIndexOf('.');
            return dot < 0 ? qualified : qualified[(dot + 1)..];
        }
    }
}
```

- [ ] **Step 2: 돌려서 착수 조건 R1 을 판정한다**

Run: `dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~SelectAnchorPairCorpusTests" --logger "console;verbosity=detailed" 2>&1 | grep -E "SELECT 앵커|\[Job\]|통과!|실패!"`

Expected: `SELECT 앵커 16 · 불일치 0`, Job 내역은 `Batch1 7/0 · Batch5 1/0 · Batch7 8/0`.

**R1 — 여기서 멈출 조건:** 16 이 아니면 진행하지 마라. **16 미만**이면 스캐너가 리더가 보는 자리를 못 보는 것이고, **16 초과**면 아직도 못 센 앵커가 있다는 뜻이라 분모부터 다시 정해야 한다. 숫자를 맞추려고 스캐너를 고치지 마라.

> **이 수는 한 번 틀렸다.** 계획서 초판이 8 이라 적었는데 착수 전 실측이 블록 주석만 보는 자를 썼다 — 실물의 절반이 `-- SELECT n:` 줄 주석이다. 16 은 **배송된 `ReadSelectAnchors` 를 실물 코퍼스에 돌려** 얻은 값이고 열여섯 전부가 명세서 서수와 일치한다(설계서 §1 정정 참고).

- [ ] **Step 3: 되돌림으로 판정한다 (명세서 쪽)**

```bash
S=src/ReSet.Core/Services/SpecStatementFactsExtractor.cs
cp "$S" /tmp/s.bak; B=$(md5 -q "$S")
python3 - "$S" <<'PY'
import sys; p=sys.argv[1]; s=open(p,encoding='utf-8').read()
old = 'int.Parse(match.Groups["ordinal"].Value),'
assert s.count(old) == 1, f"치환 자리가 {s.count(old)} 개다"
open(p,'w',encoding='utf-8').write(s.replace(old, '99,'))
PY
[ "$(md5 -q "$S")" = "$B" ] && { echo "!!! 안 바뀌었다"; exit 1; }
dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~SelectAnchorPairCorpusTests"
cp /tmp/s.bak "$S"; [ "$(md5 -q "$S")" = "$B" ] && echo "복원 확인"
```

Expected: 변이에서 이 시험이 **RED**(명세서 서수가 전부 99 가 되어 열여섯이 전부 불일치). 초록이면 시험이 명세서를 실제로 안 읽고 있는 것이다.

- [ ] **Step 4: 기존 수가 안 움직였는지 확인한다 (R2)**

Run: `dotnet test --no-build --filter "FullyQualifiedName~AnchorKindOrdinalPairTests" --logger "console;verbosity=detailed" 2>&1 | grep "대조한 단계"`
Expected: `대조한 단계 56 · 앵커 보유 문장 354 · 불일치 0` — **하나도 안 움직여야 한다.**

- [ ] **Step 5: 커밋**

```bash
git add -- tests/ReSet.Core.Tests/SelectAnchorPairCorpusTests.cs
git commit -m "test: SELECT n 앵커가 명세서에 실재하는 행을 가리키는지 코퍼스 전수로 잰다" \
  -- tests/ReSet.Core.Tests/SelectAnchorPairCorpusTests.cs
```

---

### Task 3: 계약 조항과 프롬프트 잠금

**Files:**
- Modify: `src/ReSet.Core/Services/AiService.cs` (`AppendStatementAnchorRules`, 4834행 부근)
- Test: `tests/ReSet.Core.Tests/StatementAnchorClauseTests.cs`

**Interfaces:**
- Consumes: 없음 (Task 1·2 와 독립이다 — 병렬 가능)
- Produces: 없음

- [ ] **Step 1: 실패하는 잠금 시험을 쓴다**

`tests/ReSet.Core.Tests/StatementAnchorClauseTests.cs` 의 `AnchorClause_DefinesDeleteNotation` **바로 뒤**에 더한다. `AssertBothCarryAsync` 가 이미 두 경로를 함께 단언한다.

```csharp
        [Fact]
        public async Task AnchorClause_DefinesSelectNotation()
        {
            // 명세서 DML 범위 표(ReadDmlRows 가 읽는 기계 확정 표)는 SELECT 행을 10 개
            // (SP 다섯: PROC_ETC 6 · INS_EXTRA 1 · Summary_AcqManual 1 · SUMMARY_ETC 1 ·
            // SUMMARY_EXTRA 1) 갖는데 계약이 그 표기를 정하지 않았다(파일 전체를 grep
            // 하면 80 - 집합 술어 표 등 표 밖의 다른 셀도 걸린다. ReadDmlRows 가 보는
            // 것은 표 하나뿐이다). 모델은 이미 SELECT 앵커를 적고 있지만(코퍼스 16 —
            // `/* SELECT n: … */` 8 · `-- SELECT n: … ` 8, 전부 명세서와 일치) 계약이
            // 없으면 다음 회차에 다른 표기가 나온다.
            // 재는 자: SelectAnchorPairCorpusTests
            await AssertBothCarryAsync(
                "/* SELECT 1:",
                "SELECT 표기를 정의하지 않으면 모델이 회차마다 발명합니다.");
        }
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

Run: `dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~StatementAnchorClauseTests"`
Expected: `AnchorClause_DefinesSelectNotation` 만 **RED**, 나머지 여섯은 초록.

- [ ] **Step 3: 조항 한 줄을 더한다**

`AppendStatementAnchorRules` 의 첫 불릿에서 `DELETE` 형식 문장 **바로 뒤**에 끼운다. **다른 바이트를 건드리지 마라** — 이 블록은 캐시 접두사 안쪽이다.

바꾸기 전:
```
                "`INSERT`는 `/* INSERT 1: 정산 원장 적재 */`, `DELETE`는 `/* DELETE 1: 당일분 삭제 */` 형식입니다. " +
```
바꾼 뒤:
```
                "`INSERT`는 `/* INSERT 1: 정산 원장 적재 */`, `DELETE`는 `/* DELETE 1: 당일분 삭제 */` 형식입니다. " +
                "명세서 DML 범위 표에 `SELECT` 행이 있으면 그 문장에도 `/* SELECT 1: 커서 원천 조회 */` 형식으로 답니다. " +
```

- [ ] **Step 4: 시험이 통과하는지 확인한다**

Run: `dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~StatementAnchorClauseTests"`
Expected: 실패 0 · 통과 7 · 건너뜀 0

- [ ] **Step 5: 되돌림으로 판정한다**

```bash
A=src/ReSet.Core/Services/AiService.cs
cp "$A" /tmp/a.bak; B=$(md5 -q "$A")
python3 - "$A" <<'PY'
import sys; p=sys.argv[1]; s=open(p,encoding='utf-8').read()
old = '                "명세서 DML 범위 표에 `SELECT` 행이 있으면 그 문장에도 `/* SELECT 1: 커서 원천 조회 */` 형식으로 답니다. " +\n'
assert s.count(old) == 1, f"치환 자리가 {s.count(old)} 개다"
open(p,'w',encoding='utf-8').write(s.replace(old,''))
PY
[ "$(md5 -q "$A")" = "$B" ] && { echo "!!! 안 바뀌었다"; exit 1; }
dotnet build -warnaserror && dotnet test --no-build --filter "FullyQualifiedName~StatementAnchorClauseTests"
cp /tmp/a.bak "$A"; [ "$(md5 -q "$A")" = "$B" ] && echo "복원 확인"
```

Expected: `AnchorClause_DefinesSelectNotation` 만 RED, 나머지 여섯은 초록. **다른 것이 함께 빨개지면 조항을 잘못 끼운 것이다.**

- [ ] **Step 6: `AiServiceTests` 의 기존 프롬프트 잠금이 살아 있는지 본다 (R4)**

Run: `dotnet test --no-build --filter "FullyQualifiedName~AiServiceTests"`
Expected: 실패 0. 빨개지면 그 시험이 조항 문구를 축자로 단언하는 것이다 — `2026-09-08` 회차에서 실제로 났다. **그 시험은 「앵커를 요구한다는 사실」만 지키게 두고 표기 내용은 `StatementAnchorClauseTests` 가 지킨다**는 규약에 맞춰 갱신하라.

- [ ] **Step 7: 커밋**

```bash
git add -- src/ReSet.Core/Services/AiService.cs tests/ReSet.Core.Tests/StatementAnchorClauseTests.cs
git commit -m "feat: 계약에 SELECT n 표기를 정한다 - 두 경로 모두 잠근다" \
  -- src/ReSet.Core/Services/AiService.cs tests/ReSet.Core.Tests/StatementAnchorClauseTests.cs
```

---

### Task 4: 전량 게이트와 설계서 §9 착수 기록

**Files:**
- Modify: `docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md` (§9 이어붙임)

**Interfaces:**
- Consumes: Task 1·2·3 의 결과 전부
- Produces: 없음

- [ ] **Step 1: 전량 게이트를 돌린다**

```bash
dotnet build -warnaserror --no-incremental 2>&1 | tail -4
dotnet test --no-build 2>&1 | tail -6
git status --short --untracked-files=all
ls -1 output/Jobs
```

Expected: 경고 0 · 오류 0 · **실패 0 · 건너뜀 0** · 트리 청결 · Jobs 넷. 통과 수는 돌려서 확인하고 그 값을 §9-3 에 적어라 — **절대 통과 수를 합격 기준으로 쓰지 마라**(환경 안에서도 흔들린다). 합격 기준은 실패 0 · 건너뜀 0 · 경고 0 이다.

- [ ] **Step 2: 스윕이 안 움직였는지 확인한다 (R2)**

```bash
dotnet run --project src/ReSet.Cli -- --sweep
```
그 보고서의 「검사별 발화량」 표가 `A 0 · B 11 · C 1 · D 0 · E 0 · P 2 · 미분류 38` 그대로여야 한다. **하나라도 움직이면 멈추고 원인을 적어라** — 이 변경은 문장 집합을 안 건드리므로 움직일 이유가 없다.

보고서 파일은 커밋하지 마라(`git status` 에서 지워라). 기준선 비교용이다.

- [ ] **Step 3: 설계서에 §9 를 이어붙인다**

`docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md` 끝에 더한다. **실제로 잰 값만 적어라.**

```markdown
## 9. 착수 기록 — 2026-09-12

**설계대로 착수했다.** 코드 변경은 스캐너 하나 · 코퍼스 시험 하나 · 조항 한 줄 · 잠금 하나다.

### 9-1. 착수 조건 판정

| # | 조건 | 결과 |
| ---: | :--- | :--- |
| 1 | 재료 | `SELECT n` 앵커 **<실측>** (Batch5 <n> · Batch7 <n>) |
| 2 | 도달 | 주석은 설계서 §2 의 세 겹과 무관 — <실측>/16 |
| 4 | 발화 | 불일치 **<실측>** |
| 5 | 오탐 | 되돌림으로 쟀다(아래 9-2) |

### 9-2. 없앴을 때로 쟀다

| 변이 | 기대 | 결과 |
| :--- | :--- | :--- |
| `SelectAnchorPattern` 의 줄 첫머리 요구 제거 | 설명 안 `SELECT n` 을 앵커로 오인 | <실측> |
| 명세서 서수를 상수로 뭉갬 | 코퍼스 시험 RED | <실측> |
| 계약 조항 삭제 | 잠금 하나만 RED | <실측> |

**치환 적용은 파일 해시로 판정했다** — 정규식 치환이 조용히 안 먹어 빌드도 초록도 나는 판을
이 저장소가 겪었다.

### 9-3. 게이트

<실측: 빌드 · 실패 · 통과 · 건너뜀 · 스윕 일곱 값 · Jobs 수>

### 9-4. 남은 것

- **계약이 먹었는지는 다음 Job 재생성이 판정한다.** 그 Job 의 `SELECT n` 앵커가 0 이 아니고
  불일치가 0 이면 먹은 것이다. 0 이면 표기를 좁혀 모델이 아예 안 붙이게 된 것이므로 되돌린다.
- **L1 검사는 그때 붙인다** — 발화 표본이 생긴 뒤가 순서다(§7).
- **`IF n` 은 여전히 범위 밖이다** — DML 범위 표에 오라클이 없다(§7).
```

- [ ] **Step 4: 커밋**

```bash
git add -- "docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md"
git commit -m "docs: SELECT 앵커 대조 착수 기록 - 조건 판정·되돌림·게이트" \
  -- "docs/superpowers/specs/2026-09-12-SELECT앵커-대조-설계.md"
```
