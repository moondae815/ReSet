# 대입 우변의 감쌈 벗기기 구현 계획서

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 두 대입 추출기가 우변의 NULL 보존 감쌈을 벗겨 갈래를 정하고 우변 원문을 표에 싣게 해서, 명세서 층 결함 여덟 자리 중 🔴 하나와 🟠 하나 반을 **다음 재생성부터** 닫는다.

**Architecture:** 새 정적 헬퍼 `AssignmentExpressionUnwrapper` 하나가 「벗기기」와 「원문 텍스트」를 전담하고, 기존 두 추출기(`NonAggregateAssignmentExtractor`·`AggregateAssignmentExtractor`)가 그것을 불러 갈래를 정한다. 렌더러(`ExecutionSemanticsFacts`)는 사실에 새로 담긴 우변 원문을 찍는다. 갈래는 **배타적**이고, 판정 못 하는 모양은 **지금처럼 침묵한다**.

**Tech Stack:** C# / .NET 10 · Microsoft.SqlServer.TransactSql.ScriptDom 180.37.3 (`TSql160Parser`) · xUnit + Xunit.SkippableFact

**Spec:** `docs/superpowers/specs/2026-09-06-대입-감쌈-벗기기-design.md`

---

## Global Constraints

이 절의 값은 **모든 태스크의 요구사항에 암묵적으로 포함된다.**

- **기준 커밋은 main `a5a72532`다.** 격리 워크트리를 여기서 딴다.
- **공유 체크아웃(`/Users/payletter/git-root/ReSet`)에서 빌드·테스트하지 마라.** 다른 세션의 통제 실험이 돌고 `bin`/`obj`가 겹친다. 그 체크아웃은 지금 `feat/settlement-policy-redesign`(tip `ccd3717f`)에 서 있다 — **거기에 커밋하지 마라.**
- **`git stash` 금지.** 스태시는 저장소 공유다.
- **코퍼스 심링크 넷을 다 걸고 시작한다.** 일부만 걸면 다른 테스트가 대신 꺼지는데 총 건너뜀 수는 줄어 성공처럼 보인다(`CorpusSetupGuardTests`).
  ```bash
  ln -s <메인 저장소>/output output
  ln -s <메인 저장소>/output.bak-2026-08-22 output.bak-2026-08-22
  ln -s <메인 저장소>/output.bak-stage4-control-20260828 output.bak-stage4-control-20260828
  ln -s <메인 저장소>/output.bak-batch1-preregen-20260904 output.bak-batch1-preregen-20260904
  ```
- **게이트는 실패 0 · 건너뜀 0 · 경고 0.** 절대 통과 수는 게이트가 아니다(환경 내에서도 최대 5까지 흔들린다).
- **「실행 의미」 표는 「기계 확정 — 수정 금지」다.** L1이 축자 전사를 강제하므로 **거짓 행 하나가 그대로 하류로 배송된다.** 판정 못 하는 모양에는 행을 내지 마라 — 이 계획의 어느 태스크도 「모르는 것을 확정 표에 싣는다」 방향으로 한 발도 가지 않는다.
- **새 멤버를 기존 문서 주석 블록과 그 멤버 사이에 끼우지 마라.** `<summary>` 둘이 한 블록으로 묶여 `OrphanedDocCommentTests`가 잡는다(이 저장소에서 세 번 났다). 삽입은 **다음 멤버의 문서 블록 앞**에 한다.
- **커밋은 경로를 명시한다**: `git commit -m "..." -- <경로들>`. `git add .` 금지.
- 커밋 메시지 꼬리:
  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
  ```

### 이 계획이 닫지 않는 것 (명시)

- 🔴 `UF_Get_ExtraCardCommissionAmt` — `TOP` 재료 부재. 별건(접근법 B).
- 🟠 `PROC_ETC`의 `CAST(… AS MONEY)` — `ExpressionTypePathExtractor` 관할이고 `target != Int → return`이라 침묵한다. **이 계획은 `PROC_ETC`의 `ISNULL` 절반만 닫는다.**
- 공통 (다) 「산문이 표를 뒤집음」 — 계약이 자연어 판정을 금지했다. 손대지 마라.

### 테스트 실행 명령 (모든 태스크 공통)

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~<클래스명>" -v n
```

전체 게이트:

```bash
dotnet build 2>&1 | tail -20            # 경고 0 · 오류 0
dotnet test 2>&1 | tail -20             # 실패 0 · 건너뜀 0
```

---

## File Structure

| 파일 | 책임 |
| :--- | :--- |
| **신규** `src/ReSet.Core/Services/AssignmentExpressionUnwrapper.cs` | 우변 식 하나를 받아 ① NULL 보존 감쌈을 한 겹 벗기고 ② 안쪽이 「전부 컬럼 참조인 분기식」인지 판정하고 ③ 원문 텍스트를 만든다. 두 추출기가 공유하는 유일한 새 단위 |
| 수정 `src/ReSet.Core/Services/NonAggregateAssignmentExtractor.cs` | 우변 가드를 「맨 컬럼」에서 「벗긴 뒤 전부 컬럼 참조」로 넓힌다. 사실에 원문을 담는다 |
| 수정 `src/ReSet.Core/Services/AggregateAssignmentExtractor.cs` | 우변 가드를 「최상위가 집계 `FunctionCall`」에서 「벗긴 뒤 집계」로 넓힌다. `ISNULL(<집계>, 리터럴)` 네 번째 문장 갈래를 더한다. 사실에 원문을 담는다 |
| 수정 `src/ReSet.Core/Services/ExecutionSemanticsFacts.cs` | 두 갈래의 `Target` 칸을 원문으로 찍는다 |
| 수정 `src/ReSet.Core/Services/CacheManager.cs` | 캐시 형식 버전 18 → 19 |
| 수정 `tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs` | 코퍼스 대장을 External 까지 넓힌다 + 새 갈래 단위 테스트 |
| 수정 `tests/ReSet.Core.Tests/AggregateAssignmentExtractorTests.cs` | 코퍼스 대장 신설 + 새 갈래 단위 테스트 |
| 수정 `tests/ReSet.Core.Tests/ExecutionSemanticsFactsTests.cs` | 렌더 배선 테스트 |
| 수정 `docs/superpowers/specs/2026-09-06-대입-감쌈-벗기기-design.md` | §9 실측 결과를 덧붙인다(사전선언 파일이 이것이다) |

---

## Task 1: 코퍼스 대장을 External 까지 넓힌다 (조건 ①④의 자)

**왜 이것이 먼저인가.** 🔴#1의 대상 객체 `UF_GET_COMM4CLIENT4PARTIALCANCEL`은 `output/External/SETTLE_CARD_DB/Objects/` 에 있는데, **지금 코퍼스 스윕은 `output/Objects` 24개만 훑는다**(`CorpusRoot()`가 그 한 자리만 돌려준다). 즉 이 설계가 존재하는 이유인 그 객체를 **자가 못 본다.** 자를 먼저 고치지 않으면 조건 ④(행 증분)와 조건 ⑤(증분 전수 판정)가 성립하지 않는다.

이 태스크는 **제품 코드를 한 줄도 바꾸지 않는다.** 변경 전 대장을 못박는 것이 전부다.

**Files:**
- Modify: `tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs` (`CorpusRoot()` 및 `Extract_OverTheCorpus_…`)
- Modify: `tests/ReSet.Core.Tests/AggregateAssignmentExtractorTests.cs` (코퍼스 대장 신설)

**Interfaces:**
- Consumes: `NonAggregateAssignmentExtractor.Extract(string?)` → `IReadOnlyList<NonAggregateAssignmentFact>` (`Line`·`Variable`·`Column`·`Sentence`), `AggregateAssignmentExtractor.Extract(string?)` → `IReadOnlyList<AggregateAssignmentFact>` (`Line`·`Variable`·`Aggregate`·`HasInitializer`·`Sentence`), `CorpusSkip.Reason`
- Produces: `private static IEnumerable<(string Name, string Ddl)> CorpusObjects()` — 두 테스트 클래스가 **각자** 갖는다(테스트 프로젝트에 공용 헬퍼 클래스를 새로 만들지 않는다. 형제 코퍼스 테스트들이 이미 각자 `CorpusRoot()`를 갖는 관행이다). Task 6이 이 두 대장의 diff를 읽는다.

- [ ] **Step 1: `NonAggregateAssignmentExtractorTests`의 코퍼스 열거를 두 뿌리로 넓힌다**

`CorpusRoot()`를 지우지 말고 **그 자리에** 아래 둘을 둔다(`CorpusRoot`는 `RepoRoot`로 이름을 바꾼다 — 「Objects 한 자리」라는 뜻이 이름에 남아 있으면 다음 사람이 또 좁게 읽는다).

```csharp
        /// <summary>
        /// 저장소 뿌리. "output/Objects를 가진 첫 조상"으로 찾으면 안 된다 - 다른 테스트가
        /// 실행 중에 bin/Debug/net10.0/output/Objects에 가짜 객체를 만들어 두어, 그쪽이
        /// 먼저 걸리면 남의 테스트 찌꺼기를 코퍼스로 착각한다. 그래서 src/ReSet.Core를
        /// 가진 조상을 찾는다.
        /// </summary>
        private static string? RepoRoot()
        {
            var dir = new DirectoryInfo(AppContext.BaseDirectory);
            while (dir != null)
            {
                if (Directory.Exists(Path.Combine(dir.FullName, "src", "ReSet.Core")))
                {
                    return dir.FullName;
                }

                dir = dir.Parent;
            }

            return null;
        }

        /// <summary>
        /// 코퍼스 객체 전량 - 로컬 <c>output/Objects</c> **와** 외부 DB
        /// <c>output/External/[DB]/Objects</c> 둘 다.
        ///
        /// [왜 External을 함께 훑는가 - 2026-09-06] 이 대장은 오래도록 로컬 24개만 훑었다.
        /// 그런데 명세서가 만들어지는 객체는 그 24개가 아니라 **참조 폐포**이고, 폐포에는
        /// 외부 DB 함수 7개가 들어 있다(reset-consistency-audit SKILL.md 1-1절). 실제로
        /// 축 A 🔴 하나의 대상 <c>UF_GET_COMM4CLIENT4PARTIALCANCEL</c>이 그 7개 안에
        /// 있어서, 로컬만 훑는 자로는 그 결함이 이 대장에 **한 번도 나타나지 않았다.**
        /// 자가 관할을 좁게 잡으면 결함이 아니라 자가 침묵한다.
        ///
        /// 이름은 외부 DB만 <c>[DB]/</c>로 접두한다 - 로컬 이름 24개가 그대로 남아야
        /// 이 회차의 증분이 diff에서 바로 읽힌다.
        /// </summary>
        private static IEnumerable<(string Name, string Ddl)> CorpusObjects()
        {
            var root = RepoRoot();
            if (root == null) yield break;

            var roots = new List<(string Prefix, string Dir)>();

            var local = Path.Combine(root, "output", "Objects");
            if (Directory.Exists(local)) roots.Add((string.Empty, local));

            var external = Path.Combine(root, "output", "External");
            if (Directory.Exists(external))
            {
                foreach (var db in Directory.GetDirectories(external).OrderBy(x => x, StringComparer.Ordinal))
                {
                    var objects = Path.Combine(db, "Objects");
                    if (Directory.Exists(objects)) roots.Add((Path.GetFileName(db) + "/", objects));
                }
            }

            foreach (var (prefix, dir) in roots)
            {
                foreach (var objectDir in Directory.GetDirectories(dir).OrderBy(x => x, StringComparer.Ordinal))
                {
                    var path = Path.Combine(objectDir, "raw", "object_definition.sql");
                    if (!File.Exists(path)) continue;
                    yield return (prefix + Path.GetFileName(objectDir), File.ReadAllText(path));
                }
            }
        }
```

`Extract_OverTheCorpus_ShouldCollectExactlyEightRowsSevenOfThemNullCertain`의 본문에서 순회를 갈아 끼운다(단언 배열은 **아직 손대지 않는다** — Step 2가 실측으로 채운다).

```csharp
            var objects = CorpusObjects().ToList();
            Skip.If(objects.Count == 0, CorpusSkip.Reason);

            var collected = new List<string>();
            var cteNodes = 0;
            var setVariables = 0;
            var compoundSetVariables = 0;

            foreach (var (name, ddl) in objects)
            {
                foreach (var fact in NonAggregateAssignmentExtractor.Extract(ddl))
                {
                    var branch = fact.Sentence.Contains("NULL이 그대로 남습니다") ? "NULL확정" : "중립";
                    collected.Add($"{name}:{fact.Line} {fact.Variable} = {fact.Column} [{branch}]");
                }

                var (cte, setVariable, compound) = CountGuardInputs(ddl);
                cteNodes += cte;
                setVariables += setVariable;
                compoundSetVariables += compound;
            }
```

- [ ] **Step 2: 실패시켜 실측값을 읽고, 각 새 행을 원본 DDL에서 확인한 뒤 단언에 적는다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~NonAggregateAssignmentExtractorTests.Extract_OverTheCorpus" -v n`

Expected: **FAIL.** xUnit이 `Assert.Equal(string[], string[])`의 diff로 실제 배열 전문을 찍는다. 늘어난 행은 전부 `SETTLE_CARD_DB/` 접두를 가진 것이다.

**여기서 멈추고, 늘어난 행을 하나씩 원본에서 확인한다.**

```bash
# 예: 늘어난 행이 SETTLE_CARD_DB/dbo.UF_GET_COMM4CLIENT.Function:31 이면
sed -n '25,40p' output/External/SETTLE_CARD_DB/Objects/dbo.UF_GET_COMM4CLIENT.Function/raw/object_definition.sql
```

각 행에 대해 **두 가지**를 눈으로 확인한다.
1. 그 줄이 실제로 `SELECT @변수 = <컬럼>` 이고 FROM 절이 있는가.
2. 확정 문장 갈래(`[NULL확정]` / `[중립]`)가 그 자리에서 참인가 — `NULL확정`이면 그 문장 앞에 그 변수에 대한 대입이 정말 하나도 없어야 한다.

확인한 실제 배열을 `Assert.Equal`의 기대값에 그대로 적는다. `Assert.Equal(7, …[NULL확정]…)`·`Assert.Equal(1, …[중립]…)`·`Assert.Equal(0, cteNodes)`·`Assert.Equal(26, setVariables)`·`Assert.Equal(0, compoundSetVariables)` 다섯도 **실측값으로 갱신한다**(External 7개가 더해지므로 `setVariables`는 반드시 늘어난다).

그리고 테스트 이름의 「Eight」와 클래스 주석의 「8행」이 이제 거짓이므로 함께 고친다. 이름은 수를 빼고 뜻으로 바꾼다:

```csharp
        public void Extract_OverTheCorpus_ShouldCollectExactlyTheseRows()
```

- [ ] **Step 3: 테스트를 다시 돌려 통과를 확인한다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~NonAggregateAssignmentExtractorTests" -v n`
Expected: PASS, 건너뜀 0

- [ ] **Step 4: 집계 쪽 코퍼스 대장을 신설한다**

`AggregateAssignmentExtractorTests.cs` 파일 머리의 `using`을 늘린다.

```csharp
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using Xunit;
using ReSet.Core.Services;
```

클래스 **맨 끝**(마지막 멤버 뒤)에 아래를 더한다. `CorpusObjects()`/`RepoRoot()`는 Step 1의 것을 **그대로 복사한다** — 두 클래스가 각자 갖는다.

```csharp
        [SkippableFact]
        public void Extract_OverTheCorpus_ShouldCollectExactlyTheseRows()
        {
            // 이 클래스에는 코퍼스 대장이 없었다. 형제 NonAggregateAssignmentExtractorTests가
            // 대장을 가진 덕에 그쪽 회차의 증분은 눈에 보였지만, 집계 쪽은 규칙이 흘러도
            // 단위 테스트가 그대로 통과한다. 감쌈 벗기기 회차(2026-09-06)가 이 표의 행을
            // 늘리므로, 늘기 **전에** 여기 대장을 세워 증분이 diff로 읽히게 한다.
            //
            // 갈래를 행에 함께 싣는다 - 이 추출기의 요점은 무결과 귀결이 갈린다는 것이고
            // (GROUP BY / COUNT / NULL), 갈래가 바뀌는 것은 대상 칸이 바뀌는 것보다
            // 훨씬 무겁다(확정 문장이 거짓이 된다).
            var objects = CorpusObjects().ToList();
            Skip.If(objects.Count == 0, CorpusSkip.Reason);

            var collected = new List<string>();
            foreach (var (name, ddl) in objects)
            {
                foreach (var fact in AggregateAssignmentExtractor.Extract(ddl))
                {
                    var branch =
                        fact.Sentence.Contains("이전 값을 그대로 유지합니다") ? "유지"
                        : fact.Sentence.Contains("COUNT는 0을 넣습니다") ? "COUNT0"
                        : "NULL대입";
                    collected.Add($"{name}:{fact.Line} {fact.Variable} = {fact.Aggregate} [{branch}]");
                }
            }

            Assert.Equal(
                new[]
                {
                    "실측으로 채운다"
                },
                collected.OrderBy(x => x, StringComparer.Ordinal).ToArray());
        }
```

- [ ] **Step 5: 실패시켜 실측값을 읽고, 각 행을 원본에서 확인한 뒤 적는다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AggregateAssignmentExtractorTests.Extract_OverTheCorpus" -v n`
Expected: FAIL — `"실측으로 채운다"` 대 실제 배열의 diff.

Step 2와 같은 절차로 각 행을 원본에서 확인하고(집계 함수 이름과 갈래가 맞는가) 실제 배열을 적는다. `"실측으로 채운다"` 문자열이 코드에 남으면 안 된다.

- [ ] **Step 6: 두 대장이 다 통과하는지 확인한다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AssignmentExtractorTests" -v n`
Expected: PASS, 건너뜀 0

- [ ] **Step 7: 커밋**

```bash
git commit -m "$(cat <<'EOF'
test: 대입 추출기 코퍼스 대장을 External 까지 넓히고 집계 쪽에도 세운다

대장이 output/Objects 24 개만 훑고 있었다. 명세서가 만들어지는 대상은
참조 폐포이고 거기엔 외부 DB 함수 7 개가 들어 있다 — 축 A 🔴 하나의 대상
UF_GET_COMM4CLIENT4PARTIALCANCEL 이 그 7 개 안에 있어서, 그 결함이 이 대장에
한 번도 나타난 적이 없다. 자가 관할을 좁게 잡으면 결함이 아니라 자가 침묵한다.

집계 쪽에는 대장 자체가 없었다. 다음 회차가 이 표의 행을 늘리므로 늘기 전에
세워 증분이 diff 로 읽히게 한다. 제품 코드는 한 줄도 안 바꿨다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
EOF
)" -- tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs tests/ReSet.Core.Tests/AggregateAssignmentExtractorTests.cs
```

---

## Task 2: 감쌈 벗기기 헬퍼 (`AssignmentExpressionUnwrapper`)

**설계서 용어 정정 하나.** 설계서 §3-1은 벗기는 대상을 「NULL 보존 감쌈」이라 불렀는데 정확하지 않다 — `ISNULL`은 NULL을 **보존하지 않고 대체한다**. 보존되는 것은 값이 아니라 **질의가 돌려주는 행 수**다. 그래서 코드와 주석에서는 「행 수를 바꾸지 않는 스칼라 감쌈」이라고 쓴다. 집계 갈래에서 `ISNULL`이 값을 바꾸는 것이 정확히 네 번째 문장이 필요한 이유다(Task 4).

이 태스크는 **소비자를 하나도 바꾸지 않는다.** 헬퍼와 그 시험만 만든다.

**Files:**
- Create: `src/ReSet.Core/Services/AssignmentExpressionUnwrapper.cs`
- Create: `tests/ReSet.Core.Tests/AssignmentExpressionUnwrapperTests.cs`

**Interfaces:**
- Consumes: ScriptDom `ScalarExpression`·`FunctionCall`·`CoalesceExpression`·`IIfCall`·`SearchedCaseExpression`·`SimpleCaseExpression`·`ColumnReferenceExpression`·`Literal`·`TSqlFragment` (모두 `Microsoft.SqlServer.TransactSql.ScriptDom`, 180.37.3에 실재함을 어셈블리로 확인함)
- Produces (Task 3·4가 이 이름과 형을 그대로 쓴다):
  - `readonly record struct UnwrapResult(ScalarExpression Inner, string? WrapperName, Literal? Fallback)`
  - `static UnwrapResult Unwrap(ScalarExpression expression)`
  - `static bool TryColumnBranches(ScalarExpression expression, out IReadOnlyList<ColumnReferenceExpression> branches)`
  - `static string TextOf(TSqlFragment? fragment)`

- [ ] **Step 1: 실패하는 시험을 쓴다**

Create `tests/ReSet.Core.Tests/AssignmentExpressionUnwrapperTests.cs`:

```csharp
using System.Collections.Generic;
using System.IO;
using System.Linq;
using Microsoft.SqlServer.TransactSql.ScriptDom;
using Xunit;
using ReSet.Core.Services;

namespace ReSet.Core.Tests
{
    public class AssignmentExpressionUnwrapperTests
    {
        /// <summary>DDL 한 조각에서 첫 `SELECT @v = <식>`의 우변을 꺼낸다.</summary>
        private static ScalarExpression RightHandSide(string ddl)
        {
            var parser = new TSql160Parser(true);
            using var reader = new StringReader(ddl);
            var fragment = parser.Parse(reader, out var errors);
            Assert.Empty(errors);

            var finder = new SetVariableFinder();
            fragment!.Accept(finder);
            return Assert.Single(finder.Expressions);
        }

        private sealed class SetVariableFinder : TSqlFragmentVisitor
        {
            public List<ScalarExpression> Expressions { get; } = new();

            public override void Visit(SelectSetVariable node)
            {
                if (node.Expression != null) Expressions.Add(node.Expression);
            }
        }

        private static string Ddl(string rhs) =>
            "CREATE PROCEDURE dbo.P AS BEGIN DECLARE @v INT SELECT @v = " + rhs +
            " FROM dbo.T WITH(NOLOCK) END";

        [Fact]
        public void Unwrap_PlainColumn_ReturnsItUnchangedWithNoWrapper()
        {
            var result = AssignmentExpressionUnwrapper.Unwrap(RightHandSide(Ddl("A.CommissionRate")));

            Assert.IsType<ColumnReferenceExpression>(result.Inner);
            Assert.Null(result.WrapperName);
            Assert.Null(result.Fallback);
        }

        [Fact]
        public void Unwrap_IsNullWithLiteralDefault_PeelsOneLayerAndKeepsTheDefault()
        {
            // UP_UTIL_SETTLE_PROC_ETC:116 실측 모양 - 이 자리가 두 추출기 사이로 새던
            // 곳이다. 집계 추출기는 "최상위가 집계 이름이 아님"으로, 비집계 추출기는
            // "맨 컬럼이 아님"으로 각각 떨어뜨렸다.
            var result = AssignmentExpressionUnwrapper.Unwrap(RightHandSide(Ddl("ISNULL(SUM(A.CLTotal), 0)")));

            var call = Assert.IsType<FunctionCall>(result.Inner);
            Assert.Equal("SUM", call.FunctionName.Value);
            Assert.Equal("ISNULL", result.WrapperName);
            Assert.Equal("0", Assert.IsAssignableFrom<Literal>(result.Fallback).Value);
        }

        [Fact]
        public void Unwrap_Coalesce_IsTreatedTheSameAsIsNull()
        {
            var result = AssignmentExpressionUnwrapper.Unwrap(RightHandSide(Ddl("COALESCE(MAX(A.ID), 0)")));

            Assert.IsType<FunctionCall>(result.Inner);
            Assert.Equal("COALESCE", result.WrapperName);
            Assert.NotNull(result.Fallback);
        }

        [Fact]
        public void Unwrap_IsNullWhoseDefaultIsNotALiteral_DoesNotPeel()
        {
            // 기본값이 리터럴이 아니면 무결과 시 무엇이 들어가는지 기계가 말할 수 없다.
            // 벗기면 네 번째 문장이 "<식>이 대입됩니다"를 지어내게 된다 - 침묵한다.
            var result = AssignmentExpressionUnwrapper.Unwrap(RightHandSide(Ddl("ISNULL(SUM(A.CLTotal), @v)")));

            Assert.IsType<FunctionCall>(result.Inner);
            Assert.Equal("ISNULL", ((FunctionCall)result.Inner).FunctionName.Value);
            Assert.Null(result.WrapperName);
            Assert.Null(result.Fallback);
        }

        [Fact]
        public void Unwrap_PeelsOnlyOneLayer()
        {
            // 겹겹이 감싼 것을 끝까지 벗기면 판정 범위가 소리 없이 넓어진다. 한 겹만 벗기고,
            // 그래도 안쪽이 판정 가능한 모양이 아니면 소비자가 침묵한다.
            var result = AssignmentExpressionUnwrapper.Unwrap(
                RightHandSide(Ddl("ISNULL(ISNULL(A.Rate, 0), 0)")));

            var inner = Assert.IsType<FunctionCall>(result.Inner);
            Assert.Equal("ISNULL", inner.FunctionName.Value);
            Assert.Equal("ISNULL", result.WrapperName);
        }

        [Fact]
        public void TryColumnBranches_Iif_WhenBothBranchesAreColumns_Succeeds()
        {
            // UF_GET_COMM4CLIENT4PARTIALCANCEL:43 실측 - 축 A 🔴 #1 의 자리다.
            var ok = AssignmentExpressionUnwrapper.TryColumnBranches(
                RightHandSide(Ddl("IIF(@v IN (0,2), A.CommissionRate, A.FreeInterestInstCommRate)")),
                out var branches);

            Assert.True(ok);
            Assert.Equal(2, branches.Count);
        }

        [Fact]
        public void TryColumnBranches_Case_WhenEveryBranchIncludingElseIsAColumn_Succeeds()
        {
            var ok = AssignmentExpressionUnwrapper.TryColumnBranches(
                RightHandSide(Ddl("CASE WHEN A.Flag = 1 THEN A.RateA ELSE A.RateB END")),
                out var branches);

            Assert.True(ok);
            Assert.Equal(2, branches.Count);
        }

        [Fact]
        public void TryColumnBranches_CaseWithoutElse_Fails()
        {
            // ELSE가 없으면 어느 WHEN도 참이 아닐 때 NULL이 대입된다. 분기가 "전부 컬럼"이라는
            // 전제가 깨지므로 담지 않는다 - 거짓 행보다 없는 행이 낫다.
            Assert.False(AssignmentExpressionUnwrapper.TryColumnBranches(
                RightHandSide(Ddl("CASE WHEN A.Flag = 1 THEN A.RateA END")), out _));
        }

        [Fact]
        public void TryColumnBranches_WhenAnyBranchIsALiteralOrExpression_Fails()
        {
            Assert.False(AssignmentExpressionUnwrapper.TryColumnBranches(
                RightHandSide(Ddl("IIF(@v = 1, A.RateA, 0)")), out _));
            Assert.False(AssignmentExpressionUnwrapper.TryColumnBranches(
                RightHandSide(Ddl("IIF(@v = 1, A.RateA, A.RateB + 1)")), out _));
        }

        [Fact]
        public void TryColumnBranches_Aggregate_Fails()
        {
            // 집계는 잎이 아니므로 컬럼 분기 판정에 걸리지 않는다. 이것이 두 갈래를
            // 배타적으로 만드는 기전이다 - 비집계 소비자가 집계를 담을 길이 없다.
            Assert.False(AssignmentExpressionUnwrapper.TryColumnBranches(
                RightHandSide(Ddl("SUM(A.CLTotal)")), out _));
        }

        [Fact]
        public void TextOf_NormalizesWhitespaceAndKeepsTheOriginalTokens()
        {
            var text = AssignmentExpressionUnwrapper.TextOf(
                RightHandSide(Ddl("IIF(@v IN (0,2),   A.CommissionRate,\n A.FreeInterestInstCommRate)")));

            Assert.Equal("IIF(@v IN (0,2), A.CommissionRate, A.FreeInterestInstCommRate)", text);
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AssignmentExpressionUnwrapperTests" -v n`
Expected: 컴파일 실패 — `AssignmentExpressionUnwrapper`가 없다

- [ ] **Step 3: 헬퍼를 만든다**

Create `src/ReSet.Core/Services/AssignmentExpressionUnwrapper.cs`:

```csharp
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;
using Microsoft.SqlServer.TransactSql.ScriptDom;

namespace ReSet.Core.Services
{
    /// <param name="Inner">감쌈을 한 겹 벗긴 안쪽 식. 벗길 것이 없으면 원래 식 그대로.</param>
    /// <param name="WrapperName">벗겨낸 감쌈의 이름(<c>ISNULL</c>·<c>COALESCE</c>). 안 벗겼으면 null.</param>
    /// <param name="Fallback">그 감쌈의 기본값 리터럴. 안 벗겼으면 null.</param>
    public readonly record struct UnwrapResult(
        ScalarExpression Inner, string? WrapperName, Literal? Fallback);

    /// <summary>
    /// 변수 대입의 우변에서 **행 수를 바꾸지 않는 스칼라 감쌈**을 한 겹 벗기고, 안쪽이
    /// 「전부 컬럼 참조인 분기식」인지 판정한다.
    ///
    /// [왜 필요한가 - 2026-09-06 축 A 감사] 두 대입 추출기의 우변 가드가 각각
    /// <c>Expression is not ColumnReferenceExpression</c>(비집계)와
    /// <c>Expression is not FunctionCall</c> + 집계 이름 화이트리스트(집계)라,
    /// <c>ISNULL(SUM(x),0)</c> 같은 자리가 **두 그물 사이로 샜다** - 집계 쪽에는
    /// "집계 이름이 아님"으로, 비집계 쪽에는 "맨 컬럼이 아님"으로 각각 떨어진다.
    /// <c>IIF(...)</c>로 감싼 대입도 같은 이유로 통째로 빠졌다.
    ///
    /// [이름을 조심할 것 - 보존되는 것은 NULL이 아니라 행 수다] <c>ISNULL</c>은 NULL을
    /// 보존하지 않고 **대체한다.** 이 클래스가 벗기는 근거는 그 감쌈이 질의가 돌려주는
    /// **행 수**를 바꾸지 않는다는 것이다. 값이 바뀌는 쪽은 소비자가 문장으로 말해야 한다
    /// (<see cref="AggregateAssignmentExtractor"/>의 <c>ISNULL(&lt;집계&gt;, 리터럴)</c> 갈래).
    ///
    /// [한 겹만 벗긴다] 끝까지 벗기면 판정 범위가 소리 없이 넓어진다. 한 겹만 벗기고,
    /// 안쪽이 판정 가능한 모양이 아니면 소비자가 침묵한다 - 이 클래스는 「모르는 것을
    /// 확정 표에 싣는다」 방향으로 한 발도 가지 않는다(그 표는 「수정 금지」다).
    /// </summary>
    public static class AssignmentExpressionUnwrapper
    {
        private static readonly Regex Whitespace = new(@"\s+", RegexOptions.Compiled);

        /// <summary>
        /// <c>ISNULL(X, 리터럴)</c>·<c>COALESCE(X, 리터럴)</c>를 한 겹 벗긴다.
        /// 기본값이 리터럴이 아니면 벗기지 않는다 - 무결과 시 무엇이 들어가는지
        /// 기계가 말할 수 없어서, 벗기면 소비자가 없는 문장을 지어내게 된다.
        /// </summary>
        public static UnwrapResult Unwrap(ScalarExpression expression)
        {
            switch (expression)
            {
                case FunctionCall call
                    when string.Equals(call.FunctionName?.Value, "ISNULL", System.StringComparison.OrdinalIgnoreCase)
                         && call.Parameters.Count == 2
                         && call.Parameters[1] is Literal isNullDefault:
                    return new UnwrapResult(call.Parameters[0], "ISNULL", isNullDefault);

                case CoalesceExpression coalesce
                    when coalesce.Expressions.Count == 2
                         && coalesce.Expressions[1] is Literal coalesceDefault:
                    return new UnwrapResult(coalesce.Expressions[0], "COALESCE", coalesceDefault);

                default:
                    return new UnwrapResult(expression, null, null);
            }
        }

        /// <summary>
        /// 식이 컬럼 참조이거나, 모든 분기가 컬럼 참조인 <c>IIF</c>/<c>CASE</c>인지 본다.
        ///
        /// [왜 "전부"를 요구하는가] 한 분기라도 리터럴·산술식이면 그 분기가 골라졌을 때
        /// 대입값이 컬럼에서 오지 않는다. 그러면 비집계 확정 문장("무결과면 대입 자체가
        /// 일어나지 않는다")은 여전히 참이지만 대상 칸이 실을 것이 흐려진다. 좁게 잡는다.
        ///
        /// [ELSE 없는 CASE는 거른다] 어느 WHEN도 참이 아니면 NULL이 대입되므로
        /// "분기가 전부 컬럼"이라는 전제가 깨진다.
        /// </summary>
        public static bool TryColumnBranches(
            ScalarExpression expression, out IReadOnlyList<ColumnReferenceExpression> branches)
        {
            var found = new List<ColumnReferenceExpression>();
            branches = found;

            switch (expression)
            {
                case ColumnReferenceExpression column:
                    if (column.ColumnType != ColumnType.Regular) return false;
                    found.Add(column);
                    return true;

                case IIfCall iif:
                    return Collect(found, iif.ThenExpression, iif.ElseExpression);

                case SearchedCaseExpression searched:
                    return Collect(
                        found,
                        searched.WhenClauses.Select(w => w.ThenExpression)
                            .Concat(new[] { searched.ElseExpression }).ToArray());

                case SimpleCaseExpression simple:
                    return Collect(
                        found,
                        simple.WhenClauses.Select(w => w.ThenExpression)
                            .Concat(new[] { simple.ElseExpression }).ToArray());

                default:
                    return false;
            }
        }

        private static bool Collect(
            List<ColumnReferenceExpression> found, params ScalarExpression?[] candidates)
        {
            foreach (var candidate in candidates)
            {
                if (candidate is not ColumnReferenceExpression column) return false;
                if (column.ColumnType != ColumnType.Regular) return false;
                found.Add(column);
            }

            return found.Count > 0;
        }

        /// <summary>
        /// 조각의 원문 텍스트. 토큰 스트림에서 잘라 온다 - 재구성하면 원문에 없는 표기가
        /// 생긴다(형제 <see cref="DerivedTableColumnExtractor"/>·<see cref="CaseBranchExtractor"/>와
        /// 같은 방식이다).
        /// </summary>
        public static string TextOf(TSqlFragment? fragment)
        {
            if (fragment?.ScriptTokenStream == null) return string.Empty;

            var text = string.Concat(
                fragment.ScriptTokenStream
                    .Skip(fragment.FirstTokenIndex)
                    .Take(fragment.LastTokenIndex - fragment.FirstTokenIndex + 1)
                    .Select(t => t.Text));

            return Whitespace.Replace(text, " ").Trim();
        }
    }
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AssignmentExpressionUnwrapperTests" -v n`
Expected: PASS (11 tests)

`TextOf_NormalizesWhitespace…`가 토큰 사이 공백 때문에 어긋나면, **단언을 실측값으로 고치지 말고** 먼저 실제 출력을 확인하라 — 토큰 스트림은 원문 공백을 토큰으로 갖고 있으므로 `Whitespace.Replace` 뒤에는 위 기대값이 나와야 한다. 다르면 그것은 이 헬퍼의 결함이다.

- [ ] **Step 5: 전체 게이트**

Run: `dotnet build 2>&1 | tail -20` → 경고 0 · 오류 0
Run: `dotnet test 2>&1 | tail -20` → 실패 0 · 건너뜀 0

- [ ] **Step 6: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat: 대입 우변의 스칼라 감쌈을 벗기는 헬퍼를 만든다

두 대입 추출기의 우변 가드 사이로 ISNULL(SUM(x),0) 같은 자리가 샌다 —
집계 쪽엔 「집계 이름이 아님」, 비집계 쪽엔 「맨 컬럼이 아님」으로 각각 떨어진다.
IIF 로 감싼 대입도 같은 이유로 통째로 빠졌다.

벗기는 근거는 그 감쌈이 질의의 행 수를 바꾸지 않는다는 것이다 — NULL 을
보존해서가 아니다(ISNULL 은 NULL 을 대체한다). 값이 바뀌는 쪽은 소비자가
문장으로 말한다. 한 겹만 벗기고, 기본값이 리터럴이 아니면 벗기지 않는다.

소비자는 아직 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
EOF
)" -- src/ReSet.Core/Services/AssignmentExpressionUnwrapper.cs tests/ReSet.Core.Tests/AssignmentExpressionUnwrapperTests.cs
```

---

## Task 3: 비집계 대입 — 감쌈을 벗기고 우변 원문을 싣는다 (🔴#1을 닫는다)

**Files:**
- Modify: `src/ReSet.Core/Services/NonAggregateAssignmentExtractor.cs` (레코드 선언 · 클래스 주석 · `NonAggregateAssignmentVisitor.Visit`)
- Modify: `src/ReSet.Core/Services/ExecutionSemanticsFacts.cs:113-119`
- Modify: `tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs`
- Modify: `tests/ReSet.Core.Tests/ExecutionSemanticsFactsTests.cs`

**Interfaces:**
- Consumes: Task 2의 `AssignmentExpressionUnwrapper.Unwrap`·`TryColumnBranches`·`TextOf`
- Produces: `NonAggregateAssignmentFact(int Line, string Variable, string Expression, string Sentence)` — 세 번째 위치 인자의 **이름이 `Column`에서 `Expression`으로 바뀐다.** Task 4·5·6이 이 이름을 쓴다. 맨 컬럼 대입에서는 값이 전과 **완전히 동일하다**(원문 텍스트가 곧 컬럼 표기라서), 그래서 Task 1이 못박은 기존 대장 행은 한 줄도 안 바뀐다.

- [ ] **Step 1: 실패하는 시험을 쓴다**

`NonAggregateAssignmentExtractorTests.cs`의 **클래스 끝, 코퍼스 테스트 앞**에 더한다.

```csharp
        [Fact]
        public void Extract_IifOverTwoColumns_IsCarriedWithTheBranchExpressionVerbatim()
        {
            // UF_GET_COMM4CLIENT4PARTIALCANCEL:43 실측 - 축 A 🔴 #1 이다. 이 대입이 표에
            // 없어서 명세서가 수수료율 분기를 한 줄도 서술하지 않았고, 그 함수를 부르는
            // 모든 SP 의 금액이 무이자 여부에 따라 갈리는 사실이 통째로 사라졌다.
            //
            // 대상 칸은 분기식을 **축자로** 실어야 한다. 컬럼 이름만 실으면 어느 쪽
            // 컬럼인지가 조건에 달렸다는 사실이 그대로 지워진다.
            const string ddl = @"
CREATE FUNCTION dbo.F(@pi_intFreeInterestFlag INT) RETURNS INT
AS
BEGIN
    DECLARE @v_intCommissionRate INT
    SELECT TOP 1
           @v_intCommissionRate = IIF(@pi_intFreeInterestFlag IN (0,2), A.CommissionRate, A.FreeInterestInstCommRate)
    FROM   dbo.TClientCardContractDtlHist A WITH(NOLOCK)
    WHERE  A.ClientID = '1'
    RETURN @v_intCommissionRate
END";

            var fact = Assert.Single(NonAggregateAssignmentExtractor.Extract(ddl));

            Assert.Equal("@v_intCommissionRate", fact.Variable);
            Assert.Equal(
                "IIF(@pi_intFreeInterestFlag IN (0,2), A.CommissionRate, A.FreeInterestInstCommRate)",
                fact.Expression);
            Assert.Contains("대입 자체가 일어나지 않습니다", fact.Sentence);
        }

        [Fact]
        public void Extract_CaseOverColumns_IsCarried()
        {
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = CASE WHEN A.Flag = 1 THEN A.RateA ELSE A.RateB END
    FROM   dbo.T A WITH(NOLOCK)
END";

            var fact = Assert.Single(NonAggregateAssignmentExtractor.Extract(ddl));

            Assert.Equal("CASE WHEN A.Flag = 1 THEN A.RateA ELSE A.RateB END", fact.Expression);
        }

        [Fact]
        public void Extract_IsNullOverAColumn_IsCarriedWithTheWrapperInTheTargetCell()
        {
            // 감쌈이 벗겨져 갈래가 정해지지만, 대상 칸에는 **감싼 원문**이 실린다.
            // 확정 문장은 그대로 참이다 - 행이 0이면 대입 자체가 없으므로 ISNULL 이
            // 돌 자리가 없다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = ISNULL(A.Rate, 0)
    FROM   dbo.T A WITH(NOLOCK)
END";

            var fact = Assert.Single(NonAggregateAssignmentExtractor.Extract(ddl));

            Assert.Equal("ISNULL(A.Rate, 0)", fact.Expression);
            Assert.Contains("대입 자체가 일어나지 않습니다", fact.Sentence);
        }

        [Fact]
        public void Extract_IifWithANonColumnBranch_StaysSilent()
        {
            // 한 분기가 리터럴이면 담지 않는다. 거짓 행보다 없는 행이 낫다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = IIF(A.Flag = 1, A.RateA, 0)
    FROM   dbo.T A WITH(NOLOCK)
END";

            Assert.Empty(NonAggregateAssignmentExtractor.Extract(ddl));
        }

        [Fact]
        public void Extract_IsNullWrappingAnAggregate_StaysSilentHereAndBelongsToTheAggregateSide()
        {
            // 두 갈래는 배타적이다. 벗긴 안쪽이 집계면 집계 추출기가 가져간다 -
            // 집계는 잎이 아니므로 TryColumnBranches 에 걸리지 않는 것이 그 기전이다.
            // 이 문장이 어느 표에도 안 실리던 것이 UP_UTIL_SETTLE_PROC_ETC 🟠 이었다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = ISNULL(SUM(A.CLTotal), 0)
    FROM   dbo.T A WITH(NOLOCK)
END";

            Assert.Empty(NonAggregateAssignmentExtractor.Extract(ddl));
        }
```

`ExecutionSemanticsFactsTests.cs`에는 렌더 배선을 더한다(클래스 끝).

```csharp
        [Fact]
        public void Collect_NonAggregateAssignmentWithABranchExpression_RendersTheExpressionVerbatim()
        {
            // 대상 칸이 컬럼 이름만 실으면 분기가 지워진다 - 이 배선이 🔴 #1 의 나머지 절반이다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = IIF(A.Flag = 1, A.RateA, A.RateB) FROM dbo.T A WITH(NOLOCK)
END";

            var facts = ExecutionSemanticsFacts.Collect(ddl, null, null, new Dictionary<string, string>());

            var fact = Assert.Single(
                facts, f => f.Kind == ExecutionSemanticsFacts.NonAggregateAssignmentKind);
            Assert.Equal("SELECT @v = IIF(A.Flag = 1, A.RateA, A.RateB)", fact.Target);
        }
```

- [ ] **Step 2: 실패를 확인한다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~NonAggregateAssignmentExtractorTests" -v n`
Expected: 컴파일 실패 — `NonAggregateAssignmentFact`에 `Expression`이 없다

- [ ] **Step 3: 레코드와 클래스 주석을 고친다**

`NonAggregateAssignmentExtractor.cs:10-15`:

```csharp
    /// <param name="Line">대입문의 원본 줄 번호.</param>
    /// <param name="Variable">대입 대상 변수명.</param>
    /// <param name="Expression">대입식 우변의 원문(감쌈·분기식을 벗기지 않은 그대로).</param>
    /// <param name="Sentence">확정 사실 문장.</param>
    public sealed record NonAggregateAssignmentFact(
        int Line, string Variable, string Expression, string Sentence);
```

클래스 주석의 **「왜 컬럼 참조만 담는가」 문단을 통째로** 아래로 바꾼다(나머지 문단은 그대로 둔다).

```
    /// [무엇을 담는가 - 2026-09-06 넓힘] 우변이 컬럼 참조이거나, 모든 분기가 컬럼 참조인
    /// `IIF`/`CASE`이거나, 그 둘을 `ISNULL(X, 리터럴)`/`COALESCE(X, 리터럴)`로 한 겹 감싼
    /// 것이면 담는다(<see cref="AssignmentExpressionUnwrapper"/>).
    ///
    /// 넓혀도 확정 문장이 거짓이 되지 않는 이유가 중요하다 - 이 문장은 **행이 없다**를
    /// 말하지 우변의 모양을 말하지 않는다. 0행이면 대입 자체가 일어나지 않으므로 우변이
    /// 무엇으로 감싸여 있든 결론이 같다. 위험은 정반대쪽, 즉 집계 갈래에만 있다
    /// (거기서는 `ISNULL`이 대입되는 값을 실제로 바꾼다 -
    /// <see cref="AggregateAssignmentExtractor"/>가 그 갈래를 따로 말한다).
    ///
    /// [그래도 좁게 잡는 자리들] 한 분기라도 리터럴·산술식이면 담지 않고, `ELSE` 없는
    /// `CASE`도 담지 않으며, 감쌈의 기본값이 리터럴이 아니면 벗기지 않는다. 대입식이
    /// 집계를 품으면 결론이 정반대로 뒤집히므로(무결과여도 한 행이 돌아온다) 그쪽은
    /// 집계 추출기의 몫이다 - 집계는 잎이 아니라 컬럼 분기 판정에 걸리지 않고, 그것이
    /// 두 갈래를 배타적으로 만드는 기전이다. 실물: 101행 `MAX(ID)+1`, 116행
    /// `ISNULL(SUM(...),0)`.
    ///
    /// [넓히기 전에 무엇이 빠졌었나] `UF_GET_COMM4CLIENT4PARTIALCANCEL:43`의
    /// `IIF(@pi_intFreeInterestFlag IN (0,2), A.CommissionRate, A.FreeInterestInstCommRate)`가
    /// 어떤 기계 확정 표에도 없었고, 명세서가 수수료율 분기를 한 줄도 서술하지 않았다
    /// (2026-09-06 축 A 감사 🔴).
```

- [ ] **Step 4: 방문자의 우변 가드를 넓힌다**

`NonAggregateAssignmentVisitor.Visit`에서 아래 다섯 줄을

```csharp
                    // 컬럼 참조만 담는다(클래스 주석의 "왜 컬럼 참조만 담는가").
                    if (setVariable.Expression is not ColumnReferenceExpression column) continue;
                    if (column.ColumnType != ColumnType.Regular) continue;

                    var parts = column.MultiPartIdentifier?.Identifiers;
                    if (parts == null || parts.Count == 0) continue;
                    var columnText = string.Join(".", parts.Select(id => id.Value));
                    if (string.IsNullOrWhiteSpace(columnText)) continue;
```

이렇게 바꾼다.

```csharp
                    // 감쌈을 한 겹 벗겨 안쪽이 "전부 컬럼 참조인 분기식"인지 본다
                    // (클래스 주석의 "무엇을 담는가"). 대상 칸에는 벗기기 **전** 원문을
                    // 싣는다 - 벗긴 것을 실으면 원문에 없는 문장이 표에 들어간다.
                    if (setVariable.Expression == null) continue;

                    var unwrapped = AssignmentExpressionUnwrapper.Unwrap(setVariable.Expression);
                    if (!AssignmentExpressionUnwrapper.TryColumnBranches(unwrapped.Inner, out _)) continue;

                    var expressionText = AssignmentExpressionUnwrapper.TextOf(setVariable.Expression);
                    if (string.IsNullOrWhiteSpace(expressionText)) continue;
```

그리고 `Facts.Add`의 인자 이름을 맞춘다.

```csharp
                    Facts.Add(new NonAggregateAssignmentFact(
                        setVariable.StartLine, variable, expressionText, sentence));
```

- [ ] **Step 5: 렌더러를 고친다**

`ExecutionSemanticsFacts.cs:113-119`의 `$"SELECT {fact.Variable} = {fact.Column}"`를 `$"SELECT {fact.Variable} = {fact.Expression}"`로 바꾼다.

- [ ] **Step 6: 기존 시험의 `fact.Column`을 `fact.Expression`으로 바꾼다**

```bash
grep -rn 'fact\.Column\|\.Column)' tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs
```

**기대값 문자열은 바꾸지 않는다** — 맨 컬럼 대입에서는 원문 텍스트가 곧 컬럼 표기라 값이 동일하다. 어긋나는 것이 있으면 그것은 이 회차의 **결함이지 자의 문제가 아니다**(예: `A . Rate` 처럼 공백이 끼면 `TextOf`의 정규화가 덜 된 것이다).

- [ ] **Step 7: 시험을 돌린다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~NonAggregateAssignmentExtractorTests|FullyQualifiedName~ExecutionSemanticsFactsTests" -v n`
Expected: 새 단위 시험 6개 PASS. **코퍼스 대장은 FAIL이 정상이다** — 행이 늘었으니 Step 8이 그것을 판정한다.

- [ ] **Step 8: ★ 조건 ⑤ — 늘어난 행을 전수로 열어 참·거짓을 판정한다**

이 계획의 **게이트**다. 「실행 의미」 표는 「수정 금지」라 거짓 행을 뒤에서 거를 장치가 없다.

코퍼스 대장 실패 diff에서 **늘어난 행만** 뽑아, 한 행씩 아래 셋을 확인한다.

1. **원본을 연다.** `sed -n '<줄-5>,<줄+15>p' <그 객체의 object_definition.sql>`
2. **대상 칸이 원문 그대로인가.** 표기가 원본과 다르면(공백·대소문자·괄호) `TextOf`의 결함이다.
3. **확정 문장이 그 자리에서 참인가.**
   - 그 `SELECT`에 `FROM`이 있고 집계가 없어 무결과 시 0행인가.
   - `[NULL확정]`이면 그 문장 앞에 그 변수에 대한 대입이 정말 하나도 없는가.

**거짓 행이 하나라도 나오면 되돌림 조건 R1이다** — 진행하지 말고 §6으로 간다.

전수 판정이 끝나면 실제 배열을 코퍼스 대장 단언에 적고, `[NULL확정]`/`[중립]` 개수도 실측값으로 갱신한다.

- [ ] **Step 9: 통과와 전체 게이트**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~NonAggregateAssignmentExtractorTests" -v n` → PASS
Run: `dotnet build 2>&1 | tail -20` → 경고 0 · 오류 0
Run: `dotnet test 2>&1 | tail -20` → 실패 0 · 건너뜀 0

- [ ] **Step 10: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat: 비집계 대입이 감쌈·분기식을 담고 우변 원문을 싣는다

우변 가드가 「맨 컬럼」이라 IIF·CASE·ISNULL 로 감싼 대입이 통째로 빠졌다.
UF_GET_COMM4CLIENT4PARTIALCANCEL:43 의 수수료율 분기가 어떤 기계 확정 표에도
없었고, 명세서가 그 분기를 한 줄도 서술하지 않았다(축 A 🔴).

넓혀도 확정 문장이 거짓이 되지 않는다 — 그 문장은 「행이 없다」를 말하지 우변의
모양을 말하지 않는다. 0 행이면 대입 자체가 없으므로 감쌈이 무엇이든 결론이 같다.

대상 칸은 컬럼 이름이 아니라 우변 원문을 싣는다. 컬럼 이름만으로는 어느 쪽
컬럼인지가 조건에 달렸다는 사실이 그대로 지워진다. 그래서 레코드의 Column 을
Expression 으로 바꿨다 — 맨 컬럼 대입에서는 값이 전과 동일하다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
EOF
)" -- src/ReSet.Core/Services/NonAggregateAssignmentExtractor.cs src/ReSet.Core/Services/ExecutionSemanticsFacts.cs tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs tests/ReSet.Core.Tests/ExecutionSemanticsFactsTests.cs
```

---

## Task 4: 집계 대입 — 감쌈을 벗기고 네 번째 문장을 더한다 (`PROC_ETC` 🟠 절반)

**Files:**
- Modify: `src/ReSet.Core/Services/AggregateAssignmentExtractor.cs` (레코드 · 클래스 주석 · `AggregateAssignmentVisitor.Visit`)
- Modify: `src/ReSet.Core/Services/ExecutionSemanticsFacts.cs:102-108`
- Modify: `tests/ReSet.Core.Tests/AggregateAssignmentExtractorTests.cs`
- Modify: `tests/ReSet.Core.Tests/ExecutionSemanticsFactsTests.cs`

**Interfaces:**
- Consumes: Task 2의 `AssignmentExpressionUnwrapper.Unwrap`·`TextOf`, Task 3의 렌더러 패턴
- Produces: `AggregateAssignmentFact(int Line, string Variable, string Aggregate, string Expression, bool HasInitializer, string Sentence)` — **네 번째 위치에 `Expression`이 새로 들어간다.** `Aggregate`는 남는다(갈래 판정과 코퍼스 대장이 쓴다).

**문장 갈래의 우선순위를 여기서 못박는다** — 넷이 겹칠 수 있고 순서가 곧 판정이다.

| 순위 | 조건 | 문장 |
| ---: | :--- | :--- |
| 1 | `GROUP BY`가 있다 | 0행을 돌려주므로 대입이 일어나지 않는다 — 이전 값 유지 |
| 2 | `COUNT`/`COUNT_BIG` | COUNT는 0을 넣는다 |
| 3 | 감쌈이 벗겨졌다(`ISNULL`/`COALESCE` + 리터럴) | 무결과 시 집계는 NULL이지만 그 기본값이 대입된다 |
| 4 | 그 외 | 무결과 시 NULL이 대입된다 |

**1이 2·3보다 먼저인 이유**: 0행이면 `ISNULL`도 `COUNT`도 **돌 자리가 없다.** **2가 3보다 먼저인 이유**: `COUNT`는 NULL을 내지 않으므로 `ISNULL(COUNT(x),0)`의 감쌈은 무동작이고, 「기본값이 대입된다」고 말하면 원인을 틀리게 짚는다.

- [ ] **Step 1: 실패하는 시험을 쓴다**

`AggregateAssignmentExtractorTests.cs`의 **코퍼스 대장 앞**에 더한다.

```csharp
        [Fact]
        public void Extract_IsNullWrappingAnAggregate_SaysTheDefaultIsAssigned()
        {
            // UP_UTIL_SETTLE_PROC_ETC:116 실측 - 이 자리가 두 그물 사이로 샜다.
            // 집계 쪽엔 「최상위가 집계 이름이 아님」, 비집계 쪽엔 「맨 컬럼이 아님」.
            // 그 결과 대사 집계식이 어떤 기계 확정 표에도 없었다(축 A 🟠).
            //
            // 여기서 문장이 갈리는 것이 요점이다 - 감싸지 않은 SUM 은 NULL 을 넣지만
            // ISNULL 로 감싸면 0 이 들어간다. 「무결과 시 NULL 이 대입됩니다」를 그대로
            // 실으면 거짓이다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v_intTotal INT
    SELECT @v_intTotal = ISNULL(SUM(A.CLTotal), 0)
    FROM   dbo.T A WITH(NOLOCK)
END";

            var fact = Assert.Single(AggregateAssignmentExtractor.Extract(ddl));

            Assert.Equal("@v_intTotal", fact.Variable);
            Assert.Equal("SUM", fact.Aggregate);
            Assert.Equal("ISNULL(SUM(A.CLTotal), 0)", fact.Expression);
            Assert.Contains("대입이 항상 일어납니다", fact.Sentence);
            Assert.Contains("0", fact.Sentence);
            Assert.DoesNotContain("NULL이 대입됩니다", fact.Sentence);
        }

        [Fact]
        public void Extract_BareAggregate_KeepsSayingNullIsAssigned()
        {
            // 감쌈이 없으면 셋째 갈래 그대로다. 넷째 갈래를 더하면서 셋째가 잠식되지
            // 않았음을 못박는다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = SUM(A.CLTotal) FROM dbo.T A WITH(NOLOCK)
END";

            var fact = Assert.Single(AggregateAssignmentExtractor.Extract(ddl));

            Assert.Equal("SUM(A.CLTotal)", fact.Expression);
            Assert.Contains("무결과 시 NULL이 대입됩니다", fact.Sentence);
        }

        [Fact]
        public void Extract_IsNullWrappingCount_KeepsTheCountSentence()
        {
            // 우선순위 2 > 3. COUNT 는 NULL 을 내지 않으므로 이 ISNULL 은 무동작이다 -
            // 「기본값이 대입된다」고 말하면 원인을 틀리게 짚는다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = ISNULL(COUNT(A.ID), 0) FROM dbo.T A WITH(NOLOCK)
END";

            var fact = Assert.Single(AggregateAssignmentExtractor.Extract(ddl));

            Assert.Contains("COUNT는 0을 넣습니다", fact.Sentence);
        }

        [Fact]
        public void Extract_IsNullWrappingAnAggregateUnderGroupBy_KeepsTheGroupBySentence()
        {
            // 우선순위 1 > 3. 0 행이면 ISNULL 이 돌 자리가 없다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = ISNULL(SUM(A.CLTotal), 0)
    FROM   dbo.T A WITH(NOLOCK)
    GROUP BY A.ClientID
END";

            var fact = Assert.Single(AggregateAssignmentExtractor.Extract(ddl));

            Assert.Contains("이전 값을 그대로 유지합니다", fact.Sentence);
        }

        [Fact]
        public void Extract_IsNullWhoseDefaultIsNotALiteral_StaysSilent()
        {
            // 기본값이 리터럴이 아니면 무엇이 들어가는지 말할 수 없다. 벗기지 않으므로
            // 최상위가 ISNULL 인 채로 남고, 집계 이름 화이트리스트에 걸리지 않아 침묵한다.
            // 넓히면서도 「모르는 것을 확정 표에 싣는다」 방향으로는 한 발도 안 간다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    DECLARE @d INT = 0
    SELECT @v = ISNULL(SUM(A.CLTotal), @d) FROM dbo.T A WITH(NOLOCK)
END";

            Assert.Empty(AggregateAssignmentExtractor.Extract(ddl));
        }
```

`ExecutionSemanticsFactsTests.cs`에 렌더 배선을 더한다.

```csharp
        [Fact]
        public void Collect_AggregateAssignment_RendersTheRightHandSideInsteadOfElidingTheArgument()
        {
            // 2026-09-06 이전에는 대상 칸이 `SELECT @v = SUM(...)` 로 **인자를 생략**했다.
            // 읽는 사람이 무엇을 합산하는지 표에서 알 수 없었다. 원문을 싣는다.
            const string ddl = @"
CREATE PROCEDURE dbo.P
AS
BEGIN
    DECLARE @v INT
    SELECT @v = SUM(A.CLTotal) FROM dbo.T A WITH(NOLOCK)
END";

            var facts = ExecutionSemanticsFacts.Collect(ddl, null, null, new Dictionary<string, string>());

            var fact = Assert.Single(
                facts, f => f.Kind == ExecutionSemanticsFacts.AggregateAssignmentKind);
            Assert.Equal("SELECT @v = SUM(A.CLTotal)", fact.Target);
        }
```

- [ ] **Step 2: 실패를 확인한다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AggregateAssignmentExtractorTests" -v n`
Expected: 컴파일 실패 — `AggregateAssignmentFact`에 `Expression`이 없다

- [ ] **Step 3: 레코드와 클래스 주석을 고친다**

```csharp
    /// <param name="Line">대입문의 원본 줄 번호.</param>
    /// <param name="Variable">대입 대상 변수명.</param>
    /// <param name="Aggregate">집계 함수 이름(대문자). 감쌈을 벗긴 뒤의 이름이다.</param>
    /// <param name="Expression">대입식 우변의 원문(감쌈을 벗기지 않은 그대로).</param>
    /// <param name="HasInitializer">DECLARE에 초기값이 있었는가.</param>
    /// <param name="Sentence">확정 사실 문장.</param>
    public sealed record AggregateAssignmentFact(
        int Line, string Variable, string Aggregate, string Expression, bool HasInitializer, string Sentence);
```

클래스 주석의 **「왜 비집계 대입은 담지 않는가」 문단 뒤에** 아래를 끼운다(그 문단은 그대로 둔다). 삽입 위치는 `[실측]` 문단 **앞**이다.

```
    /// [감쌈을 한 겹 벗긴다 - 2026-09-06] 우변이 `ISNULL(<집계>, 리터럴)`이나
    /// `COALESCE(<집계>, 리터럴)`이면 최상위가 집계 이름이 아니라 이 추출기가 놓쳤고,
    /// 비집계 추출기도 "맨 컬럼이 아님"으로 떨어뜨려 **두 그물 사이로 샜다**
    /// (`UP_UTIL_SETTLE_PROC_ETC:116`, 2026-09-06 축 A 감사 🟠).
    /// <see cref="AssignmentExpressionUnwrapper"/>로 한 겹 벗겨 안쪽으로 갈래를 정한다.
    ///
    /// 그런데 이 감쌈은 **대입되는 값을 실제로 바꾼다** - 무결과 시 집계는 NULL이지만
    /// 기본값이 그것을 덮는다. 그래서 문장 갈래가 셋에서 넷이 됐다. 순서가 곧 판정이다:
    /// GROUP BY(0행이면 ISNULL이 돌 자리가 없다) > COUNT(NULL을 내지 않으므로 감쌈이
    /// 무동작이다) > 감쌈 있음 > 맨 집계. 기본값이 리터럴이 아니면 벗기지 않는다 -
    /// 무엇이 들어가는지 말할 수 없으면 담지 않는 것이 이 표의 규칙이다.
```

- [ ] **Step 4: 방문자를 고친다**

`AggregateAssignmentVisitor.Visit`에서 아래를

```csharp
                    if (setVariable.Expression is not FunctionCall call) continue;
```

이렇게 바꾼다.

```csharp
                    if (setVariable.Expression == null) continue;

                    // 감쌈을 한 겹 벗겨 안쪽으로 갈래를 정한다(클래스 주석). 대상 칸에는
                    // 벗기기 **전** 원문을 싣는다 - 벗긴 것을 실으면 원문에 없는 문장이
                    // 표에 들어간다.
                    var unwrapped = AssignmentExpressionUnwrapper.Unwrap(setVariable.Expression);
                    if (unwrapped.Inner is not FunctionCall call) continue;
```

그리고 문장 갈래에 세 번째 분기를 끼운다. `else if (isCount)` **뒤**, 마지막 `else` **앞**이다.

```csharp
                    else if (unwrapped.Fallback != null)
                    {
                        // 감쌈이 대입되는 값을 바꾼다 - 무결과 시 집계는 NULL이지만
                        // 기본값이 그것을 덮는다. 「무결과 시 NULL이 대입됩니다」를 그대로
                        // 실으면 거짓이다.
                        sentence = "집계 SELECT는 무결과여도 한 행을 돌려주므로 대입이 항상 일어납니다. "
                                   + $"무결과 시 집계가 NULL이지만 `{unwrapped.WrapperName}`의 "
                                   + $"기본값 `{unwrapped.Fallback.Value}`이 대입됩니다.";
                    }
```

`Facts.Add`에 원문을 더한다.

```csharp
                    Facts.Add(new AggregateAssignmentFact(
                        setVariable.StartLine,
                        variable,
                        upper,
                        AssignmentExpressionUnwrapper.TextOf(setVariable.Expression),
                        hasInitializer,
                        sentence));
```

`hasInitializer` 문구는 마지막 `else`에만 붙어 있다 — **넷째 갈래에는 붙이지 않는다.** 기본값이 들어가므로 DECLARE 초기값이 유지되지 않는 것은 같지만, 그 문장이 말하려던 「NULL이 초기값을 덮는다」는 여기서 성립하지 않는다.

- [ ] **Step 5: 렌더러를 고친다**

`ExecutionSemanticsFacts.cs:102-108`의 `$"SELECT {fact.Variable} = {fact.Aggregate}(...)"`를 `$"SELECT {fact.Variable} = {fact.Expression}"`로 바꾼다.

- [ ] **Step 6: 시험을 돌린다**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AggregateAssignmentExtractorTests|FullyQualifiedName~ExecutionSemanticsFactsTests" -v n`
Expected: 새 단위 시험 6개 PASS. 코퍼스 대장은 FAIL이 정상이다(Step 7).

기존 시험 중 `AggregateAssignmentFact` 위치 인자를 세던 것이 있으면 `Expression` 자리만큼 밀린다. 컴파일러가 잡는다.

- [ ] **Step 7: ★ 조건 ⑤ — 집계 쪽 증분을 전수로 판정한다**

Task 3 Step 8과 같은 절차다. 늘어난 행마다 원본을 열고 셋을 확인한다.

1. 대상 칸이 원문 그대로인가.
2. `Aggregate` 칸이 **벗긴 뒤의 집계 이름**인가.
3. 갈래(`[유지]`/`[COUNT0]`/`[NULL대입]`/새 갈래)가 그 자리에서 참인가 — 특히 감쌈이 있는 행은 기본값이 정말 리터럴인가.

**추가로, 늘지 않고 바뀐 행도 본다.** 이 회차는 기존 행의 대상 칸을 `MIN(...)`에서 `MIN(A.YMD)`로 바꾼다. 대장 diff의 **변경 행**이 전부 「인자만 채워진 것」인지 확인하라 — 집계 이름이 바뀐 행이 있으면 벗기기가 과하게 먹은 것이고 되돌림 조건 R4다.

거짓 행이 하나라도 나오면 **R1**이다.

전수 판정이 끝나면 실제 배열을 코퍼스 대장 단언에 적는다.

- [ ] **Step 8: 통과와 전체 게이트**

Run: `dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj --filter "FullyQualifiedName~AssignmentExtractorTests" -v n` → PASS
Run: `dotnet build 2>&1 | tail -20` → 경고 0 · 오류 0
Run: `dotnet test 2>&1 | tail -20` → 실패 0 · 건너뜀 0

- [ ] **Step 9: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat: 집계 대입이 ISNULL·COALESCE 감쌈을 벗기고 네 번째 문장을 낸다

우변 최상위가 집계 이름이어야 담았기 때문에 ISNULL(SUM(x),0) 이 이 그물에도
비집계 그물에도 걸리지 않았다. UP_UTIL_SETTLE_PROC_ETC:116 의 대사 집계식이
어떤 기계 확정 표에도 없었다(축 A 🟠).

이 감쌈은 대입되는 값을 실제로 바꾼다 — 무결과 시 집계는 NULL 이지만 기본값이
덮는다. 그래서 문장 갈래가 넷이 됐고 순서가 곧 판정이다: GROUP BY > COUNT >
감쌈 있음 > 맨 집계. 기본값이 리터럴이 아니면 벗기지 않는다.

대상 칸이 인자를 생략하던 것도 고쳤다 — `SELECT @v = SUM(...)` 로는 무엇을
합산하는지 표에서 알 수 없었다. 이 회차는 기존 행의 대상 칸을 전부 바꾼다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
EOF
)" -- src/ReSet.Core/Services/AggregateAssignmentExtractor.cs src/ReSet.Core/Services/ExecutionSemanticsFacts.cs tests/ReSet.Core.Tests/AggregateAssignmentExtractorTests.cs tests/ReSet.Core.Tests/ExecutionSemanticsFactsTests.cs
```

---

## Task 5: 캐시 형식 버전 19

설계서 §3-4가 이것을 요구한다.

**왜 별도 태스크인가.** 이 한 줄을 빠뜨리면 **이 계획 전체가 무효가 된다** — 옛 엔트리가 히트하면 새 표가 없는 옛 명세서가 그대로 살아남고, 이 회차가 넓힌 재료는 재생성에서 영영 안 실린다. 리뷰어가 이것만 따로 받아 볼 가치가 있다.

**Files:**
- Modify: `src/ReSet.Core/Services/CacheManager.cs` (버전 사유 주석 · `CurrentCacheFormatVersion`)
- Modify: `tests/ReSet.Core.Tests/CacheManagerTests.cs` (버전 상수를 단언하는 시험이 있으면)

**Interfaces:**
- Consumes: Task 3·4가 바꾼 표의 모양
- Produces: `CurrentCacheFormatVersion = 19`

- [ ] **Step 1: 버전을 단언하는 시험이 있는지 본다**

```bash
grep -rn 'CurrentCacheFormatVersion\|CacheFormatVersion' tests/ src/ | grep -v 'CacheManager.cs:'
```

있으면 그 기대값을 19로 고친다(이것이 이 태스크의 RED다). 없으면 Step 2로 간다.

- [ ] **Step 2: 사유를 적고 버전을 올린다**

`CacheManager.cs`의 버전 주석 목록 **끝(18 다음)**에 더한다.

```csharp
        // 19: 2026-09-06 축 A 감사 - 「실행 의미」 표의 `집계 대입`·`비집계 대입` 두 갈래가
        //     함께 넓어졌고, 두 갈래의 대상 칸 모양이 바뀌었다. 표 종류는 늘지 않았다.
        //     (1) 비집계 대입: 우변 가드가 「맨 컬럼」에서 「감쌈을 한 겹 벗긴 뒤 전부 컬럼
        //     참조인 분기식」으로 넓어졌다. `IIF`·`CASE`·`ISNULL(X,리터럴)`로 감싼 대입이
        //     새로 실린다. 실물은 UF_GET_COMM4CLIENT4PARTIALCANCEL:43 의 수수료율 분기로,
        //     넓히기 전에는 어떤 기계 확정 표에도 없고 명세서가 한 줄도 서술하지 않았다.
        //     (2) 집계 대입: `ISNULL(<집계>, 리터럴)`·`COALESCE(<집계>, 리터럴)`이 새로
        //     실리고, 그 행은 **네 번째 문장 갈래**를 쓴다(무결과 시 집계는 NULL이지만
        //     기본값이 대입된다). 실물은 UP_UTIL_SETTLE_PROC_ETC:116 이다.
        //     (3) 두 갈래의 대상 칸이 우변 **원문**을 싣는다. 집계 행은 인자를 생략하던
        //     `SELECT @v = SUM(...)`에서 `SELECT @v = SUM(A.CLTotal)`로, 비집계 행은
        //     컬럼 이름만 싣던 것에서 분기식 축자로 바뀐다. 기존 행이 전부 바뀌는
        //     회차라 옛 엔트리와 새 엔트리를 섞으면 같은 표 안에서 표기가 갈린다.
        //     프롬프트 입력이 달라진 것이므로 옛 엔트리를 재사용하면 새 행이 없는 옛
        //     산출물이 그대로 남는다 - 수수료율 분기가 산문에도 표에도 없는 명세서,
        //     대사 집계식이 어디에도 없는 명세서가 그대로 살아남는다.
        private const int CurrentCacheFormatVersion = 19;
```

- [ ] **Step 3: 전체 게이트**

Run: `dotnet build 2>&1 | tail -20` → 경고 0 · 오류 0
Run: `dotnet test 2>&1 | tail -20` → 실패 0 · 건너뜀 0

- [ ] **Step 4: 커밋**

```bash
git commit -m "$(cat <<'EOF'
chore: 캐시 형식 버전을 19 로 올린다 — 실행 의미 표 두 갈래가 넓어졌다

집계·비집계 대입이 감쌈과 분기식을 담고 대상 칸이 우변 원문을 싣는다.
프롬프트 입력이 달라졌으므로 옛 엔트리를 재사용하면 새 행이 없는 옛 산출물이
그대로 남는다 — 수수료율 분기가 어디에도 없는 명세서가 살아남는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
EOF
)" -- src/ReSet.Core/Services/CacheManager.cs
```

---

## Task 6: 조건 ④⑤⑥ 정산 — 되돌림 뮤턴트와 실측 결과 기록

설계서 §5의 여섯 조건 중 ①②③⑤는 Task 1·3·4가 치렀다. 이 태스크가 **④(행 증분)를 수치로 적고 ⑥(없앴을 때)을 뮤턴트로 확인한 뒤, 그 둘을 사전선언 파일에 덧붙여 커밋한다.**

사전선언 파일은 **설계서 자신**이다(`docs/superpowers/specs/2026-09-06-대입-감쌈-벗기기-design.md`, main `db89f915`에 착수 전 커밋됨). 결과를 §9로 덧붙인다.

**Files:**
- Modify: `docs/superpowers/specs/2026-09-06-대입-감쌈-벗기기-design.md` (§9 신설)

**Interfaces:**
- Consumes: Task 1이 못박은 변경 **전** 대장, Task 3·4가 갱신한 변경 **후** 대장

- [ ] **Step 1: 조건 ④ — 행 증분을 센다**

```bash
git log --oneline -6
# Task 1 커밋의 대장과 Task 4 커밋의 대장을 직접 비교한다
git diff <Task1커밋> HEAD -- tests/ReSet.Core.Tests/NonAggregateAssignmentExtractorTests.cs \
                              tests/ReSet.Core.Tests/AggregateAssignmentExtractorTests.cs
```

기록할 수치는 넷이다 — 비집계 **전/후**, 집계 **전/후**. 그리고 늘어난 행을 객체별로 나열한다.

**증분이 0이면 되돌림 조건 R2다**(재료가 도달하지 않았다). 진행하지 마라.

- [ ] **Step 2: 조건 ⑥ — 뮤턴트 셋을 돌린다**

각 뮤턴트마다 ① 코드를 되돌리고 ② `dotnet test`를 돌리고 ③ **어느 시험이 실패하는지 이름을 적고** ④ 코드를 복구한다. **되돌린 채로 다음 뮤턴트로 넘어가지 마라.**

| 뮤턴트 | 되돌리는 것 | 실패해야 하는 시험 |
| :--- | :--- | :--- |
| M1 | `NonAggregateAssignmentVisitor`의 벗기기를 원래 `is not ColumnReferenceExpression` 가드로 되돌린다 | `Extract_IifOverTwoColumns_…` · `Extract_CaseOverColumns_…` · `Extract_IsNullOverAColumn_…` · 비집계 코퍼스 대장 |
| M2 | `AggregateAssignmentVisitor`의 벗기기를 원래 `is not FunctionCall` 가드로 되돌린다 | `Extract_IsNullWrappingAnAggregate_…` · `Extract_IsNullWrappingCount_…` · 집계 코퍼스 대장 |
| M3 | 렌더러 둘을 원래 `{fact.Aggregate}(...)`·`{fact.Column}`으로 되돌린다 | `Collect_AggregateAssignment_…` · `Collect_NonAggregateAssignmentWithABranchExpression_…` |

**어느 뮤턴트에서도 전 스위트가 초록이면 되돌림 조건 R3다** — 그 부분은 아무것도 안 잠근 것이다.

- [ ] **Step 3: §9를 쓴다**

설계서 끝에 아래 모양으로 덧붙인다. **예측(§5의 「사전 예측」 칸)과 실측을 나란히 둔다** — 어긋난 칸이 다음 회차가 읽을 가장 값진 줄이다.

```markdown
## 9. 실측 결과 (착수 후 기록)

기준 커밋 <구현 첫 커밋>…<마지막 커밋>. 게이트: 빌드 경고 0 · 오류 0 · 실패 0 · 건너뜀 0.

### 조건별

| # | 조건 | 사전 예측 | 실측 | 어긋났나 |
| ---: | :--- | :--- | :--- | :--- |
| 1 | 재료 | 감쌈 자리 10~12 | <파서 실측> | |
| 2 | 도달 | 닿는다 | | |
| 3 | 그 자 | 해당 없음(앵커 미사용) | | |
| 4 | 행 증분 | 늘어난다 | 비집계 A→B · 집계 C→D | |
| 5 | 오탐 | 거짓 0 | | |
| 6 | 되돌림 | 실패 ≥ 1 | M1/M2/M3 각각 <시험 이름들> | |

### 늘어난 행 전량과 그 판정

| 객체:줄 | 대상 칸 | 갈래 | 원본에서 확인한 것 |
| :--- | :--- | :--- | :--- |

### 자에 대해 밝힐 것

- §2-4의 「29·감쌈 12」는 **정규식 상한**이었다. 파서 실측은 <수>다.
- 코퍼스 대장은 이 회차에 `output/External` 을 새로 훑기 시작했다. **그 전의 모든 수치는
  로컬 24 객체만의 것**이므로 이 회차 이전 기록과 직접 비교하지 마라.

### 닫힌 것과 안 닫힌 것

닫혔다고 적을 수 있는 것은 **추출기 출력**까지다. 명세서는 아직 재생성하지 않았으므로
🔴 · 🟠 는 **이 판에서 여전히 열려 있다**(설계서 §0의 「생성기만 고친다」).
```

- [ ] **Step 4: 전체 게이트를 마지막으로 한 번 더 돌린다**

Run: `dotnet build 2>&1 | tail -20` → 경고 0 · 오류 0
Run: `dotnet test 2>&1 | tail -20` → 실패 0 · 건너뜀 0

뮤턴트 복구가 덜 됐으면 여기서 걸린다.

- [ ] **Step 5: 커밋**

```bash
git commit -m "$(cat <<'EOF'
docs: 감쌈 벗기기의 여섯 조건 실측 결과를 설계서에 덧붙인다

행 증분·오탐 전수 판정·뮤턴트 셋을 예측과 나란히 적었다. 코퍼스 대장이 이
회차에 External 을 새로 훑기 시작했으므로 이전 수치와 직접 비교하지 말라는
것도 함께 적었다 — 자가 바뀐 회차의 수치를 옛 수치와 나란히 놓으면 증분이
과장된다.

닫혔다고 적은 것은 추출기 출력까지다. 명세서는 재생성 전이라 🔴·🟠 는 이
판에서 여전히 열려 있다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011vaBedm1HcUihLKaaLj4tq
EOF
)" -- docs/superpowers/specs/2026-09-06-대입-감쌈-벗기기-design.md
```

---

## 이 계획을 끝낸 뒤에 남는 것 (다음 세션용)

- **명세서 재생성은 이 계획 밖이다.** 사용자가 「생성기만 고친다」를 골랐다. 재생성 시점은 사람이 정한다.
- **`PROC_ETC`의 `CAST` 절반**은 `ExpressionTypePathExtractor` 관할로 열려 있다.
- **🔴 `UF_Get_ExtraCardCommissionAmt`**(`TOP` 재료)는 접근법 B.
- **N5 조인 쌍 세 번째 사각지대**와 **두 단위 판정이 갈린 자리 둘**은 운영 DB 질의가 있어야 닫힌다.
- **손대지 마라**: `rescue/narrow-neighbor-warning` 브랜치는 reset-70 의 `ccd3717f` 을 담은 보험이다. 그쪽이 main 으로 옮긴 것이 확인되기 전에는 지우면 안 된다(reset-f4 인계).
- **공유 체크아웃에 추적 안 되는 사본** `docs/superpowers/specs/2026-09-06-대입-감쌈-벗기기-design.md` 가 남아 있다. main 의 블롭과 해시가 같아 지워도 잃는 것이 없지만, 지울지는 사람이 정한다.
