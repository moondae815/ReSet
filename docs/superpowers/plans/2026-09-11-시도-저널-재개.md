# 시도 저널 2단계(재개 + 쿼터 판정) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 중단된 배치 계획 생성을 이어서 하고(결함 없는 단계는 재사용), 쿼터 소진을 실패와 갈라 안내한다.

**Architecture:** 읽기는 `PlanAttemptJournal.TryResume` 에 둔다 — 쓰기와 같은 클래스라 manifest 계약이 한 곳에 남는다. 오케스트레이터는 목차 확정 직후 후보를 찾아 사람에게 묻고, 승인되면 `lastSkeleton`·`lastStepSections`·피드백을 채운 뒤 **새 판을 열고 읽어 온 것을 다시 기록**한다. 쿼터는 `AiRetryVerdict` 에 `Exhausted` 를 더해 `Fatal` 에서 갈라내고, 그 사유가 `ConsolidatedPipelineResult` 를 타고 CLI 까지 간다.

**Tech Stack:** C# / .NET 10, xunit, NSubstitute, `System.Text.Json`, Serilog

**Spec:** `docs/superpowers/specs/2026-09-11-시도-저널-재개-design.md`

## Global Constraints

- **격리 워크트리에서만 빌드·테스트한다**(AGENTS.md). 워크트리에 코퍼스 심링크가 필요하다: `ln -s /Users/payletter/git-root/ReSet/output <워크트리>/output` — 안 걸면 전체 테스트가 건너뜀 수십 개를 낸다.
- **게이트는 실패 0 · 건너뜀 0 · 경고 0.** 절대 통과 수를 기준으로 쓰지 마라. 경고는 `dotnet clean && dotnet build 2>&1 | grep -cE "warning CS"` 로 센다.
- **저널 쓰기는 소프트페일**이고 저널은 **Null Object** 다 — 호출부가 `if (journal != null)` 로 분기하지 않는다.
- **저널이 쓰는 파일에 BOM 이 붙으면 안 된다**(`Utf8NoBom`). `EveryWrittenFile_HasNoUtf8Bom` 이 바이트로 잠그고 있다.
- 문서·커밋 메시지는 한국어. 코드는 **심볼로** 인용하고 행 번호를 쓰지 않는다.
- **3단계 넘는 태스크는 중간 커밋을 한다.** 1단계에서 워커가 100턴 한계에 두 번 걸렸고 중간 커밋이 두 번 다 작업을 건졌다.

### ★ 이 저장소가 1단계에서 열 건 넘게 밟은 함정 (설계서 §7)

1. **「지워도 통과하는 분기」가 반복 결함이었다.** 새 코드마다 「이걸 망가뜨리면 무엇이 빨개지나」를 답하고, 답이 없으면 시험을 먼저 만들어라.
2. **시험이 보는 통로가 결함을 가릴 수 있다.** BOM 이 그랬다 — `File.ReadAllText` 가 벗겨 읽어 왕복 시험 스물둘이 전부 초록이었고 실물에서야 드러났다. **재개는 파일을 읽는 기능이라 같은 함정이 더 크다.**
3. **자리 수는 `grep` 으로 세라.** 배선 자리 수가 문서에서 네 번 틀렸다.
4. 리포트에 **`SYMMETRY SWEEP`** 을 채워라 — 빠진 대칭을 스스로 훑고, 못 찾았으면 어디를 봤는지 적어라.

### 기존 실패 1건 (베이스라인)

`AnchorKindOrdinalPairTests.EveryUAnchorPointsAtAStatementTheSpecActuallyDeclares` 는 이 작업과 무관하게 착수 전부터 실패한다. 사람이 베이스라인으로 두기로 했다. **고치려 하지 마라.**

---

## File Structure

| 파일 | 책임 |
| :--- | :--- |
| `src/ReSet.Core/Services/PlanAttemptJournal.cs` (수정) | `TryResume` 와 후보 타입. **manifest 해석이 이 파일 하나에만 있다** |
| `tests/ReSet.Core.Tests/PlanAttemptJournalTests.cs` (수정) | 판 고르기 단위 시험 |
| `src/ReSet.Core/Services/IVerificationUserInteraction.cs` (수정) | `ConfirmResumeAsync` |
| `src/ReSet.Cli/ConsoleUserInteraction.cs` · `src/ReSet.Cli/Program.cs` · `tests/ReSet.Core.Tests/CliArgsTests.cs` (수정) | 그 인터페이스의 **구현체 셋** — 메서드를 더하면 셋 다 고쳐야 컴파일된다 |
| `src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs` (수정) | 재개 배선 · `AbortReason` 전달 |
| `tests/ReSet.Core.Tests/VerificationPipelineOrchestratorTests.cs` (수정) | 통합 시험 |
| `src/ReSet.Core/Services/AiRetryPolicy.cs` · `AiCallRetry.cs` (수정) | `Exhausted` 판정과 감싸기 |
| `src/ReSet.Core/Models/ConsolidatedPipelineResult.cs` (수정) | `AbortReason` 필드 |

---

### Task 1: `TryResume` — 판 고르기와 후보 타입

**Files:**
- Modify: `src/ReSet.Core/Services/PlanAttemptJournal.cs`
- Test: `tests/ReSet.Core.Tests/PlanAttemptJournalTests.cs`

**Interfaces:**
- Consumes: 기존 `PlanAttemptManifest` · `PlanAttemptReuseKey` · `PlanAttemptArtifact` · `ComputeSha256`
- Produces:
  - `PlanAttemptResumeCandidate` (아래 레코드)
  - `journal.TryResume(string planStructure) → PlanAttemptResumeCandidate?`

- [ ] **Step 1: 실패하는 시험을 쓴다**

`PlanAttemptJournalTests` 에 덧붙인다. `NewJournal()` 헬퍼가 이미 있다(`Create(_root, "Job_Test", "OpenAI", "gpt-4", "high", "C#", "specshash")`).

```csharp
        /// <summary>
        /// 재개 후보를 만드는 헬퍼 — 판 하나를 완성된 모양으로 써 둔다.
        /// 골격 1개 · 건강한 섹션 2개 · 결함 섹션 1개 · 리뷰 2개.
        /// </summary>
        private PlanAttemptJournal WriteResumableRun(string planStructure = "## 목차 A")
        {
            var journal = NewJournal();
            journal.OpenRun(planStructure, "run-start");
            journal.RecordSkeleton(1, "골격 본문");
            journal.RecordStepSection(1, "S01", "S01 건강");
            journal.RecordStepSection(1, "S02", "S02 건강");
            journal.RecordStepSection(2, "S03", "S03 미달",
                new StepDefect(StepDefectKind.QualityFloor, "S03 (하한 미달: 이유)"));
            journal.RecordReview(1, new ReviewResult
            {
                HasDefects = true, FeedbackComment = "회차 1 지적",
                ScoreAccuracy = 5, ScoreCrud = 5, ScoreInterface = 5,
                ScoreException = 5, ScoreReadability = 5
            });
            journal.RecordReview(2, new ReviewResult
            {
                HasDefects = true, FeedbackComment = "회차 2 지적",
                ScoreAccuracy = 7, ScoreCrud = 7, ScoreInterface = 7,
                ScoreException = 7, ScoreReadability = 7
            });
            return journal;
        }

        [Fact]
        public void TryResume_FindsTheRunAndSplitsHealthyFromDefective()
        {
            WriteResumableRun();

            var candidate = NewJournal().TryResume("## 목차 A");

            Assert.NotNull(candidate);
            Assert.Equal(1, candidate!.Run);
            Assert.Equal("골격 본문", candidate.Skeleton);
            Assert.Equal(1, candidate.SkeletonAttempt);

            // DefectKind 가 있는 S03 은 재사용 대상이 아니다 — 다시 만들 목록으로 간다.
            Assert.Equal(new[] { "S01", "S02" }, candidate.ReusableSections.Keys.OrderBy(k => k).ToArray());
            Assert.Equal("S01 건강", candidate.ReusableSections["S01"]);
            Assert.Equal(new[] { "S03" }, candidate.DefectiveStepCodes.ToArray());
            Assert.Equal(3, candidate.TotalStepsInManifest);

            // 원래 Attempt 를 그대로 옮긴다(설계 §3-5) — 새 판에 다시 기록할 때 쓴다.
            Assert.Equal(1, candidate.SectionAttempts["S01"]);

            // 리뷰는 시도 번호가 큰 순으로 최근 3개까지(설계 §3-3).
            Assert.Equal(new[] { 2, 1 }, candidate.PriorReviews.Select(r => r.Attempt).ToArray());
            Assert.Equal("회차 2 지적", candidate.PriorReviews[0].Review.FeedbackComment);
        }

        // 일곱 항목 각각을 재야 한다 — 하나라도 안 재면 그 항목은 무방비다(설계 §5-1).
        // ContractVersion 은 const 라 이 Theory 로 못 바꾼다 — 바로 아래 별도 시험이 맡는다.
        [Theory]
        [InlineData("planStructure")]
        [InlineData("specsSha")]
        [InlineData("provider")]
        [InlineData("model")]
        [InlineData("effort")]
        [InlineData("targetLanguage")]
        public void TryResume_WhenAnyReuseKeyItemDiffers_FindsNothing(string differing)
        {
            WriteResumableRun();

            var journal = PlanAttemptJournal.Create(
                _root, "Job_Test",
                differing == "provider" ? "OtherProvider" : "OpenAI",
                differing == "model" ? "other-model" : "gpt-4",
                differing == "effort" ? "low" : "high",
                differing == "targetLanguage" ? "Java" : "C#",
                differing == "specsSha" ? "otherspecshash" : "specshash");

            var candidate = journal.TryResume(differing == "planStructure" ? "## 다른 목차" : "## 목차 A");

            Assert.Null(candidate);
        }

        /// <summary>
        /// 일곱 번째 항목. <c>PlanAttemptJournal.ContractVersion</c> 은 <c>const</c> 라
        /// 위 Theory 로 바꿀 수 없다 — manifest 를 직접 고쳐 「옛 계약으로 쓰인 판」을 만든다.
        ///
        /// <b>이 시험이 없으면 일곱 번째 항목이 무방비다.</b> 계약 버전이 올라간 뒤에도
        /// 옛 판을 주워 와, 새 규약을 안 지키는 섹션이 문서에 섞인다.
        /// </summary>
        [Fact]
        public void TryResume_WhenTheContractVersionDiffers_FindsNothing()
        {
            var journal = WriteResumableRun();
            var manifestPath = Path.Combine(journal.CurrentRunDirectory!, "manifest.json");

            var text = File.ReadAllText(manifestPath);
            var bumped = text.Replace(
                $"\"ContractVersion\": {PlanAttemptJournal.ContractVersion}",
                $"\"ContractVersion\": {PlanAttemptJournal.ContractVersion + 1}");
            Assert.NotEqual(text, bumped);          // 치환이 실제로 일어났는지 먼저 확인한다
            File.WriteAllText(manifestPath, bumped);

            Assert.Null(NewJournal().TryResume("## 목차 A"));
        }

        // 해시가 안 맞는 항목은 버린다 — 사람이 손댔거나 반쯤 쓰였다는 뜻이다(설계 §3-2).
        [Fact]
        public void TryResume_WhenASectionsHashDoesNotMatch_DropsThatSectionOnly()
        {
            var journal = WriteResumableRun();
            var dir = journal.CurrentRunDirectory!;
            File.WriteAllText(Path.Combine(dir, "steps", "S01.md"), "누군가 손댄 본문");

            var candidate = NewJournal().TryResume("## 목차 A");

            Assert.NotNull(candidate);
            Assert.False(candidate!.ReusableSections.ContainsKey("S01"));
            Assert.True(candidate.ReusableSections.ContainsKey("S02"));
        }

        // 골격도 섹션도 없는 판은 재개 불가다(설계 §11-5 — 단일 호출 폴백 판).
        [Fact]
        public void TryResume_WhenTheRunHasNoSkeletonAndNoSections_FindsNothing()
        {
            var journal = NewJournal();
            journal.OpenRun("## 목차 A", "run-start");
            journal.RecordReview(1, new ReviewResult { HasDefects = false });

            Assert.Null(NewJournal().TryResume("## 목차 A"));
        }

        [Fact]
        public void TryResume_WhenSeveralRunsMatch_PicksTheNewest()
        {
            WriteResumableRun();                 // run-001
            var second = NewJournal();
            second.OpenRun("## 목차 A", "run-start");
            second.RecordSkeleton(1, "둘째 판 골격");
            second.RecordStepSection(1, "S01", "둘째 판 S01");

            var candidate = NewJournal().TryResume("## 목차 A");

            Assert.Equal(2, candidate!.Run);
            Assert.Equal("둘째 판 골격", candidate.Skeleton);
        }

        [Fact]
        public void TryResume_WhenNoRunExists_FindsNothing()
        {
            Assert.Null(NewJournal().TryResume("## 목차 A"));
        }
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~PlanAttemptJournalTests`
Expected: 빌드 실패 — `TryResume`·`PlanAttemptResumeCandidate` 가 없다

- [ ] **Step 3: 후보 타입과 `TryResume` 를 쓴다**

`PlanAttemptJournal.cs` 에 레코드를 더한다(`PlanAttemptArtifact` 옆):

```csharp
    /// <summary>
    /// 재개 후보 하나. <b>2단계가 이 타입만 보고 재개를 판단한다</b> — manifest 해석은
    /// <c>TryResume</c> 안에서 끝난다(설계 §3-2).
    /// </summary>
    public sealed record PlanAttemptResumeCandidate(
        string RunDirectory,
        int Run,
        string StartedAt,
        string Skeleton,
        int SkeletonAttempt,
        IReadOnlyDictionary<string, string> ReusableSections,
        IReadOnlyDictionary<string, int> SectionAttempts,
        IReadOnlyList<string> DefectiveStepCodes,
        IReadOnlyList<(int Attempt, ReviewResult Review)> PriorReviews,
        int TotalStepsInManifest);
```

그리고 `PlanAttemptJournal` 에 메서드를 더한다:

```csharp
        /// <summary>
        /// 이어서 할 수 있는 판을 찾는다. 없으면 <c>null</c>.
        ///
        /// [왜 쓰기와 같은 클래스인가] 1단계 설계가 「읽는 쪽이 생길 때 같은 클래스에
        /// 들어가 규약이 갈라지지 않는다」고 약속한 자리다. manifest 의 키 대소문자·
        /// 진실 계약(§11-1)을 아는 곳이 둘이 되면 조용히 갈린다.
        ///
        /// [무엇을 거르나] <c>ReuseKey</c> 일곱 항목이 전부 같아야 한다 — 특히 모델이
        /// 다르면 문서의 목소리가 섞인다. 골격도 섹션도 없는 판(단일 호출 폴백)은
        /// 재개 불가다(§11-5). <c>Sha256</c> 이 안 맞는 항목은 버린다.
        ///
        /// [왜 소프트페일인가] 못 읽는 판은 <b>없는 것으로 친다.</b> 재개는 편의이지
        /// 파이프라인의 전제가 아니다.
        /// </summary>
        public PlanAttemptResumeCandidate? TryResume(string planStructure)
        {
            try
            {
                var attemptsRoot = Path.Combine(_outputRoot, "Jobs", _jobName, "raw", "attempts");
                if (!Directory.Exists(attemptsRoot)) return null;

                var wanted = new PlanAttemptReuseKey(
                    ContractVersion, ComputeSha256(planStructure), _specsSha256,
                    _provider, _model, _effort, _targetLanguage);

                foreach (var runDir in Directory.EnumerateDirectories(attemptsRoot, "run-*")
                             .OrderByDescending(d => d, StringComparer.Ordinal))
                {
                    var candidate = TryReadCandidate(runDir, wanted);
                    if (candidate != null) return candidate;
                }

                return null;
            }
            catch (Exception ex)
            {
                Log.Debug(ex, "[PlanAttemptJournal] 재개 후보를 찾지 못했습니다 - 처음부터 만듭니다.");
                return null;
            }
        }

        private static PlanAttemptResumeCandidate? TryReadCandidate(string runDir, PlanAttemptReuseKey wanted)
        {
            var manifestPath = Path.Combine(runDir, "manifest.json");
            if (!File.Exists(manifestPath)) return null;

            var manifest = JsonSerializer.Deserialize<PlanAttemptManifest>(
                File.ReadAllText(manifestPath), Options);
            if (manifest?.ReuseKey == null || !manifest.ReuseKey.Equals(wanted)) return null;

            // 골격
            string? skeleton = null;
            var skeletonAttempt = 0;
            var skeletonPath = Path.Combine(runDir, "skeleton.md");
            if (manifest.Skeleton != null && File.Exists(skeletonPath))
            {
                var text = File.ReadAllText(skeletonPath);
                if (ComputeSha256(text) == manifest.Skeleton.Sha256)
                {
                    skeleton = text;
                    skeletonAttempt = manifest.Skeleton.Attempt;
                }
            }

            // 섹션 — DefectKind 가 있으면 재사용하지 않고 「다시 만들 것」으로 보낸다.
            var reusable = new Dictionary<string, string>(StringComparer.Ordinal);
            var attempts = new Dictionary<string, int>(StringComparer.Ordinal);
            var defective = new List<string>();
            foreach (var (code, artifact) in manifest.Steps.OrderBy(p => p.Key, StringComparer.Ordinal))
            {
                if (artifact.DefectKind != null) { defective.Add(code); continue; }

                var path = Path.Combine(runDir, "steps", code + ".md");
                if (!File.Exists(path)) { defective.Add(code); continue; }

                var text = File.ReadAllText(path);
                if (ComputeSha256(text) != artifact.Sha256) { defective.Add(code); continue; }

                reusable[code] = text;
                attempts[code] = artifact.Attempt;
            }

            // 골격도 섹션도 없으면 재개할 재료가 없다(§11-5).
            if (skeleton == null && reusable.Count == 0) return null;

            return new PlanAttemptResumeCandidate(
                runDir, manifest.Run, manifest.StartedAt,
                skeleton ?? string.Empty, skeletonAttempt,
                reusable, attempts, defective,
                ReadPriorReviews(runDir), manifest.Steps.Count);
        }

        /// <summary>
        /// 시도 번호가 큰 순으로 최근 <c>CriticFeedbackLog.MaxRetainedRounds</c> 개.
        /// 점수로 고르지 않는다 — <c>CriticFeedbackLog.Record</c> 가 시간순으로 쌓고
        /// 넘치면 앞에서 버리는 그 규칙과 같아야 <b>재개 전후로 동작이 같다</b>(설계 §3-3).
        /// </summary>
        private static IReadOnlyList<(int Attempt, ReviewResult Review)> ReadPriorReviews(string runDir)
        {
            var reviewsDir = Path.Combine(runDir, "reviews");
            if (!Directory.Exists(reviewsDir)) return new List<(int, ReviewResult)>();

            var result = new List<(int Attempt, ReviewResult Review)>();
            foreach (var path in Directory.EnumerateFiles(reviewsDir, "attempt-*.json"))
            {
                try
                {
                    var doc = JsonSerializer.Deserialize<PlanAttemptReview>(File.ReadAllText(path), Options);
                    if (doc == null || doc.Attempt <= 0) continue;
                    result.Add((doc.Attempt, new ReviewResult
                    {
                        HasDefects = doc.HasDefects,
                        FeedbackComment = doc.FeedbackComment,
                        DefectiveSteps = doc.DefectiveSteps?.ToList() ?? new List<string>(),
                        SkeletonDefective = doc.SkeletonDefective,
                        StructureDefective = doc.StructureDefective,
                        AxisThresholdForced = doc.AxisThresholdForced,
                        ScoreAccuracy = doc.ScoreAccuracy,
                        ScoreCrud = doc.ScoreCrud,
                        ScoreInterface = doc.ScoreInterface,
                        ScoreException = doc.ScoreException,
                        ScoreReadability = doc.ScoreReadability
                    }));
                }
                catch (Exception ex)
                {
                    Log.Debug(ex, "[PlanAttemptJournal] 리뷰 파일을 건너뜁니다 - {Path}", path);
                }
            }

            return result
                .OrderByDescending(r => r.Attempt)
                .Take(CriticFeedbackLog.MaxRetainedRounds)
                .ToList();
        }
```

**`PlanAttemptReview` 를 `private sealed record` 에서 `internal sealed record` 로 올려야** 역직렬화가 된다(같은 어셈블리 안이므로 `internal` 이면 충분하다). 그 레코드의 생성자 인자 이름이 JSON 키와 같아야 하므로 **건드리지 마라.**

- [ ] **Step 4: 시험이 통과하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~PlanAttemptJournalTests`
Expected: PASS(기존 23 + 이번 11), 실패 0, 건너뜀 0

- [ ] **Step 5: 커밋한다**

```bash
git add src/ReSet.Core/Services/PlanAttemptJournal.cs tests/ReSet.Core.Tests/PlanAttemptJournalTests.cs
git commit -m "feat: 시도 저널이 이어서 할 판을 찾는다"
```

---

### Task 2: `ConfirmResumeAsync` — 사람에게 묻는 통로

**Files:**
- Modify: `src/ReSet.Core/Services/IVerificationUserInteraction.cs`
- Modify: `src/ReSet.Cli/ConsoleUserInteraction.cs`
- Modify: `src/ReSet.Cli/Program.cs` (내부 클래스 `RecursiveAnalysisUserInteraction`)
- Modify: `tests/ReSet.Core.Tests/CliArgsTests.cs` (내부 클래스 `RecordingUserInteraction`)

**Interfaces:**
- Consumes: Task 1 의 `PlanAttemptResumeCandidate`
- Produces: `Task<bool> ConfirmResumeAsync(string jobName, PlanAttemptResumeCandidate candidate)`

> **이 인터페이스의 구현체는 셋이다**(좌표자가 `grep` 으로 셌다): `ConsoleUserInteraction` · `RecursiveAnalysisUserInteraction`(Program.cs) · `RecordingUserInteraction`(CliArgsTests.cs). 메서드를 더하면 **셋 다** 고쳐야 컴파일된다. 기본 구현(default interface method)을 쓰지 마라 — 이 저장소의 다른 메서드가 전부 명시 구현이고, 기본 구현은 「누가 실제로 묻는가」를 흐린다.

- [ ] **Step 1: 인터페이스에 메서드를 더한다**

`IVerificationUserInteraction.cs` 의 `ConfirmMetadataSyncAsync` 옆에:

```csharp
        /// <summary>
        /// 이어서 할 수 있는 판을 찾았을 때 사람에게 묻는다. <c>true</c> 면 재개한다.
        ///
        /// [왜 자동이 아닌가 - 2026-09-11 사람 결정] 조용한 재사용은 이 저장소가 반복해
        /// 덴 모양이다 — 산출물이 왜 그 모양인지 나중에 설명할 수 없다.
        /// </summary>
        Task<bool> ConfirmResumeAsync(string jobName, PlanAttemptResumeCandidate candidate);
```

- [ ] **Step 2: 빌드해서 구현체 셋이 깨지는 것을 확인한다**

Run: `dotnet build`
Expected: FAIL — `ConsoleUserInteraction`·`RecursiveAnalysisUserInteraction`·`RecordingUserInteraction` 셋이 인터페이스를 구현하지 않는다는 CS0535 오류 **셋**

이 실패가 「구현체가 셋이다」의 확인이다. 셋보다 적거나 많으면 멈추고 보고하라.

- [ ] **Step 3: `ConsoleUserInteraction` 에 실제 구현을 쓴다**

```csharp
        public Task<bool> ConfirmResumeAsync(string jobName, PlanAttemptResumeCandidate candidate)
        {
            var reusable = candidate.ReusableSections.Count;
            var total = candidate.TotalStepsInManifest;

            AnsiConsole.Write(new Panel(new Markup(
                $"[bold]{Markup.Escape(jobName)}[/] - 이어서 할 수 있는 판을 찾았습니다.\n\n" +
                $"판: run-{candidate.Run:D3} (시작 {Markup.Escape(candidate.StartedAt)})\n" +
                $"재사용 가능한 단계: [green]{reusable}/{total}[/]\n" +
                $"다시 만들 단계: {(candidate.DefectiveStepCodes.Count == 0 ? "없음" : Markup.Escape(string.Join(", ", candidate.DefectiveStepCodes)))}\n" +
                $"이어받을 리뷰 피드백: {candidate.PriorReviews.Count}회차"))
            {
                Border = BoxBorder.Rounded,
                Header = new PanelHeader(" 재개 후보 ")
            });

            return Task.FromResult(AnsiConsole.Confirm("이어서 하시겠습니까?", defaultValue: true));
        }
```

- [ ] **Step 4: 나머지 구현체 둘을 채운다**

`RecursiveAnalysisUserInteraction`(Program.cs) — 이 경로는 무인 재귀 분석이라 **묻지 않고 거절**한다:

```csharp
            // 무인 경로다 - 물을 사람이 없으므로 재개하지 않는다. 조용히 재사용하면
            // 산출물이 왜 그 모양인지 설명할 수 없다(설계 §2 사람 결정).
            public Task<bool> ConfirmResumeAsync(string jobName, PlanAttemptResumeCandidate candidate)
                => Task.FromResult(false);
```

`RecordingUserInteraction`(CliArgsTests.cs) — 시험용이라 기록만 한다:

```csharp
            public PlanAttemptResumeCandidate? ResumeAsked { get; private set; }
            public bool ResumeAnswer { get; set; }

            public Task<bool> ConfirmResumeAsync(string jobName, PlanAttemptResumeCandidate candidate)
            {
                ResumeAsked = candidate;
                return Task.FromResult(ResumeAnswer);
            }
```

- [ ] **Step 5: 빌드가 통과하는지 확인한다**

Run: `dotnet clean && dotnet build 2>&1 | grep -cE "warning CS"`
Expected: `0`

- [ ] **Step 6: 커밋한다**

```bash
git add src/ReSet.Core/Services/IVerificationUserInteraction.cs src/ReSet.Cli/ConsoleUserInteraction.cs src/ReSet.Cli/Program.cs tests/ReSet.Core.Tests/CliArgsTests.cs
git commit -m "feat: 재개 여부를 사람에게 묻는 통로를 만든다"
```

---

### Task 3: 오케스트레이터 재개 배선

**Files:**
- Modify: `src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs`
- Test: `tests/ReSet.Core.Tests/VerificationPipelineOrchestratorTests.cs`

**Interfaces:**
- Consumes: `journal.TryResume(planStructure)` · `_userInteraction.ConfirmResumeAsync(jobName, candidate)`
- Produces: 없음(마지막 재개 조각)

> **이 태스크는 3단계를 넘는다 — 중간 커밋을 하라.** (a) 후보 찾기·묻기·복원 (b) 새 판에 재기록, 둘로 나눠 커밋해도 된다.

- [ ] **Step 1: 실패하는 시험을 쓴다**

`VerificationPipelineOrchestratorTests` 에 덧붙인다. 기존 `RunConsolidatedPipeline_WhenSplitRuns_ReportsStepCoverage` 의 셋업을 본떴다.

```csharp
        /// <summary>
        /// 재개가 실제로 비용을 줄였다는 **유일한 직접 증거**는 AI 호출 수다(설계 §5-2).
        /// 재사용한 단계는 GenerateBatchStepSectionAsync 가 불리지 않아야 한다.
        /// </summary>
        [Fact]
        public async Task RunConsolidatedPipeline_WhenResumeIsApproved_OnlyDefectiveStepsAreRegenerated()
        {
            var stepsJson = "```json\n{\n  \"Steps\": [\n" +
                "    { \"Code\": \"S01\", \"Name\": \"첫\", \"LegacyProcedures\": [\"USP_Spec1\"], \"TargetTables\": [\"dbo.T1\"], \"ErrorCodes\": [\"-1\"] },\n" +
                "    { \"Code\": \"S02\", \"Name\": \"둘\", \"LegacyProcedures\": [\"USP_Spec1\"], \"TargetTables\": [\"dbo.T2\"], \"ErrorCodes\": [\"-2\"] }\n" +
                "  ]\n}\n```";
            var planStructure = "## 목차\n" + stepsJson;

            // 1) 이어서 할 판을 미리 써 둔다 — S01 은 건강, S02 는 하한 미달.
            var seed = PlanAttemptJournal.Create(
                _consolidatedOutputRoot, "Job_Test", "OpenAI", "gpt-4", "high", "C#",
                PlanAttemptJournal.ComputeSha256("dbo.USP_Spec1\ncontent1"));
            seed.OpenRun(planStructure, "run-start");
            seed.RecordSkeleton(1, SkeletonMarkdown);
            seed.RecordStepSection(1, "S01", HealthyStepSection("S01", "dbo.T1", "-1"));
            seed.RecordStepSection(1, "S02", "## S02\n(하한 미달)",
                new StepDefect(StepDefectKind.QualityFloor, "S02 (하한 미달)"));

            var aiService = Substitute.For<IAiService>();
            aiService.ModelName.Returns("gpt-4");
            aiService.BrainstormBatchPlanAsync(Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = "Brainstorm" });
            aiService.DraftBatchPlanStructureAsync(Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<IReadOnlyList<string>>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = planStructure });
            aiService.GenerateBatchPlanSkeletonAsync(Arg.Any<IReadOnlyList<BatchStepPlan>>(), Arg.Any<string>(), Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string?>(), Arg.Any<IReadOnlyList<StepInterface>>(), Arg.Any<SkeletonRevision?>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = SkeletonMarkdown });

            var regenerated = new List<string>();
            aiService.GenerateBatchStepSectionAsync(Arg.Any<BatchStepPlan>(), Arg.Any<IReadOnlyList<BatchStepPlan>>(), Arg.Any<string>(), Arg.Any<List<(string, string)>>(), Arg.Any<IReadOnlyList<StepInterface>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<IReadOnlyDictionary<string, IReadOnlyList<string>>>(), Arg.Any<CancellationToken>())
                .Returns(call =>
                {
                    var step = call.Arg<BatchStepPlan>();
                    regenerated.Add(step.Code);
                    return new AiResult { Content = HealthyStepSection(step.Code, step.TargetTables[0], step.ErrorCodes[0]) };
                });
            aiService.ReviewConsolidatedPlanAsync(Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<CancellationToken>())
                .Returns(new ReviewResult { HasDefects = false, ScoreAccuracy = 10, ScoreCrud = 10, ScoreInterface = 10, ScoreException = 10, ScoreReadability = 10 });

            var interaction = Substitute.For<IVerificationUserInteraction>();
            interaction.ConfirmResumeAsync("Job_Test", Arg.Any<PlanAttemptResumeCandidate>())
                .Returns(Task.FromResult(true));

            var orchestrator = new VerificationPipelineOrchestrator(
                Substitute.For<IDbMetadataService>(), aiService, new MechanicalValidator(),
                interaction, "2", "gpt-4", null, aiService, aiService, "high", "high", "default", 8);

            await orchestrator.RunConsolidatedPipelineAsync(
                new List<(string, string)> { ("dbo.USP_Spec1", "content1") },
                "C#", "Job_Test", "OpenAI", _consolidatedOutputRoot, isBatchMode: true);

            // S01 은 재사용됐으므로 AI 가 안 불린다. S02 만 다시 만든다.
            Assert.Equal(new[] { "S02" }, regenerated.Distinct().ToArray());

            // 새 판이 열렸고 재사용한 것이 거기 다시 기록됐다(설계 §3-5).
            var runs = Directory.GetDirectories(
                Path.Combine(_consolidatedOutputRoot, "Jobs", "Job_Test", "raw", "attempts"), "run-*");
            Assert.Equal(2, runs.Length);
            var newest = runs.OrderBy(d => d, StringComparer.Ordinal).Last();
            var manifest = JsonDocument.Parse(File.ReadAllText(Path.Combine(newest, "manifest.json"))).RootElement;
            Assert.Equal("run-001", manifest.GetProperty("ResumedFrom").GetString());
            Assert.Equal(1, manifest.GetProperty("Steps").GetProperty("S01").GetProperty("Attempt").GetInt32());
        }

        [Fact]
        public async Task RunConsolidatedPipeline_WhenResumeIsDeclined_EverythingIsRegenerated()
        {
            var stepsJson = "```json\n{\n  \"Steps\": [\n" +
                "    { \"Code\": \"S01\", \"Name\": \"첫\", \"LegacyProcedures\": [\"USP_Spec1\"], \"TargetTables\": [\"dbo.T1\"], \"ErrorCodes\": [\"-1\"] }\n" +
                "  ]\n}\n```";
            var planStructure = "## 목차\n" + stepsJson;

            var seed = PlanAttemptJournal.Create(
                _consolidatedOutputRoot, "Job_Test", "OpenAI", "gpt-4", "high", "C#",
                PlanAttemptJournal.ComputeSha256("dbo.USP_Spec1\ncontent1"));
            seed.OpenRun(planStructure, "run-start");
            seed.RecordSkeleton(1, SkeletonMarkdown);
            seed.RecordStepSection(1, "S01", HealthyStepSection("S01", "dbo.T1", "-1"));

            var aiService = Substitute.For<IAiService>();
            aiService.ModelName.Returns("gpt-4");
            aiService.BrainstormBatchPlanAsync(Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = "Brainstorm" });
            aiService.DraftBatchPlanStructureAsync(Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<IReadOnlyList<string>>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = planStructure });
            aiService.GenerateBatchPlanSkeletonAsync(Arg.Any<IReadOnlyList<BatchStepPlan>>(), Arg.Any<string>(), Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string?>(), Arg.Any<IReadOnlyList<StepInterface>>(), Arg.Any<SkeletonRevision?>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = SkeletonMarkdown });

            var regenerated = new List<string>();
            aiService.GenerateBatchStepSectionAsync(Arg.Any<BatchStepPlan>(), Arg.Any<IReadOnlyList<BatchStepPlan>>(), Arg.Any<string>(), Arg.Any<List<(string, string)>>(), Arg.Any<IReadOnlyList<StepInterface>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<IReadOnlyDictionary<string, IReadOnlyList<string>>>(), Arg.Any<CancellationToken>())
                .Returns(call =>
                {
                    var step = call.Arg<BatchStepPlan>();
                    regenerated.Add(step.Code);
                    return new AiResult { Content = HealthyStepSection(step.Code, step.TargetTables[0], step.ErrorCodes[0]) };
                });
            aiService.ReviewConsolidatedPlanAsync(Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<CancellationToken>())
                .Returns(new ReviewResult { HasDefects = false, ScoreAccuracy = 10, ScoreCrud = 10, ScoreInterface = 10, ScoreException = 10, ScoreReadability = 10 });

            var interaction = Substitute.For<IVerificationUserInteraction>();
            interaction.ConfirmResumeAsync("Job_Test", Arg.Any<PlanAttemptResumeCandidate>())
                .Returns(Task.FromResult(false));   // 거절

            var orchestrator = new VerificationPipelineOrchestrator(
                Substitute.For<IDbMetadataService>(), aiService, new MechanicalValidator(),
                interaction, "2", "gpt-4", null, aiService, aiService, "high", "high", "default", 8);

            await orchestrator.RunConsolidatedPipelineAsync(
                new List<(string, string)> { ("dbo.USP_Spec1", "content1") },
                "C#", "Job_Test", "OpenAI", _consolidatedOutputRoot, isBatchMode: true);

            Assert.Contains("S01", regenerated);
        }
```

**시험 둘을 더 쓰십시오** (2026-09-11 추가 — Task 2 재리뷰가 연 NEW ISSUE):

```csharp
        /// <summary>
        /// 무인 배치는 묻지 않고 재개한다(설계 §3-4-b). 게이트가 없으면 AnsiConsole.Confirm 이
        /// 비대화형에서 예외를 던져 **재개 후보가 있는 모든 무인 배치가 하드 실패로 죽는다** —
        /// Task 2 재리뷰어가 Spectre 0.55.2 로 실측했다.
        /// </summary>
        [Fact]
        public async Task RunConsolidatedPipeline_InBatchMode_ResumesWithoutAsking()
        {
            // 위 …WhenResumeIsApproved… 와 같은 셋업을 쓰되 interaction 은 묻지 않아야 한다.
            // isBatchMode: true 로 호출하고 아래를 단언한다:
            //   await interaction.DidNotReceive().ConfirmResumeAsync(Arg.Any<string>(), Arg.Any<PlanAttemptResumeCandidate>());
            //   재사용이 실제로 일어났는지(regenerated 에 결함 단계만)
        }

        /// <summary>
        /// 위임을 지키는 시험. Task 2 워커가 RED/GREEN 으로 확인했으나 그때는
        /// PlanAttemptResumeCandidate 가 아직 통합 전이라 임시 시험을 지웠다. 이제 실물이
        /// 있으므로 영구화한다 — 「지워도 통과하는 분기」는 이 저장소의 반복 결함이다.
        /// </summary>
        [Fact]
        public async Task RecursiveAnalysisUserInteraction_ForwardsConfirmResumeAsync()
        {
            // tests/ReSet.Core.Tests/CliArgsTests.cs 에 두는 편이 맞다(RecordingUserInteraction 이 거기 있다).
            // 리플렉션으로 internal nested Program.RecursiveAnalysisUserInteraction 을 만들고
            // RecordingUserInteraction(ResumeAnswer = true)을 감싸, ConfirmResumeAsync 가
            // 감싼 UI 까지 도달하고 답과 candidate 가 그대로 돌아오는지 단언한다.
        }
```

**위 둘은 뼈대다 — 본문을 채워라.** 첫째는 이 파일의 `…WhenResumeIsApproved…` 셋업을 재사용하고, 둘째는 `CliArgsTests.cs` 로 가는 것이 맞다(그 파일에 `RecordingUserInteraction` 이 있다). **둘 다 되돌림으로 확인하라.**

- [ ] **Step 2: 시험이 실패하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~WhenResumeIsApproved`
Expected: FAIL — `regenerated` 에 `S01` 이 들어 있다(재개가 아직 없어 전량 재생성)

- [ ] **Step 3: manifest 에 `ResumedFrom` 을 더한다**

`PlanAttemptManifest` 에 속성 하나:

```csharp
        /// <summary>이 판이 어느 판에서 재개했나. 재개가 아니면 null(설계 §3-5).</summary>
        public string? ResumedFrom { get; set; }
```

`OpenRun` 에 선택 인자를 더한다(기존 호출부는 그대로 컴파일된다):

```csharp
        public void OpenRun(string planStructure, string openedBy, string? resumedFrom = null)
```
그리고 `_manifest` 를 만들 때 `ResumedFrom = resumedFrom` 를 채운다.

- [ ] **Step 4: 오케스트레이터에 후보 찾기·묻기·복원을 배선한다**

> **★ 계획 정정 (2026-09-11, Wave 1 중 좌표자 발견) — 이 Step 이 첫 판에서 틀렸다.**
>
> 첫 판은 주입 자리를 「목차를 새로 만들어 `PlanStructure.md` 를 쓴 직후」로 잡았다.
> **그러면 재개가 영영 발동하지 않는다.** `currentPlanStructure` 는 `string.Empty` 로
> 시작하고 `raw/PlanStructure.md` 는 **쓰기만 하고 읽는 코드가 없어서**, 재실행하면 목차를
> 매번 새로 만든다 → `PlanStructureSha256` 이 매번 달라진다 → `TryResume` 이 후보를 못 찾는다.
>
> **고친 흐름**: 브레인스토밍 **전에** `raw/PlanStructure.md` 를 읽고, 그 목차로 후보를 찾고,
> 승인되면 **그 목차를 그대로 쓴다**(브레인스토밍과 목차 생성 AI 호출 둘을 아낀다).
> 목차의 진위는 manifest 의 `PlanStructureSha256` 대조로 보장된다 — 해시가 안 맞으면
> `TryResume` 이 후보를 안 낸다.

`rawDir` 변수가 지금은 브레인스토밍 블록 **안**에서 만들어진다. 그 선언을 블록 **밖**(`if (string.IsNullOrEmpty(currentPlanStructure))` 바로 안쪽 첫 줄)으로 옮겨라 — 재개 판정이 그것을 먼저 쓴다.

그 블록을 아래 모양으로 바꾼다:

```csharp
                        if (string.IsNullOrEmpty(currentPlanStructure))
                        {
                            var rawDir = System.IO.Path.Combine(outputRoot, "Jobs", jobName, "raw");
                            if (!System.IO.Directory.Exists(rawDir)) System.IO.Directory.CreateDirectory(rawDir);

                            // [재개] 브레인스토밍 앞에서 먼저 묻는다. 목차를 새로 만든 뒤에
                            // 물으면 PlanStructureSha256 이 이전 판과 절대 안 맞아 후보가
                            // 영영 안 잡힌다 - 이 순서가 재개의 전제다(설계 §3-1 정정).
                            var priorStructurePath = System.IO.Path.Combine(rawDir, "PlanStructure.md");
                            PlanAttemptResumeCandidate? resumeCandidate = null;
                            string? resumedFrom = null;

                            if (System.IO.File.Exists(priorStructurePath))
                            {
                                var priorStructure = await System.IO.File.ReadAllTextAsync(priorStructurePath, cancellationToken);
                                var found = attemptJournal.TryResume(priorStructure);
                                // 무인 배치는 묻지 않고 재개한다(설계 §3-4-b). AnsiConsole.Confirm 이
                                // 비대화형에서 예외를 던지므로 게이트 없이 부르면 재개 후보가 있는
                                // 모든 무인 배치가 하드 실패로 죽는다. 이 저장소가 이미
                                // RequestHumanReviewAsync·ConfirmMetadataSyncAsync 를 같은 축으로 가른다.
                                if (found != null &&
                                    (isBatchMode || await _userInteraction.ConfirmResumeAsync(jobName, found)))
                                {
                                    resumeCandidate = found;
                                    resumedFrom = System.IO.Path.GetFileName(found.RunDirectory);

                                    // 목차를 그대로 쓴다 - 새로 만들면 그 순간 재사용 키가 깨진다.
                                    currentPlanStructure = priorStructure;

                                    lastSkeleton = string.IsNullOrEmpty(found.Skeleton) ? null : found.Skeleton;
                                    lastStepSections = new Dictionary<string, string>(found.ReusableSections);
                                    pendingDefectiveSteps.Clear();
                                    pendingDefectiveSteps.AddRange(found.DefectiveStepCodes);

                                    // 피드백을 이어받지 않으면 첫 회차가 백지에서 시작해 이미
                                    // 정리된 결함이 되살아난다(설계 §3-3).
                                    foreach (var (priorAttempt, priorReview) in found.PriorReviews.Reverse())
                                    {
                                        CriticFeedbackLog.Record(feedbackHistory, priorAttempt, priorReview, _criticScoreThreshold);
                                    }
                                    if (feedbackHistory.Count > 0)
                                    {
                                        feedbackLog = CriticFeedbackLog.Compose(
                                            feedbackHistory,
                                            "이어서 하는 회차입니다. 위 누적 피드백에서 이미 반영한 내용 교정의 " +
                                            "서술 수준을 낮추지 마십시오. 원본 DDL을 절대적 기준으로 삼으십시오.");
                                    }

                                    // 무인 모드면 이 줄이 「왜 이 산출물이 이 모양인가」에 답하는
                                    // 유일한 화면 기록이다(설계 §3-4-b). manifest 의 ResumedFrom 과 짝이다.
                                    _userInteraction.NotifyStatus(
                                        $"[yellow]{jobName}[/] - run-{found.Run:D3} 에서 이어서 합니다 " +
                                        $"(재사용 {found.ReusableSections.Count}/{found.TotalStepsInManifest}, " +
                                        $"브레인스토밍·목차 생성을 건너뜁니다)" +
                                        (isBatchMode ? " — 무인 모드라 묻지 않았습니다." : "."));
                                }
                            }

                            // 재개가 아니면 종전대로 목차를 만든다.
                            if (string.IsNullOrEmpty(currentPlanStructure))
                            {
                                progressScope.AddTask("phase1", "1/3. 브레인스토밍 중...");
                                var brainstormResult = await WrapWithProgress(_consolidatorService.BrainstormBatchPlanAsync(specsCopy, targetLanguage, jobName, _consolidatorEffort, cancellationToken), progressScope, "phase1");
                                await System.IO.File.WriteAllTextAsync(System.IO.Path.Combine(rawDir, "Brainstorming.md"), brainstormResult.Content);
                                currentBrainstorming = brainstormResult.Content;

                                progressScope.AddTask("phase2", "2/3. 목차 설계 중...");
                                var planResult = await WrapWithProgress(_consolidatorService.DraftBatchPlanStructureAsync(brainstormResult.Content, targetLanguage, jobName, sourceProcedureRoster, _consolidatorEffort, cancellationToken: cancellationToken), progressScope, "phase2");
                                var planEnrichment = PlanStructureEnricher.Enrich(planResult.Content, specReturnCodes, specTargetTables);
                                currentPlanStructure = planEnrichment.Markdown;
                                NotifyDroppedTableDeclarations(jobName, planEnrichment);
                                await System.IO.File.WriteAllTextAsync(priorStructurePath, currentPlanStructure);
                            }

                            attemptJournal.OpenRun(currentPlanStructure, "run-start", resumedFrom);
```

**기존 블록의 나머지(브레인스토밍·목차 생성 코드)는 위 `if` 안으로 옮겨진 것이지 새로 쓰는 것이 아니다.** 실물을 열어 그대로 옮겨라 — 위 스니펫의 그 부분은 지금 코드를 옮겨 적은 것이고, 실물과 다르면 **실물이 옳다.**

- [ ] **Step 5: 재사용한 것을 새 판에 다시 기록한다**

바로 위 `OpenRun` **직후**에:

```csharp
                            // 새 판이 자기만으로 완결되게 한다(설계 §3-5). 원래 Attempt 를
                            // 그대로 옮긴다 - 구제 재기록이 전량을 한 Attempt 로 적어
                            // 과대 표기를 낸 것과 같은 자리를 반복하지 않는다.
                            if (resumeCandidate != null && resumedFrom != null)
                            {
                                if (lastSkeleton != null)
                                {
                                    attemptJournal.RecordSkeleton(resumeCandidate.SkeletonAttempt, lastSkeleton);
                                }
                                foreach (var (code, markdown) in resumeCandidate.ReusableSections)
                                {
                                    attemptJournal.RecordStepSection(
                                        resumeCandidate.SectionAttempts[code], code, markdown);
                                }
                            }
```

- [ ] **Step 6: 시험이 통과하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~VerificationPipelineOrchestratorTests`
Expected: PASS, 실패 0 · 건너뜀 0

- [ ] **Step 7: 되돌림으로 확인한다**

Step 4 의 `lastStepSections = new Dictionary<...>(resumeCandidate.ReusableSections);` 한 줄을 주석 처리하고 그 필터를 다시 돌린다.
Expected: `..._WhenResumeIsApproved_OnlyDefectiveStepsAreRegenerated` **하나만** 실패(`regenerated` 에 `S01` 이 들어온다). 복구 후 재확인.

**실제 출력을 보고에 적어라.** 그 시험만 빨개지는지도 확인하라.

- [ ] **Step 8: 커밋한다**

```bash
git add src/ReSet.Core/Services/PlanAttemptJournal.cs src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs tests/ReSet.Core.Tests/VerificationPipelineOrchestratorTests.cs
git commit -m "feat: 배치 계획 생성이 이전 판에서 이어서 한다"
```

---

### Task 4: `Exhausted` 판정

**Files:**
- Modify: `src/ReSet.Core/Services/AiRetryPolicy.cs`
- Modify: `src/ReSet.Core/Services/AiCallRetry.cs`
- Test: `tests/ReSet.Core.Tests/` 의 기존 재시도 시험 파일(`AiRetryPolicyTests` 또는 `AiCallRetryTests` — 없으면 `AiRetryPolicyTests.cs` 를 새로 만든다)

**Interfaces:**
- Consumes: 기존 `CliInvocationException` · `CliFailureKind`
- Produces: `AiRetryVerdict.Exhausted` · `AiCallFailedException.Verdict`

- [ ] **Step 1: 실패하는 시험을 쓴다**

```csharp
        // CLI 프로바이더는 분류가 이미 있다 - 그대로 Exhausted 로 간다(설계 §4-1).
        [Fact]
        public void Classify_CliQuotaExhausted_IsExhaustedNotFatal()
        {
            var ex = new CliInvocationException("한도 소진", CliFailureKind.QuotaExhausted);

            Assert.Equal(AiRetryVerdict.Exhausted, AiRetryPolicy.Classify(ex, CancellationToken.None));
        }

        // 429 는 「초당 한도에 잠깐 막힘」일 수 있다. 첫 429 에 포기하면 넘길 수 있는
        // 막힘에 포기하는 것이다 - 재시도를 다 쓴 뒤에야 Exhausted 다(설계 §4-1).
        [Fact]
        public void Classify_Http429_IsTransientSoTheRetryStillRuns()
        {
            var ex = new HttpRequestException("too many requests", null, HttpStatusCode.TooManyRequests);

            Assert.Equal(AiRetryVerdict.Transient, AiRetryPolicy.Classify(ex, CancellationToken.None));
        }

        [Fact]
        public async Task ExecuteAsync_WhenQuotaIsExhausted_StopsImmediatelyAndCarriesTheVerdict()
        {
            var calls = 0;
            var thrown = await Assert.ThrowsAsync<AiCallFailedException>(() =>
                AiCallRetry.ExecuteAsync<string>(() =>
                {
                    calls++;
                    throw new CliInvocationException("한도 소진", CliFailureKind.QuotaExhausted);
                }, CancellationToken.None, RetryPlan.NoDelay));

            Assert.Equal(1, calls);                                   // 재시도하지 않는다
            Assert.Equal(AiRetryVerdict.Exhausted, thrown.Verdict);   // 사유가 위로 간다
        }

        // 429 가 재시도를 다 쓰면 그때는 짧은 막힘이 아니다(설계 §4-1).
        [Fact]
        public async Task ExecuteAsync_WhenHttp429ExhaustsRetries_EndsAsExhausted()
        {
            var thrown = await Assert.ThrowsAsync<AiCallFailedException>(() =>
                AiCallRetry.ExecuteAsync<string>(
                    () => throw new HttpRequestException("429", null, HttpStatusCode.TooManyRequests),
                    CancellationToken.None, RetryPlan.NoDelay));

            Assert.Equal(AiRetryVerdict.Exhausted, thrown.Verdict);
        }
```

`CliInvocationException` 의 실제 생성자 시그니처를 소스에서 확인하고 맞춰라 — 위는 `(message, kind)` 를 가정한 것이다. 다르면 **실물에 맞추고 보고하라.**

- [ ] **Step 2: 시험이 실패하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~AiRetryPolicy`
Expected: 빌드 실패 — `AiRetryVerdict.Exhausted` 와 `AiCallFailedException.Verdict` 가 없다

- [ ] **Step 3: 판정을 더한다**

`AiRetryPolicy.cs` 의 열거형에:

```csharp
        /// <summary>
        /// 지금은 안 되고 나중엔 된다. <c>Fatal</c>(다시 해도 같다)도 <c>Transient</c>
        /// (금방 풀린다)도 아닌 것이 없어서 종전에는 <c>Fatal</c> 로 뭉개졌다 —
        /// 쿼터 소진이 재시도 없이 「실패」로 보고됐다(설계 §1-3).
        /// </summary>
        Exhausted,
```

`Classify` 의 CLI 갈래를 고친다:

```csharp
            if (ex is Clients.Cli.CliInvocationException cliEx)
            {
                return cliEx.Kind switch
                {
                    Clients.Cli.CliFailureKind.Timeout => AiRetryVerdict.Transient,
                    Clients.Cli.CliFailureKind.QuotaExhausted => AiRetryVerdict.Exhausted,
                    _ => AiRetryVerdict.Fatal
                };
            }
```

**HTTP 429 는 `Transient` 그대로 둔다.** 승격은 재시도를 다 쓴 뒤에 `AiCallRetry` 가 한다.

- [ ] **Step 4: `AiCallRetry` 가 사유를 싣는다**

`AiCallFailedException` 에 속성을 더한다:

```csharp
        /// <summary>왜 포기했나. 호출부가 쿼터 소진을 실패와 가르는 근거다(설계 §4-2).</summary>
        public AiRetryVerdict Verdict { get; }
```
생성자에 인자를 더하고(기존 호출부를 함께 고친다) 루프를 고친다:

```csharp
                    // 지금은 안 된다 - 재시도해도 같다. 다만 사유를 실어 올려보낸다.
                    if (verdict == AiRetryVerdict.Exhausted)
                    {
                        throw new AiCallFailedException(
                            "AI 호출이 쿼터 소진으로 중단됐습니다.", ex, attempt, AiRetryVerdict.Exhausted);
                    }
```
그리고 재시도를 다 쓴 뒤 만드는 `AiCallFailedException` 에, 마지막 실패가 429였으면 `Exhausted` 를 싣는다:

```csharp
            var finalVerdict = AiRetryPolicy.Classify(lastFailure!, cancellationToken) == AiRetryVerdict.Transient
                && lastFailure is HttpRequestException { StatusCode: HttpStatusCode.TooManyRequests }
                ? AiRetryVerdict.Exhausted
                : AiRetryVerdict.Transient;
```

- [ ] **Step 5: 시험이 통과하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~AiRetryPolicy`
Expected: PASS, 실패 0 · 건너뜀 0

- [ ] **Step 6: 전체 시험으로 회귀를 확인한다**

`AiRetryVerdict` 는 `AiCallRetry` 한 곳에서만 쓰이지만 `AiCallFailedException` 생성자를 바꿨다.

Run: `dotnet test`
Expected: 실패 1(베이스라인 그 하나) · 건너뜀 0

- [ ] **Step 7: 커밋한다**

```bash
git add src/ReSet.Core/Services/AiRetryPolicy.cs src/ReSet.Core/Services/AiCallRetry.cs tests/ReSet.Core.Tests/
git commit -m "fix: 쿼터 소진을 Fatal 에서 갈라낸다"
```

---

### Task 5: `AbortReason` 전달과 CLI 안내

**Files:**
- Modify: `src/ReSet.Core/Models/ConsolidatedPipelineResult.cs`
- Modify: `src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs`
- Modify: `src/ReSet.Cli/Program.cs`
- Test: `tests/ReSet.Core.Tests/VerificationPipelineOrchestratorTests.cs`

**Interfaces:**
- Consumes: Task 4 의 `AiCallFailedException.Verdict`
- Produces: `ConsolidatedPipelineResult.AbortReason`

- [ ] **Step 1: 실패하는 시험을 쓴다**

```csharp
        /// <summary>
        /// 쿼터 소진은 실패가 아니다 — 사유가 결과에 실려 CLI 까지 가야 「이어서 하십시오」를
        /// 말할 수 있다(설계 §4-3).
        /// </summary>
        [Fact]
        public async Task RunConsolidatedPipeline_WhenQuotaIsExhausted_ResultCarriesTheAbortReason()
        {
            var specs = new List<(string, string)> { ("dbo.USP_Test1", "내용") };

            _aiService.BrainstormBatchPlanAsync(Arg.Any<List<(string, string)>>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = "Brainstorm" });
            _aiService.DraftBatchPlanStructureAsync(Arg.Any<string>(), Arg.Any<string>(), Arg.Any<string>(), Arg.Any<IReadOnlyList<string>>(), Arg.Any<string>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>())
                .Returns(new AiResult { Content = "Plan Structure" });
            _aiService.GenerateConsolidatedBatchPlanAsync(Arg.Any<string>(), Arg.Any<List<(string, string)>>(), "C#", "Job_Test", Arg.Any<string>(), Arg.Any<IReadOnlyList<StepInterface>>(), Arg.Any<string?>(), Arg.Any<CancellationToken>())
                .Returns<AiResult>(_ => throw new AiCallFailedException(
                    "쿼터 소진", new Exception("한도"), 1, AiRetryVerdict.Exhausted));

            var result = await _orchestrator.RunConsolidatedPipelineAsync(
                specs, "C#", "Job_Test", "OpenAI", _consolidatedOutputRoot);

            Assert.Null(result.Plan);
            Assert.Equal(PipelineAbortReason.QuotaExhausted, result.AbortReason);
        }
```

- [ ] **Step 2: 시험이 실패하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~WhenQuotaIsExhausted_ResultCarriesTheAbortReason`
Expected: 빌드 실패 — `PipelineAbortReason` 과 `AbortReason` 이 없다

- [ ] **Step 3: 사유 타입과 필드를 더한다**

`ConsolidatedPipelineResult.cs` 에:

```csharp
/// <summary>
/// 파이프라인이 왜 멈췄나. <c>VerificationOutcome</c> 에 값을 넣지 않는다 — 그 열거형은
/// 「문서의 검증 상태」를 뜻하고 배너·헤더가 읽는다. 중단 사유는 다른 축이라 섞으면
/// 두 뜻이 한 필드에 산다(설계 §4-3).
/// </summary>
public enum PipelineAbortReason
{
    QuotaExhausted
}
```
그리고 레코드에 선택 인자로 더한다(기존 생성 자리 넷이 그대로 컴파일된다):

```csharp
    VerificationCoverage? Coverage = null,
    PipelineAbortReason? AbortReason = null);
```

- [ ] **Step 4: 오케스트레이터가 사유를 잡아 싣는다**

`RunConsolidatedPipelineAsync` 의 생성 실패 `catch` 를 고친다:

```csharp
                catch (Exception ex) when (ex is not OperationCanceledException)
                {
                    _userInteraction.NotifyError($"{jobName} - AI 통합 계획 생성 실패 (시도 {attempt}): {ex.Message}");
                    if (ex is AiCallFailedException { Verdict: AiRetryVerdict.Exhausted })
                    {
                        abortReason = PipelineAbortReason.QuotaExhausted;
                    }
                }
```
`abortReason` 지역 변수를 루프 **밖**에 선언하고(`PipelineAbortReason? abortReason = null;`), 반환 자리 **넷 전부**에 `AbortReason: abortReason` 를 넘긴다. `grep -n "new ConsolidatedPipelineResult" src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs` 로 자리를 세어 확인하라 — 좌표자가 셌을 때 **넷**이었다.

- [ ] **Step 5: CLI 가 갈라서 말한다**

`Program.cs` 의 두 자리(`grep -n "통합 배치 설계서 작성이 중단되었거나 실패" src/ReSet.Cli/Program.cs` 로 찾아라 — **둘**이다) 를 고친다:

```csharp
                            if (string.IsNullOrEmpty(consolidatedPlan))
                            {
                                if (pipelineResult.AbortReason == PipelineAbortReason.QuotaExhausted)
                                {
                                    var saved = PlanAttemptJournal.DescribeLatestRun(outputDir, jobName);
                                    AnsiConsole.MarkupLine("[yellow]쿼터 소진으로 멈췄습니다 — 실패가 아닙니다.[/]");
                                    if (saved != null) AnsiConsole.MarkupLine($"  여기까지 저장됐습니다: {Markup.Escape(saved)}");
                                    AnsiConsole.MarkupLine(
                                        $"  이어서 하려면 쿼터가 풀린 뒤 같은 Job 이름([bold]{Markup.Escape(jobName)}[/])으로 다시 실행하십시오.");
                                }
                                else
                                {
                                    AnsiConsole.MarkupLine("[red]통합 배치 설계서 작성이 중단되었거나 실패했습니다.[/]");
                                }
                                continue;   // 358행 자리는 `return false;` 다 - 그 자리의 기존 흐름을 유지하라
                            }
```

`PlanAttemptJournal` 에 그 한 줄을 만드는 정적 메서드를 더한다:

```csharp
        /// <summary>
        /// 최신 판에 무엇이 저장됐는지 한 줄로. <b>manifest 에서 읽는다</b> — 짐작이 아니라
        /// 실제 기록이다(설계 §4-4). 못 읽으면 null.
        /// </summary>
        public static string? DescribeLatestRun(string outputRoot, string jobName)
        {
            try
            {
                var attemptsRoot = Path.Combine(outputRoot, "Jobs", jobName, "raw", "attempts");
                if (!Directory.Exists(attemptsRoot)) return null;

                var newest = Directory.EnumerateDirectories(attemptsRoot, "run-*")
                    .OrderByDescending(d => d, StringComparer.Ordinal).FirstOrDefault();
                if (newest == null) return null;

                var manifestPath = Path.Combine(newest, "manifest.json");
                if (!File.Exists(manifestPath)) return null;

                var manifest = JsonSerializer.Deserialize<PlanAttemptManifest>(
                    File.ReadAllText(manifestPath), Options);
                if (manifest == null) return null;

                var healthy = manifest.Steps.Count(p => p.Value.DefectKind == null);
                var reviews = Directory.Exists(Path.Combine(newest, "reviews"))
                    ? Directory.EnumerateFiles(Path.Combine(newest, "reviews"), "attempt-*.json").Count()
                    : 0;
                var skeleton = manifest.Skeleton != null ? " · 골격" : "";
                return $"run-{manifest.Run:D3} — 단계 {healthy}/{manifest.Steps.Count}{skeleton} · 회차 리뷰 {reviews}개";
            }
            catch (Exception ex)
            {
                Log.Debug(ex, "[PlanAttemptJournal] 최신 판을 요약하지 못했습니다.");
                return null;
            }
        }
```

- [ ] **Step 6: 시험이 통과하는지 확인한다**

Run: `dotnet test --filter FullyQualifiedName~VerificationPipelineOrchestratorTests`
Expected: PASS, 실패 0 · 건너뜀 0

- [ ] **Step 7: 전체 게이트**

Run: `dotnet clean && dotnet build 2>&1 | grep -cE "warning CS"` → `0`
Run: `dotnet test` → 실패 1(베이스라인) · 건너뜀 0

- [ ] **Step 8: 커밋한다**

```bash
git add src/ReSet.Core/Models/ConsolidatedPipelineResult.cs src/ReSet.Core/Services/PlanAttemptJournal.cs src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs src/ReSet.Cli/Program.cs tests/ReSet.Core.Tests/VerificationPipelineOrchestratorTests.cs
git commit -m "feat: 쿼터 소진을 실패와 갈라 이어서 하는 길을 안내한다"
```

---

### Task 6: 문서와 실물 검증

**Files:**
- Modify: `docs/output-artifacts.md`
- Modify: `docs/architecture/4.4-verification-pipeline.md`
- Create: `docs/audit-reports/2026-09-11-재개-실물-확인.md`

**Interfaces:**
- Consumes: Task 1~5 전부
- Produces: 없음

- [ ] **Step 1: 「재개가 없다」는 서술을 고친다**

`docs/output-artifacts.md` 에 이 문단이 있다:

```
`raw/attempts/`는 1단계(쓰기 전용)의 산출물이다 — 지금은 재개(읽기)가 없으므로 중단된
실행을 이어서 하는 데는 아직 못 쓴다.
```

**이 서술이 이제 거짓이다.** 재개 절차로 바꿔 쓰라: 언제 후보가 되는지(`ReuseKey` 일곱 항목 일치), 무엇이 재사용되는지(`DefectKind` 가 없는 섹션), 무엇을 다시 만드는지, 사람이 승인해야 한다는 것, `ResumedFrom` 이 무엇을 뜻하는지.

`docs/architecture/4.4-verification-pipeline.md` 의 「지금은 **쓰기만 한다.**」 문단도 같이 고친다.

- [ ] **Step 2: 호출 자리를 세어 문서와 대조한다**

Run:
```bash
grep -cE '\.(RecordSkeleton|RecordStepSection|RecordReview)\(' src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs
grep -c '\.OpenRun(' src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs
grep -c '\.TryResume(' src/ReSet.Core/Services/VerificationPipelineOrchestrator.cs
```

**세어 본 숫자를 문서에 적어라.** 1단계에서 이 자리 수가 문서에서 **네 번** 틀렸다(넷→다섯→여섯). 재개가 기록 자리를 둘 더한다(골격·섹션 재기록) — 문서를 문서로 고치면 다섯 번째가 난다.

- [ ] **Step 3: 복사본으로 실물 재개를 확인한다**

> **★ 계획 정정 (2026-09-11) — 원본 `POQSettleBatch7` 을 쓰지 않는다.**
>
> 병행 세션(`reset-3a`)이 지적했다: **Batch7 은 커밋된 기록 둘의 측정 기준값**이다 —
> U-앵커 설계서 §8-7(`cab64851`)의 「앵커 보유 문장 92 · 불일치 0」, 파라미터 타입 설계서의
> 「8/14 미표기」와 `9b120f9b` 스윕의 「미정의 SQL 블록 13 단계」. 재개가 성공하면
> `agent/steps/*.md` 를 덮어써 이 수들이 움직이고, **다음 사람이 다시 재면 원인이 재개인지
> 다른 것인지 못 가린다.** 판마다 Job 이름을 가르는 이 저장소 관례와도 복사본이 맞다.

먼저 복사본을 만든다:

```bash
cp -R output/Jobs/POQSettleBatch7 output/Jobs/POQSettleBatch7-resume
```

복사본의 `raw/attempts/run-001/manifest.json` 에 `"Job": "POQSettleBatch7"` 이 남아 있다.
**`TryResume` 은 경로로 판을 찾고 manifest 의 `Job` 필드를 읽지 않으므로 동작에는 지장이
없다.** 다만 감사에서 혼동하지 않게 그 값을 `POQSettleBatch7-resume` 으로 고치고, **고친
뒤 그 파일에 BOM 이 안 붙었는지** 확인하라(`head -c3 … | xxd -p` 가 `efbbbf` 가 아니어야 한다).

그 판의 상태(좌표자가 읽은 값): 단계 17개 · 결함 넷 — **S03·S07·S08 `QualityFloor` · S10 `Unverifiable`**.

같은 Job 이름(`POQSettleBatch7-resume`)으로 배치 계획 생성을 돌려 확인하라:

1. **재개 후보가 제안되는가** — 「13/17 재사용」이 화면에 뜨는가. **브레인스토밍과 목차 설계가 건너뛰어지는가**(정정된 Task 3 의 이득이다)
2. 승인했을 때 **다시 만드는 단계가 넷인가**
3. 새 판(`run-002`)의 manifest 에 **`ResumedFrom: "run-001"`** 과 재사용 섹션의 **원래 `Attempt`** 가 남는가
4. 새 판의 파일에 **BOM 이 없는가**

**★ S03 의 기대 사유는 이름 둘이다.** 병행 세션이 실측으로 알려 왔다 — `9b120f9b`(미정의 SQL 블록 검사의 자기 가림 수정) 뒤에 S03 을 다시 검증하면 저널에 없던 이름이 하나 더 나온다: **`SQL_CURRENT_RUN_ID` 와 `SQL_CREATE_AND_CAPTURE_SHADOW`**. 생성 당시엔 산문 언급에 가려 안 잡혔다. 이걸 모르면 「하나만 나왔으니 고쳐졌다」로 잘못 읽는다.

**앵커 쌍 시험도 보라.** 복사본이 새 Job 이라 `AnchorKindOrdinalPairTests` 에 새 줄이 생긴다. **게이트 실패 수가 베이스라인 1 에서 늘어나는지** 확인하고 결과를 적어라 — 늘면 그 복사본이 게이트를 오염시킨 것이다.

**예상이 아니라 관측을 적어라.** 다르게 나왔다면 그 사실이 더 중요하다 — 1단계에서 BOM 이 리뷰 여섯 번과 되돌림 열 번을 통과하고 실물에서야 드러났다.

기록: `docs/audit-reports/2026-09-11-재개-실물-확인.md`

- [ ] **Step 4: 복사본을 코퍼스에서 치운다**

> 병행 세션의 부탁이고 근거가 타당하다: 코퍼스 시험·스윕이 `output/Jobs/*` 를 전부 훑는데,
> **같은 원본 SP·같은 목차에서 나온 복사본이 남으면 같은 표본을 두 번 센다.**
> `LegacyErrorCodeInventionCorpusTests`·`LocalVariableTypeContractCorpusTests` 같은 하한
> 단언과 L1 스윕의 발화 수가 한 Job 분만큼 부풀고, **그 부풀음이 다음 차분의 기준선이 된다.**
> 이 저장소가 2026-09-07 에 반복 생성 표본 20 편을 지운 이유가 그것이다.

**Step 3 의 관측을 감사 보고서에 다 옮긴 뒤에** 치운다:

```bash
mv output/Jobs/POQSettleBatch7-resume /tmp/POQSettleBatch7-resume-$(date +%s)
```

치운 뒤 확인하라:
- `ls output/Jobs/` 에 `POQSettleBatch7-resume` 이 없다
- 전체 시험의 실패가 **베이스라인 1건**으로 돌아왔다(복사본이 만든 줄이 사라졌다)

**원본 `output/Jobs/POQSettleBatch7` 은 건드리지 마라.**

- [ ] **Step 5: 커밋한다**

```bash
git add docs/output-artifacts.md docs/architecture/4.4-verification-pipeline.md docs/audit-reports/2026-09-11-재개-실물-확인.md
git commit -m "docs: 재개 절차와 실물 확인 결과"
```
