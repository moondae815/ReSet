# 지역 변수 타입 계약 강등 금지 구현 계획서

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 배치 스텝의 `execute(SQL, {바인딩})` 관용구가 삼키던 지역 변수 타입 계약을 계약·예시·검사 세 층으로 못박아, 축 B 🔴 둘(`Batch5/S05`·`S07`)을 **다음 재생성부터** 닫는다.

**Architecture:** 새 사실 하나(`LocalVariableDeclarationExtractor.ExtractConstants` — 상수 초기화이면서 재대입되지 않는 지역 변수)와 새 추출기 하나(`StepBindingExtractor` — 의사코드 펜스의 바인딩 자리)를 만들고, `MechanicalValidator.CheckLocalVariableTypeContract` 가 둘을 맞대 **드라이버 경계로 새는 타입 계약**만 발화한다. 프롬프트에는 규칙 `5-1` 과 대조 Few-Shot 을 더한다. **판정 못 하는 모양은 침묵한다.**

**Tech Stack:** C# / .NET 10 · Microsoft.SqlServer.TransactSql.ScriptDom (`TSql160Parser`) · xUnit + Xunit.SkippableFact

**Spec:** `docs/superpowers/specs/2026-09-07-지역변수-타입계약-강등금지-design.md`

---

## Global Constraints

이 절의 값은 **모든 태스크의 요구사항에 암묵적으로 포함된다.**

- **기준 커밋은 `353f9a09`(브랜치 `feat/settlement-policy-redesign`)다.** 격리 워크트리를 여기서 딴다.
- **공유 체크아웃(`/Users/payletter/git-root/ReSet`)에서 빌드·테스트하지 마라.** 다른 세션의 통제 실험이 돌고 `bin`/`obj` 가 겹친다.
- **`git stash` 금지.** 스태시는 저장소 공유다.
- **코퍼스 심링크 넷을 다 걸고 시작한다.** 일부만 걸면 다른 테스트가 대신 꺼지는데 총 건너뜀 수는 줄어 성공처럼 보인다(`CorpusSetupGuardTests`).
  ```bash
  ln -s <메인 저장소>/output output
  ln -s <메인 저장소>/output.bak-2026-08-22 output.bak-2026-08-22
  ln -s <메인 저장소>/output.bak-stage4-control-20260828 output.bak-stage4-control-20260828
  ln -s <메인 저장소>/output.bak-batch1-preregen-20260904 output.bak-batch1-preregen-20260904
  ```
- **게이트는 실패 0 · 건너뜀 0 · 경고 0.** 절대 통과 수는 게이트가 아니다(환경 내에서도 최대 5까지 흔들린다).
- **새 멤버를 기존 문서 주석 블록과 그 멤버 사이에 끼우지 마라.** `<summary>` 둘이 한 블록으로 묶여 `OrphanedDocCommentTests` 가 잡는다(이 저장소에서 세 번 났다). 삽입은 **다음 멤버의 문서 블록 앞**에 한다.
- **커밋은 경로를 명시한다**: `git commit -m "..." -- <경로들>`. `git add .` 금지.
- 커밋 메시지 꼬리:
  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee
  ```

### 테스트 실행 명령 (모든 태스크 공통)

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~<클래스명>" -v n
```

전체 게이트:

```bash
dotnet build 2>&1 | tail -20            # 경고 0 · 오류 0
dotnet test 2>&1 | tail -20             # 실패 0 · 건너뜀 0
```

### 이 계획이 닫지 않는 것 (명시)

- **F2 트랜잭션 경계** — 구조 조건이 3/3 재현되지만 별건이다. 손대지 마라.
- **F3 반환값 계약 · F4 축자 위반 · F6 서술↔의사코드** — 재현율 미측정. 손대지 마라.
- **F5 예약어 별칭**(`Batch5/S08` 의 `AS Exists`) — 386 중 1 건이라 기각했다. **도구로 닫지 마라.**
- **문자열 길이 계약의 바인딩 유출** — 규칙 `5-1` 의 관할이지만 **검사는 다루지 않는다**(사람 결정).
- **재생성** — Task 7 이 관문만 문서화한다. **이 계획의 어느 태스크도 재생성을 돌리지 않는다**(2026-09-05 사람 결정: 동료 세션 둘의 발화 표본이 지금 산출물 안에 있다).

### 이 결함이 지금 무엇을 우연히 덮고 있나 (착수 전 선언)

세 결함 자리(`Batch4/S06`·`Batch5/S05`·`Batch5/S07`)는 **어떤 테스트의 픽스처도 아니다**(측정 완료). 기존 테스트가 쓰는 `@v_valIncVat` 는 명세서·DDL 층의 합성 픽스처다. **고쳐도 잃는 커버리지가 없다.**

기존 `CheckSpecLocalVariablesDeclared`(검사 D, 단계 층)와의 관계도 미리 못박는다. 그 검사는 **이름 기반**이라 `@v_valIncVat` → `@p_incVat` 개명에 세 자리 모두 침묵한다(그래서 감사가 🔴 로 열어 뒀다). 새 검사는 그 **이름 눈먼 보완재**다. **둘이 같은 자리에서 동시에 발화하면 안 된다** — Task 4 Step 7 이 그것을 잠근다.

---

## File Structure

| 파일 | 책임 |
| :--- | :--- |
| `src/ReSet.Core/Services/LocalVariableDeclarationExtractor.cs` (수정) | `ExtractConstants` 추가 — **사실**: 상수 초기화이면서 재대입 없는 지역 변수 |
| `src/ReSet.Core/Services/StepBindingExtractor.cs` (신규) | **사실**: 단계 본문 의사코드 펜스의 바인딩 자리(호출명·키·값) |
| `src/ReSet.Core/Services/MechanicalValidator.cs` (수정) | **정책**: `CheckLocalVariableTypeContract` — 타입·값 필터와 발화 |
| `src/ReSet.Core/Services/AiService.cs` (수정) | **계약**: 규칙 `5-1` 과 Few-Shot 대조 쌍 |
| `tests/ReSet.Core.Tests/LocalVariableDeclarationExtractorTests.cs` (신규 또는 수정) | `ExtractConstants` 단위 시험 |
| `tests/ReSet.Core.Tests/StepBindingExtractorTests.cs` (신규) | 바인딩 추출 단위 시험 |
| `tests/ReSet.Core.Tests/MechanicalValidatorTests.cs` (수정) | 검사의 발화·침묵 단위 시험 |
| `tests/ReSet.Core.Tests/LocalVariableTypeContractCorpusTests.cs` (신규) | **코퍼스 효력** — 3 발화 · 23 침묵 |
| `tests/ReSet.Core.Tests/BatchStepPromptContractTests.cs` (신규) | 규칙 `5-1` 과 예시가 프롬프트에서 조용히 사라지지 않게 잠근다 |
| `docs/audit-reports/2026-09-07-지역변수-타입계약-도달가능성.md` (신규) | Task 1 의 측정 기록 |

---

## Task 1: 앞 층 2번 — 도달 가능성을 재고 기록한다

**명세서 §6 이 「✘ 미측정. 착수 전에 잰다」로 열어 둔 유일한 자리다.** 검사가 바인딩 목록에 닿지 못하면 뒤의 모든 태스크가 값 0 을 게이트 통과시킨다(T22 가 그랬다).

**Files:**
- Create: `docs/audit-reports/2026-09-07-지역변수-타입계약-도달가능성.md`

**Interfaces:**
- Consumes: 없음
- Produces: 이후 태스크가 **의사코드 펜스를 대상으로 삼아도 되는지**에 대한 판정. 만약 ③이 「다르다」로 나오면 **Task 3 의 대상 문자열이 바뀌므로 즉시 보고하고 멈춘다.**

- [ ] **Step 1: 바인딩이 어느 펜스 안에 있는지 잰다**

```bash
cd <메인 저장소>
for f in output/Jobs/POQSettleBatch4/agent/steps/S06.md \
         output/Jobs/POQSettleBatch5/agent/steps/S05.md \
         output/Jobs/POQSettleBatch5/agent/steps/S07.md; do
  echo "=== $f ==="
  awk '/^```/{fence=!fence; if(fence){lang=$0} next} fence && /execute\(/{print lang" | "$0}' "$f"
done
```

기대: 세 자리 모두 ` ```pseudocode ` 펜스 안. **다른 언어 태그가 섞여 나오면 그 태그도 Task 3 의 대상에 넣는다.**

- [ ] **Step 2: 단계 본문에 쓰이는 펜스 언어 태그를 전수로 센다**

```bash
grep -rhoE '^```[a-zA-Z]+' output/Jobs/*/agent/steps/*.md | sort | uniq -c | sort -rn
```

관측된 태그 전량을 기록한다. Task 3 이 훑을 태그 집합이 여기서 정해진다.

- [ ] **Step 3: 조립본 섹션과 번들 파일의 내용이 같은지 잰다**

`ValidateBatchStep` 은 조립본(`docs/BatchMigrationPlan.md`)에서 잘라낸 섹션을 받는다. 번들 파일(`agent/steps/S05.md`)과 다르면 **코퍼스 측정이 검사가 실제로 보는 것을 안 잰 것이 된다.**

```bash
for j in POQSettleBatch4 POQSettleBatch5; do
  for s in S05 S06 S07; do
    p="output/Jobs/$j/agent/steps/$s.md"
    [ -f "$p" ] || continue
    b=$(grep -c 'execute(' "$p")
    a=$(awk -v code="$s" '
      $0 ~ "^### .*"code {inside=1}
      inside && /^### / && $0 !~ code {inside=0}
      inside' "output/Jobs/$j/docs/BatchMigrationPlan.md" | grep -c 'execute(')
    echo "$j/$s  번들:$b  조립본:$a"
  done
done
```

기대: 같은 수. **다르면 그 사실을 기록하고 좌표자에게 보고한 뒤 멈춘다.**

- [ ] **Step 4: 측정 기록을 쓴다**

`docs/audit-reports/2026-09-07-지역변수-타입계약-도달가능성.md` 에 다음을 **관측값 그대로** 적는다. 값을 반올림하거나 요약하지 마라.

```markdown
# 지역 변수 타입 계약 검사 — 도달 가능성 측정 (앞 층 2번)

측정 2026-09-07. 명세서 `docs/superpowers/specs/2026-09-07-지역변수-타입계약-강등금지-design.md` §6 이
「착수 전에 잰다」로 열어 둔 자리다. 판정 전에 잰 것이다.

## 1. 세 결함 자리의 바인딩이 사는 펜스

| 자리 | 펜스 언어 태그 |
| :--- | :--- |
| `Batch4/S06:52` | (관측값) |
| `Batch5/S05:71` | (관측값) |
| `Batch5/S07:57` | (관측값) |

## 2. 단계 본문의 펜스 언어 태그 전수

(Step 2 출력 그대로)

## 3. 조립본 ↔ 번들 대조

(Step 3 출력 그대로)

## 4. 판정

- 도달 가능성: (닿는다 / 안 닿는다)
- Task 3 이 훑을 태그 집합: (목록)
- 조립본과 번들이 (같다 / 다르다)
```

- [ ] **Step 5: 커밋**

```bash
git commit -m "docs: 지역 변수 타입 계약 검사의 도달 가능성을 착수 전에 잰다

앞 층 2번. 검사가 바인딩 목록에 닿지 못하면 뒤의 모든 태스크가 값 0 을
게이트 통과시킨다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- 'docs/audit-reports/2026-09-07-지역변수-타입계약-도달가능성.md'
```

---

## Task 2: `ExtractConstants` — 「상수 초기화 · 재대입 없음」이라는 사실

**Files:**
- Modify: `src/ReSet.Core/Services/LocalVariableDeclarationExtractor.cs`
- Test: `tests/ReSet.Core.Tests/LocalVariableDeclarationExtractorTests.cs`

**Interfaces:**
- Consumes: 기존 `LocalVariableDeclarationFact(string Name, string DataType, string InitialValue)`
- Produces: `public static IReadOnlyList<LocalVariableDeclarationFact> ExtractConstants(string? ddlText)` — Task 4 가 부른다.

**왜 `SetAssignmentExtractor` 를 재사용하지 않는가:** 그 추출기는 `Visit(SetVariableStatement)` 만 갖고 있어 **`SELECT @x = …` 를 못 본다**(실측). 재대입을 놓치면 누산기가 상수로 분류돼 오탐이 된다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/ReSet.Core.Tests/LocalVariableDeclarationExtractorTests.cs` 에 추가한다(파일이 없으면 만든다).

```csharp
using System.Linq;
using ReSet.Core.Services;
using Xunit;

namespace ReSet.Core.Tests
{
    public class LocalVariableDeclarationExtractorConstantsTests
    {
        // COMM_UPD 실물의 모양이다 - 상수 하나, 초기값 없는 것 하나,
        // SET 으로 재대입되는 누산기 하나, SELECT 로 재대입되는 것 하나.
        private const string Ddl = @"
CREATE PROCEDURE dbo.UP_TEST
    @pi_strYMD CHAR(8)
AS
BEGIN
    DECLARE @v_valIncVat DECIMAL(2,1) = 1.1;
    DECLARE @v_cnt INT;
    DECLARE @v_acc MONEY = 0;
    DECLARE @v_rate DECIMAL(5,2) = 1.25;
    SET @v_acc = @v_acc + 1;
    SELECT @v_rate = 2.5;
    UPDATE dbo.T SET Amt = CAST(Amt / @v_valIncVat AS INT);
END";

        [Fact]
        public void ExtractConstants_KeepsOnlyInitializedAndNeverReassigned()
        {
            var facts = LocalVariableDeclarationExtractor.ExtractConstants(Ddl);

            var fact = Assert.Single(facts);
            Assert.Equal("@v_valIncVat", fact.Name);
            Assert.Equal("DECIMAL(2,1)", fact.DataType, ignoreCase: true);
            Assert.Equal("1.1", fact.InitialValue);
        }

        [Fact]
        public void ExtractConstants_ExcludesVariableReassignedBySelect()
        {
            // SetAssignmentExtractor 는 SetVariableStatement 만 본다 - 그것을 그대로
            // 재사용하면 이 변수가 상수로 분류돼 오탐이 된다.
            var facts = LocalVariableDeclarationExtractor.ExtractConstants(Ddl);

            Assert.DoesNotContain(facts, f => f.Name == "@v_rate");
        }

        [Fact]
        public void ExtractConstants_ExcludesProcedureParameters()
        {
            var facts = LocalVariableDeclarationExtractor.ExtractConstants(
                "CREATE PROCEDURE dbo.UP_TEST @pi_rate DECIMAL(2,1) = 1.1 AS BEGIN SELECT 1; END");

            Assert.Empty(facts);
        }

        [Fact]
        public void ExtractConstants_OnUnparsableDdl_IsEmptyNotPartial()
        {
            var facts = LocalVariableDeclarationExtractor.ExtractConstants("CREATE PROCEDURE (((");

            Assert.Empty(facts);
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~LocalVariableDeclarationExtractorConstantsTests" -v n
```

기대: 컴파일 오류 — `ExtractConstants` 가 없다.

- [ ] **Step 3: 최소 구현을 쓴다**

`LocalVariableDeclarationExtractor` 안, **`Extract` 메서드 바로 뒤이며 `DeclarationVisitor` 클래스의 문서 블록 앞**에 넣는다(Global Constraints 의 문서 블록 규칙).

```csharp
        /// <summary>
        /// 「상수 초기화이면서 `DECLARE` 이후 한 번도 재대입되지 않는」 지역 변수만 낸다.
        ///
        /// [왜 별도 메서드인가] <see cref="Extract"/>의 결과는 「선언된 것 전부」다.
        /// 타입 계약이 드라이버로 새는지를 판정하려면 「값이 컴파일 시점에 확정된 것」만
        /// 봐야 한다 - 런타임에 값이 정해지는 변수는 애초에 바인딩이 옳을 수 있다.
        ///
        /// [왜 SetAssignmentExtractor를 안 쓰는가] 그 추출기는
        /// `Visit(SetVariableStatement)`만 갖고 있어 `SELECT @x = …`를 보지 못한다
        /// (실측). 재대입을 놓치면 누산기가 상수로 분류돼 오탐이 된다.
        ///
        /// [같은 파스를 쓴다] 선언과 재대입을 서로 다른 파스로 재면 한쪽이 조용히
        /// 낡는다 - 이 저장소가 오라클 드리프트로 반복해 물린 자리다.
        /// </summary>
        public static IReadOnlyList<LocalVariableDeclarationFact> ExtractConstants(string? ddlText)
        {
            if (string.IsNullOrWhiteSpace(ddlText)) return Array.Empty<LocalVariableDeclarationFact>();

            TSqlFragment? fragment;
            try
            {
                var parser = new TSql160Parser(true);
                using var reader = new StringReader(ddlText);
                fragment = parser.Parse(reader, out var errors);
                if (fragment == null || (errors != null && errors.Count > 0))
                {
                    return Array.Empty<LocalVariableDeclarationFact>();
                }
            }
            catch (Exception)
            {
                return Array.Empty<LocalVariableDeclarationFact>();
            }

            var declarations = new DeclarationVisitor();
            fragment.Accept(declarations);

            var reassigned = new ReassignmentVisitor();
            fragment.Accept(reassigned);

            return declarations.Facts
                .Where(f => !string.IsNullOrWhiteSpace(f.InitialValue))
                .Where(f => !reassigned.Names.Contains(f.Name))
                .ToList();
        }

        /// <summary>
        /// `SET @x = …`와 `SELECT @x = …` 양쪽을 본다. 한쪽만 보면 누산기가 상수로
        /// 분류된다.
        /// </summary>
        private sealed class ReassignmentVisitor : TSqlFragmentVisitor
        {
            public HashSet<string> Names { get; } = new(StringComparer.OrdinalIgnoreCase);

            public override void Visit(SetVariableStatement node)
            {
                var name = node.Variable?.Name;
                if (!string.IsNullOrWhiteSpace(name)) Names.Add(name!);
            }

            public override void Visit(SelectSetVariable node)
            {
                var name = node.Variable?.Name;
                if (!string.IsNullOrWhiteSpace(name)) Names.Add(name!);
            }
        }
```

- [ ] **Step 4: 통과를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~LocalVariableDeclarationExtractorConstantsTests" -v n
```

기대: 4/4 PASS.

- [ ] **Step 5: 실물 DDL 로 값을 확인한다**

합성 픽스처가 실물보다 단순해 코퍼스 발화 0 이 난 적이 이 저장소에 있다. 실물에 대고 한 번 잰다.

```bash
cd <메인 저장소> && grep -rhiE "^\s*DECLARE\s+@[A-Za-z_0-9]+\s+(DECIMAL|NUMERIC|FLOAT|REAL|MONEY|SMALLMONEY)\b[^=]*=" \
  output/Objects/*.Procedure/raw/object_definition.sql | sed 's/^\s*//' | sort | uniq -c
```

기대: 명세서 §2-3 의 표와 같은 7 행(`@v_valIncVat` ×2 · `MONEY = 0` ×5). **다르면 명세서가 낡은 것이므로 보고한다.**

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 상수 초기화이면서 재대입 없는 지역 변수를 사실로 뽑는다

SetAssignmentExtractor 는 SetVariableStatement 만 보아 SELECT @x = 를
놓친다 - 그것을 재사용하면 누산기가 상수로 분류돼 오탐이 된다.
선언과 재대입을 같은 파스에서 본다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- src/ReSet.Core/Services/LocalVariableDeclarationExtractor.cs \
     tests/ReSet.Core.Tests/LocalVariableDeclarationExtractorTests.cs
```

---

## Task 3: `StepBindingExtractor` — 의사코드 펜스의 바인딩 자리

**Files:**
- Create: `src/ReSet.Core/Services/StepBindingExtractor.cs`
- Test: `tests/ReSet.Core.Tests/StepBindingExtractorTests.cs`

**Interfaces:**
- Consumes: Task 1 이 확정한 펜스 언어 태그 집합
- Produces:
  - `public sealed record StepBindingFact(string CallName, string StatementName, string Key, string Value)`
  - `public static IReadOnlyList<StepBindingFact> Extract(string? stepMarkdown)`

**대상 호출**: `execute` · `queryScalar` · `queryRow` · `queryList`. 이 넷은 실측으로 확정한 목록이다(호출 형태를 넷으로 넓혀 다시 훑어도 소수 리터럴 바인딩은 여전히 3 이었다).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```csharp
using System.Linq;
using ReSet.Core.Services;
using Xunit;

namespace ReSet.Core.Tests
{
    public class StepBindingExtractorTests
    {
        // Batch5/S05:71 · S07:57 실물의 모양이다.
        private const string Section = @"### S05 수수료 갱신

```pseudocode
runId = queryScalar(SQL_CURRENT_RUN_ID, { p_jobName: ""J"", p_ymd: batchYmd })
execute(SQL_UPDATE_13, { p_YMD: p_YMD, p_v_valIncVat: 1.1 })   // 지역 변수 계약 DECIMAL(2,1)
```
```sql
-- SQL_UPDATE_13
UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / @p_v_valIncVat AS INT);
```";

        [Fact]
        public void Extract_ReadsEveryBindingInPseudocodeFences()
        {
            var facts = StepBindingExtractor.Extract(Section);

            Assert.Equal(4, facts.Count);
            Assert.Contains(facts, f =>
                f.CallName == "execute" &&
                f.StatementName == "SQL_UPDATE_13" &&
                f.Key == "p_v_valIncVat" &&
                f.Value == "1.1");
        }

        [Fact]
        public void Extract_KeepsQueryScalarBindings()
        {
            var facts = StepBindingExtractor.Extract(Section);

            Assert.Contains(facts, f => f.CallName == "queryScalar" && f.Key == "p_ymd");
        }

        [Fact]
        public void Extract_IgnoresSqlFences()
        {
            // SQL 펜스에 execute( 문자열이 있어도 바인딩이 아니다 - 그것을 세면
            // 검사가 SQL 주석에 반응한다.
            var markdown = "### S01\n\n```sql\n-- execute(SQL_X, { p_a: 1.1 })\nSELECT 1;\n```";

            Assert.Empty(StepBindingExtractor.Extract(markdown));
        }

        [Fact]
        public void Extract_OnEmptyInput_IsEmpty()
        {
            Assert.Empty(StepBindingExtractor.Extract(null));
            Assert.Empty(StepBindingExtractor.Extract("   "));
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~StepBindingExtractorTests" -v n
```

기대: 컴파일 오류 — `StepBindingExtractor` 가 없다.

- [ ] **Step 3: 최소 구현을 쓴다**

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;

namespace ReSet.Core.Services
{
    /// <param name="CallName">`execute`·`queryScalar`·`queryRow`·`queryList` 중 하나.</param>
    /// <param name="StatementName">첫 인자로 준 SQL 문장 이름(`SQL_UPDATE_13`).</param>
    /// <param name="Key">바인딩 키(`p_v_valIncVat`). **검사는 이 값을 판정에 쓰지 않는다** -
    /// 모델이 개명하기 때문이다. 오류 메시지에만 싣는다.</param>
    /// <param name="Value">바인딩 값 원문(`1.1`·`batchYmd`).</param>
    public sealed record StepBindingFact(string CallName, string StatementName, string Key, string Value);

    /// <summary>
    /// 단계 본문의 의사코드 펜스에서 `execute(SQL_X, { k: v, … })` 꼴의 바인딩을 전수 뽑는다.
    ///
    /// [왜 SQL 펜스를 안 보는가] SQL 주석 안의 `execute(` 문자열에 반응하면 검사가
    /// 주석을 고발한다. 바인딩이라는 사실은 의사코드 층에만 있다.
    ///
    /// [왜 값만 쓰고 키는 안 쓰는가] 이 저장소 실측: `Batch5/S07`이
    /// `@v_valIncVat`를 `p_incVat`으로 **개명**했다. 키(이름)로 판정하면 세 자리 중
    /// 둘을 놓친다. 키는 오류 메시지의 가독성에만 쓴다.
    /// </summary>
    public static class StepBindingExtractor
    {
        /// <summary>
        /// 훑을 펜스 언어 태그. Task 1의 실측이 정한 집합이다 - 새 태그가 관측되면
        /// 여기에 더한다. 빈 태그(``` 만 있는 펜스)도 포함한다: 모델이 태그를 빠뜨린
        /// 펜스에 바인딩을 적으면 검사가 통째로 눈이 먼다.
        /// </summary>
        private static readonly string[] Calls = { "execute", "queryScalar", "queryRow", "queryList" };

        public static IReadOnlyList<StepBindingFact> Extract(string? stepMarkdown)
        {
            if (string.IsNullOrWhiteSpace(stepMarkdown)) return Array.Empty<StepBindingFact>();

            var facts = new List<StepBindingFact>();

            foreach (Match fence in Regex.Matches(
                stepMarkdown!, @"```(?<lang>[a-zA-Z]*)\r?\n(?<body>.*?)```",
                RegexOptions.Singleline))
            {
                var lang = fence.Groups["lang"].Value;
                // sql 펜스는 제외한다. 나머지(pseudocode·빈 태그·기타)는 다 본다.
                if (string.Equals(lang, "sql", StringComparison.OrdinalIgnoreCase)) continue;

                var body = fence.Groups["body"].Value;
                var callPattern =
                    $@"\b(?<call>{string.Join("|", Calls)})\(\s*(?<stmt>[A-Za-z_0-9]+)\s*,\s*\{{(?<args>[^}}]*)\}}";

                foreach (Match call in Regex.Matches(body, callPattern))
                {
                    var callName = call.Groups["call"].Value;
                    var statement = call.Groups["stmt"].Value;

                    foreach (Match arg in Regex.Matches(
                        call.Groups["args"].Value,
                        @"(?<key>[A-Za-z_0-9]+)\s*:\s*(?<value>[^,}]+)"))
                    {
                        facts.Add(new StepBindingFact(
                            callName,
                            statement,
                            arg.Groups["key"].Value.Trim(),
                            arg.Groups["value"].Value.Trim()));
                    }
                }
            }

            return facts;
        }
    }
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~StepBindingExtractorTests" -v n
```

기대: 4/4 PASS.

- [ ] **Step 5: 실물에 대고 값을 확인한다**

```bash
cd <메인 저장소>
grep -rhoE "(execute|queryScalar|queryRow|queryList)\([A-Za-z_0-9]+, *\{[^}]*\}" \
  output/Jobs/*/agent/steps/*.md | wc -l
```

기대: 380 근처. **0 이 나오면 정규식이 실물 모양과 어긋난 것이다 — 고치고 다시 잰다.**

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 단계 본문 의사코드 펜스의 바인딩 자리를 사실로 뽑는다

키(이름)로 판정하지 않는다 - Batch5/S07 이 @v_valIncVat 를 p_incVat 으로
개명했고 이름 기반 검사는 세 자리 중 둘을 놓친다. 키는 메시지에만 쓴다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- src/ReSet.Core/Services/StepBindingExtractor.cs \
     tests/ReSet.Core.Tests/StepBindingExtractorTests.cs
```

---

## Task 4: `CheckLocalVariableTypeContract` — 정책과 배선

**Files:**
- Modify: `src/ReSet.Core/Services/MechanicalValidator.cs`
- Test: `tests/ReSet.Core.Tests/MechanicalValidatorTests.cs`

**Interfaces:**
- Consumes: `LocalVariableDeclarationExtractor.ExtractConstants(string?)` (Task 2) · `StepBindingExtractor.Extract(string?)` (Task 3) · 기존 `ddlByProcedure` 인자
- Produces: `result.Errors` 에 실리는 발화 문자열. Task 5 가 그 문자열로 코퍼스를 판정한다.

**정책 판별자 (명세서 §5-1):**
- 타입이 `DECIMAL`·`NUMERIC`·`FLOAT`·`REAL`·`MONEY`·`SMALLMONEY` 로 **시작**한다(대소문자 무시)
- 초기값이 **`0` 이 아니다**(값이 `0` 이면 어느 수치 타입이어도 `0` 이라 손해가 없다)
- 그 초기값 리터럴이 **바인딩 값 자리에 나타난다**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`MechanicalValidatorTests.cs` 끝에 추가한다.

```csharp
        // ─────────────────────────────────────────────────────────────────────
        // 지역 변수 타입 계약 강등 금지 — 2026-09-07
        //
        // Batch5/S05·S07 · Batch4/S06 실물: 타입이 걸린 상수가 방출 SQL 의 DECLARE 가
        // 아니라 드라이버 바인딩으로 샌다. Batch5/S07 은 변수를 @p_incVat 으로 개명해
        // 이름 기반 검사(CheckSpecLocalVariablesDeclared)가 침묵했다.
        // ─────────────────────────────────────────────────────────────────────

        private const string CommUpdDdl = @"
CREATE PROCEDURE dbo.UP_UTIL_SETTLE_COMM_UPD
    @pi_strYMD CHAR(8)
AS
BEGIN
    DECLARE @v_valIncVat DECIMAL(2,1) = 1.1;
    UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / @v_valIncVat AS INT);
END";

        private static System.Collections.Generic.IReadOnlyDictionary<string, string> CommUpdDdlMap()
            => new System.Collections.Generic.Dictionary<string, string>(
                System.StringComparer.OrdinalIgnoreCase)
            {
                ["UP_UTIL_SETTLE_COMM_UPD"] = CommUpdDdl
            };

        private static BatchStepPlan CommUpdPlan() => new(
            "S05", "수수료 갱신",
            new[] { "UP_UTIL_SETTLE_COMM_UPD" }, new[] { "dbo.TSettleMst" },
            new[] { "-1" }, false, System.Array.Empty<string>());

        [Fact]
        public void ValidateBatchStep_BindsTypedConstantToDriver_IsReported()
        {
            // Batch5/S05:71 실물. 이름이 p_v_valIncVat 으로 바뀌어 있어도 잡아야 한다.
            var section = @"### S05 수수료 갱신

TSettleMst 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_UPDATE_13, { p_YMD: p_YMD, p_v_valIncVat: 1.1 })
```
```sql
-- SQL_UPDATE_13
UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / @p_v_valIncVat AS INT);
```";

            var result = _validator.ValidateBatchStep(
                section, CommUpdPlan(), System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: CommUpdDdlMap());

            Assert.Contains(result.Errors, e => e.Contains("@v_valIncVat") && e.Contains("DECIMAL(2,1)"));
        }

        [Fact]
        public void ValidateBatchStep_BindsRenamedTypedConstant_IsStillReported()
        {
            // Batch5/S07:57 실물. 키 이름에 valIncVat 이 아예 없다 - 이름으로 판정하면
            // 놓친다.
            var section = @"### S05 수수료 갱신

TSettleMst 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_UPDATE5, { p_ymd: @pi_strYMD, p_incVat: 1.1 })
```
```sql
-- SQL_UPDATE5
UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / @p_incVat AS INT);
```";

            var result = _validator.ValidateBatchStep(
                section, CommUpdPlan(), System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: CommUpdDdlMap());

            Assert.Contains(result.Errors, e => e.Contains("@v_valIncVat"));
        }

        [Fact]
        public void ValidateBatchStep_DeclaresTypedConstantInSql_IsSilent()
        {
            // Batch4/S08:333 실물 - 보존한 쪽. 23 자리가 이 모양이다.
            var section = @"### S05 수수료 갱신

TSettleMst 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_UPDATE_13, { p_YMD: p_YMD })
```
```sql
-- SQL_UPDATE_13
DECLARE @v_valIncVat DECIMAL(2,1) = 1.1;
UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / @v_valIncVat AS INT);
```";

            var result = _validator.ValidateBatchStep(
                section, CommUpdPlan(), System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: CommUpdDdlMap());

            Assert.DoesNotContain(result.Errors, e => e.Contains("@v_valIncVat"));
        }

        [Fact]
        public void ValidateBatchStep_InlinesTypedConstantIntoSql_IsSilent()
        {
            // T-SQL 리터럴 1.1 은 그 자체로 numeric(2,1) 이다 - 인라인은 타입이 안전하다.
            // 규칙 5-1 은 DECLARE 를 요구하지만 검사는 해로운 부분집합만 발화한다
            // (명세서 §5-2 의 「규칙 ⊃ 검사」).
            var section = @"### S05 수수료 갱신

TSettleMst 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_UPDATE_13, { p_YMD: p_YMD })
```
```sql
-- SQL_UPDATE_13
UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / 1.1 AS INT);
```";

            var result = _validator.ValidateBatchStep(
                section, CommUpdPlan(), System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: CommUpdDdlMap());

            Assert.DoesNotContain(result.Errors, e => e.Contains("@v_valIncVat"));
        }

        [Fact]
        public void ValidateBatchStep_BindsZeroValuedMoneyConstant_IsSilent()
        {
            // @v_intCLComm MONEY = 0. 값이 0 이면 어느 수치 타입이어도 0 이라 손해가 없다.
            var ddl = @"
CREATE PROCEDURE dbo.UP_ZERO AS
BEGIN
    DECLARE @v_intCLComm MONEY = 0;
    UPDATE dbo.T SET A = @v_intCLComm;
END";
            var section = @"### S05 영집계

T 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_U, { p_acc: 0 })
```
```sql
-- SQL_U
UPDATE dbo.T SET A = @p_acc;
```";
            var plan = new BatchStepPlan("S05", "영집계",
                new[] { "UP_ZERO" }, new[] { "dbo.T" }, new[] { "-1" }, false,
                System.Array.Empty<string>());

            var result = _validator.ValidateBatchStep(
                section, plan, System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: new System.Collections.Generic.Dictionary<string, string>(
                    System.StringComparer.OrdinalIgnoreCase) { ["UP_ZERO"] = ddl });

            Assert.DoesNotContain(result.Errors, e => e.Contains("@v_intCLComm"));
        }

        [Fact]
        public void ValidateBatchStep_BindsIntegerConstant_IsSilent()
        {
            // 청크 크기 10000 같은 INT 리터럴에 반응하면 안 된다.
            var ddl = @"
CREATE PROCEDURE dbo.UP_INT AS
BEGIN
    DECLARE @v_size INT = 10000;
    UPDATE dbo.T SET A = @v_size;
END";
            var section = @"### S05 청크

T 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_U, { p_size: 10000 })
```
```sql
-- SQL_U
UPDATE dbo.T SET A = @p_size;
```";
            var plan = new BatchStepPlan("S05", "청크",
                new[] { "UP_INT" }, new[] { "dbo.T" }, new[] { "-1" }, false,
                System.Array.Empty<string>());

            var result = _validator.ValidateBatchStep(
                section, plan, System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: new System.Collections.Generic.Dictionary<string, string>(
                    System.StringComparer.OrdinalIgnoreCase) { ["UP_INT"] = ddl });

            Assert.DoesNotContain(result.Errors, e => e.Contains("@v_size"));
        }

        [Fact]
        public void ValidateBatchStep_WithoutDdl_IsSilent()
        {
            // 오라클이 없으면 침묵한다 - 종전 동작 그대로다.
            var section = @"### S05 수수료 갱신

TSettleMst 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_UPDATE_13, { p_v_valIncVat: 1.1 })
```
```sql
-- SQL_UPDATE_13
SELECT 1;
```";

            var result = _validator.ValidateBatchStep(
                section, CommUpdPlan(), System.Array.Empty<string>(), NoConditions);

            Assert.DoesNotContain(result.Errors, e => e.Contains("타입 계약"));
        }

        [Fact]
        public void ValidateBatchStep_TypeContractDefect_DoesNotAlsoFireNameBasedCheck()
        {
            // 새 검사는 CheckSpecLocalVariablesDeclared(이름 기반)의 보완재다.
            // 같은 자리에서 둘이 동시에 발화하면 재생성 프롬프트에 같은 지적이 두 번
            // 실린다.
            var section = @"### S05 수수료 갱신

TSettleMst 를 갱신한다. 오류코드 -1.

```pseudocode
execute(SQL_UPDATE_13, { p_v_valIncVat: 1.1 })
```
```sql
-- SQL_UPDATE_13
UPDATE dbo.TSettleMst SET CLComm = CAST(CLComm / @p_v_valIncVat AS INT);
```";

            var result = _validator.ValidateBatchStep(
                section, CommUpdPlan(), System.Array.Empty<string>(), NoConditions,
                ddlByProcedure: CommUpdDdlMap());

            Assert.Single(result.Errors, e => e.Contains("@v_valIncVat"));
        }
```

- [ ] **Step 2: 실패를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~ValidateBatchStep_BindsTypedConstantToDriver_IsReported" -v n
```

기대: FAIL — 발화가 없다.

- [ ] **Step 3: 검사를 구현한다**

`MechanicalValidator.cs` 의 `CheckSpecLocalVariablesDeclared` **바로 뒤이며 그다음 멤버의 문서 블록 앞**에 넣는다.

```csharp
        /// <summary>
        /// 타입이 걸린 상수가 방출 SQL의 `DECLARE`가 아니라 **드라이버 바인딩**으로
        /// 새는 자리를 든다.
        ///
        /// [왜 「DECLARE가 없다」로 판정하지 않는가] T-SQL에서 리터럴 `1.1`은 그 자체로
        /// `numeric(2,1)`이다. 즉 `CAST(CLComm / 1.1 AS INT)`처럼 SQL 안에 인라인하는
        /// 것은 **타입이 안전하다.** 해가 나는 자리는 오직 드라이버 경계다 - 바인딩된
        /// 값은 선언 타입이 없어 드라이버가 고른다. `DECIMAL(2,1)`이 `FLOAT`으로
        /// 도착하면 `CAST(… AS INT)`의 절사가 달라져 금액이 어긋난다.
        /// 규칙 5-1은 `DECLARE` 보존을 요구하지만 이 검사는 **해로운 부분집합**만
        /// 발화한다. 그 간격은 의도된 것이다(설계 §5-2).
        ///
        /// [왜 이름을 안 보는가] 실측: `Batch5/S07`이 `@v_valIncVat`를 `p_incVat`으로
        /// 개명했다. 이름 기반인 <see cref="CheckSpecLocalVariablesDeclared"/>는 세
        /// 자리 중 둘에 침묵했다 - 이 검사는 그 **이름 눈먼 보완재**다. 초기값
        /// 리터럴로 맞댄다.
        ///
        /// [오라클] 원본 DDL(<paramref name="ddlByProcedure"/>). 명세서도 단계 본문도
        /// 아니라 **비순환**이다. 없으면 침묵한다 - 종전 동작 그대로다.
        /// </summary>
        private static void CheckLocalVariableTypeContract(
            string stepMarkdown,
            BatchStepPlan step,
            IReadOnlyDictionary<string, string>? ddlByProcedure,
            StepValidationResult result)
        {
            if (ddlByProcedure == null || ddlByProcedure.Count == 0) return;
            if (step.LegacyProcedures == null || step.LegacyProcedures.Count == 0) return;

            var bindings = StepBindingExtractor.Extract(stepMarkdown);
            if (bindings.Count == 0) return;

            var reported = new HashSet<string>(StringComparer.OrdinalIgnoreCase);

            foreach (var source in step.LegacyProcedures)
            {
                var bare = BareObjectName(source);
                if (bare.Length == 0) continue;
                if (!ddlByProcedure.TryGetValue(bare, out var ddl)) continue;

                foreach (var fact in LocalVariableDeclarationExtractor.ExtractConstants(ddl))
                {
                    if (!IsPrecisionBearingNumeric(fact.DataType)) continue;
                    if (IsZeroLiteral(fact.InitialValue)) continue;

                    var leaked = bindings.FirstOrDefault(b =>
                        string.Equals(b.Value, fact.InitialValue, StringComparison.OrdinalIgnoreCase));
                    if (leaked == null) continue;

                    if (!reported.Add(fact.Name)) continue;

                    result.Errors.Add(
                        $"{step.Code} 섹션이 `{fact.Name}`의 타입 계약 `{fact.DataType}`을(를) " +
                        $"드라이버 바인딩(`{leaked.CallName}({leaked.StatementName}, {{ {leaked.Key}: {leaked.Value} }})`)에 " +
                        "맡깁니다 — 바인딩된 값에는 선언 타입이 없어 드라이버가 고릅니다. " +
                        $"`{leaked.StatementName}` 블록 안에 `DECLARE {fact.Name} {fact.DataType} = {fact.InitialValue};`을(를) " +
                        "두고 바인딩 목록에서 그 값을 빼십시오(규칙 5-1). 타입을 주석에 적는 것은 못박는 것이 아닙니다.");
                }
            }
        }

        /// <summary>
        /// 정밀도·자릿수가 산술 결과를 바꾸는 수치 타입인가. 정수 타입은 뺀다 -
        /// 드라이버가 어떤 정수 타입을 골라도 산술이 같고, 뺌으로써 청크 크기
        /// (`10000` 같은 `INT` 리터럴) 오탐도 함께 사라진다.
        /// </summary>
        private static bool IsPrecisionBearingNumeric(string? dataType)
        {
            if (string.IsNullOrWhiteSpace(dataType)) return false;
            var t = dataType!.TrimStart();
            return t.StartsWith("DECIMAL", StringComparison.OrdinalIgnoreCase)
                || t.StartsWith("NUMERIC", StringComparison.OrdinalIgnoreCase)
                || t.StartsWith("FLOAT", StringComparison.OrdinalIgnoreCase)
                || t.StartsWith("REAL", StringComparison.OrdinalIgnoreCase)
                || t.StartsWith("SMALLMONEY", StringComparison.OrdinalIgnoreCase)
                || t.StartsWith("MONEY", StringComparison.OrdinalIgnoreCase);
        }

        /// <summary>
        /// 값이 `0`인가. `0`은 어느 수치 타입으로 묶여도 `0`이라 드라이버가 타입을
        /// 골라도 손해가 없다 - 코퍼스의 `MONEY = 0` 다섯이 이 갈래다.
        /// </summary>
        private static bool IsZeroLiteral(string? value)
            => decimal.TryParse(value, out var d) && d == 0m;
```

- [ ] **Step 4: 배선한다**

`ValidateBatchStep` 의 최상위 `SafeCheck` 무리(현재 `CheckLegacyStepErrorCodeInvention` 호출 줄) **바로 뒤**에 한 줄 넣는다. **`statementFactsByProcedure` 블록 안에 넣지 마라** — 이 검사는 명세서 사실이 없어도 돌아야 한다.

```csharp
            SafeCheck(() => CheckLocalVariableTypeContract(stepMarkdown, step, ddlByProcedure, result));
```

- [ ] **Step 5: 통과를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~ValidateBatchStep_Binds|FullyQualifiedName~ValidateBatchStep_Declares|FullyQualifiedName~ValidateBatchStep_Inlines|FullyQualifiedName~ValidateBatchStep_WithoutDdl|FullyQualifiedName~ValidateBatchStep_TypeContractDefect" -v n
```

기대: 8/8 PASS.

- [ ] **Step 6: 전체 게이트를 돌린다**

```bash
dotnet build 2>&1 | tail -20
dotnet test 2>&1 | tail -20
```

기대: 경고 0 · 실패 0 · 건너뜀 0. **기존 테스트가 깨지면 그것이 이 검사가 다른 검사와 겹친다는 증거다 — 고치기 전에 어느 검사와 겹치는지 적어 보고한다.**

- [ ] **Step 7: 커밋**

```bash
git commit -m "feat: 타입 계약이 드라이버 바인딩으로 새는 자리를 L1 이 든다

DECLARE 유무가 아니라 「값이 드라이버를 건너가는가」로 판정한다 - T-SQL
리터럴 1.1 은 그 자체로 numeric(2,1) 이라 인라인은 안전하고, 해가 나는
자리는 드라이버 경계뿐이다. 규칙 5-1 보다 좁은 것은 의도된 것이다.

이름을 보지 않는다 - Batch5/S07 이 개명해 이름 기반 검사가 침묵했다.
오라클은 원본 DDL 이라 비순환이다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- src/ReSet.Core/Services/MechanicalValidator.cs \
     tests/ReSet.Core.Tests/MechanicalValidatorTests.cs
```

---

## Task 5: 뒤 층 4·5·6 — 코퍼스 효력을 잰다

**단위 시험 통과는 효력이 아니다.** 이 태스크가 명세서 §6 의 뒤 층 셋을 전부 채운다.

**Files:**
- Create: `tests/ReSet.Core.Tests/LocalVariableTypeContractCorpusTests.cs`
- Create: `docs/audit-reports/2026-09-07-지역변수-타입계약-효력측정.md`

**Interfaces:**
- Consumes: Task 4 의 발화 문자열
- Produces: 없음(종단 태스크)

- [ ] **Step 1: 코퍼스 시험을 쓴다**

`LocalVariableTableCorpusTests.cs` 의 관례를 따른다(`SkippableFact` · `CorpusPaths.RepoRoot()` · **하한으로 단언**).

```csharp
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using ReSet.Core.Services;
using Xunit;
using Xunit.Abstractions;

namespace ReSet.Core.Tests
{
    /// <summary>
    /// 새 검사가 실물 코퍼스에서 **발화하는지**와 **오탐이 다수가 아닌지**를 잰다.
    ///
    /// [왜 하한·상한으로 단언하는가] 숫자로 못박으면 코퍼스에 Job 이 하나 늘 때마다
    /// 빨개지고 다음 사람이 관측을 읽는 대신 기대값을 고친다
    /// (`ErrorCodeTableCorpusTests`·`LocalVariableTableCorpusTests`와 같은 근거).
    ///
    /// [측정된 표본] 2026-09-06 실측: 유출 3(Batch4/S06 · Batch5/S05 · Batch5/S07)
    /// 대 보존 23(Proc 계열 전량 + Batch1/S07 + Batch4/S08 + Prco20).
    /// </summary>
    public class LocalVariableTypeContractCorpusTests
    {
        private readonly ITestOutputHelper _output;

        public LocalVariableTypeContractCorpusTests(ITestOutputHelper output) => _output = output;

        [SkippableFact]
        public void TypedConstantBoundToDriver_FiresOnEveryKnownLeak()
        {
            var root = CorpusPaths.RepoRoot();
            var jobs = Path.Combine(root, "output", "Jobs");
            Skip.IfNot(Directory.Exists(jobs), "코퍼스가 없다.");

            var leaks = new List<string>();

            foreach (var step in Directory.EnumerateFiles(jobs, "*.md", SearchOption.AllDirectories)
                         .Where(p => p.Replace('\\', '/').Contains("/agent/steps/")))
            {
                var markdown = File.ReadAllText(step);
                foreach (var binding in StepBindingExtractor.Extract(markdown))
                {
                    // 소수 리터럴 바인딩만이 이 검사의 후보다.
                    if (!binding.Value.Contains('.')) continue;
                    if (!decimal.TryParse(binding.Value, out var d) || d == 0m) continue;
                    leaks.Add($"{Rel(root, step)} :: {binding.CallName}({binding.StatementName}) " +
                              $"{binding.Key}: {binding.Value}");
                }
            }

            foreach (var l in leaks) _output.WriteLine(l);

            // 뒤 층 4: 발화한다. 알려진 세 자리가 전부 들어 있어야 한다.
            Assert.Contains(leaks, l => l.Contains("POQSettleBatch4") && l.Contains("S06"));
            Assert.Contains(leaks, l => l.Contains("POQSettleBatch5") && l.Contains("S05"));
            Assert.Contains(leaks, l => l.Contains("POQSettleBatch5") && l.Contains("S07"));

            // 뒤 층 5: 오탐이 다수가 아니다. 발화 후보가 알려진 셋보다 크게 늘면
            // 전부 열어 봐야 한다 - 그 판단을 사람에게 강제하는 상한이다.
            Assert.True(leaks.Count <= 6,
                $"발화 후보가 {leaks.Count} 다. 전부 열어 보고 상한을 갱신하라:\n" +
                string.Join("\n", leaks));
        }

        [SkippableFact]
        public void PreservedSites_StaySilent()
        {
            var root = CorpusPaths.RepoRoot();
            var jobs = Path.Combine(root, "output", "Jobs");
            Skip.IfNot(Directory.Exists(jobs), "코퍼스가 없다.");

            var preserved = Directory.EnumerateFiles(jobs, "*.md", SearchOption.AllDirectories)
                .Where(p => p.Replace('\\', '/').Contains("/agent/steps/"))
                .Where(p => File.ReadAllText(p).Contains("DECLARE @v_valIncVat",
                            StringComparison.OrdinalIgnoreCase))
                .ToList();

            // 측정 근거: 보존 23 자리. 하한으로 잠근다.
            Assert.True(preserved.Count >= 20,
                $"DECLARE 로 보존한 자리가 {preserved.Count} 뿐이다. 코퍼스나 추출기가 바뀌었다.");

            foreach (var step in preserved)
            {
                var decimals = StepBindingExtractor.Extract(File.ReadAllText(step))
                    .Where(b => b.Value.Contains('.') &&
                                decimal.TryParse(b.Value, out var d) && d != 0m)
                    .ToList();

                Assert.True(decimals.Count == 0,
                    $"{Rel(root, step)} 는 DECLARE 로 보존했는데 소수 바인딩도 있다: " +
                    string.Join(", ", decimals.Select(b => $"{b.Key}: {b.Value}")));
            }
        }

        private static string Rel(string root, string path)
            => Path.GetRelativePath(root, path).Replace('\\', '/');
    }
}
```

- [ ] **Step 2: 코퍼스 시험을 돌린다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~LocalVariableTypeContractCorpusTests" -v n
```

기대: 2/2 PASS, 건너뜀 0. 출력에 **유출 세 자리가 이름으로 찍힌다.**

- [ ] **Step 3: 뒤 층 5 — 발화 후보를 전부 열어 본다**

Step 2 의 출력에 찍힌 후보를 **하나도 빠짐없이** 열어 실물을 확인한다. 셋이 아니면 초과분마다 다음을 적는다: 파일·줄·바인딩·그것이 진짜 결함인지·아니라면 왜 아닌지.

**브리프를 믿지 마라.** 이 회차 감사에서 상위의 대상 확정이 이미 한 번 단위에게 기각당했다.

- [ ] **Step 4: 뒤 층 6 — 없앴을 때로 잰다**

검사를 되돌려 무엇이 지나가는지 본다.

```bash
# 배선 한 줄을 주석 처리한다
sed -i.bak 's|^\( *\)SafeCheck(() => CheckLocalVariableTypeContract|\1// SafeCheck(() => CheckLocalVariableTypeContract|' \
  src/ReSet.Core/Services/MechanicalValidator.cs
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~ValidateBatchStep_Binds" -v n 2>&1 | tail -20
```

기대: **단위 시험 둘이 실패한다.** 실패 수를 기록한다. 통과하면 **검사가 아무것도 안 잠근 것**이다.

되돌린다:

```bash
mv src/ReSet.Core/Services/MechanicalValidator.cs.bak src/ReSet.Core/Services/MechanicalValidator.cs
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~ValidateBatchStep_Binds" -v n
```

기대: 다시 PASS.

- [ ] **Step 5: 좁힌 처방을 뮤턴트로 확인한다**

`CheckLocalVariableTypeContract` 의 발화 조건을 「`DECLARE` 가 없으면 발화」로 바꾼 뮤턴트를 만들어 전 스위트를 돌린다. `IsPrecisionBearingNumeric`·`IsZeroLiteral` 을 지나 **바인딩 대조 대신 SQL 펜스의 `DECLARE` 유무**로 판정하게 한다.

```bash
dotnet test 2>&1 | tail -20
```

기대: `ValidateBatchStep_InlinesTypedConstantIntoSql_IsSilent` 가 **깨진다** — 안전한 인라인에 발화한다는 뜻이고, 그것이 좁힌 처방(§5-1)이 옳았다는 증거다. **깨지지 않으면 그 시험이 아무것도 안 잠근 것이므로 시험을 고친다.**

뮤턴트를 되돌리고 전체 게이트를 다시 돌린다.

- [ ] **Step 6: 측정 기록을 쓴다**

`docs/audit-reports/2026-09-07-지역변수-타입계약-효력측정.md` 에 다음을 적는다.

```markdown
# 지역 변수 타입 계약 검사 — 효력 측정 (뒤 층 4·5·6)

측정 2026-09-07. 명세서 §6 의 뒤 층 셋과 「좁힌 처방 뮤턴트」의 결과다.

## 4. 발화한다
- 코퍼스 발화 후보: (수) — (목록)
- 알려진 세 자리가 전부 들어 있는가: (예/아니오)

## 5. 오탐이 다수가 아니다
- 열어 본 후보: (수)/(수)
- 진짜 결함: (수) · 오탐: (수) — 오탐마다 파일·줄·왜 아닌지

## 6. 없앴을 때로 잰다
- 배선을 주석 처리했을 때 실패한 시험: (수) — (이름 목록)
- 되돌린 뒤 다시 통과: (예/아니오)

## 좁힌 처방 뮤턴트
- 「DECLARE 없음」 판정으로 바꿨을 때 깨진 시험: (이름 목록)
- 판정: 좁힌 처방이 (옳다/틀렸다)
```

- [ ] **Step 7: 커밋**

```bash
git commit -m "test: 지역 변수 타입 계약 검사의 코퍼스 효력을 잰다

발화 3 · 침묵 23 을 시험으로 잠갔다. 되돌림과 좁힌 처방 뮤턴트의 결과를
함께 기록했다 - 발화 수는 활동이지 효력이 아니다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- tests/ReSet.Core.Tests/LocalVariableTypeContractCorpusTests.cs \
     'docs/audit-reports/2026-09-07-지역변수-타입계약-효력측정.md'
```

---

## Task 6: 계약 층 — 규칙 `5-1` 과 Few-Shot

**검사만으로는 안 닫힌다.** 이 저장소는 검사 D 를 강제 완료하고도 침묵했고, 프롬프트 **주석**만 고쳤을 때 배송본이 안 닫혔다 — **생성물은 예시를 베끼지 주석을 베끼지 않는다.**

**Files:**
- Modify: `src/ReSet.Core/Services/AiService.cs`
- Create: `tests/ReSet.Core.Tests/BatchStepPromptContractTests.cs`

**Interfaces:**
- Consumes: 없음
- Produces: 없음(종단 태스크)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

프롬프트가 조용히 낡는 것을 막는다. 규칙과 예시 **둘 다** 잠근다 — 하나만 잠그면 다른 하나가 사라져도 초록이다.

```csharp
using System.Reflection;
using ReSet.Core.Services;
using Xunit;

namespace ReSet.Core.Tests
{
    /// <summary>
    /// 배치 스텝 프롬프트의 규칙 5-1 과 그 Few-Shot 예시가 조용히 사라지지 않게
    /// 잠근다.
    ///
    /// [왜 예시까지 잠그는가] 실측: 프롬프트 주석만 고쳤을 때 배송본이 안 닫혔다 -
    /// 생성물은 **예시를 베끼지 주석을 베끼지 않는다.** 규칙만 잠그면 예시가
    /// 사라져도 초록이다.
    /// </summary>
    public class BatchStepPromptContractTests
    {
        private static string PromptText()
        {
            // 프롬프트 상수는 private이다. 리플렉션으로 전부 이어 붙여 본다 -
            // 이 저장소가 제품 필드를 리플렉션으로 읽는 관례를 따른다.
            var sb = new System.Text.StringBuilder();
            foreach (var f in typeof(AiService).GetFields(
                         BindingFlags.NonPublic | BindingFlags.Static | BindingFlags.Public))
            {
                if (f.FieldType == typeof(string) && f.IsLiteral)
                    sb.AppendLine((string?)f.GetRawConstantValue() ?? string.Empty);
            }
            return sb.ToString();
        }

        [Fact]
        public void BatchStepPrompt_CarriesLocalVariableTypeContractRule()
        {
            var text = PromptText();

            Assert.Contains("Local Variable Type Contract", text);
            Assert.Contains("rule 5-1", text);
        }

        [Fact]
        public void BatchStepPrompt_ShowsLegacyLocalPreservedAsDeclare()
        {
            // 예시의 DECLARE 는 신설 인프라 변수(@v_shadow·@v_sql)가 아니라
            // **레거시 지역 변수 보존**의 본이어야 한다.
            var text = PromptText();

            Assert.Contains("DECLARE @v_someRate DECIMAL(5,2) = 1.25;", text);
        }

        [Fact]
        public void BatchStepPrompt_DoesNotShipCorpusLiteralsInTheExample()
        {
            // 실물 이름·값을 예시에 쓰면 생성물이 그 리터럴을 아무 데나 베낀다.
            var text = PromptText();

            Assert.DoesNotContain("DECLARE @v_valIncVat", text);
        }
    }
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~BatchStepPromptContractTests" -v n
```

기대: 앞 둘 FAIL. **셋째가 FAIL 이면 프롬프트에 이미 실물 리터럴이 있다는 뜻이니 그 자리를 보고한다.**

- [ ] **Step 3: 규칙 `5-1` 을 넣는다**

`AiService.cs` 의 배치 스텝 규칙 목록에서 **규칙 5 바로 뒤**에 넣는다(규칙 5 는 *A step's interface is exactly the original procedure's parameter list*). **14·15 를 번호 바꾸지 마라** — 다른 자리가 번호로 참조한다.

```
5-1. [Local Variable Type Contract] A local variable the source procedure DECLAREs *with an initial value* keeps its DECLARE in the SQL this step sends. Write it inside the ```sql block as `DECLARE @<name> <declared type> = <initial value>;`, spelled exactly as the machine-derived local variable table gives it. Do NOT demote it to a binding parameter of `execute(...)` - the binding list carries the original procedure's parameters only (rule 5). A bound value has no declared type: the driver picks one, and a `DECIMAL(2,1)` that arrives as `FLOAT` changes what `CAST(... AS INT)` truncates. Stating the type in a comment does not pin it.
```

- [ ] **Step 4: Few-Shot 대조 쌍을 넣는다**

`[Few-Shot Examples for Modernization Patterns]` 절의 마지막 예시 뒤에 넣는다. **이름과 값을 코퍼스와 다르게 쓴다.**

````
* Legacy Local Variable Pattern (whenever the source procedure DECLAREs a variable with an
  initial value - rule 5-1):
```pseudocode
// 바인딩 목록에는 원본 프로시저의 파라미터만 들어간다 (rule 5-1).
// 레거시 지역 변수의 타입 계약은 SQL 안에 남는다.
execute(SQL_APPLY_RATE, { p_batchDate: batchYmd })
```
```sql
-- SQL_APPLY_RATE
-- (rule 5-1) 원본이 초기값과 함께 선언한 지역 변수는 여기 그대로 남는다.
DECLARE @v_someRate DECIMAL(5,2) = 1.25;
UPDATE dbo.TargetTable SET Amt = CAST(Amt / @v_someRate AS INT) WHERE BatchDate = @p_batchDate;
```
The binding list above carries one name and it is the step's own parameter. Passing the rate
there instead would leave its type to the driver, and a `DECIMAL(5,2)` that arrives as `FLOAT`
truncates differently. Writing the type in a comment does not pin it.
````

**부정 예시를 블록 안에 넣지 마라.** 「이렇게 하지 마라」는 위처럼 **블록 밖 산문에만** 둔다 — 생성물은 블록을 베낀다.

- [ ] **Step 5: 통과를 확인한다**

```bash
dotnet test tests/ReSet.Core.Tests/ReSet.Core.Tests.csproj \
  --filter "FullyQualifiedName~BatchStepPromptContractTests" -v n
```

기대: 3/3 PASS.

- [ ] **Step 6: 전체 게이트를 돌린다**

```bash
dotnet build 2>&1 | tail -20
dotnet test 2>&1 | tail -20
```

기대: 경고 0 · 실패 0 · 건너뜀 0.

- [ ] **Step 7: 커밋**

```bash
git commit -m "feat: 규칙 5-1 과 레거시 지역 변수 보존 Few-Shot 을 프롬프트에 싣는다

지금 예시의 DECLARE 둘은 신설 인프라 변수라 「레거시 지역 변수가 DECLARE
로 살아남는」 모양을 한 번도 보여주지 않았다 - 생성물이 베낄 본이 없었다.
이름과 값을 코퍼스와 다르게 써서 리터럴 전이를 막고, 부정 예시는 블록
밖에만 둔다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- src/ReSet.Core/Services/AiService.cs \
     tests/ReSet.Core.Tests/BatchStepPromptContractTests.cs
```

---

## Task 7: 재생성 관문을 문서화한다 — **재생성은 돌리지 않는다**

**이 태스크는 재생성을 실행하지 않는다.** 2026-09-05 사람 결정으로 계획서 재생성은 검사 뒤로 미뤄져 있고, 지금 산출물 안에 동료 세션 둘의 발화 표본이 들어 있다. 그것을 덮어쓰면 그 표본을 잃는다.

**Files:**
- Modify: `docs/superpowers/specs/2026-09-07-지역변수-타입계약-강등금지-design.md`

**Interfaces:**
- Consumes: Task 1·5 의 측정 기록
- Produces: 없음(종단 태스크)

- [ ] **Step 1: 명세서 §6 의 체크 상태를 실측값으로 갱신한다**

앞 층 1·2·3 과 뒤 층 4·5·6 의 `✔`/`✘` 를 Task 1·5 의 실제 결과로 바꾸고, 각 항목에 **측정 기록 파일 경로**를 단다.

- [ ] **Step 2: 재생성 관문 절을 명세서 끝에 더한다**

```markdown
## 9. 재생성 관문 (아직 통과하지 않았다)

**계약 층(규칙 5-1 · Few-Shot)의 효력은 되돌림으로 못 잰다 — 재생성으로만 잰다.**
이 저장소는 검사 D 를 강제 완료하고도 명세서를 재생성하지 않아 침묵했고, 프롬프트 주석만
고쳤을 때 배송본이 안 닫혔다.

**재생성은 사람이 결정한다**(2026-09-05 결정으로 검사 뒤로 미뤄져 있고, 지금 산출물 안에
동료 세션 둘의 발화 표본이 있다). 돌릴 때의 조건을 미리 못박는다.

- **얼어붙은 입력 트리** — 재생성 입력은 이 커밋의 트리를 그대로 쓴다.
- **모델을 못박는다** — 모델 이름과 버전을 기록에 남긴다.
- **판마다 Job 이름과 로그를 가른다** — 같은 이름에 덮어쓰면 전후 대조가 불가능해진다.
- **커밋 해시를 기록한다.**

**재생성 뒤에 확인할 것 둘.**

1. 유출 세 자리(`Batch4/S06`·`Batch5/S05`·`Batch5/S07`)의 바인딩이 `DECLARE` 로 바뀌었는가.
2. **보존 23 자리가 여전히 `DECLARE` 인가** — 계약이 반대로 밀지 않았는지 본다.
```

- [ ] **Step 3: 커밋**

```bash
git commit -m "docs: 지역 변수 타입 계약 설계에 실측값과 재생성 관문을 반영한다

여섯 한 벌의 체크 상태를 측정 기록으로 갱신하고, 계약 층은 되돌림이 아니라
재생성으로만 잰다는 관문을 못박는다. 재생성 자체는 사람이 결정한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_018H7EHNfCLz8LrKk9ReW8ee" \
  -- 'docs/superpowers/specs/2026-09-07-지역변수-타입계약-강등금지-design.md'
```

- [ ] **Step 4: 좌표자에게 보고한다**

다음을 한 문단으로 보고한다: 코퍼스 발화 수와 그 목록 · 오탐 수 · 되돌림 실패 수 · 뮤턴트 결과 · **재생성이 아직 안 돌았다는 사실**. 마지막 항목을 빠뜨리면 「닫혔다」로 오독된다 — **배송본은 아직 안 닫혔다.**
