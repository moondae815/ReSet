# U-앵커 계약에 `INSERT n`·`DELETE n` 표기를 명시한다 — 설계

**작성 2026-09-08.** 기준 커밋 `22bac170`(main). **코드는 안 만졌다 — 이 문서는 결정 자료다.**

**출처**: `2026-09-08-Batch6-재생성-판독.md` §2 가 「뿌리를 찾았다」로 지목한 자리.

## 1. 결함 — 계약이 「각 DML 문장」을 요구하면서 UPDATE 표기만 정의한다

`AiService.cs:4605`(`AppendStatementAnchorRules`, 조항은 4611):

> 「**각 DML 문장** 바로 앞에 명세서의 **갱신 번호**를 주석으로 답니다.
>  `/* U13: 카드사 원가 반영 */` 형식입니다(**`갱신 13`·`UPDATE 13`도 인정됩니다**).」

**요구는 INSERT·UPDATE·DELETE 전부인데, 인정하는 표기 셋이 전부 UPDATE 용어다.**
`INSERT 1`·`DELETE 1` 을 어떻게 적어야 하는지 **어디에도 없다.**

### 그래서 모델이 회차마다 발명한다 — 실측 넷

| 판 | 표기 | 리더가 읽는 서수 |
| :--- | :--- | :--- |
| `Batch1`(09-04) | `-- SQL_INSERT1 (INSERT 1, 오류코드 -2)` + `/* INSERT1: … */` | 1 (우연히 맞음) |
| `Batch5/S13`(09-06) | `/* U13-DELETE 1: … */` | **13** ← 틀림 |
| `Batch6/S09`(09-08) | `/* U22: DELETE 1 … */` (`U` = **원본 라인**) | **22** ← 틀림 |
| `Batch6/S13`(09-08) | `/* U1: … (DELETE 1) */` (`U` = **자체 순번**) | 1 (우연히 맞음) |

**해가 조용하다** — 서수가 틀리면 명세서에 그 `(Kind,서수)` 행이 없어 **앵커 계열 검사
(B·C·D)가 대조할 행을 못 찾고 그냥 지나간다.** 발화도 오류도 없이 커버리지가 사라지고,
**앵커가 있어 보이기까지 한다.**

## 2. ★ 리더를 또 고치지 마라 — 오늘 네 번 고친 것이 전부 이 구멍의 보상이었다

```
1bb76b67  유일성 → 일치        (기계 라벨과 U-앵커가 같은 서수를 말하는데 버려졌다)
c3762dcc  패턴 완화 \s+ → \s*   (`DELETE1` 처럼 공백 없는 표기를 못 읽었다)
8e971b4f  `U<n>-` 배제          (`U13-DELETE 1` 이 13 을 집었다)
379e2b39  탐지기               (그 오귀속을 자동으로 잡는다)
```

**넷 다 「모델이 발명한 표기를 리더가 따라가는」 일이었다.** 발명이 계속되는 한 리더는
계속 뒤쫓는다 — `Batch6` 의 `U<원본라인>` 이 그 증거다(리더 셋을 다 고친 뒤에 나왔다).
**고칠 자리는 리더가 아니라 계약이다.**

## 3. 처방 — 조항 한 줄을 갈아 끼운다

```
- **각 DML 문장 바로 앞에 명세서 DML 범위 표의 그 문장 표기를 주석으로 답니다.**
  `UPDATE` 는 `/* U13: 카드사 원가 반영 */`(`갱신 13`·`UPDATE 13`도 인정),
  `INSERT` 는 `/* INSERT 1: … */`, `DELETE` 는 `/* DELETE 1: … */` 형식입니다.
  **번호는 명세서 표의 서수를 그대로 씁니다** — 원본 라인 번호나 이 단계의 자체 순번을
  쓰지 마십시오. **종류가 다르면 서수가 겹쳐도 됩니다**(`DELETE 1` 과 `INSERT 1` 은
  서로 다른 문장입니다). **`U13-DELETE 1` 같은 복합 라벨은 쓰지 마십시오.**
```

**네 문장이 각각 실측된 오답 하나씩을 막는다.**

| 문장 | 막는 실측 오답 |
| :--- | :--- |
| `INSERT n`·`DELETE n` 형식을 명시 | 표기가 없어 발명하는 것(뿌리) |
| 「명세서 표의 서수를 그대로」 | `U22`(원본 라인) · `U1`(자체 순번) |
| 「종류가 다르면 겹쳐도 된다」 | 서수 충돌을 피하려 번호를 옮기는 것 |
| 「복합 라벨을 쓰지 마라」 | `U13-DELETE 1` |

## 4. ★★ 비용 — **캐시 인상도 명세서 재생성도 필요 없다**

**이것이 이 설계의 가장 중요한 사실이다.** 스킬(`reset-l1-check`)의 인상 규정은
「프롬프트에 실리는 것이 달라졌으면 `CurrentCacheFormatVersion` 을 올린다」인데,
**이 조항이 실리는 프롬프트는 그 캐시의 관할이 아니다.**

**코드로 확인했다.**

```
AiService.cs                          CacheManager 참조 0 회
CurrentCacheFormatVersion             CacheManager.cs 안에서만 쓰인다(소비자 5 중 4 가 그 파일)
VerificationPipelineOrchestrator
  RunCodeObjectPipelineCoreAsync      _cacheManager 사용 (SP 분석 = 명세서 경로)
  RunConsolidatedPipelineAsync        _cacheManager 사용 0 회 (Job 계획·단계 경로)
```

**단계 생성은 아예 캐시를 안 탄다.** 2026-09-05 F1′ 회차가 같은 이유로 인상하지 않은 전례가 있다.

| | |
| :--- | :--- |
| 캐시 인상 | **불필요** |
| 명세서 전건 재생성 | **불필요** |
| 드는 것 | **Job 재생성 1 회** — 새 이름으로 돌리면 기존 산출물도 안 덮는다 |

**비용이 「전건 재생성」이 아니라 「Job 하나」다.** 이 사실을 모르면 이 항목이 부당하게
비싸 보여 계속 미뤄진다 — 실제로 오늘까지 그랬다.

## 5. 착수 조건 — 앞 셋은 이미 실측됐다

| # | 조건 | 값 |
| ---: | :--- | :--- |
| 1 | 재료 | 실측 오답 **넷**(§1). 영향 문장은 `Batch6` 에서 **8** |
| 2 | 도달 | 조항이 `GenerateBatchStepSectionAsync` 와 단일 호출 폴백 **둘 다**에 실린다(한 곳에 둔 이유가 `AppendStatementAnchorRules` 주석에 있다) |
| 3 | 그 자 | **`AnchorKindOrdinalPairTests` 가 이미 있다** — 지금 불일치 8 로 빨갛고, 고쳐지면 0 이 된다 |
| 4 | 발화 | 계약 층이라 「발화」가 아니라 **다음 Job 의 불일치 수**로 잰다. 목표 **0** |
| 5 | 오탐 | 프롬프트가 넓어지는 것이라 오탐 축이 없다. **대신 「모델이 새 표기를 또 발명하는가」를 본다** |
| 6 | 없앴을 때 | **계약 층은 되돌림으로 못 잰다**(F1′ 회차가 같은 한계를 적었다). **재생성으로만 재진다** |

## 6. 되돌림 조건

- **R1**: 다음 Job 에서 `AnchorKindOrdinalPairTests` 불일치가 **0 이 아니면** 조항을 다시 본다.
  **되돌리지는 않는다** — 표기를 안 정한 상태보다 나쁠 수 없다.
- **R2**: 앵커 **보유 문장 수가 줄면** 되돌린다. 표기를 좁혀 모델이 아예 안 붙이게 되는 것이
  이 변경의 유일한 진짜 위험이다.
- **R3**: 프롬프트 잠금 시험(`AiService` 문구 고정)이 있으면 함께 갱신한다 —
  안 하면 게이트가 그 문구로 빨개진다.

## 7. 안 하는 것 · 알려진 한계

- **리더를 더 안 고친다**(§2).
- **기존 산출물은 안 고쳐진다.** `Batch1`·`Batch5`·`Batch6` 의 오귀속은 그대로 남고,
  **게이트는 다음 Job 이 나올 때까지 빨갛다.** 그 빨간불을 없애려고 탐지기를 약화시키지 마라.
- **`Batch6/S09` 의 `U22`·`U31` 이 원본 라인 번호라는 것은 관측이지 확인이 아니다** —
  명세서 표의 `라인` 칸이 22·31 인 것과 일치하지만, 모델이 그 칸을 봤다는 증거는 없다.
- **표기를 정해도 모델이 지킬지는 재생성으로만 안다**(조건 6).

---

## 8. 착수 기록 — 2026-09-08

**설계대로 착수했다.** 코드 변경은 조항 한 줄과 시험 둘이다.

### 8-1. R2 의 기준값을 먼저 못박았다 — 이게 없으면 R2 는 측정 불가였다

`AnchorKindOrdinalPairTests` 가 합계만 찍고 있어 **Job 별 내역**을 찍게 했다. 새 이름으로
Job 을 하나 더 돌리면 합계는 무조건 늘기 때문에, 합계로는 「앵커 보유 문장 수가 줄면」을
잴 수 없다. **변경 전** 실측:

| Job | 대조 단계 | 앵커 보유 문장 | 불일치 |
| :--- | ---: | ---: | ---: |
| `POQSettleBatch1` | 14 | 92 | 0 |
| `POQSettleBatch4` | 14 | 78 | 0 |
| `POQSettleBatch5` | 14 | 92 | 0 |
| **`POQSettleBatch6`** | **14** | **80** | **8** |
| 합계 | 56 | 342 | 8 |

**R2 의 오라클은 `Batch6` 의 80 이다** — 재생성본을 이 수와 맞대고, 줄면 되돌린다.
비교는 **같은 원본 SP·같은 14 단계**일 때만 성립한다.

> **근사치를 기준값으로 쓰지 마라.** 착수 전 grep 근사치는 `Batch6` 을 **270** 으로 셌다
> (리더가 세는 것은 80). 자가 다르면 R2 가 초록인 채로 실패한다.

### 8-2. 조항 교체 — 설계 §3 그대로

`AppendStatementAnchorRules` 한 곳만 고쳤고, 호출 둘(`GenerateBatchStepSectionAsync` ·
`GenerateConsolidatedBatchPlanAsync`)이 그대로 받는다.

### 8-3. 새 잠금 — `StatementAnchorClauseTests` 6

표기 넷을 각각 잠그고(§3 의 네 문장), **두 경로의 프롬프트를 각각 잡아 둘 다** 단언한다.
여섯째는 두 경로의 앵커 절이 **글자까지 같은지**를 본다.

**★ 없앴을 때로 쟀고, 판정이 하나 나왔다.** 호출을 한쪽씩 지운 뮤턴트 둘:

| 뮤턴트 | `FallbackPlanPromptParityTests`(기존) | `StatementAnchorClauseTests`(새것) |
| :--- | :--- | :--- |
| 폴백 경로의 호출을 지움 | 잡는다 | 6/6 빨강 |
| **분할 경로의 호출을 지움** | **12/12 초록 — 눈이 멀었다** | 6/6 빨강 |

**분할 생성이 실제로 도는 경로다.** 기존 잠금은 폴백만 봤다.

### 8-4. R3 이 현실이 됐다 — 잠금 시험 하나가 빨개졌고 갱신했다

`AiServiceTests.GenerateBatchStepSectionAsync_DemandsAnchorCommentsAndForbidsSemanticSubstitutions`
가 `"갱신 번호"` 를 축자로 단언하고 있었다. 그 시험은 **앵커를 요구한다는 사실**만 지키게
두고(`"각 DML 문장 바로 앞에"`), 표기의 내용은 새 시험이 지킨다.

> 착수 전 조사에서 나는 「분할 경로에는 문구 잠금 시험이 없다」고 적었다. **틀렸다** —
> `AiServiceTests` 안에 있었고 절 제목이 아니라 다른 조각을 단언해서 안 걸렸다.
> 프롬프트 잠금을 찾을 땐 절 제목 하나로 grep 하지 마라.

### 8-5. 게이트 상태

`실패 1 · 건너뜀 0 · 경고 0 · 통과 3974`. 그 하나가 `AnchorKindOrdinalPairTests`(불일치 8)이고,
**설계 §7 이 인정한 그 빨간불이다** — 기존 산출물은 안 고쳐지므로 다음 Job 재생성까지 남는다.
**이 빨간불을 없애려고 탐지기를 약화시키지 마라.**

### 8-6. 남은 것 — 사람이 돌려야 판정된다

**Job 재생성 1 회.** 새 이름으로 돌린 뒤 이 시험을 다시 보고:

- **조건 4**: 그 Job 의 불일치 **0** 이면 계약이 먹은 것이다.
- **R2**: 그 Job 의 앵커 보유 문장이 **80 밑으로 떨어지면 되돌린다.**
- **조건 5**: 0 이 아니면 **표기를 또 발명했는지**를 본다(어떤 새 모양인지가 다음 재료다).

### 8-7. P2 판정 — 계약이 먹었다 (2026-09-11)

`reset-68` 이 이 판정을 다른 세션에 넘겼고, 사람이 **`POQSettleBatch7`**(새 이름, 09-11 14:21 생성)을
돌렸다. 측정은 main `31e352ea`(시험·리더는 `0a600a28` 이후 불변), 격리 워크트리.

| Job | 대조 단계 | 앵커 보유 문장 | 불일치 |
| :--- | ---: | ---: | ---: |
| `POQSettleBatch1` | 14 | 92 | 0 |
| `POQSettleBatch4` | 14 | 78 | 0 |
| `POQSettleBatch5` | 14 | 92 | 0 |
| `POQSettleBatch6` | 14 | 80 | 8 |
| **`POQSettleBatch7`** | **14** | **92** | **0** |
| 합계 | 70 | 434 | 8 |

- **조건 4 ✔** — 새 Job 불일치 **0**.
- **R2 해당 없음** — 앵커 보유 문장 **80 → 92**. 걱정했던 「표기를 좁혀 아예 안 붙인다」의 반대다.
- **R1 해당 없음.** **조건 6 ✔** — 계약 층은 재생성으로만 잰다고 적었고, 재생성 한 번으로 쟀다.

**비교가 성립하는 이유(§8-1 의 조건).** Batch7 은 단계 번호가 한 칸씩 밀리고 순서도 바뀌었다
(신설 S01·S16·S17, `EXPECT_PROC` S06→S09, `STAT_PGCOLLECT` S09→S14). 그래도 **레거시 단계 14 개가
각각 SP 하나를 1:1 로 흡수하고 그 14 SP 집합이 Batch6 과 같다** — 시험의 「대조 단계 14」가 그
14 개다. Job 합계가 같은 14 SP 에 대한 수이므로 단계 번호가 달라도 비교가 선다.

**P1 공백은 판정을 막지 않았다.** 명세서가 v20·v22 로 재생성됐는데(캐시 인덱스 `{22: 31}`,
09-08 스냅숏 31 편 전부 달라짐) P1 은 아무도 남기지 않았다. 그래도 같은 실행에서 **Batch6 이 v22
명세서 아래서 80·8 그대로**다 — 오라클이 안 움직였으므로 80 이 R2 기준으로 유효하고, 그 8 이 같은
실행 안의 **양성 대조군**이다(탐지기가 살아 있으니 Batch7 의 0 은 「자가 죽어서 0」이 아니다).

#### 기제 — 맞는 이유로 0 이다

리더의 `AnchorPattern` 을 **SQL 펜스 안 주석 줄에만** 대서 잰 앵커:

| SP | 명세서(v22) DML 범위 | Batch6 | Batch7 |
| :--- | :--- | :--- | :--- |
| `UP_Util_PG_Client_CMRate_Ins` | `DELETE 1`·`INSERT 1` … `DELETE 5`·`INSERT 5` | `U1` … `U10` — 종류를 넘은 순번 | `DELETE 1`·`INSERT 1` … `INSERT 5` — 순서까지 같다 |
| `UP_UTIL_STAT_PGCOLLECT_INS` | `DELETE 1`·`INSERT 1` | `U22`·`U31` — 원본 라인 번호 | `DELETE 1`·`INSERT 1` |
| `UP_UTIL_SETTLE_SUMMARY_ETC` | `SELECT 1`·`DELETE 1`·`INSERT 1` | `U1`·`U2` — 단계 자체 순번 | `DELETE 1`·`INSERT 1` |

앵커 주석과 **바로 뒤 문장의 종류**를 짝지어 Job 전체를 세면 버릇이 통째로 사라졌다
(주석 스캔 기준이라 시험의 「앵커 보유 문장」과는 다른 자다 — Batch6 89 · Batch7 93):

| | `U` → UPDATE | `U` → INSERT·DELETE | `U`·`UPDATE` → SELECT |
| :--- | ---: | ---: | ---: |
| Batch6 | 33 | **16** | **12** |
| Batch7 | 44 | **0** | **0** |

#### 탐지기의 한계 — 불일치 8 은 하한이었다

이 시험은 **(종류, 서수) 쌍이 명세서에 있는지만** 본다. 그래서 Batch6/S01 의 `U2`~`U5` 는
**있는 쌍을 엉뚱한 문장에 붙이고도 통과했다** — `INSERT 1` 문장에 붙은 `U2` 는 (INSERT, 2) 로 읽히고
명세서에 `INSERT 2` 가 있으니 통과한다. S01 한 곳에서만 넷이다. 즉 Batch6 의 실제 오귀속은 8 보다
많고, **Batch7 은 쌍의 존재가 아니라 순서까지** 명세서와 같다는 것을 위 표가 따로 보여 준다.

#### 조건 5 — 계약이 정한 종류에서 새 표기 0, 정하지 않은 종류엔 명세서 행 이름을 따랐다

Batch7 에서 「앵커처럼 생겼는데 리더가 못 읽는 주석」 19 개를 전부 갈랐다.

| 모양 | 수 | 판정 |
| :--- | ---: | :--- |
| `-- 브랜치1:`·`-- 분기1:`·`-- branch1:` | 10 | UNION 분기 이름표. 앵커가 아니다 |
| `/* SELECT 1: … */` | 8 | 계약이 표기를 안 정한 종류에 **명세서 표의 문장 칸 이름을 그대로** 붙였다 |
| `/* IF 1: … */` | 1 | 같은 모양 |

`AnchorPattern` 은 `U`·`갱신`·`UPDATE`·`INSERT`·`DELETE` 만 받는다. 그래서 **`SELECT n`·`IF n` 9 곳은
앵커로 읽히지 않고 대조도 받지 않는다** — 불일치로는 절대 안 드러나는 조용한 공백이다. 새 표기를
**발명**한 것은 아니다(명세서가 쓰는 이름이다). 리더를 넓힐지(§7 「리더를 더 안 고친다」와 부딪친다),
계약에 `SELECT n` 을 정할지는 **사람 결정으로 남긴다.** 이것이 조건 5 가 말한 다음 재료다.

#### 게이트

측정 시점(`31e352ea`) `실패 1 · 건너뜀 0 · 통과 4157`. 그 하나가 Batch6 줄의 8 이다. **Batch7 이
코퍼스에 들어와도 다른 코퍼스 시험은 하나도 안 깨졌다.**

#### 남은 것

- **Batch6 의 빨간불은 Batch6 이 코퍼스에 있는 한 남는다.** 빼면 초록이 되지만, 이 시험이 살아 있는
  코퍼스에서 가진 **유일한 양성 대조군**을 잃는다 — 이후 모든 Job 이 0 이면 그 0 이 계약 덕인지
  탐지기가 죽어서인지 못 가른다. 뺄 거면 그 8 건과 위 `U2`~`U5` 넷을 **커밋된 픽스처로 먼저 옮겨라.**
- `SELECT n`·`IF n` 표기(위 조건 5).

> **판독 중 내 자가 한 번 틀렸다.** 처음엔 `INSERT n` 문자열을 파일 전체에서 긁어 「Batch6 도 명세서
> 서수와 같다」고 봤다 — 산문과 오류 코드 표의 언급까지 셌기 때문이다. 앵커를 셀 때는 **리더의
> 정규식을 리더가 보는 자리(SQL 펜스 안 주석 줄)에만** 대라. 위 두 표는 그렇게 다시 잰 값이다.
