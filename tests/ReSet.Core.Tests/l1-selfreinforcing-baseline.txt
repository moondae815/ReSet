# 「한 객체의 연속한 두 시도에서 같은 검사키가 발화」한 자리 중, 사람이 판정한 것.
#
# 형식: <객체> <검사키>  # 판정과 근거(커밋 해시 포함)
#
# 여기 적는 것은 「봐줬다」가 아니라 「봤고 이렇게 판정했다」이다. 근거 없이 적지 마라 -
# 근거 없는 줄은 화이트리스트가 조용히 넓어지는 자리가 된다.
#
# 이 게이트의 관할은 「만족 불가능한 지시」이지 「나쁜 문구」가 아니다. 단발로 끝난
# 결함은 여기 안 걸린다(2026-09-09 MachineTableShapeBroken 이 그랬다).
#
# ★ 2026-09-10 첫 실물 승격(dbo.UP_UTIL_SETTLE_EXCEPTION_PROC) 판정 머리말 — 이
# 게이트가 내는 것은 「자기강화 후보」이지 「자기강화 판결」이 아니다. 아래 두 줄이 같은
# 서명(연속 발화)으로 잡혔지만 원인이 다르다. 계획 Task 6 Step 2 의 두 갈래(오탐이면
# 고치고 해시와 함께 적는다 / 진짜 결함이면 근거와 함께 적는다)를 그대로 따른다 —
# 셋째 범주는 안 쓴다.
#   - CheckErrorCodeUniquenessClaim = 오탐. 고쳤다(아래 항목의 해시 참고).
#   - AddUpdateMappingError = 진짜 결함(모델이 헤딩 레벨을 틀렸다). 다만 검사
#     메시지 자체가 실재하지 않는 부재를 주장하므로 그 문구는 별건으로 고쳐야 한다 —
#     아래 항목 참고.
# 다음 사람이 이 파일을 훑고 「연속 발화 = 전부 자기강화 결함」이라고 일반화하면 안
# 된다. 서명은 후보를 내리는 것까지고, 원인 판정은 그 아래 근거를 읽어야 나온다.

dbo.UP_UTIL_SETTLE_EXCEPTION_PROC CheckErrorCodeUniquenessClaim  # 오탐 — 고쳤다(8ae36da1).
  # [2026-09-10 Fix Round 1 정정] 최초 판정(위 커밋 이전 버전)은 원인을
  # 「UniquenessDenialTokens 에 선행 부정 「안」이 없다」로 적었다 — 이 진단은
  # 실증적으로 틀렸다. UniquenessDenialTokens 에 "안"을 넣어도 같은 문장이 여전히
  # 발화한다(재현: 두 배열 각각으로 HasUnnegatedUniquenessClaim 을 돌려 True/True 확인).
  #
  # 실제 원인: l1-attempts.json 시도 4 메시지가 인용한 문장 "대부분의 코드는 서로
  # 다르지만, 위 두 쌍이 코드를 공유하므로 반환 코드 전체를 "고유하다"거나 "서로
  # 다르다"고 서술해서는 안 됩니다" 가 UniquenessClauseBoundaryRegex 의 「지만」
  # 경계에서 절 둘로 갈린다. 앞 절 "…코드는 서로 다르"만 PredicativeUniquenessRegex
  # (서술 용법 "코드는 고유하다"를 잡으려던 예방 가지, 그 문서 주석이 스스로 "코퍼스에서
  # 관측된 적이 없다"고 적고 있었다)에 걸려 유일성 주장으로 판정되고, 부정 표지("안")는
  # 뒤 절에만 있어 같은 절 안에서 안 보인다 — 배열에 무엇을 추가해도 절이 이미 갈린
  # 이상 못 고친다. 그리고 이 문장은 모델이 지어낸 게 아니라 프롬프트 공지문(규칙 층
  # 수정 8019ecfc)을 본문에 그대로 옮겨 쓴 것 — 규칙 층 수정이 L1 오탐의 새 입력을
  # 만든 사례다.
  #
  # 처방(8ae36da1): PredicativeUniquenessRegex 를 걷어냈다. 그 가지를 잠그던 합성
  # 시험(WhenTheClaimIsPredicative_ShouldStillReport)도 함께 걷어냈다. 되돌림으로
  # 확인: 가지를 되살리면 실물 오라클 시험(Attempt4_WhenHedgedDenialSplitsAcrossAClauseBoundary_ShouldStaySilent,
  # l1-attempts.json 시도 4 문장을 한 글자도 안 고치고 오려 온 것) 하나만 빨개지고
  # 나머지(같은 파일의 "…ShouldStillReportTheRealClaims" 등, 진짜 거짓 주장 포착 시험)는
  # 전부 그대로 초록 — 그 가지가 사 주던 커버리지가 합성 픽스처 하나뿐이었다는 뜻이다.
  #
  # 시도 1·2·3·5(HEAD 20ac803c 기준 재확인, 8ae36da1 이후에도 여전히 발화)는 실제로
  # "고유"·"서로 다른"을 유일성 주장으로 쓴 진짜 결함 — 그 넷은 이 게이트가 옳게
  # 잡은 것이고 고치지 않는다.

dbo.UP_UTIL_SETTLE_EXCEPTION_PROC AddUpdateMappingError  # 진짜 결함(모델이 헤딩 레벨을 틀렸다) — 별건으로 닫아야 한다.
  # 기계적 원인: raw/prompt-context.md 20행이 정확히 "### UPDATE 대상 테이블: …"
  # (헤딩 레벨 3)을 예시로 준다. MechanicalValidator.cs:2456 의 UpdateHeadingPrefix
  # = "### UPDATE 대상 테이블:" 도 같은 레벨을 요구한다. 그런데 실제 배송본
  # (docs/Spec.md 99행 이하, 18개 UPDATE 절 전부)은 "#### UPDATE 대상 테이블: …"
  # (헤딩 레벨 4)로 썼다 — "####"는 "### "로 시작하지 않으므로 CollectUpdateSections
  # 가 0 건으로 못 찾고 ResolveSectionBody 가 매번 "매핑 표가 없습니다"를 낸다(시도
  # 1·2·5·6 메시지가 문자 그대로 동일한 이유). 시도 3·4 에서는 이 검사가 조용했다 —
  # 즉 그 두 시도는 헤딩 레벨을 실제로 맞췄다는 뜻이다(만족 가능함의 증거이자, 5 에서
  # 모델이 다시 레벨 4로 퇴행했다는 뜻).
  #
  # ★ [Fix Round 1 반영] 최초 판정의 결론 "검사도 프롬프트도 고칠 필요가 없다"는
  # 과하다 — 지운다. 검사 메시지 "`## CRUD 분석`에 UPDATE 대상 테이블 …의 매핑 표가
  # 없습니다"는 사실이 아니다 — 완전한 매핑 표가 "####" 헤딩 아래 18개 전부 있다
  # (docs/Spec.md 99~247행에서 직접 확인). 메시지가 실재하지 않는 부재를 주장하고
  # 진짜 원인(헤딩 레벨 한 단계 차이)을 한 마디도 말하지 않는다 — 이 저장소는 같은
  # 부류를 이미 결함으로 인정하고 고친 전례가 있다(1e362c0b, "칸을 지우라"로 읽히던
  # 문구를 "두 행이 붙었으니 나누라"로). 다음 사람이 이 게이트의 「고칠 필요 없음」을
  # 읽고 닫힌 것으로 취급하면, 헤딩이 또 한 단계 어긋날 때 같은 오해를 반복한다.
  # 이 검사 자체는 이 태스크에서 고치지 않는다(src/ 는 CheckErrorCodeUniquenessClaim
  # 항목의 오탐 하나만 고쳤다) — 메시지를 "부재"에서 "헤딩 레벨 불일치"로 바꾸는
  # 작업은 별건으로 열어 닫아야 한다.
