## 로직 흐름 요약

1. `PaymentDB.dbo.TTxMst`, `dbo.TSettleMst`, `dbo.TCardAllotInterest`를 결합하여 할부이자를 계산합니다.
   - 대상은 `TSettleMst.YMD = @pi_strYMD`, `TTxMst.ClientInterest = 1`인 행입니다.
   - 오류 시 `@po_intRetVal = -1`을 설정하고 롤백합니다.

2. 해외카드 정상거래의 고객사 및 PG 수수료를 계산합니다.
   - 오류 시 `@po_intRetVal = -2`를 설정하고 롤백합니다.

3. 티모넷 정산 예외처리 블록은 전체가 주석 처리되어 실행되지 않습니다.
   - 주석 블록이 활성화될 경우 오류 시 `@po_intRetVal = -4`를 설정하도록 작성되어 있습니다.

4. 취소거래의 금액 및 수수료 관련 컬럼을 `-1`배 처리합니다.
   - 대상은 `YMD = @pi_strYMD`이고 `UseState IN (1,2,3)`인 행입니다.
   - 오류 시 `@po_intRetVal = -4`를 설정하고 롤백합니다.

5. `CheckPay`, `Toss`, `TossPoint`의 전체취소 거래에 대한 취소수수료를 적용합니다.
   - PG 처리 오류 시 `@po_intRetVal = -5`, 고객사 처리 오류 시 `@po_intRetVal = -6`을 설정하고 롤백합니다.

6. `Toss`, `TossPoint`의 부분취소 거래 중 `UseState = 2`인 행은 우선 수수료를 0으로 설정합니다.
   - 오류 시 `@po_intRetVal = -7`을 설정하고 롤백합니다.

7. Toss 및 TossPoint 부분취소 거래 중 부분취소 금액 합계가 승인금액과 같아지는 경우 최종 수수료를 적용합니다.
   - 오류 시 `@po_intRetVal = -8`을 설정하고 롤백합니다.

8. `inivacct` 전체취소 거래의 취소수수료를 처리합니다.
   - 오류 시 `@po_intRetVal = -9`를 설정하고 롤백합니다.

9. `easybank` 취소 거래의 취소수수료를 처리합니다.
   - 오류 시 `@po_intRetVal = -10`을 설정하고 롤백합니다.

10. `KFTC`, `INIBANK`의 부분취소 또는 익일 이후 전체취소 거래에 대해 환불서비스 조건을 적용합니다.
    - 오류 시 `@po_intRetVal = -11`을 설정하고 롤백합니다.

11. `hectofirm`의 전체취소 거래는 `PGComm`, `PGVT`를 0으로 설정합니다.
    - 오류 시 `@po_intRetVal = -12`를 설정하고 롤백합니다.

12. 페이레터 관련 테스트 고객사의 수수료를 0으로 처리하는 블록은 주석 처리되어 실행되지 않습니다.
    - 주석 블록이 활성화된 상태에서 오류가 발생하면 `@po_intRetVal = -15`를 설정하도록 작성되어 있습니다.

13. 모든 정산 대상일 행의 총수수료를 계산합니다.
    - 오류 시 `@po_intRetVal = -20`을 설정하고 롤백합니다.

14. 고객사 부가가치세 유형이 `1`인 행은 부가가치세 포함 방식으로 재계산합니다.
    - 오류 시 `@po_intRetVal = -21`을 설정하고 롤백합니다.

15. 분할정산 사용 고객사의 분할정산금액을 계산합니다.
    - 오류 시 `@po_intRetVal = -22`를 설정하고 롤백합니다.

16. 승인일 기준 외화정산 고객사의 통화와 외화정산금액을 계산합니다.
    - 오류 시 `@po_intRetVal = -23`을 설정하고 롤백합니다.

17. 모든 활성 단계가 성공하면 `COMMIT TRAN`을 수행하고 값 없이 반환합니다.

> **문서 작성일시**: 2026-08-05 13:16:02
